# Fix-check ledger — 2026-08-26 (overnight polish pass)

One adversarial checker per fix from RELEASE_AUDIT_2026-08-25.md, run against the
current dev tree (post-5.7.6). `git log` proved which commits carried each fix;
every issue below is a checker-traced claim with file:line in the current tree.
Statuses: **OK** = original bug dead and fix clean. **OK+notes** = bug dead,
side observations. **INCOMPLETE** = bug dead but the fix left a sibling path
open or introduced a defect. **STILL-LIVE** = the original scenario still
reproduces.

Full machine-readable traces: scratchpad fixcheck_results.json (also mirrored
in the wf_ce665b74-c21 journal).


## #11 — INCOMPLETE — activeDataNodes() excludes YIELDED, so the net-voltage-scan exclusion drops exactly while the user's

- **[high]** `src/sensing/NetVoltageScan.cpp:1313` — Sibling path bypasses the exclusion the fix repairs. pairTapSlot() iterates globalState.connections.paths[] directly (1310-1320) and never consults displayService.activeDataNodes(). A display bridge is row<->node135/136 (pathType BBtoNANO, duplicate 0, skip 0); pairTapEligible() (763-768) rejects only ADC0-4 and ADC7_PROBE, so BOTH ends qualify -> seqPairBegin/seqPairComplete close a sense route onto the live SDA/SCL net. Worse, the caller at line 1483 tries pairTapSlot BEFORE the round-robin, and debug.net_scan_pair_taps defaults to 1 (src/config.h:183), so on a stock V5 the rebuildScanList exclusion at line 811 is bypassed on most passes. Net effect: the exact harm in finding #11 ('background sense taps start closing crosspoints onto the live bus mid-transaction') still happens - not just while YIELDED but while ALIVE too. The fix widened what activeDataNodes reports without teaching the dominant tap path to read it.
- **[low]** `src/displays/DisplayService.cpp:234` — Resume path re-opens the window the fix closed, briefly. pollParts()'s YIELDED resume does detach() then attach() (232-236); detach() sets partIdx=-1 (DisplayService.cpp:205) so activeDataNodes returns 0 for that gap, connectionsFingerprint (NetVoltageScan.cpp:794) flips, rebuildScanList re-includes the bus nets, and core 1 can tap them while attach() is re-routing/re-initing. Bounded (one attach), same class of exposure, and it still costs the double measurement-cache wipe on every resume that the fix_sketch hoped to remove (yield no longer wipes; resume still does, twice).
- **[low]** `src/displays/DisplayService.cpp:448` — Collateral, user-visible and intended-but-unbounded: while YIELDED the whole net carrying SDA/SCL is now dropped from scanNodes, so every breadboard row on the display bus loses its voltage/current reading (and current ants) for the duration - old code kept scanning them while YIELDED. Normally <=750 ms past the script's release, but pollParts early-returns under jOS.inModalContext() (line 220), so a long menu/probe interaction holds YIELDED - and therefore the readings blackout - indefinitely after the user's script already released the pins.

<details><summary>checker notes</summary>

FIX FOUND: src/displays/DisplayService.cpp:445-452, landed in 57cf63d. The old `if (inst.state != DispState::ROUTED && inst.state != DispState::ALIVE) return 0;` (added in 7773318) is deleted; the guard is now only `if (inst.partIdx < 0 || inst.sdaPin < 20) return 0;` (line 448) with the comment at 446-447 naming YIELDED explicitly.

ORIGINAL SCENARIO, narrowly: DEAD. service() flips ALIVE->YIELDED at line 305 without calling detach()/displayBusRelease(), so sdaPin/sclPin keep 24/25 (SOFT_SDA_PIN/SOFT_SCL_PIN, DisplayBus.cpp:19-20) and partIdx stays >=0. activeDataNodes now returns 2 while YIELDED, so rebuildScanList (NetVoltageScan.cpp:811-823) keeps excluding the bus nets and connectionsFingerprint (794-796) no longer flips on the yield.

ATTACK RESULTS:
- Boundary: guard tests sdaPin only, but sdaPin/sclPin are always written and cleared as a pair (DisplayBus.cpp:156-157, 186, 197), so out[1] can't be built from a lone stale sclPin. sdaPin's only values are 24/25 or -1, so a torn cross-core read degrades to returning 0, never to bogus nodes.
- Dropping the state check does NOT introduce over-exclusion: every `state = DispState::EMPTY` assignment (118, 137, 155, 205) is paired with `partIdx = -1` on the same path, so EMPTY-with-partIdx>=0 is not reachable outside the in-flight attach window.
- OG (RP2040): correct and dormant. board_og.cpp:114 sets breadboardDisplays=false; service() returns IDLE at DisplayService.cpp:298 before pollParts, and displayBusAcquire double-guards at DisplayBus.cpp:141, so partIdx never leaves -1 and activeDataNodes returns 0. V5: board_v5.cpp:117 true. Both targets handled.
- Callers: only two, both in NetVoltageScan.cpp (794 fingerprint, 811 rebuildScanList). Neither needed a change. The gap is a NON-caller - pairTapSlot - see issue 1.

COLLATERAL: see issue 3. No other behavior change; printStats' stateNames index is masked (&3) and unaffected.

Not compiled (pio blocked under py3.14 per project memory); the change is a pure guard deletion with no type/signature impact.

</details>

## #2 — STILL-LIVE — partsTapForRow accepts a resting/parked probe as a tap the instant the prompt opens

- **[high]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/PartsApp.cpp:281` — Deadtime (400 ms) is SHORTER than the duplicate re-emission period it is meant to defeat (500 ms, Probing.cpp:7678), so a parked probe still self-accepts - just half a second later. Trace on the current tree: prompt opens, openMs=t0. First loop call to justReadProbe(true) hits the duplicate branch (Probing.cpp:7676) with lastDuplicateTime stale, returns the parked row AND sets lastDuplicateTime=t0 (Probing.cpp:7692); PartsApp discards it (t-t0 < 400). Every subsequent call returns -1 via Probing.cpp:7689 WITHOUT touching lastDuplicateTime, so the next accept lands at t0+~500ms - which passes `>= 400` unconditionally. Row is flashed, returned, and for the DIP flow partsCommitPlacement runs (power/GND bridges) and the app exits (PartsApp.cpp:673/694/698) with zero user action. Worse case: if lastDuplicateTime was NOT stale at prompt-open (some other consumer read it <500 ms prior), the very first emission arrives at t0+400..500 and is accepted on the first try. Guaranteed on BOTH targets - PartsApp.cpp is in src/ root and jumperless_og inherits v5's build_src_filter (platformio.ini:253-256), and the 500 ms window lives in the shared Probing.cpp with no OG variant. Note this is structural, not mis-tuning: a parked tip re-emits forever, so NO wall-clock deadtime can close it. The finding's own sketch (require a real no-contact gap / lift-then-touch before the first accept) is the only shape that works - drain and discard emissions until one decode-quiet gap (probeRead<=0) has been seen.
- **[medium]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/PartsApp.cpp:281` — Collateral: legitimate fast taps in the first 400 ms are now silently swallowed - no flash, no serial nudge, no OLED change. A brief jab (<400 ms of contact) is lost outright, because justReadProbe already consumed the row transition into lastRowProbed/lastDuplicateTime (Probing.cpp:7712-7722) and PartsApp throws the value away; with the tip lifted, nothing re-emits and the user must tap again with no indication why the first tap did nothing. Compounded in the per-signal SIP flow: each accepted tap costs delay(180) (PartsApp.cpp:289) before the next prompt paints and sets openMs, so every step carries ~580 ms of dead input. Old code accepted a tap at any moment.
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/PartsApp.cpp:264` — New defect from the same commit's typed-row usedRows check: the `if (rowUsed) {...}` block (lines 251-256) does not return or `else`-guard the tail, so it falls through to `Serial.println("  row must be 1-60")` at line 264. Typing an already-used, perfectly valid row (e.g. 12 for a second SIP signal) prints BOTH "row 12 is already one of this part's pins" AND "row must be 1-60" - a misleading bench message that reads as a range error. Fix: make line 264 reachable only from the invalid-range path (`else { Serial.println(...); }` on the 1-60 branch).
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/Menus.cpp:3031` — Sibling path the fix skipped: the finding asked for the guard to land in the shared modal-consumer contract, but it is a local in partsTapForRow. Menus.cpp:3031 and Peripherals.cpp:3451 still take a parked row on the first justReadProbe(true) of their loops - the voltage picker jumps to the parked row's mapped voltage the instant it opens. Blast radius is transient (a slider value the user then turns), and it is pre-existing rather than introduced, but it is the same mechanism and remains open.
- **[medium]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/Probing.cpp:7676` — Root cause left untouched: the same-row branch accepts a SINGLE decode whenever >=500 ms have elapsed, while a different row needs two strictly-consecutive decodes (7712-7717); neither lastRowProbed nor lastDuplicateTime ever expires - the probeRead<=0 early return at 7649-7652 clears nothing. This is the ever-armed row that makes the PartsApp parked accept possible in the first place (the related low finding at 7667 in the same audit). Expiring both after N ms of decode-level quiet at 7649 would close the PartsApp hole and the sibling pickers at once.

<details><summary>checker notes</summary>

FIX FOUND, BUG NOT DEAD. The fix is commit 57cf63d, two hunks in partsTapForRow: (a) `unsigned long openMs = millis();` at PartsApp.cpp:213 plus `&& millis() - openMs >= 400` appended to the accept condition at PartsApp.cpp:281; (b) a usedRows scan added to the typed-row branch at PartsApp.cpp:247-256 (that one addresses the separate medium finding, and it works, modulo the fall-through message bug). No shared-contract change landed in Probing.cpp; dec1b45's "probe contract" work did not touch justReadProbe's duplicate window.

The headline scenario survives verbatim with a ~500 ms delay. justReadProbe's duplicate branch rate-limits a held row to one emission per 500 ms (Probing.cpp:7678); the PartsApp guard is 400 ms, measured from prompt-open. The first emission is burned by the guard but it re-arms lastDuplicateTime, and the rejection path (7689) never re-arms it, so the second emission at ~t0+500 clears `>= 400` every time. Parked tip in row 42 -> pin 1 placed on 42, confirm flash, power/GND bridges, app exits - the user has had half a second, not zero, to read a 3-line OLED prompt. I verified the loop has no competing consumer: `probing` is registered HIGH (main.cpp:558) and Service::inInnerSet() defaults to CRITICAL-only (JumperlOS.h:172), so jOS.serviceInner() inside the modal loop does not drain the emission; probeButton is CRITICAL/inner but samples the button line only.

Both targets equally affected - no target-specific gap, but no target-specific fix either. PartsApp.cpp lives in src/ root and jumperless_og's build_src_filter extends v5's minus boards/v5 and the V5 router (platformio.ini:253-256); the 500 ms window is in the shared Probing.cpp. On OG (switchPosition parks at 0) readProbeRaw's two-agreeing-bursts gate (7501-7507) is satisfied by a steady parked reading, and the measureTouchLatched latch (7520-7525) only arms when connectOrClearProbe==1, which PartsApp is not.

Old-code behavior NOT preserved in one respect: taps landing in the first 400 ms are now dropped with no feedback at all (issue 2). The right shape is the audit's own sketch - refuse whatever row is already decodable at prompt-open until one genuine no-contact gap is observed - which simultaneously kills the parked accept for good and lets a fast tap through immediately after a lift.

</details>

## #22 — INCOMPLETE — addNodeToNet writes nodes[0x7f] when a net's node table is full (findFirstUnusedNodeIndex fallback u

- **[high]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/routing/NetManager.cpp:769` — SIBLING 0x7f FALLBACK LEFT OPEN — same bug family, still writes OOB through the *fixed* function. Commit 57cf63d bounded two of the three 0x7f fallbacks (findFirstUnusedBridgeIndex :977, findFirstUnusedNodeIndex :995) but not findFirstUnusedNetIndex (:961, returns 0x7f at :974). createNewNet() takes it unchecked: nets[127].number = 127 (:770), nets[127].name = netNameConstants[127] (:773 — netNameConstants is char*[MAX_NETS]=60 at MatrixState.cpp:29 and only 50 entries are initialized, so this is an OOB read of a pointer that is then stored and later printed/strcmp'd), nets[127].specialFunction (:775), then addNodeToNet(127, ...) (:777,:779) and addBridgeToNet(127, ...) (:781). Crucially the new guard at :951 validates only newNodeIndex, never netToAddNode, so the OOB store at :957 (nets[127].nodes[k]) is still live — the fix's own write site remains reachable, just driven by a bad net index instead of a bad node index. nets[] is netStruct[60] (~290 B/struct on V5), so nets[127] lands ~19 KB past the array, inside/after globalState. Reachable on BOTH targets: free net slots are 6..59 (nets 0..5 pre-populated, MatrixState.cpp:18-27) = 54, while numBridges can reach MAX_BRIDGES = 128 (V5) / 72 (OG) and nothing caps net creation (getNodesToConnect :293-311, searchExistingNets :510). ~54 disjoint pairs (1-2, 3-4, 5-6, ...) from a saved netlist reproduces it on every rebuild. Same fix shape as :951 — check the index against MAX_NETS in createNewNet, and range-check netToAddNode/netToAddBridge in addNodeToNet/addBridgeToNet.
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/routing/NetManager.cpp:929` — STATE LEFT BEHIND ON THE NEW EARLY-EXIT PATH: populateSpecialFunctions(netToAddNode, node) runs at :929, before the refusal at :951-956. On a refused add it has already published gpioNet[0..9] = netToAddNode (:800-885) for RP_GPIO_1..8 / RP_UART_TX / RP_UART_RX, so a GPIO/UART node is recorded as belonging to a net whose nodes[] does not contain it. findNet() reads gpioNet[] as an authoritative fallback (:747-754), so a GPIO can be reported on a net it was refused from — a misleading bench readout. Pre-fix the node was (corruptly) stored, so gpioNet matched intent; this divergence is new. Move populateSpecialFunctions below the bounds check.
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/routing/NetManager.cpp:952` — COLLATERAL, minor: the refusal message is an unconditional Serial.print in the net-build hot path, while every other diagnostic in addNodeToNet is gated by debugNM (:936-945). addNodeToNet is called once per node per rebuild (searchExistingNets :451-453, combineNets :562-563/:580/:598), so one full net emits a burst of ~N lines on EVERY rebuild, not once — interleaving into the CDC stream the desktop app parses. Same pattern at :791-793 in addBridgeToNet. Rate-limit or gate to a single line per rebuild.
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/routing/NetManager.cpp:1024` — PRE-EXISTING sibling the audit's bounding pass missed, on exactly the state the fix now preserves: checkDoNotIntersectsByNet uses j <= MAX_NODES (:1024, :1061) and i <= MAX_DNI (:1019, :1056). nodes[MAX_NODES] aliases bridges[0][0] in netStruct (MatrixState.h:17-19) and doNotIntersectNodes[8] aliases visible/virtual_net (MatrixState.h:25-29). The j overrun only fires when nodes[] has no 0 terminator — i.e. the full-net condition the fix keeps alive instead of corrupting — and a match against the aliased bridges[0][0] silently sets paths[].skip = true and net = -1 (:1048-1049), dropping a wire with no message. Not introduced by this fix, but it is the residual failure mode of a full net on both targets.
- **[low]** `/Users/kevinsanto/Documents/GitHub/JumperlOS/src/routing/NetsToChipConnections.cpp:1877` — Residual OOB read from the same batch: nets[n].bridges[] is [MAX_NODES][2] but this scan still runs to MAX_BRIDGES (128 V5 / 72 OG), reading past the table into specialFunction/doNotIntersectNodes when a net's bridge table is full — the exact pattern 57cf63d bounded at combineNets :583, shiftNets :715 and findFirstUnusedBridgeIndex :983. Identical copy at NetsToChipConnections_OG.cpp:1967, so both targets. Impact is limited to an over-count into bridgeCount[] (declared :1945 in the OG file, written but never read), so read-only and low, but it should be MAX_NODES for consistency with the rest of the batch.

<details><summary>checker notes</summary>

FIX FOUND — src/routing/NetManager.cpp:951-956, landed in 57cf63d (`git log -p -S"is full - node not added"`).

  if (newNodeIndex < 0 || newNodeIndex >= MAX_NODES) { Serial.print("net "); ... return; }

placed between the dedup scan and the store at :957.

1. ORIGINAL SCENARIO DEAD? Yes, for the exact write. findFirstUnusedNodeIndex (:995-1008) returns 0x7f=127 only when all MAX_NODES slots are non-zero; 127 >= MAX_NODES on both targets (JumperlessDefines.h:191-196: OG 24 / V5 40, compile-time gated, and both envs build the same NetManager.cpp — platformio.ini:257-258 has jumperless_og inherit the V5 build_src_filter), so the nodes[127] store is unreachable. No second copy of addNodeToNet/findFirstUnusedNodeIndex exists (only NetManager.cpp:926/995 + NetManager.h:58,64). The only other writer of nets[].nodes[] is the clear loop at :712; every JSON/file load path is separately bounded (JsonState.cpp:549-563, States.cpp:166-175, :1926, :1949, :2019). BOTH TARGETS COVERED by the single check.

2. ATTACK. Boundaries fine (0 <= idx <= MAX_NODES-1 only when a real free slot was found; the `< 0` arm is dead but harmless). Dedup return at :946 precedes the guard and is correct (node already present = nothing to add). The failure is one level up: the guard checks the node index but not the NET index, and the third 0x7f fallback — findFirstUnusedNetIndex (:974) consumed unchecked by createNewNet (:769) — feeds net 127 straight into addNodeToNet/addBridgeToNet, restoring an OOB write ~19 KB past nets[]. That is the front-door/sibling case. See issue 1.

3. COLLATERAL. Old behavior preserved for every net below MAX_NODES: the guard is a pure no-op unless the table is full, in which case the old code only ever corrupted memory. Refusal semantics are sane — routing consumes nets[].bridges (NetsToChipConnections.cpp:1465-1476), which addBridgeToNet refuses independently at :790, and buildNodeToNetIndex (:331) rebuilds from nodes[] so a refused node reads as netless rather than stale. Nothing added to the persisted state. OG's MAX_NODES=24 predates this work (6dc3271), so the lower OG refusal threshold is not collateral from this fix. Only real collateral is the unguarded Serial spam (issue 3) and the gpioNet leak (issue 2).

NOT VERIFIED: did not compile either env (pio is broken under py3.14 here); change is a plain bounds check in a TU that already uses Serial in the same function.

</details>

## #23 — OK+notes — GPIO pad menu passes 0-based index to 1-based chooseGPIOinputOutput: OOB writes gpioState[-1]/gpioDi

- **[high]** `src/Probing.cpp:4483` — Sibling OOB of the same class, PRE-EXISTING (not introduced or covered by the #23 fix). attachPadsToSettings pad-case 37..53 does `gpioChosen = chooseGPIO(1)` (4438), then only normalizes when the return is in RP_GPIO_1..RP_GPIO_8 (4447-4449). chooseGPIO returns -1 on probe-button exit and on the 15 s idle-timeout/runtime-cap backstops (5433-5446), so gpioChosen stays -1 and the code falls through to `gpioState[gpioChosen - 1]` read at 4455, `sfOptionColors[gpioChosen - 1]` read at 4466, and `gpioState[gpioChosen - 1] = 0` WRITE at 4483 -> gpioState[-2], two bytes before the uint8_t[50] (Peripherals.cpp:310), followed by setGPIO() at 4485. Repro: tap the settings logo pad -> 'GPIO to output' -> back out of the GPIO chooser with the probe button. settingOption = -2 (4484) is inert (only commented-out consumers). Same on V5 and OG.
- **[low]** `src/Probing.cpp:5457` — No defensive guard on the gpioDef lookup sentinel. `int gpioChosen = -1` (5457) is only overwritten if gpioDef[i][1] == function matches (5459-5464). Unreachable today (chooseGPIO only ever sets function to RP_GPIO_1..RP_GPIO_8 at 5329-5423, all present in gpioDef), but if it ever missed, 5480 becomes chooseGPIOinputOutput(0) -> gpioState[-1]/gpioDirection[-1] again, and 5482/5492 become gpioDef[-1][2]. A `if (gpioChosen < 0) return function;` would make the invariant local instead of inferred.
- **[low]** `src/Probing.h:432` — The 1-based contract for `chooseGPIOinputOutput(int gpioChosen)` is still undocumented in the header, and the function itself does no range check before indexing gpioState[gpioChosen-1] / gpioDirection[gpioChosen-1] (5197-5210) and sfOptionColors[gpioChosen-1] (5111, 5152). Both callers now agree (2492 passes subIndex+1, 5480 passes gpioChosen+1) but nothing enforces or records the rule for the next caller.
- **[low]** `src/Probing.cpp:5480` — Pre-existing asymmetry, unchanged by the fix: the outIn==2 branch (chooseGPIOinputOutput) writes gpioState/gpioDirection and markDirty (5197-5210) but never calls updateStateFromGPIOConfig, while the outIn==1/0 siblings do (5485, 5495). The pin's actual dir/pulls are therefore not applied until the next setGPIO()/refreshConnections. Cosmetic-latency only, and on OG both updateStateFromGPIOConfig (PersistentStuff.cpp:379-386) and setGPIO (Peripherals.cpp:762-768) are deliberate no-ops.

<details><summary>checker notes</summary>

FIX FOUND, bug is dead. src/Probing.cpp:5480 now reads `chooseGPIOinputOutput( gpioChosen + 1 );` — landed in 57cf63d (`git log -p -1 57cf63d -- src/Probing.cpp` shows exactly the one-line `-gpioChosen` / `+gpioChosen + 1` hunk, nothing else in that region). Working tree is clean for src/ (no uncommitted drift).

BOUNDS PROOF: chooseGPIO only ever assigns function = RP_GPIO_1..RP_GPIO_8 (5329-5423); gpioDef (Peripherals.h:167-178) holds all eight with gpioDef[i][2] == i, so the lookup at 5459-5464 always yields gpioChosen in 0..7 and the call passes 1..8. Inside chooseGPIOinputOutput that indexes gpioState[0..7] of uint8_t[50] (Peripherals.cpp:310), gpioDirection[0..7] of int[10] (routing/States.h:229), sfOptionColors[0..7] of [12] (Probing.cpp:1817). No index can reach -1 anymore. ConfigState::railPrior is no longer clobbered.

ALL CALLERS: only two exist repo-wide (grep over src/ lib/ include/). Probing.cpp:2492 passes `subIndex + 1` where ZONE_GPIO clamps subIndex to 0..7 (maxSubIndex 7 at 2040, wrap-max 7 at 1997) — already 1-based, untouched, still correct. So the fix normalizes the pad path onto the existing convention rather than inventing a third rule.

BOTH TARGETS: platformio.ini:186-188 — [env:jumperless_og] `extends = env:jumperless_v5` and inherits the same build_src_filter (line 104), so src/Probing.cpp compiles identically for RP2350 and RP2040. The changed line carries no #ifdef, so OG gets the fix verbatim. (Downstream, updateStateFromGPIOConfig and setGPIO self-skip on OG by design — unrelated to this index bug.)

SIBLING PATHS in chooseGPIO checked: outIn==1 (5482-5485) and outIn==0 (5492-5495) use 0-based `gpioDef[gpioChosen][2]` == gpioChosen and gpioDirection[gpioChosen], and updateStateFromGPIOConfig(onlyIdx) is 0-based (PersistentStuff.cpp:397-409) — all consistent, no OOB. Direction polarity is right on both branches (0=output/1=input per States.h:229): outIn==1 is the input-side pad rows and sets gpioDirection=1. The gpioState values written there are immediately overwritten by updateStateFromGPIOConfig, so the apparent 0/4 mismatch is inert on V5.

REENTRANCY/EARLY EXIT: chooseGPIOinputOutput sets inPadMenu=1 at 5083 and clears it at 5224 with no early return on any path (the while at 5122 can only break/fall through), so a cancel leaves no stuck menu flag. A -1 (cancel) return is ignored at 5480, exactly as before the fix; the encoder caller still honors it (2495-2498).

COLLATERAL: one user-visible change, and it is a correction, not a regression — gpioNumStr (5092) and the sfOptionColors tint (5111/5152) now render the real GPIO number 1..8 instead of the old 0..7 when the menu is reached from the pad path, matching what the encoder path already showed. Nothing else about the menu, its LEDs, its timing, or the config it writes differs from the old code.

</details>

## #25 — STILL-LIVE — Snake app wipes all graphic overlays and the loss is auto-saved to the slot file

- **[high]** `src/eyecandy/GraphicOverlays.cpp:683` — ORIGINAL BUG STILL LIVE. Only the second half of the fix sketch landed (clearAll's markDirty gate). runSnakeGame still calls graphicOverlayState.clearAll() every frame (:683) and at exit (:773), and clearAll() still calls clear() (:217) which unconditionally destroys ALL overlay slots -- including the user's persistent overlays deserialized from the active slot YAML. On frame 1 the new hadPersistent scan (:210-216) sees them, so markDirty() (:222) fires exactly once; the flag is sticky. SlotManager::service()'s idle auto-save (src/routing/States.cpp:4789) then runs saveActiveSlot -> toYAML, which is a wholesale rewrite (see the comment at States.cpp:1425), and serializeOverlaysToYAML returns early at :381 because persistable==0. The overlays section is durably deleted from the slot file within seconds of exiting Snake -- exactly the reported scenario. The fix only reduced the dirty marks from ~30/s to 1; it did not stop the data loss. Fix sketch's primary half (draw into a session-only '_SNAKE_' overlay and removeOverlay just that one per frame/exit) was never implemented.
- **[high]** `src/eyecandy/GraphicOverlays.cpp:683` — BOTH TARGETS AFFECTED. Snake is registered unconditionally in src/Apps.cpp:93 ({"Snake",17,1,runSnakeGame}) and reachable from the overlay debug menu (:1042) on V5 and OG alike. Overlay serialize/deserialize are NOT OG-gated (src/routing/States.cpp:1430 and :1628); only renderGraphicOverlays is (GraphicOverlays.cpp:320) and MAX_GRAPHIC_OVERLAYS shrinks to 1 (GraphicOverlays.h:40). So an OG user with one saved overlay loses it identically. jumperless_og is not covered.
- **[medium]** `src/eyecandy/GraphicOverlays.cpp:371` — COLLATERAL / NEW DEFECT introduced by the fix: adding _DIRECT_PIXELS_ to overlayIsSessionOnly() created a load/save asymmetry. deserializeOverlaysFromYAML (:481) has no session-only filter and still LOADS a _DIRECT_PIXELS_ overlay out of an existing slot file via addOverlay, but serializeOverlaysToYAML now skips it on write (:390). Any user who drew persistent pixel art through the user-facing MicroPython API jl_overlay_set_pixel (src/JumperlessMicroPythonAPI.cpp:3764 -> setPixel :236 -> addOverlay("_DIRECT_PIXELS_") :249) previously had it saved and restored; it is now silently erased from the file on the next save. Old-code behavior not preserved, and it is the same class of harm the fix was meant to remove. If _DIRECT_PIXELS_ is to be scratch, deserialize must also refuse it so the loaded/rendered state matches the file.
- **[low]** `src/eyecandy/GraphicOverlays.cpp:178` — COLLATERAL (same root as the _DIRECT_PIXELS_ change): addOverlay no longer calls markDirty() when creating _DIRECT_PIXELS_, so the first pixel a user draws from Python no longer arms the auto-save. Combined with the serializer skip, jl_overlay_set_pixel went from persistent to volatile with no user-visible signal.
- **[low]** `src/eyecandy/GraphicOverlays.cpp:1030` — SIBLING PATH the fix left open (pre-existing, not introduced): debugMenu's 'w' wave animation calls addOverlay("wave",...) once per ~30ms frame and 'b' calls addOverlay("ball",...) -- both persistable names, so each frame hits markDirty() at :144, and removeOverlay marks dirty again on exit (:202). Same 'app overlay pollutes persistent state' class as the Snake finding; these loops use delay() (no serviceInner) so no mid-loop auto-save, but they leave the state falsely dirty and can arm the USB-eject save. Should use a session-only name like the fix sketch prescribed for Snake.

<details><summary>checker notes</summary>

Fix located in commit 57cf63d, sole hunk on this file: GraphicOverlays.cpp:209-223 (clearAll hadPersistent gate) + :371 (_DIRECT_PIXELS_ added to overlayIsSessionOnly). git log -p -S"hadPersistent" confirms nothing else changed here.

Verdict: the reported bug is NOT dead. clearAll() still destroys every overlay slot; Snake still calls it per frame (:683) and at exit (:773). When the user actually has persistent overlays -- the only case where the finding does harm -- frame 1's hadPersistent scan evaluates true, markDirty() fires, and the idle auto-save (States.cpp:4789) rewrites the slot with serializeOverlaysToYAML bailing at :381 (persistable==0). Durable silent deletion, unchanged. The fix only suppresses the dirty mark in the harmless no-persistent-overlay case.

Timing checked: SlotManager is HIGH priority and does not override inInnerSet() (States.h:717, JumperlOS.h:172), so the auto-save cannot fire from the jOS.serviceInner() inside Snake's inner loop (:714) -- it fires from the main loop right after exit, matching "within seconds of exiting Snake". runApp (Apps.cpp:196) does no overlay restore on app exit, so RAM loss is final.

Correct remaining work: give Snake its own session-only overlay (e.g. "_SNAKE_" added to overlayIsSessionOnly) and removeOverlay("_SNAKE_") per frame/exit instead of clearAll(); and make deserializeOverlaysFromYAML skip session-only names so _DIRECT_PIXELS_ stops loading from files it can no longer be saved to.

</details>

## #30 — INCOMPLETE — Runtime OLED width/height change desyncs geometry; hold-stash memcpy overflows the 512-byte framebuf

- **[high]** `src/oled.cpp:3197` — Incomplete fix: the hold-stash memcpy still trusts a size captured at arm time. oledHoldStashAfterPriorityFlush() copies oledHoldShadowSize bytes into _displayPtr->getBuffer() and never re-validates that size against the CURRENT framebuffer, so the original overflow survives on a sibling path. Reachability: priorityFlushHeldFrame() takes its wavegen/I2C0 early return at src/oled.cpp:1685-1688 BEFORE calling the stash and without clearing oledHoldArmed, and oled::show() (src/oled.cpp:2269) never stashes -- so the arm latches for an unbounded time. Sequence: connection_type 2 + wavegen streaming + an undo toast (src/remembering/Undo.cpp:2724 oledHoldBegin -> 2727 priorityFlushHeldFrame, deferred) leaves oledHoldArmed=true with oledHoldShadowSize=1024 (128x64). User then sets OLED > Display Size > Height > 32 (src/Menus.cpp:4791-4795) -> oled.init() recreates the object at 128x32 and begin() allocates 512 bytes; init()'s own clearPrintShow(..., show=true) at src/oled.cpp:675 reaches priorityFlushHeldFrame -> stash -> memcpy(live[512], oledHoldShadow, 1024) = 512-byte heap overflow, the same defect at the same line as finding #30. The companion memcpy at src/oled.cpp:3195 reads oledPanelShadowSize (1024) out of the 512-byte live buffer in the same call. This is the unimplemented second half of the finding's own fix_sketch ('size the hold/panel shadows and all dump paths from the display object's geometry ... rather than from config-backed displayWidth/displayHeight'); only the first half (the recreate gate) landed. Neither oled::init() nor initDisplayForConnectionType() frees, resizes, or invalidates oledHoldShadow/oledPanelShadow, and nothing clears oledHoldArmed or holdUntilMs on a geometry change.
- **[medium]** `src/oled.cpp:657` — NEW defect introduced by the fix: the recreate it made routine has no allocation-failure guard. oled::init() ignores begin()'s bool return at src/oled.cpp:646, then calls getDisplay().clearDisplay() unconditionally at src/oled.cpp:657 -- which is memset(buffer, 0, WIDTH*((HEIGHT+7)/8)) with no null check (Adafruit_SSD1306.cpp:697-699). Adafruit_SSD1306::begin() returns false at Adafruit_SSD1306.cpp:499-500 without allocating when the malloc fails, so buffer stays nullptr and the memset writes to address 0 -> hard fault. (The read side, _displayPtr->display(), merely streams boot-ROM garbage on RP2040/RP2350; the write is the crash.) Made one-step reachable by the fix: the menu offers Width/Height presets 32/64/128/256 plus Custom i(16)(2048) (src/remembering/menuTree.h:206-211) and calls oled.init() immediately (src/Menus.cpp:4788, 4795). 2048x2048 -> malloc(524288) fails on both jumperless_v5 (RP2350) and jumperless_og (RP2040); OG's ~96KB MicroPython-shared heap makes far more moderate values fail too. Pre-fix the identical menu action did NOT recreate the object, so begin() reused the existing buffer (the !buffer guard at Adafruit_SSD1306.cpp:499) and nothing crashed -- that is the evidence this is fix-introduced. There is no clamp on top_oled.width/height anywhere in src/. Also unchecked: the `new Adafruit_SSD1306` at src/oled.cpp:156 (arduino-pico runs with exceptions off).
- **[medium]** `src/oled.cpp:3214` — Sibling path the fix left open: oledGetDumpBuffer() returns oledPanelShadow whenever oledHoldActive() && oledPanelShadow != nullptr && oledPanelShadowSize > 0, with no check that oledPanelShadowSize matches the current geometry -- while every consumer indexes it with the NEW displayWidth/displayHeight: dumpFrameBufferQuarterSize (src/oled.cpp:3591, 3602, 3614), dumpFrameBuffer (src/oled.cpp:3674-3680), dumpFrameBufferEncoded (src/oled.cpp:3715-3717, 3746-3747) and streamFrameToSer3 (src/oled.cpp:3794). Grow the panel inside an active hold (Height 32 -> 64: panel shadow still 512 bytes, displayWidth*ceil(displayHeight/8) now 1024) and the terminal mirror / USBSer3 stream reads 512 bytes past the allocation. Harm at the bench: a garbage OLED frame reported to the terminal and to any host reading the Ser3 backchannel -- a misleading reading -- plus a heap OOB read. Window is bounded by the toast hold (~1800ms, src/remembering/Undo.cpp:2724), hence medium. Same root cause as issue 1: the shadows are sized from config-backed displayWidth/displayHeight at arm time and never re-keyed.
- **[low]** `src/oled.cpp:138` — The gate compares Adafruit_GFX's ROTATION-ADJUSTED accessors, not the raw geometry begin() sizes the buffer from. width()/height() return _width/_height (Adafruit_GFX.h:205, 213), which setRotation() swaps for rotation 1 and 3 (Adafruit_GFX.cpp:1573-1587), whereas the framebuffer malloc uses the raw WIDTH/HEIGHT (Adafruit_SSD1306.cpp:499). Two consequences. (a) Over-trigger: with top_oled.rotation 1 or 3 on a non-square panel the gate can never match config.width/height, so EVERY oled::init() now destroys and reallocates the display object -- boot, each disconnected->connected reconnect (src/oled.cpp:3406), every menu/config reinit -- heap churn plus one more trip through the unguarded allocation path of issue 2 each time. Newly sticky because commit 47325d3 itself made top_oled.rotation persist across reboots for the first time (it is set via `config top_oled rotation` and src/Menus.cpp:4798-4801; there is no menuTree item). (b) Under-trigger: with rotation 1/3 the gate PASSES whenever the object's HEIGHT==config.width and WIDTH==config.height, and that is not size-equivalent for non-multiple-of-8 dimensions -- e.g. config 32x100 against an object built 100x32: oledHoldComputeBufferSize(32,100)=416 > the actual buffer 100*4=400, a 16-byte overflow that slips through the new gate entirely. WIDTH/HEIGHT are protected (Adafruit_GFX.h:244-245) so width()/height() was the only accessible option -- the repair is file-static created-geometry vars set beside _currentDisplayWire at src/oled.cpp:149-150, not the GFX accessors.

<details><summary>checker notes</summary>

Fix located: src/oled.cpp:137-141 (commit 47325d3, the only oled.cpp hunk in that commit). The original scenario is dead -- Menus.cpp:4784-4795 sets config + oled.displayWidth/Height then calls oled.init(), which re-syncs displayWidth/Height from config (src/oled.cpp:580-581) and now recreates the Adafruit_SSD1306 on geometry mismatch (src/oled.cpp:137-141, 149-158), so begin()'s malloc (WIDTH*ceil(HEIGHT/8)) always matches oledHoldComputeBufferSize(displayWidth, displayHeight) at the next oledHoldBegin. The documented 1024-into-512 overflow via the menu path no longer fires.

Target parity: satisfied. jumperless_og extends env:jumperless_v5 (platformio.ini:186-187), same src/oled.cpp, and the fix contains no #ifdef -- both targets get the identical gate and the identical residual gaps. OG's smaller RP2040 heap makes issue 2 strictly worse there.

Checked and benign: the terminal `config top_oled width/height` path (src/configManager.cpp:4311-4312) writes jumperlessConfig but never touches oled.displayWidth/Height or calls oled.init(), so config desyncs from displayWidth -- but every buffer-sized quantity stays keyed to the still-current object until the next init() re-syncs both. No overflow there. Same for the config-file parse at src/configManager.cpp:768-769. No clamp on width/height exists on any of these paths, which is what feeds issue 2.

No build attempted: the fix is a landed condition in a shipped release, parity is established by the shared PlatformIO env, and pio is broken under Python 3.14 in this environment.

</details>

## #31 — INCOMPLETE — Setting top_oled.sda_pin/scl_pin via config interface hits arduino-pico's setSDA panic (crash)

- **[high]** `src/configManager.cpp:4327` — Validation is parity-only, not port-aware, so the 'illegal pin' panic is still live. Every even pin 0-28 is a legal SDA for exactly one I2C port and every odd pin 1-29 a legal SCL for exactly one port (framework Wire.cpp setSDA/setSCL valid[] sets; identical on RP2040 and RP2350A). The check never verifies the new pin belongs to the port initI2C() will actually program -- that port is derived solely from the SDA pin via gpioI2Cmap (src/Peripherals.cpp:647-658). Repro on both targets: default connection_type 0 (sda 26 / scl 27), enter `top_oled.scl_pin = 21`. 21 is odd so it passes; initI2C(26,21) resolves portFound=1 from SDA 26; Wire1.setSCL(21) with the bus stopped, 21 not in I2C1's SCL set {3,7,11,15,19,23,27} and _scl(27)!=21 -> panic("FATAL: Attempting to set Wire1.SCL to illegal pin 21"). Same crash for scl_pin = 1, 5, 25. (9/13/17/29 are absent from gpioI2Cmap so initI2C returns -1 and fails gracefully.) The finding's own fix_sketch called for validating against the port's legal pin sets; only parity was implemented.
- **[high]** `src/configManager.cpp:4335` — The bus that gets torn down is chosen from connection_type, not from the port the NEW pin resolves to, so the original 'set while running' panic survives on the I2C0 side. With connection_type 0/1/3 only Wire1.end() runs; Wire (I2C0) is still running on GPIO 4/5, begun at boot by initDAC (src/Peripherals.cpp:533-549, V5) and initINA219 (src/Peripherals.cpp:1786-1790, OG) -- so both targets. Repro: connection_type 0, enter `top_oled.sda_pin = 20` (or 24, both in gpioI2Cmap as port 0) -> initI2C(20,27) -> portFound=0 -> Wire.setSDA(20) with _running && _sda==4 -> panic("...set Wire.SDA while running"). Repro 2: `top_oled.sda_pin = 4` -- setSDA survives on the `_sda == pin` escape, then Wire.setSCL(27) with _scl==5 -> panic("...set Wire.SCL while running"). GPIO 4/5 is the documented rev-7 hardwired pair, so typing 4 here is a plausible user move. This is the same class as the original bug, through the same handler.
- **[high]** `src/configManager.cpp:4335` — `if (connection_type == 2) Wire.end();` does exactly what teardownOldOledBus (src/oled.cpp:4511-4530) documents as forbidden, despite the comment at 4322 claiming to mirror that discipline: Wire is shared with the MCP4728 DAC (0x60) and both INA219 current sensors (0x40/0x41) on hardwired GPIO 4/5. From connection_type 2, any pin change that resolves to I2C1 ends Wire and never re-begins it -- initI2C takes the port-1 branch (src/Peripherals.cpp:692-729) and only Wire1.begin() runs -- leaving DAC outputs and both current sensors dead until reboot (dead/misleading bench readings). Most such inputs panic first (type 2 + sda_pin 6/22/26 -> Wire1.setSCL(5) illegal-pin panic; type 2 + scl_pin 27 -> Wire.setSCL(27) illegal-pin panic), but `top_oled.sda_pin = 20` from type 2 completes: Wire is re-begun on 20/5, silently orphaning the DAC and both INAs that are physically on GPIO 4.
- **[medium]** `src/configManager.cpp:4328` — Collateral / misleading readout on the new refusal path: the refused branch prints its message and then falls through to the common tail of updateConfigValue, which calls saveConfigToFile("/config.txt") and printSettingChange(section, key, oldValue, value) (src/configManager.cpp:4390-4396; printSettingChange at 3288 echoes newValue verbatim). So `top_oled.sda_pin = 7` prints "refused: ..." immediately followed by "top_oled.sda_pin: 26 -> 7", asserting a change that did not happen. The refusal also returns no status to callers, so fastParseAndUpdateConfig/the config UI report success.

<details><summary>checker notes</summary>

FIX LOCATED: src/configManager.cpp:4319-4343 (landed in 47325d3; old code was two bare `oled.disconnect(); <field>=parseInt(value); oled.init();` blocks).

1. ORIGINAL SCENARIO: dead. connection_type 0 (sda 26/scl 27), `top_oled.sda_pin = 22` -> 22 passes the even-SDA check (4327), Wire1.end() runs (4336), pin updated, oled.init() -> initI2C(22,27) -> portFound=1, Wire1 stopped, setSDA(22) legal for I2C1 -> no panic. Verified against the actual framework source at ~/.platformio/packages/framework-arduinopico/libraries/Wire/src/Wire.cpp:57-106 (note this version has an `if (_sda == pin) return true;` escape, so same-pin writes on a running bus are safe).

2. ATTACK: the fix closes the front door and leaves three siblings open, all through the same handler -- see issues. Root cause is that both the validation and the teardown decision use the wrong key: parity instead of the port's legal pin set, and connection_type instead of the port gpioI2Cmap will actually select. Correct shape: resolve (sdaPin, sclPin) -> port through the same table initI2C uses, reject if either pin is not legal for that port or the two disagree, then end the Wire that owns that port (refusing outright rather than ending Wire, per teardownOldOledBus).

3. BOTH TARGETS: equally affected, not one-sided. The handler has no OG_JUMPERLESS guard, src/oled.cpp has none either, the framework valid-pin sets are identical for RP2040 and RP2350A (the RP2350B-only wider sets are behind `!PICO_RP2350A`), and Wire is running on 4/5 on both (initDAC on V5, initINA219 on OG). So the fix is incomplete identically on jumperless_v5 and jumperless_og.

4. NO PERSISTENT BRICK: the panics fire inside oled.init() at 4341, before the tail saveConfigToFile at 4392, so a crashing pin value never reaches /config.txt and there is no boot loop. The pre-existing unvalidated file-load path (configManager.cpp:775-776) would still accept a hand-edited bad pin, but the fix did not make that worse.

5. OTHER COLLATERAL CHECKED, CLEAN: applyOledConnectionType/teardownOldOledBus (src/oled.cpp:4524-4566) and the hot-plug reconnect reset (src/oled.cpp:3388-3402) are unchanged and panic-free for all four connection types. No previously-working input is newly refused -- anything the parity check now rejects would have panicked before.

</details>

## #32 — INCOMPLETE — Double-tap undo/redo silently dead on the CPU button path: 28-sample confirm cannot fit in the 60ms 

- **[high]** `src/Probing.cpp:539` — The new `longEnough` term measures elapsed time SINCE THE CANDIDATE ARMED, not accumulated contact: `(now - (s_dblCandidateDeadline - kDblConfirmWindowMs)) >= 16`. Released samples never reset or penalize it — the release branch returns early at :372 and only the deadline cancels (:309-314). So a candidate armed by a single bounce sample now confirms as soon as ANY same-button pressed sample lands in [arm+16ms, arm+60ms], with s_dblConfirmCount as low as 2. That is exactly the 1–2-sample bounce signature kDblConfirmSamples=28 was added to reject (commit ac24c8a, two rounds of Kevin's phantom-undo feedback; see the intent comment at :246-250). On the default merged PIO path (~1.8 kHz, :743) 28 samples ≈ 15.7 ms ≈ the 16 ms threshold, so the count branch is now effectively unreachable and the OR is the sole gate — the net effect on the shipping default path is a loosening of phantom rejection, not a no-op. Harm: a scratchy click fires a spurious undo that silently reverts routing. Suggested: require both, with a cadence-safe floor — `s_dblConfirmCount >= kDblConfirmSamples || (longEnough && s_dblConfirmCount >= 4)` (4 samples = 16 ms at the 4 ms CPU cadence, trivially met by real contact at PIO cadence, unreachable for a 2-sample bounce) — or timestamp the first pressed sample and reset it on any released sample so the gate is true sustained contact.
- **[low]** `src/Probing.h:162` — Stale invariant comment: clearButtonStateKeepDoubleTap()'s rationale still asserts the phantom-double guards are "the 420ms window, the kDblConfirmSamples gate and the consumed-hold suppression each stand on their own." After dec1b45 the kDblConfirmSamples leg no longer stands on its own (see :539). This is the comment a future maintainer will rely on when deciding it is safe to preserve double-tap history across a clear.
- **[low]** `src/Probing.cpp:257` — Comment/code mismatch introduced by the fix: the block claims confirmation on "~16ms of accumulated same-button contact," and :237 still says the tap must "accumulate kDblConfirmSamples pressed samples." The code implements elapsed-since-arm with no accumulation. The comment documents the property that would make the fix safe but that the code does not enforce.

<details><summary>checker notes</summary>

FIX LOCATED: src/Probing.cpp:259 (new `kDblConfirmMinMs = 16`) + :537-540 (clamped increment, new `longEnough` OR). Introduced in dec1b45; verified with `git log -p -S kDblConfirmMinMs -- src/Probing.cpp`.

1) ORIGINAL BUG IS DEAD. CPU path cadence is 4 ms (Probing.h:204-205, gate at Probing.cpp:214). Candidate arms at the press edge with deadline = arm+60 (:493); samples at arm+4/8/12/16 → `longEnough` true at the first sample ≥16 ms, ~11 chances inside the 60 ms window. Arming is reachable on the CPU path too (releaseDebounceMs=30 → ~8 released samples; DOUBLE_WINDOW_MS=420). dblCandExpired no longer eats every CPU-path candidate.

2) BOTH TARGETS COVERED. src/Probing.cpp is shared: env:jumperless_og inherits v5's build_src_filter and only swaps boards/v5→boards/og (platformio.ini:257-261). No OG_JUMPERLESS conditionals anywhere in the button state machine (the 8 in Probing.cpp are all ADC/probe-sense, ≥:6175). OG is the target most affected — with PIO0 instruction memory contended it lands on PIOProbeState::UNAVAILABLE and runs the CPU path permanently (:1294), which is where the bug lived.

3) ATTACK RESULTS — arithmetic and paths are sound. `deadline - kDblConfirmWindowMs` reconstructs armTime modularly, so both the deadline compare (:529, :309) and `longEnough` are millis()-wrap-safe. Deadline expiry is checked BEFORE the confirm branch, so a stale candidate cannot fire late. Both callers of processSample (service() :220 and the PIO IRQ :890) share the same body; the IRQ is core0-only, so no reentrancy. The 255 clamp is behavior-identical (max ~107 samples per 60 ms window at 1.8 kHz). clearDoubleTapState() (:282-294) resets btn/count/deadline together; clearButtonStateKeepDoubleTap() leaves a live candidate, but the 60 ms deadline bounds it. No forgotten sibling detector: kDblConfirmSamples has no other reference in src/.

4) COLLATERAL — one real regression, issue #1. For CONTINUOUS contact the two conditions fire within ~0.3 ms of each other at PIO cadence, so genuine double-taps are unchanged; the ONLY behavioral delta on the default path is that contact-continuity is no longer required, which is precisely the case the 28-sample gate existed for. Front door closed (CPU path works), sibling path opened (PIO bounce rejection weakened) → fix_correct=false.

Not introduced by this fix, noted for context: confirmation still needs one pressed sample inside the 60 ms window, and probeMode iterations inflate to 20–50 ms (:7801), so a >60 ms starvation still expires a genuine candidate; and checkProbeButtonHardware() injects a released sample on the 600 µs multiplex bail (:1273-1276), which the new gate tolerates.

</details>

## #34 — STILL-LIVE — Trigger '^' registered twice: menu advertises 'set DAC 1 voltage' but the key runs undo

- **[high]** `src/SingleCharCommands.cpp:289` — ORIGINAL BUG STILL REPRODUCES VERBATIM. The extra menu still prints "^ = set DAC 1 voltage" while '^' is bound to cmd_undo (:578). printMenuLine(showExtraMenu, 3, ...) fires whenever showExtraMenu >= 3 (Graphics.cpp:5770), and 'e' cycles it 0..3 (:1753-1755). Repro unchanged: press 'e' three times, read the menu, type '^3.3' -> undo reverts the last connection/DAC transaction, and the residue '3','.','3' falls through to the dispatcher where '.' = connect oled (:643). Only the silent double-registration was fixed; the menu line that made it harmful was not touched. Identical on jumperless_v5 and jumperless_og (no #ifdef around the block; og reuses v5's build_src_filter, platformio.ini:257-258).
- **[high]** `src/SingleCharCommands.cpp:2763` — NEW DEFECT INTRODUCED BY THE FIX: cmd_setDAC was dead code that quarantined its own bug (the finding said so); rebinding it to ':' made it live without fixing it. It ignores the `line` argument entirely and reads raw from Jerial, unlike every other arg-taking handler which uses getCommandArgs(line, ...) (:1785-1815). With terminal_line_buffering = 1 (the DEFAULT, config.h:360) or any command relayed from the app/UART (main.cpp:1171 sets g_commandInputIsLine = true and consumes the whole line), Jerial is empty when the handler runs: it spins 1000 ms (:2771), then reads 8 bytes from an empty stream -> read() returns -1 (Jerial.h:154, :255) -> f = 0xFF x8 -> atof = 0.0 -> setDac1voltage(0.0, 1, 1) (:2781). So ':3.3' from the Jumperless app drives DAC1 to 0 V on the hardware AND persists it via globalState.setDacVoltage(1, 0.0) inside setDac1voltage (Peripherals.cpp:1639+, save=1), prints "DAC 1 = 0.00 V", and sets configChanged. Wrong rail voltage + overwritten saved state + a 1 s stall, on both targets.
- **[medium]** `src/SingleCharCommands.cpp:2773` — Revived handler's buffer bug, also untouched: f[8] is filled with exactly 8 bytes by the unconditional loop, leaving no NUL, and atof(f) (:2778) then reads past the 8-byte stack array. Triggered by ordinary input in char-by-char mode: ':3.300000' fills all 8 with digits and atof walks into adjacent stack (index/f1/timer). Any argument chars beyond the 8th also stay in the stream and dispatch as further commands - the same residue symptom finding #34 flagged for '^'. The fix should have routed this through getCommandArgs() rather than reviving the raw reader.
- **[medium]** `src/selfreflection/HelpDocs.cpp:240` — Second stale advertisement of the old binding: case '^' still prints "Set DAC voltage output" and "Usage: ^3.3  (sets DAC 1 voltage to 3.3V)" (:244). Reachable as '^?' in both input modes (main.cpp:1304 and :1338 call showCommandHelp(input); helpQuestionApplies() at main.cpp:779 does not exclude '^'). The registered help text for '^' says "Revert the last connection or DAC change" - the two docs now contradict each other and the '?' path wins.
- **[medium]** `src/selfreflection/HelpDocs.cpp:993` — Third stale advertisement: 'help voltage' lists "^  - Set DAC voltage output (^3.3 for 3.3V)" (:995). Reachable via main.cpp:1298-1300 (showCategoryHelp). Same misdirection as the menu line - a user following the help types '^3.3' and gets an undo.
- **[low]** `src/tubes/Ser3Backchannel.cpp:1112` — The trigger the fix chose collides with an existing protocol prefix on port 3: ':' is the reserved newline-terminated verb prefix, consumed before the single-char dispatch and its access check (:1112-1150). ':3.3' on the backchannel is parsed as verb "3.3", not the DAC command, so the rebound command is unreachable there despite being registered SER3_INTERACTIVE (SingleCharCommands.cpp:544). Not a crash, but the registry now claims a trigger the port already owns. Related discoverability gap: nothing in printMenu or HelpDocs advertises ':' or ',' at all, so both rebound commands are now invisible to a user reading the menu.

<details><summary>checker notes</summary>

FIND: fix is in 47325d3 (src/SingleCharCommands.cpp). DAC moved '^'->':' (:544, comment :542-543), history status moved '_'->',' (:587, comment :586), and registerCommand now logs an overwrite instead of doing it silently (:116-127). Verified no duplicate triggers remain across all 75 registrations (72 printable + '\\x0E'/'\\x0F'/'\\x13'), MAX_COMMANDS=128 not near (SingleCharCommands.h:123). The '_' half is internally consistent - menu line :268 "_ = print micros per byte" matches the surviving registration at :720.

ORIGINAL SCENARIO IS NOT DEAD. The finding's harm was "menu advertises X, key does Y"; the fix removed the shadowed registration but left all three places that advertise '^ = DAC': SingleCharCommands.cpp:289, HelpDocs.cpp:240-249, HelpDocs.cpp:993-995. The exact keystroke sequence from the report still fires undo plus stream residue.

ATTACK: the fix also un-quarantined cmd_setDAC (:2763-2786), which the finding explicitly warned about. In the shipped default config it is worse than dead - it zeroes DAC1 in hardware and in the persisted slot state on every invocation. Both board targets are affected identically (registration and menu are unconditional; jumperless_og inherits jumperless_v5's build_src_filter), so this is not a one-target gap - it is symmetric incompleteness.

COLLATERAL: no old-code behavior removed - '_' and '^' both keep the handler that actually ran pre-fix. One latent hazard added: the new collision logger prints to Serial from registerCommand, called from initializeCommands() in the global ctor at :63, i.e. before Serial.begin(); currently unreachable because no collisions remain, but it would run at static-init time the moment one is reintroduced - the opposite of when you want a diagnostic to be safe.

MINIMUM TO CLOSE: retitle SingleCharCommands.cpp:289 to ':' (or drop the line), fix HelpDocs.cpp:240 and :993, and rewrite cmd_setDAC to take its argument from getCommandArgs(line, ...) with a bounded, NUL-terminated parse.

</details>

## #39 — STILL-LIVE — OLED > Connect menu action reads menuLines[-1] (out-of-bounds String object)

- **[high]** `src/Menus.cpp:4790` — The fix (57cf63d) guarded ONLY the first arm of the else-if chain. src/Menus.cpp:4782 now reads `if ( currentAction.previousMenuPositions[2] >= 0 && menuLines[...].indexOf("Width") != -1 )`. When previousMenuPositions[2] == -1 the guard short-circuits false, and control falls straight into the unguarded `else if ( menuLines[ currentAction.previousMenuPositions[ 2 ] ].indexOf( "Height" ) != -1 )` at 4790 — menuLines[-1], the exact out-of-bounds String read finding #39 reported. The guard moved the fault one line down; it did not remove it. Original scenario is fully alive: menuTree.h:226 `"-Connect"` is a childless depth-1 row under `"OLED"` (src/remembering/menuTree.h:190,226 — verified it is the only childless depth-1 row in that subtree), getActionCategory returns OLEDACTION (src/Menus.cpp:4005-4007), clearAction leaves previousMenuPositions[2] == -1 (src/Menus.cpp:641) and populateAction only fills [0..1] for a 2-deep path (src/Menus.cpp:660-672). Affects BOTH targets: env jumperless_og inherits jumperless_v5's build_src_filter (platformio.ini:257-258), so src/Menus.cpp compiles identically into V5 and OG.
- **[high]** `src/Menus.cpp:4797` — Same defect, third arm: `else if ( menuLines[ currentAction.previousMenuPositions[ 2 ] ].indexOf( "Rotation" ) != -1 )` is also unguarded and is reached whenever the 4790 garbage read happens to not match "Height". So OLED > Connect performs TWO wild String dereferences per selection, not one. ("Rotation" has no row anywhere in menuTree.h — this arm is dead for legitimate paths and exists only as a second fault site.)
- **[medium]** `src/Menus.cpp:4782` — Incomplete-fix gap the `>= 0` form cannot cover: clearAction() runs once per menu session (src/Menus.cpp:980, at the top of the menu loop), and populateAction() (4058) overwrites only indices [0..previousMenuIndex-1] without clearing the tail. If the user visits OLED > Display Size > Width/Height earlier in the SAME session and then backs out and picks OLED > Connect, previousMenuPositions[2] is a STALE but valid index pointing at "$Width$"/"$Height$". The `>= 0` guard passes, the indexOf matches, and Connect silently writes jumperlessConfig.top_oled.width/height = currentAction.integerValue (never reset — declared src/Menus.cpp:128, not cleared in clearAction) and calls oled.init(), corrupting display geometry and setting configChanged. A depth/`previousMenuIndex > 2` check, not a `>= 0` check, is what closes this. Pre-existing, but it is the sibling path the chosen guard shape leaves open.

<details><summary>checker notes</summary>

FIND: fix is in 57cf63d, visible at src/Menus.cpp:4778-4783 (comment block + `previousMenuPositions[2] >= 0 &&`). It is the only change made for #39 — `git log -S\"previousMenuPositions[ 2 ] >= 0\" -- src/Menus.cpp` returns 57cf63d alone, and the diff hunk touches exactly one line.

ATTACK: bug NOT dead. `menuLines[-1]` is still executed on the reported path, at src/Menus.cpp:4790 and again at 4797, because the guard sits on the head of an `else if` chain and failing it routes control into the two unguarded siblings. Reproduce: fresh menu session, clickwheel > OLED > Connect. Both V5 (RP2350) and OG (RP2040) are affected identically — one shared source file, og env inherits v5's build_src_filter (platformio.ini:257-258). The finding text itself named three sites (\"and again at 4773, 4780\"); only the first was addressed.

The intended function of the row still works by accident — OLED > Connect's real handler is the `Connect` arm at src/Menus.cpp:4937 (`oled.checkConnection()`), reached via previousMenuPositions[1], which IS valid — so the wild reads happen on the way to a normal, user-visible action rather than on a dead path.

COLLATERAL: none from the change itself. Adding `>= 0 &&` is behavior-identical whenever previousMenuPositions[2] >= 0, so all legitimate OLED > Display Size > Width > <value> flows (4-deep, [2] == \"$Width$\") are unchanged. The unrelated `pickedLineContains()` helper added in the same commit (src/Menus.cpp:1795-1808, used at 1953-1973) is a different finding's fix and does not feed this chain — note it reads `previousMenuSelection[]`, not `currentAction.previousMenuPositions[]`, so it is not a drop-in substitution here.

CORRECT FIX: wrap the whole Width/Height/Rotation chain in one depth check rather than guarding a single arm, e.g. `if ( currentAction.previousMenuIndex > 2 && currentAction.previousMenuPositions[2] >= 0 ) { ...existing chain... }`. The `previousMenuIndex > 2` half is required to also kill the stale-index path (issue 3).

</details>

## #5 — INCOMPLETE — setGPIO() erases the bus's gpioState-6 marks and re-asserts config pulls on the live soft-I2C pins

- **[high]** `src/routing/States.cpp:2934` — applyStateToHardware() reproduces the ORIGINAL BUG verbatim and was not guarded. Its loop (States.cpp:2934-2982) is a copy-paste of setGPIO()'s body — gpio_set_dir(gpio_pin, false), gpio_set_pulls(gpio_pin,false,true) for gpioPulls==0, and gpioState[i]=4 — with NO gpioState==6 skip (and no gpioPwmEnabled / gpioPythonOwned skips either). Slot load runs States.cpp:3418 refreshConnections(-1,1,1) (now correctly guarded) and then States.cpp:3425 applyStateToHardware() eight lines later, which immediately re-stomps what setGPIO just skipped. With the config defaults (States.cpp:515-516 direction=1/pulls=0) and the display bus live on GP24/25 (DisplayBus.cpp:19-20), a slot load puts a PULLDOWN on SDA's ACK window (sdaRelease at DisplayBus.cpp:52-55 reads the pull, so a NAK now reads as ACK) and re-stamps gpioState[4]=4, which sends core-2 readGPIO back down the gpioReadWithFloating() pull-twiddle path (Peripherals.cpp:1372) mid-transaction. This is literally the 'state load after attach' trigger named in the original finding. Reachable from States.cpp:3425, 3459, 3642, 3670, 4024, 4435 and SingleCharCommands.cpp:4184.
- **[high]** `src/remembering/PersistentStuff.cpp:659` — readSettingsFromConfig()'s GPIO loop (PersistentStuff.cpp:659-718) is the second unguarded sibling: gpio_set_dir(gpio_pin,false) + gpio_set_pulls(gpio_pin,false,true) + gpioState[i]=4 for the default input/pulldown config, with no gpioState==6, gpioPwmEnabled, or gpioPythonOwned skip. It runs on EVERY config save — configManager.cpp:2411 is inside saveConfig() (configManager.cpp:2372) — plus configManager.cpp:967, 2746, 3649, 3917. So changing any setting (brightness, a debug flag, anything that trips configChanged) while a display is attached reproduces the full original scenario on the live soft-I2C pins. This path is far more frequent than the slot load.
- **[medium]** `src/displays/DisplayBus.cpp:186` — NEW consequence of the guard: a stale gpioState==6 now permanently excludes a routable user pin from setGPIO(). displayBusRelease() returns early on a user-claimed detach (DisplayBus.cpp:183-188) deliberately leaving gpioState[4]/gpioState[5]==6; jl_gpio_release_all_pins() (JumperlessMicroPythonAPI.cpp:850-856) then clears gpioPythonOwned but never touches gpioState. After a script touches machine.Pin(24) during a display attach and exits, GP5/GP6 sit at state 6 forever: setGPIO()'s new continue at Peripherals.cpp:796 means refreshConnections never re-asserts their configured direction/pulls again, and readGPIO takes the plain gpio_get branch (Peripherals.cpp:1362) so floating detection is silently lost on those pins. Old code self-healed on the next refresh. (The two unguarded siblings above happen to clear it on the next slot load / config save, so it only wedges until one of those fires.) Same shape for a PWM claim (displayBusUserClaimed, DisplayBus.cpp:200-207) once PWM is later disabled.
- **[low]** `src/oled.cpp:4596` — Second stale-mark path, user-visible behavior change vs. old code: oled::connect() stamps gpioState[sda-20]=gpioState[scl-20]=6 unconditionally (oled.cpp:4322-4329), but disconnect() only clears them back to 4 when !oledUsingHardwiredPins (oled.cpp:4588-4601). For connection_type 1/2 (oledUsingHardwiredPins true, pins 26/27 -> indices 6/7 per configManager.cpp:410-411,443-444) the marks are never cleared. Before this fix setGPIO() overwrote them every refresh; now GP7/GP8 keep state 6 permanently after the first OLED connect, so setGPIO() stops applying their configured dir/pulls and readGPIO stops doing floating detection on them even after the panel is unplugged. Same for initI2C()'s own marks at Peripherals.cpp:696/700, which nothing ever clears.
- **[low]** `src/displays/DisplayBus.cpp:176` — Narrow acquire race the guard does not cover: displayBusAcquire() does pinMode(INPUT_PULLUP) at DisplayBus.cpp:167-168 and only stamps gpioState=6 at :176-177, with no memory barrier (contrast jl_gpio_claim_pin's __dmb, JumperlessMicroPythonAPI.cpp:823). A setGPIO() pass on the other core landing inside that window still applies the config pulldown and stamps state 4, and nothing re-runs acquire's pinMode afterward — the pull stays wrong until the next detach/attach. Microsecond-wide and pre-existing, noted for completeness.

<details><summary>checker notes</summary>

FIX FOUND (commit 57cf63d), two hunks, both present in the current tree:
- src/Peripherals.cpp:790-798 — `if (gpioState[i] == 6) continue;` in setGPIO()'s loop, placed alongside the existing gpioPwmEnabled (778) and gpioPythonOwned (786) skips. gpioDef[i][2]==i for all 10 entries (Peripherals.h:167-178), so the bare `gpioState[i]` index is correct and matches the writers at Peripherals.cpp:696/700 and DisplayBus.cpp:176-177.
- src/Peripherals.cpp:1319-1321 — readGPIO()'s gpioNet==-1 branch no longer stamps state 4 over a 6, so the mark survives the acquire-to-route window and readGPIO keeps taking the plain gpio_get branch at Peripherals.cpp:1362 instead of gpioReadWithFloating (1372).

Is the original scenario dead? Only the setGPIO half. refreshConnections/refreshBlind/fast-refresh/probe-wire (Commands.cpp:431/547/666, Probing.cpp:4485, JsonState.cpp:764) now leave the live bus alone. But the "state load" trigger the finding itself names is still live via applyStateToHardware(), and saveConfig()->readSettingsFromConfig() adds a more frequent trigger. Both run the identical three operations on the same pins. Fix is incomplete -> fix_correct=false.

Already-correct siblings (checked, no action needed): updateGPIOConfigFromState() has `case 6: // do nothing` (PersistentStuff.cpp:534-535); updateStateFromGPIOConfig() only ever runs with onlyIdx from the probe's explicit GPIO-direction menu (Probing.cpp:5485/5495), an intentional user override.

BOARD TARGETS: V5 only, and correctly so. setGPIO() returns at Peripherals.cpp:764-770 under OG_JUMPERLESS, so the original bug never existed on OG and the new guard is inert there. displayBusAcquire() refuses on boards without caps.breadboardDisplays (DisplayBus.cpp:141-144), so OG never sets the soft-bus marks. applyStateToHardware and readSettingsFromConfig are both #if !defined(OG_JUMPERLESS). The readGPIO hunk is not OG-guarded, but on OG the only state-6 writer left is initI2C via oled.cpp, and setGPIO isn't there to correct it — cosmetic at worst. No OG regression.

COLLATERAL: the guard preserves old behavior on every path where a 6 is set and later cleared by its owner (DisplayBus.cpp:191-192, oled.cpp:4597-4600). It changes behavior only where a 6 is left stale — issues 3 and 4 — because old code silently repaired those every refresh and new code does not. Nothing else in setGPIO changed; the gpio_put(pin, gpioState[i]) output path (Peripherals.cpp:842-844) is unreachable for a state-6 pin either way.

SUGGESTED COMPLETION: lift the three skips (gpioPwmEnabled / gpioPythonOwned / gpioState==6) into one shared predicate and call it from States.cpp:2934 and PersistentStuff.cpp:659 as well; and have displayBusRelease/jl_gpio_release_all_pins clear a leftover 6 so the mark can't outlive its owner.

</details>

## #jeopixel — INCOMPLETE — JeoPixel DMA rewrite

- **[high]** `lib/Jadafruit_NeoPixel/JeoPixel.h:278` — NEW DEFECT (clamp removal). `return (int32_t)(micros() - endTime) >= 300;` with Adafruit's rollover clamp deleted. Once elapsed reaches 2^31 us (35.79 min) the signed value goes negative and canShow() reports busy until elapsed reaches 2^32 (71.58 min) -- a ~35.8 min dead window that then REPEATS every 71.58 min forever. This is not a rare edge: it is the DEFAULT V5 path. probeLEDs.endTime is armed exactly once, by the boot-time probeLEDs.show() at src/LEDs.cpp:607, and is never refreshed again, because with probe_led_on_button_pin=true (default, src/config.h:80) probeLEDhandler takes the merged branch at src/Probing.cpp:8115-8121 (probeMergedPutColourIfChanged -> raw pio_sm_put, never touches endTime) and never reaches probeLEDs.showBlocking() at src/Probing.cpp:8125. On OG the whole probeLEDs.begin()/show() block is compiled out (V5-only #if, see src/LEDs.cpp:585-608) so endTime is BSS 0 and the window is uptime [35.79, 71.58] min exactly. Consequence at src/Probing.cpp:1273: checkProbeButtonHardware()'s 600us bounded wait on probeLEDs.canShow() expires on every service tick and returns 0 before the PIO fast path, so the probe button reads as 'not pressed' for ~36 minutes out of every ~71.6, on BOTH targets. Repro: leave the unit powered 36 min, press the probe button.
- **[high]** `lib/Jadafruit_NeoPixel/JeoPixel.cpp:251` — NEW DEFECT (consequence of the same clamp removal, at the unbounded callers). show() spins `while (!canShow());` and showBlocking() does the same at JeoPixel.cpp:316. Both are unbounded, and there is NO watchdog anywhere in the tree (grep watchdog_enable|watchdog_update|rp2040.wdt over src/ returns zero hits). Any strip whose previous show() landed 35.79-71.58 min ago wedges the calling core for up to ~36 minutes on its next show(). Reachable: (a) with probe_led_on_button_pin=0 probeLEDhandler early-returns when no colour is requested (src/Probing.cpp:7944), so probeLEDs goes quiet on an idle bench and the first probe interaction after ~36 min calls showBlocking() at src/Probing.cpp:8125 -- on core 1 (main.cpp:2048 is inside loop1, main.cpp:1538); (b) a merged->legacy flip via setProbeLedMerged() (src/Probing.cpp:1119) at >35.8 min uptime hits the same call; (c) bbleds/topleds normally refresh every ~51ms via the swirl pass, but needsLedShow is forced false while ledGraphicsOwned() holds or while parked in a click menu with no transition frame due (main.cpp:1806-1930), and leds.show() at main.cpp:1956 then wedges core 1. Note ledClass::show (src/LEDs.cpp:174-198) guards only on isDMABusy() to drop frames -- it never checks canShow(), so it walks straight into the spin.
- **[medium]** `lib/Jadafruit_NeoPixel/JeoPixel.h:265` — NEW DEFECT (new FIFO gate + unbounded entry spin). The added `!pio_sm_is_tx_fifo_empty(pio, pio_sm)` gate makes canShow() fail-closed on a condition that can never clear if the state machine stops consuming. showBlocking()'s BODY is deadline-bounded at JeoPixel.cpp:346-360 and 374-381 precisely because the Jumperless probe LED shares its SM with the button-polling program and a mid-show swap leaves the SM not draining the TX FIFO (documented as hardware-confirmed core-1 wedge). Its ENTRY spin at JeoPixel.cpp:316 is not bounded. If the SM is swapped to the button program while 1-8 words remain in the joined TX FIFO, DMA reads idle, the FIFO never empties, and the entry spin never exits -- reintroducing exactly the wedge the deadline loops were added to prevent. Pre-diff canShow()'s only DMA gate was dma_channel_is_busy, which the swap also stalls, but only during an active transfer; the new gate extends the wedgeable window to the post-transfer drain.
- **[low]** `lib/Jadafruit_NeoPixel/Jeopixel_RP2.cpp:286` — NEW DEFECT (latch never armed for a zero-length strip). `dma_awaiting_latch = true` at line 289 sits inside `if (numBytes > 0)`, and show() at JeoPixel.cpp:295 deliberately skips `endTime = micros()` whenever use_dma && dma_channel >= 0. With numLEDs == 0 neither the flag nor endTime is updated, so endTime stays at whatever the last real frame left and canShow() returns true immediately. Harmless today (nothing is on the wire), but it is the one path through the new handshake that silently produces no latch state at all, and it also means a zero-length strip's endTime can age into the rollover window described above.
- **[low]** `lib/Jadafruit_NeoPixel/JeoPixel.h:269` — NEW DEFECT (unsynchronized cross-core write in an inline header). canShow() now WRITES two non-volatile members -- endTime at line 269 and dma_awaiting_latch at line 270 -- but is called from two cores on the same object: src/Probing.cpp:1273 calls probeLEDs.canShow() from the button service while core 1's probeLEDhandler (src/main.cpp:2048, inside loop1) drives the same probeLEDs. No barrier, no volatile, no lock. Benign today only because probeLEDs never takes the DMA path (it is driven by showBlocking(), so dma_awaiting_latch is never true for it) -- i.e. it is latent, not safe by construction. Pre-diff canShow() was read-only and had no such hazard.

<details><summary>checker notes</summary>

VERDICT: fix_present = true, fix_correct = false. The frame-shift handshake itself is sound and is enforced for every caller; the clamp removal is a deterministic, high-severity regression that must be repaired before this ships.

(1) HANDSHAKE CORRECTNESS PER CALLER. Grepped every canShow()/show()/showBlocking() site in src/ and lib/. The mechanism is right: DMA-idle + TX-FIFO-empty + 300us is a strictly stronger wire-idle test than the old dma-busy-only check, and it resolves the real contradiction in the previous committed state (endTime projected numBytes*10us into the future at JeoPixel.cpp:290 while canShow()'s `if (endTime > now) endTime = now;` clamp nullified that projection on the very next poll -- the projection was dead code). Callers:
  - JeoPixel.cpp:251 show() and JeoPixel.cpp:316 showBlocking() -- unbounded spins, wedgeable (issues above).
  - src/LEDs.cpp:174-198 ledClass::show -- drops frames on isDMABusy() but never consults canShow(), so the frame-drop intent does not cover the latch spin; called from core 1 at src/main.cpp:1956.
  - src/Probing.cpp:1273 -- the ONLY bounded caller (600us deadline). It fails closed (returns 0 = "no button"), which is why the rollover manifests as a silently dead probe button rather than a crash.
  - src/Probing.cpp:8125 showBlocking, core 1; src/LEDs.cpp:607 boot show, core 1 setup.
  Type check: pio_sm is `uint` (JeoPixel.h:437), so canShow()'s `pio_sm != (uint)-1` at line 265 is correct.

(2) ROLLOVER REACHABILITY -- worse than the stated concern. Not "if no show() happens for 36 min" but "guaranteed, on the default V5 configuration, forever". Merged probe-LED mode (default) arms probeLEDs.endTime once at boot and never again; OG never arms it at all. Arithmetic confirmed: elapsed in [2^31, 2^32) us => int32 negative => canShow() false, i.e. uptime window [T+35.79min, T+71.58min], recovering ~2s after each micros() wrap and repeating. bbleds/topleds are refreshed every ~51ms by the swirl pass so they are usually safe, but the ledGraphicsOwned() / click-menu suppression paths in main.cpp:1806-1930 can starve them past the threshold. No watchdog exists to recover a wedged core.

(3) BOUNDED CLAMP IS THE RIGHT MINIMAL REPAIR -- but it must use a SIGNED ahead test. The obvious unsigned form is itself a bug: `if ((uint32_t)(endTime - now) > 10000) endTime = now;` fires in NORMAL steady state (endTime 500us in the past gives 2^32-500 > 10000), clamping endTime = now on every call and making canShow() permanently false -- worse than what it fixes. Correct form:
    uint32_t now = micros();
    if ((int32_t)(endTime - now) > 10000) endTime = now;  // legit arm is +12us; anything further ahead is wrap staleness
    return (int32_t)(now - endTime) >= 300;
  Verified against four cases: legit arm (+12, no clamp, latch runs); normal past (-500, no clamp, true); stale 40 min (ahead +1.895e9, clamp, 300us extra then true); stale 90 min (ahead negative, no clamp, immediately true).
  CLEANER ALTERNATIVE: at JeoPixel.h:269 arm `endTime = micros()` (drop the +12), compare `>= 312`, and restore Adafruit's original clamp verbatim. endTime is then never legitimately in the future, so stock semantics apply unchanged and the special-case comment disappears.
  SEPARATELY, bound the two spins at JeoPixel.cpp:251 and :316 with a deadline exactly as showBlocking's body already does (JeoPixel.cpp:346-360). The clamp fixes this trigger; the deadline fixes the whole class, including the SM-swap wedge in issue 3.

(4) REST OF THE DIFF IS GOOD. The prefill removal at Jeopixel_RP2.cpp:276-290 is correct and I verified its premises: the DMA channel IS DREQ-paced (Jeopixel_RP2.cpp:74, channel_config_set_dreq(pio_get_dreq(pio, pio_sm, true))), so the 4-word prefill was never load-bearing against underrun; the old code's fixed +4 DMA offset combined with a conditional prefill (`if (!pio_sm_is_tx_fifo_full)`) genuinely dropped up to 4 words whenever the FIFO was not empty -- a real frame shift, now gone. The comment's claim that the ws2812 program stalls with the pin LOW is correct for the standard ws2812.pio (autopull stall at `out x, 1 side 0`), so a startup underrun only lengthens the reset.

PRE-EXISTING, NOT FROM THIS DIFF (flagged for the ledger, deliberately excluded from `issues`):
  - Latency: every DMA show() now burns ~312us of spin, because the latch clock starts only when someone polls canShow() rather than when the wire actually went idle. The "latch elapses while you generate the next frame" property is gone. The pre-diff committed state paid the same cost by a different route (projection + clamp), so this is not a regression, but it is ~0.6ms/frame of pure core-1 spin across bbleds+topleds.
  - showBlocking()'s three early returns (JeoPixel.cpp:322, 353, 379) all skip the `endTime = micros()` at JeoPixel.cpp:389, leaving a stale endTime so the next frame gets no latch.
  - JeoPixel.cpp:322 `if (!pio || (pio_sm < 0))` is a dead guard: pio_sm is unsigned (JeoPixel.h:437), so the second test is always false.
  - The double-buffer pending path in rp2040Show (Jeopixel_RP2.cpp:155-200) is dead weight -- the next call swaps the buffer then re-copies from pixels[] anyway, so the queued frame is never what gets sent.
  - ledClass::show's asymmetric guards (src/LEDs.cpp:182-196): topleds gates on both strips' isDMABusy(), bbleds only on its own.

BOTH TARGETS: covered identically. lib/Jadafruit_NeoPixel is compiled by both envs -- jumperless_og extends jumperless_v5 (platformio.ini:186-187), lib_ignore is only FatFSUSB/FatFS (platformio.ini:110), and build_src_filter differences touch src/ only. Every changed line is under `#if defined(ARDUINO_ARCH_RP2040)`, which arduino-pico defines for RP2040 and RP2350 alike, and pio_sm_is_tx_fifo_empty exists in both SDKs. OG compile NOT executed: `pio run -e jumperless_og` aborts with "Python version must be between 3.10 and 3.13. Current: 3.14.7" (the known platform gate). Compile risk is near zero -- the only new symbols are a bool member and an SDK function present on both parts -- but the OG binary is unverified.

FILES: /Users/kevinsanto/Documents/GitHub/JumperlOS/lib/Jadafruit_NeoPixel/JeoPixel.h, /Users/kevinsanto/Documents/GitHub/JumperlOS/lib/Jadafruit_NeoPixel/JeoPixel.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/lib/Jadafruit_NeoPixel/Jeopixel_RP2.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/src/Probing.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/src/LEDs.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/src/main.cpp

</details>

## #10 — INCOMPLETE — Power auto-routing adopts pre-existing user bridges; removing the part (or Clear parts) then silentl

- **[medium]** `src/displays/DisplayService.cpp:73` — Sibling adoption path the fix skipped. routeDataPins writes p.pins[sdaIdx].connect = nodeForRpPin(inst.sdaPin) (=RP_GPIO_26/137) and .connect for SCL with no check for a pre-existing bridge, then applyPartPlacement dedups it away at PartPlacement.cpp:360 (added=0, err empty -> treated as success at DisplayService.cpp:82). If the user had already hand-wired that row to GP26/GP27 (exactly what the OLED Connect app at Apps.cpp:1835-1837 creates, and which that app correctly tracks via addedSda/addedScl), the display part now owns the user's wire; jl_remove_part (JumperlessMicroPythonAPI.cpp:2101) or Parts>Clear (PartsApp.cpp:593) then deletes it silently. Same mechanism, same silence, no user typing required (750 ms auto-attach poll). Also bites inside routeDataPins itself: the refusal branch at :85-86 sets connect=-1 and re-applies AFTER removePartPlacement at :71 already pulled the adopted user bridge. Both targets (file is in both build filters).
- **[low]** `src/PartsApp.cpp:403` — rowNetHas is a net-membership proxy for a bridge-level fact, so it can read false while the direct bridge exists, reopening adoption. Two reachable divergences: (a) addNodeToNet now refuses when a net's nodes[] is full (same commit 57cf63d) - OG's MAX_NODES is 24 (JumperlessDefines.h:192), so a >=24-node GND bus drops later rows from the net while connections.bridges still holds row<->GND, giving rowPowered=false, pin.connect=GND, dedup at PartPlacement.cpp:360, and deletion of the user's wire on removal; (b) rowNetHas bails on net.number != netNum (PartsApp.cpp:388), which is a guard against findNodeInNet's gpio/adc fallbacks but also fails open (returns false -> auto-route proceeds) rather than closed. The fix_sketch's own predicate, st.hasConnection(node, candidateRail), is exact and was not used.
- **[low]** `src/routing/PartPlacement.cpp:407` — removePartPlacement still deletes any existing node<->pin.connect pair with no ownership record, so every remaining connect: writer inherits the bug. jl_place_part (JumperlessMicroPythonAPI.cpp:1919) accepts connect: from pins_json unchecked; a slot YAML part can carry connect: GND on a row the bridges: section also wires. In both cases remove_part/Clear deletes a bridge the part never created. Lower severity than the auto-route case because the connect: value is caller-supplied rather than machine-invented, but it is the same silent-deletion mechanism and the reason the fix had to be repeated per-caller.

<details><summary>checker notes</summary>

FIX PRESENT, at a different location than reported. src/PartsApp.cpp:395-418 (commit 57cf63d), not src/routing/PartPlacement.cpp:360, which is unchanged since pre-audit commit 7b6c870.

ORIGINAL SCENARIO IS DEAD. User hand-wires row 5 -> TOP_RAIL, then places a part whose role-VCC pin lands on row 5: findNodeInNet(5) returns the TOP_RAIL special net (rails absorb bridged rows), rowNetHas(5, TOP_RAIL) is true (PartsApp.cpp:383-394), rowPowered short-circuits at :412, pin.connect stays -1. Nothing is adopted, so removePartPlacement/partsClearAll cannot delete it. Same for the gnd-class twin at :415. Verified for the part-to-part variant too: partsCommitPlacement calls refreshConnections(-1) at :478 after every success, so part B cannot adopt part A's freshly-added rail bridge through a stale net table.

BOTH TARGETS COVERED. PartsApp.cpp has no board conditionals and jumperless_og's build_src_filter (platformio.ini:257-260) subtracts only boards/v5/ and routing/NetsToChipConnections.cpp. Same code compiles for RP2350 and RP2040.

MECHANISM CORRECTNESS. rowNetHas terminates its nodes[] scan on !=0, which matches the actual sentinel (nets cleared to {} at MatrixState.cpp:663, zero-filled) and the in-tree precedent at NetManager.cpp:36. tmp.pins[j] <-> po.pins[j] index alignment holds: partdbInstantiate (PartDb.cpp:162-207) memsets and copies in order, so po.pins[j].role at PartsApp.cpp:411 reads the right pin; the j >= po.numPins guard is dead but harmless (tmp.numPins = min(po.numPins, MAX_PART_PINS)). memset also zeroes placement -> EXPANDED, so partPinCompactEligible's connect >= 0 requirement is not in play on this path.

WHY fix_correct=false. The fix was applied at one caller instead of at the shared mechanism, and DisplayService::routeDataPins is a live second auto-router that writes connect over a possibly pre-existing user bridge with no equivalent guard. See issue 1.

COLLATERAL: CLEAN. The bundled role gate (PartsApp.cpp:411) narrows rail auto-routing to role==PARTDB_ROLE_VCC; the DB has 73 pins at pinClass=1/role=1 (still auto-routed) and exactly 6 at pinClass=1/role=0 - three VEE (PartDbData.h:576,791,808) and three VOUT (:937,942,945), precisely the regulator/charge-pump pins the separate finding names. No general part loses power routing. The gnd branch widened from "skip if row net has a rail" to "skip if row net has any power node" (was PartsApp.cpp old :382), which changes only whether connect: GND is RECORDED - the resulting board wiring is identical, since the bridge was deduped away before. Visible difference is cosmetic (list_parts / slot YAML show the pin unbound) plus the deliberate consequence that the part will not re-assert a GND bridge if the user later pulls their own wire; PartLabels' VCC_TO_GND/GND_TO_HOT warnings key on pinClass and net membership (PartLabels.cpp:234-255), not connect, so they still fire. No behavior regression found in the non-buggy old paths.

</details>

## #16 — INCOMPLETE — Parts-table mutations are invisible to undo: undone power bridges resurrect on reboot, and undoing C

- **[medium]** `src/JumperlessMicroPythonAPI.cpp:2041` — jl_place_part (MicroPython place_part()) appends to the parts table and calls applyPartPlacement with NO UndoIngestGuard, so the exact desync the PartsApp fix closed is still live through the scripting door. The bridge halves reach undo via States.cpp:659 (addConnection -> undoRecordConnect); the parts-table append records nothing. Repro on V5: place_part("R1", 10, '{"A": {"pin": 1, "connect": "TOP_RAIL"}, "B": {"pin": 2, "connect": "GND"}}') then press undo twice -- the rail/GND bridges open, the entry keeps connect=TOP_RAIL/GND with placed=true, markDirty at PartPlacement.cpp:390 makes the idle auto-save serialize it, and expandPartsToBridges (States.cpp:3402 / :3656) re-adds the undone bridges on the next boot. Identical to original scenario 1. The fix's own comment at PartsApp.cpp:459-462 declares placements non-undoable; this caller violates that contract. Fix: an UndoIngestGuard spanning lines 2041-2065 (the append, applyPartPlacement, the unwind, and refreshConnections).
- **[medium]** `src/JumperlessMicroPythonAPI.cpp:2101` — jl_remove_part (MicroPython remove_part()) is the mirror gap: removePartPlacement's per-bridge removals ARE recorded (States.cpp:703 -> undoRecordDisconnect) while the table compaction at 2103-2106 is invisible to undo. remove_part("R1") then undo re-adds R1's rail/GND bridges for a part that no longer exists in the table -- orphan power bridges the user never made, persisted into the bridges: section by the next auto-save and kept across reboot. Original scenario 2, different door. Same fix: UndoIngestGuard covering 2100-2116.

<details><summary>checker notes</summary>

FIX FOUND (commit 57cf63d). Approach taken = the fix_sketch's second option: suppress recording, parts declared outside undo. src/PartsApp.cpp:23 includes Undo.h; UndoIngestGuard at src/PartsApp.cpp:463 (partsCommitPlacement) and src/PartsApp.cpp:590 (partsClearAll). Suppression gate verified at src/remembering/Undo.cpp:2134 and :2228 (g_ingestDepth > 0 -> record* is a no-op); depth counter at :226, begin/end at :1945-1957.

ORIGINAL SCENARIO IS DEAD on the UI path. Guard scope is correct in both functions: declared after all early-return validation (no state touched before it), and it lives to function return, so it also covers the failure rollback (PartsApp.cpp:470-476) and the in-scope refreshConnections(-1) at :478 / :604. partsClearAll's guard sits after the blocking CONNECT-confirm loop (:567-585), so a cancel never enters ingest. Rollback arithmetic at PartsApp.cpp:473 is a single decrement and is correct -- removePartPlacement (src/routing/PartPlacement.cpp:395-420) only pulls bridges and clears placed, it never touches numParts.

BOTH TARGETS. OG is satisfied trivially, not accidentally: UNDO_ENABLED is forced to 0 for OG_JUMPERLESS (src/JumperlessDefines.h:65-73), so the whole subsystem compiles out (src/remembering/Undo.cpp:2797-2856 stub block, undoBeginIngest/undoEndIngest at :2822-2823) and there is no undo ring to desync. UndoIngestGuard (src/remembering/Undo.h:193-198) sits outside any UNDO_ENABLED gate, so both envs compile; on OG it is a zero-cost no-op. V5 is where the bug lived and where the remaining gap lives.

ATTACK RESULTS. Enumerated every parts-table mutation site (numParts++/--/=0): src/PartsApp.cpp:458/473/596 (guarded), src/routing/PartPlacement.cpp:722 and :846 (fromYAML, already inside the slot-load ingest region, src/main.cpp:1396/1490), and src/JumperlessMicroPythonAPI.cpp:2041/2051/2106 (UNGUARDED -- the two issues above). DisplayService::routeDataPins is guarded (src/displays/DisplayService.cpp:65). Guide/StepViewer/PartLabels/SingleCharCommands touch the table read-only. Repo-wide grep for undoBeginIngest/UndoIngestGuard finds exactly four sites: main.cpp's loadfile region plus the three RAII guards -- nothing wraps MicroPython execution, so the MP path is genuinely unsuppressed.

COLLATERAL: acceptable, and it is the sanctioned tradeoff. Behavior change a user can notice: pressing undo right after placing a part no longer removes that part's bridges -- undo now reaches past the placement to the previous user action. That is the fix_sketch's explicit "document parts as outside undo" option, and it is visible rather than silent because the undo toast labels the txn it undid (undoFormatOpLabel, src/remembering/Undo.cpp:934+). No desync in the blob direction: CLEAR_ALL/BLOB_REPLACE snapshots are full globalState.toYAML() including the parts: section (Undo.cpp:276-283, :319, :394), so undoing a clear-all restores table and bridges together. Checked and dismissed as non-issues: undoEndIngest's g_lastOpMs reset (Undo.cpp:1955) is inert because implicit txns close immediately in recordOneAction; non-atomic g_ingestDepth across cores is the pre-existing main.cpp loadfile pattern, not introduced here; clearAllConnections (States.cpp:719) not clearing the parts table is pre-existing and not an undo path.

ADJACENT, NOT #16: src/JumperlessMicroPythonAPI.cpp:2056 treats non-empty applyErr as a mere warning and still returns 0 -- the same refusal-as-success class this audit fixed at src/PartsApp.cpp:470 and in DisplayService::routeDataPins (DisplayService.cpp:82). Same forgotten caller, separate finding.

</details>

## #20 — STILL-LIVE — routeDataPins refusal path thrashes remove/reapply + markDirty every 750 ms indefinitely - idle auto

- **[medium]** `src/displays/DisplayService.cpp:92` — The markDirty half of the fix is inert. `if (priorSda != -1 || priorScl != -1) st.markDirty();` guards only DisplayService's OWN call, but the refusal cycle already dirties the state several times through nested calls: removePartPlacement sets p.placed=false + markDirty (src/routing/PartPlacement.cpp:415-418), the failing applyPartPlacement sets p.placed=true + markDirty (src/routing/PartPlacement.cpp:388-391), and every st.removeConnection/st.addConnection inside expandOnePart calls markDirty (src/routing/States.cpp:653, 700). So on a standing refusal the state goes dirty on EVERY retry regardless of the guard, the SlotManager idle auto-save (src/routing/States.cpp:4789-4806) re-serializes and writes the slot YAML, and 5 s later it is dirty again. USE_FILE_CACHE defaults to 0 (src/remembering/FileCache.h:45-46, nothing sets it in platformio.ini), so the content-dedup at src/remembering/FileCache.cpp:1104-1117 is compiled out and safeFileWriteAll falls through to safeFileWriteAllRaw (src/remembering/FilesystemStuff.cpp:4476-4481) - a real synchronous ~70 ms core-pausing flash write per pass, indefinitely. The original finding's harm survives; only the cadence changed (750 ms -> 5 s after 5 strikes).
- **[medium]** `src/displays/DisplayService.cpp:88` — Restore path discards err2 - the fix_sketch's third item was never implemented, and the discarded error hides real routing corruption. expandOnePart does NOT stop at the first refusal (src/routing/PartPlacement.cpp:346-368): it keeps going and skips duplicates. So with a full bridge table and SDA/SCL ahead of another connect-bearing pin, the failing apply at line 81 ADDS the SDA/SCL bridges and fails on the later pin; line 85-86 then sets those connect fields to -1 and re-applies, so the just-added GP20/GP21 bridges are now untracked - removePartPlacement skips pins with connect<0 (PartPlacement.cpp:404) and can never free them, while toYAML persists them. Result: phantom breadboard-to-GP routes the user cannot see in the part or delete with it. Symmetrically, if the restore apply cannot re-add the part's VCC/GND bridges, p.placed stays true with bridges missing and nobody is told.
- **[low]** `src/displays/DisplayService.cpp:85` — priorSda/priorScl are captured (lines 66-67) but used only for the dirty test, never for the actual restore: the refusal path hardcodes connect = -1, so a user-authored `connect:` on the part's SDA/SCL pin is destroyed rather than restored - and that is precisely the one case where the line-92 guard does fire, so the loss is persisted by the next auto-save. Pre-existing behavior, but the fix introduced the saved values and stopped one line short of using them.
- **[low]** `src/displays/DisplayService.cpp:156` — routeFails is uint8_t (src/displays/DisplayService.h:64) and is incremented unclamped; nothing resets it on the failure path. After 256 consecutive refusals (~21 min at the 5 s cadence) it wraps to 0, so `routeFails >= 5` goes false for four polls - the cadence snaps back to 750 ms - and `++routeFails == 5` re-prints "DISPLAY routing keeps failing", defeating the warn-once intent. Clamp (e.g. `if (routeFails < 255) routeFails++;`).
- **[low]** `src/displays/DisplayService.cpp:126` — Collateral from moving the framebuffer alloc ahead of routing: a part that resolves to a driver but has no pins named SDA/SCL makes routeDataPins return false at line 55 before any routing work, yet attach() has already allocated fb + shadow (~1.5 KB) and immediately frees them at lines 149-150. That alloc/free repeats every poll forever (750 ms, then 5 s). Pre-fix this configuration never allocated. Cheap but permanent heap churn; a `sdaIdx/sclIdx` pre-check before the alloc would restore the old behavior.

<details><summary>checker notes</summary>

FIX LOCATED: commit 57cf63d, src/displays/DisplayService.cpp:66-95 (routeDataPins refusal path) + 152-163 (attach backoff) + DisplayService.h:64 (routeFails).

WHICH HALF LANDED: the retry backoff and warn-once are real - after 5 straight refusals attach() prints one line and pushes nextPartsPollMs to millis()+5000 (lines 156-160), which correctly overrides pollParts' own `now + 750` set at line 225 because attach runs after it. detach() resets routeFails/initFails/acquireWarned/allocWarned (lines 208-215). Undo pollution is closed by UndoIngestGuard (line 64; guard only touches undo ingest depth, not dirty - src/remembering/Undo.h:193-197).

WHICH HALF IS INERT: the "only markDirty when the fields actually changed" guard (line 92). Nested markDirty in PartPlacement/States makes the state dirty every cycle anyway, so the finding's headline symptom - dirty -> idle auto-save -> slot YAML rewrite, forever - is alive at 5 s instead of 750 ms. That is why fix_present=false: the loop is 6.7x slower, not gone.

SEVERITY CONTINGENCY: the per-pass FLASH write depends on USE_FILE_CACHE staying 0 (current default). If the write-back cache is re-enabled, FileCache.cpp:1104-1117 dedups the identical YAML and the flash write disappears - but the dirty churn and the ~ms toYAML on every idle pass remain, as does the remove/reapply bridge churn.

OG TARGET: bug is unreachable on OG. service() returns IDLE before pollParts when !caps.breadboardDisplays (DisplayService.cpp:298); board_og.cpp:114 = false, board_v5.cpp:117 = true. routeDataPins/attach/pollParts are private and have no callers outside DisplayService.cpp (grepped src+lib). So the fix is V5-only by construction and that is correct here - no OG gap.

ATTACK PASSES THAT CAME BACK CLEAN: attachedName identity check is safe (PartDefinition::name is char[16], States.h:440, so strncpy(...,15) never truncates a valid name and the strncmp at line 231-233 cannot false-positive); modal reentrancy is gated by jOS.inModalContext() (line 223); backoff/poll-deadline write ordering is correct; expandOnePart is duplicate-idempotent via hasConnection (PartPlacement.cpp:360); the already-routed early return at line 59-61 correctly short-circuits after a reload.

</details>

## #24 — INCOMPLETE — Canceling the Rails voltage slider drives both rails to 0V and persists it

- **[medium]** `src/Menus.cpp:4248` — NEW defect from the fix: cancel no longer converges state to hardware when the slider sat outside the live-apply window. The slider writes globalState.power.topRail/bottomRail unconditionally (src/Menus.cpp:3101-3108, 3311-3318) but only pushes the rails to hardware for 0.0<=v<=5.0 (3111, 3321). Confirm converges via setTopRail/setBotRail at 4257-4274; the new isnan() arm at 4248-4250 applies nothing at all. Trigger: Rails > Both, drag to 7.0 V (or -2.0 V; clearAction zeroes optionVoltage at :655 so min/max are the DAC limits, default +/-8.00, src/config.h:106-107), then long-press / probe-button / any serial byte to cancel. End state: globalState.power says 7.0 V, the rails physically sit at the last <=5 V value, OLED still shows ' 7.0 V', and railCommitEdit() markDirty()s (:2946) + records an undo (:2955-2958) so the bogus value is written to the slot file. Worst consequence is deferred: the next boot / slot load hands that persisted 7.0 V to setRailsAndDACs and the rail jumps to a voltage the user never saw applied, onto a powered DUT. Pre-fix, cancel overwrote both rails with 0.0 V - the reported bug, but it left state and hardware agreeing; no pre-fix path persisted a state/hardware mismatch. Fix: on the isnan arm, re-apply the value the slider actually holds (setTopRail/setBotRail with globalState.power.topRail/bottomRail) instead of skipping the apply entirely.
- **[low]** `src/Menus.cpp:4449` — Hardening gap, no reachable trigger found: the fix taught only ONE of the two currentAction.analogVoltage consumers about NAN. The OUTPUTACTION > Voltage arm reads it unguarded at 4449, 4455, 4463 and 4468 (globalState.power.dac0/dac1/topRail/bottomRail), then markDirty()s at 4489-4491 and pushes it to hardware via setRailsAndDACs() at 4478 - a NaN there would corrupt the DAC state and the saved slot. Every cancel path I could trace arrives safe: the Output>Voltage slider is dispatched from src/Menus.cpp:1488, which returns doMenuAction() immediately, and to[] is only filled by selectNodeAction (src/Menus.cpp:2835-2843) which runs solely on the confirm branch (3013-3015) - so on cancel to[]==-1 and the loop at 4444 is skipped. The only door that could open it is the second getActionFloat call site (src/Menus.cpp:1291), which does not return doMenuAction: '--->v1' is in selectMultiple (built at :341 because the next tree line '---->n1' contains '>'), so keepSelecting==1 at :1358 breaks back into the menu loop, the user then picks a node at :1434-1440 and :1473 runs doMenuAction with from/to set and analogVoltage still NAN. I could not construct a navigation sequence that lands on that wrap-branch with actions[menuPosition]==3, so today the guard-by-construction holds - but it depends on line adjacency in src/remembering/menuTree.h:50-54, not on any check. One isnan() at 4443 closes it permanently. Related: only the RAILSACTION arm sanitizes the struct (4250); after an Output>Voltage cancel the NAN lives in currentAction until the next clearAction() (:658).

<details><summary>checker notes</summary>

FIX FOUND, ORIGINAL BUG DEAD. Three hunks, all in src/Menus.cpp (diff vs pre-fix ff00540): analogVoltage = NAN on the long-press/probe cancel exit (:2992) and the serial cancel exit (:3025), plus an isnan() guard wrapping the whole apply block in RAILSACTION (:4248-4251, closing brace :4284). Original scenario re-walked: Rails > Both > drag > cancel now skips setTopRail/setBotRail entirely, so no 0 V drive, no "Both Rails set to 0.00 V" on the OLED, and no 0 V persisted. railCommitEdit() (:2937-2961) still runs on all three exits, so the drag itself keeps its single undo entry and its dirty mark. All three doMenuAction doors that can reach RAILSACTION (:1171 up-wrap, :1371 down-wrap, :1491 slider) pass through the same guard - the unconditional "return doMenuAction() after EVERY slider exit" that caused the bug is still there, but RAILSACTION now ignores it.

BOTH TARGETS COVERED. Menus.cpp is a single shared source; env jumperless_og (platformio.ini:186-260) only swaps src/boards/og for src/boards/v5 and excludes routing/NetsToChipConnections.cpp - no OG copy of the menu code (grep RAILSACTION hits only src/Menus.cpp and src/Menus.h). Neither env sets -ffast-math or -Ofast (platformio.ini:49-70, 237-247), so isnan()/NAN are honest under -Os on RP2350 and RP2040 alike; Arduino.h (src/Menus.cpp:6) supplies both.

STRUCTURE VERIFIED. The added else/brace did not re-nest the category chain: doMenuAction spans 4051-5012 and every "currentCategory ==" arm sits at the same brace depth (1 for SHOWACTION, 2 for the rest) as in the pre-fix file - depth-checked mechanically against git show ff00540:src/Menus.cpp.

COLLATERAL. (a) The cancel path now calls requestLedShow(-1) twice (:2986 then :4249) - harmless, it is a last-write-wins mailbox post (src/Commands.cpp:66-98), and the old code also ended on -1. (b) waitCore2() is skipped on cancel - correct, nothing writes hardware there. (c) The OLED no longer gets a clearPrintShow on cancel, so it keeps the last slider value instead of the (wrong) "set to 0.00 V" line - intended, but it is a visible change. (d) printActionStruct (:754) tests analogVoltage != 0.0, which is true for NAN, so a cancelled slider now prints "analogVoltage: nan" to serial - cosmetic only.

Not verified by compilation: pio is unusable here (Python 3.14 gate, no py3.13 venv present), so the check was brace/depth analysis plus flag inspection rather than a build.

</details>

## #27 — INCOMPLETE — Per-net bridge table overflows: findFirstUnusedBridgeIndex scans/returns up to MAX_BRIDGES over brid

- **[medium]** `src/routing/NetsToChipConnections.cpp:1530` — sortPathsByNet's pathIndex++ is still unbounded while writing globalState.connections.paths[pathIndex] (paths[] is pathStruct[MAX_BRIDGES], States.h:140). This is the 5th item in #27's own fix_sketch and it did NOT land. Reachable exactly as the finding described: combineNets' special-special branch (NetManager.cpp:546-547) adds the SAME bridge to TWO surviving nets, so a wire between a GND-attached row and a rail-attached row costs two per-net entries for one master bridge. Total per-net entries can therefore reach 2*MAX_BRIDGES (144 OG / 256 V5) while paths[] holds only 72/128. Overrun clobbers numPaths, pathsCacheValid, chipStates[12] and chipXY[12] - the crossbar switch state - i.e. corrupted routing. Same defect at NetsToChipConnections_OG.cpp:1508 (sortPathsByNet, OG:1404). Note this is pre-existing and the #27 fix neither caused nor worsened it (sortPathsByNet has read k < MAX_NODES since c18f8d2), but it was named in the remediation and left open. Fix: guard `if (pathIndex >= MAX_BRIDGES) break;` before the write at :1473-1476 / OG :1461-1464.
- **[low]** `src/routing/NetsToChipConnections.cpp:1877` — Surviving sibling of the exact loop class the fix swept: fillUnusedPaths (V5:1848 / OG:1938) still scans `for (int i = 0; i < MAX_BRIDGES; i++)` over globalState.connections.nets[n].bridges[i][0], which is int16_t[MAX_NODES][2] (MatrixState.h:19). Now that addBridgeToNet legitimately fills the table to exactly MAX_NODES, bridges[MAX_NODES][0] aliases specialFunction (-1 for a regular net, non-zero) so the break doesn't fire and it reads into doNotIntersectNodes[]. Named in #27's fix_sketch ("fillUnusedPaths count loop (NetsToChipConnections.cpp:1877, read-only)") and not applied. Harm is nil in practice: read-only, it stops within the same netStruct once dni[1]==0, and the only thing it feeds is bridgeCount[] (decl :1855 / OG:1945, incremented at :1881 / OG:1971, every other use commented out - write-only dead). Same at NetsToChipConnections_OG.cpp:1967. Fix: change both bounds to MAX_NODES.

<details><summary>checker notes</summary>

PRIMARY BUG: DEAD, on both targets.
- src/routing/NetManager.cpp:983 - scan bound now `i < MAX_NODES` (was MAX_BRIDGES).
- src/routing/NetManager.cpp:790-795 - addBridgeToNet refuses `newBridgeIndex < 0 || >= MAX_NODES` before writing :796-797. The 0x7f fallback still returned at :992 is 127, >= 24 (OG) and >= 40 (V5), so it is caught on both.
- Single caller confirmed: grep for findFirstUnusedBridgeIndex hits only NetManager.cpp:789 and the NetManager.h:56 decl.
- BOTH TARGETS: NetManager.cpp is target-independent (MAX_NODES is the per-board #define, JumperlessDefines.h:191-196) and env:jumperless_og extends env:jumperless_v5 and only subtracts boards/v5/ and routing/NetsToChipConnections.cpp (platformio.ini:257-260). Same source, both boards.
- The only writers to the per-net table in the whole tree are addBridgeToNet :796-797 and shiftNets' clear :720-721 (verified by exhaustive grep of `bridges[` in src/, excluding ConnectionState's own bridges[MAX_BRIDGES][3]). No bypass path exists.

SIBLING SWEEP from the finding's fix_sketch - 3 of 5 landed:
- DONE combineNets copy loop, NetManager.cpp:583 (MAX_BRIDGES -> MAX_NODES). This was the one that read specialFunction=-1 as a live bridge.
- DONE shiftNets clear loop, NetManager.cpp:715 (MAX_BRIDGES -> MAX_NODES).
- DONE shiftNets DNI clear, NetManager.cpp:703 (`i < 6` -> MAX_DNI). Side benefit worth noting: the old MAX_BRIDGES clear loop zeroed specialFunction AFTER :701 set it to -1 when a table was full; -1 now survives the clear.
- NOT DONE fillUnusedPaths count loop (issue 2).
- NOT DONE sortPathsByNet pathIndex guard (issue 1).

COLLATERAL - no user-visible regression found:
- Over-cap connections now refuse with an unconditional Serial print per excess bridge per rebuild ("bridge table full - connection listed but not tracked per-net"). Bounded, and it matches existing convention (States.cpp:171 "Warning: Maximum bridges reached during sync"). Old code corrupted memory in exactly this regime, so behavior only changed where it was already broken.
- No data loss: globalState.connections.bridges is the source of truth and nets are rebuilt from it every refresh (loadBridgesFromState NetManager.cpp:162-259, getNodesToConnect :284). A refused per-net add means the connection is listed but unrouted while the net is over cap; it is restored the moment the net is under cap again.
- Companion guard in addNodeToNet (:951-956) is correctly placed AFTER the already-in-net dedupe return at :944, so the common path is byte-for-byte unchanged. populateSpecialFunctions still runs before the guard (:928) - same as pre-fix, not a regression.
- sortPathsByNet's per-net read bound was ALREADY `k < MAX_NODES` before the fix (git blame back to c18f8d2), so the fix did not narrow what gets routed. No connection that used to route stops routing.
- Cleared as false alarms: GuideChecks.cpp:390/430 and InfraPaths.cpp:89/718/795 index ConnectionState::bridges[MAX_BRIDGES][3], not the per-net table (confirmed `ConnectionState& c = globalState.connections` at GuideChecks.cpp:388/428). NetsToChipConnections.cpp:1607/OG:1652 iterate pathsWithCandidates[MAX_BRIDGES]. States.cpp:174-176 writes the master table under a `numBridges >= MAX_BRIDGES` guard.

OUT OF SCOPE, pre-existing, flagged for routing (not counted in issues): createNewNet (NetManager.cpp:769-770) uses findFirstUnusedNetIndex unguarded - the identical 0x7f pattern on a different table; with all 60 nets in use it writes nets[127]. Verified createNewNet is its only caller. findFirstUnusedNodeIndex's 0x7f is guarded (addNodeToNet:951, its only caller).

Compile not re-verified (pio needs the py3.13 workaround venv); the change is trivial guards in a shared file and .pio/build/jumperless_v5/firmware.uf2 is already rebuilt in the tree.

</details>

## #3 — INCOMPLETE — detach() during YIELDED stomps the user script's claimed pins

- **[medium]** `src/displays/DisplayBus.cpp:183` — The user-claimed skip is all-or-nothing across BOTH pins, but claims are per-pin (jl_gpio_claim_pin at JumperlessMicroPythonAPI.cpp:816 claims one pin; gpioPwmEnabled is per-pin too). If a script claims only GP24 (or only GP25), displayBusUserClaimed() returns true and displayBusRelease() also skips releasing the UNCLAIMED sibling: it is left as a push-pull output driving HIGH (last softStop state, DisplayBus.cpp:76-84 via sclHigh/sdaHigh -> gpio_set_dir OUT + gpio_put 1) with gpio_function_map=SIO and gpioState=6, and the instance forgets it (sdaPin=sclPin=-1), so nothing ever releases it. Old code released that pin correctly. Fix: decide per pin, not per instance.
- **[medium]** `src/displays/DisplayBus.cpp:186` — The skip also drops the gpioState[]=6 clear, but gpioState 6 is the DISPLAY's own bus-role bookkeeping (set by displayBusAcquire at DisplayBus.cpp:175-176), not state the script owns. After a YIELDED detach both marks stay 6 for the rest of the session. Consequences once the script exits and jl_gpio_release_all_pins() clears gpioPythonOwned: setGPIO() continues past GP24/GP25 forever (Peripherals.cpp:796), so refreshConnections never re-asserts the user's configured direction/pulls on those pins again; and readGPIO takes the bus-role branch (Peripherals.cpp:1362) doing a raw gpio_get instead of gpioReadWithFloating, so a floating GP24/GP25 reports a hard 1/0 in the UI - a misleading bench reading. readGPIO also stops re-stamping state 4 when unrouted (Peripherals.cpp:1319). Old code cleared both marks on detach. Recovery exists but is non-obvious: place a display part again (clean acquire/release cycle), re-route RP_GPIO_5/6 from the menu (Menus.cpp:4164,4168 set gpioState=2), or power-cycle.

<details><summary>checker notes</summary>

FIX FOUND: src/displays/DisplayBus.cpp:183-187 (landed in 57cf63d), guarded by displayBusUserClaimed() at DisplayBus.cpp:200-205 which the same commit widened to also count gpioPwmEnabled.

1. ORIGINAL BUG IS DEAD. The exact scenario - script claims GP24/25 -> DispState::YIELDED (DisplayService.cpp:304) -> part removed/edited -> pollParts gone-check (DisplayService.cpp:232) -> detach() (DisplayService.cpp:200) -> displayBusRelease - now takes the early return at DisplayBus.cpp:183-187. The script's pinMode() is no longer forced to INPUT and the SIO mark jl_gpio_claim_pin set (JumperlessMicroPythonAPI.cpp:820) is no longer zeroed. The three release call sites (DisplayService.cpp:134, 152, 200) all route through the same guard.

2. ATTACK RESULTS. Two gaps, one root cause: the guard is all-or-nothing on two axes it shouldn't be (see issues). Minimal correction: skip only pinMode()/gpio_function_map, and do it per pin (skip only the pin whose gpioPythonOwned/gpioPwmEnabled is set); ALWAYS clear gpioState[] for both. Clearing gpioState is safe while a claim is live because gpioPythonOwned already gates setGPIO (Peripherals.cpp:786) and readGPIO (Peripherals.cpp:1294), and gpioPwmEnabled gates setGPIO (Peripherals.cpp:777). gpio_function_map is secondary either way - it self-heals from the hardware at Peripherals.cpp:1961-1967.
Bounds: displayBusUserClaimed's `< 20` guard (DisplayBus.cpp:201) keeps release's `pin-20` indexing in range for every reachable value (-1, 24/25, 26/27); index math matches gpioDef (Peripherals.h:166-176, gpioDef[i][2] == pin-20 for 20..27). Resume path (DisplayService.cpp:236) is unaffected: it detaches while unclaimed, so the reset branch runs normally.

3. BOTH TARGETS CHECKED. OG is clean: caps.breadboardDisplays is false (board_og.cpp:114) and is gated twice - DisplayService::service() returns IDLE before pollParts (DisplayService.cpp:298) and displayBusAcquire refuses with a reason (DisplayBus.cpp:141-144, added by the same commit). So GP24 (CH446Q RESET) / GP25 (LED strip data) are never acquired or released on OG, and setGPIO() is a no-op there anyway (Peripherals.cpp:764 #if OG_JUMPERLESS). V5 is the only target where either issue above can fire. The only cross-target reader, DisplayService::activeDataNodes (DisplayService.cpp:445-452, called from NetVoltageScan.cpp:794,811), returns 0 with partIdx=-1.

4. COLLATERAL. Behavior beyond removing the bug: the stuck gpioState=6 described in issue 2 is a real user-noticeable change from the old code (config re-assert and float detection silently stop working on GP24/GP25). The added gpioPwmEnabled term in displayBusUserClaimed is a deliberate sibling fix, not collateral - it only widens what counts as a claim; its only cosmetic wart is that the YIELD line (DisplayService.cpp:310) says "claimed by your script" when PWM triggered it. No build attempted (pio is broken under py3.14 in this environment); verification is static.

</details>

## #33 — INCOMPLETE — printPowerSupplySense debug toggle makes core 1 do USB CDC writes - the codebase's own documented bo

- **[medium]** `src/selfreflection/Debugs.cpp:162` — Dead debug toggle left behind. The fix (47325d3) deleted the only consumer of printPowerSupplySense, but the debug menu still advertises 'v. print power supply sense', still toggles it (Debugs.cpp:351), still commits it to the live global (Debugs.cpp:256-257), and 'all off' still clears it (Debugs.cpp:311). Repo-wide grep shows main.cpp:1531 (definition) and Debugs.cpp are the only references — nothing reads the flag any more. A bench user turns 'v' on, sees the menu report '1', and gets no output ever, with no error and no indication the feature was removed. This is exactly the user-visible behavior the fix_sketch said to preserve by moving the print to core 0 (or by exposing supplySense on an existing core-0 debug path); neither was done.
- **[low]** `src/main.cpp:1591` — Orphaned producer. supplySense (main.cpp:1534) is now write-only — the only assignment is here and there is no reader anywhere in the tree (grep over src/ and lib/ finds only the decl, this store, and a comment). loop1() on core 1 still spends a 4-sample readAdcVoltage(6,4) — which takes the cross-core ADC test-and-set lock in readAdc() (Peripherals.cpp:2797) — every powerSupplySenseRate=1000 ms to compute a value nothing consumes. Harmless but pure dead work on the core the audit is trying to keep clean, and it means the supply-sense reading is now unobtainable at the bench by any route.
- **[low]** `src/main.cpp:2108` — Same-class sibling still in the tree (pre-existing, not introduced by the fix). debugWaitLoopTimingCore2 gates Serial.println/Serial.printf/Serial.flush inside core2stuff() (main.cpp:2108-2118), which runs only from loop1() on core 1 (main.cpp:1622) — the identical USB-CDC-from-core-1 wedge class the fix's own comment at main.cpp:1593-1597 forbids. It is not live today: no code assigns the flag true (main.cpp:1513 initializes it false and its comment claims an 'Enable via core2timing command' that does not exist; the only other mentions at :1561/:1565 are commented out), and the debug menu's 'w' toggles the separate debugWaitLoopTiming. So it is latent, not reachable — but it re-opens the door the moment anyone wires that flag to a command.

<details><summary>checker notes</summary>

FIX FOUND: commit 47325d3, src/main.cpp:1587-1601. The Serial.print/println/flush block that printed supplySense from loop1() (core 1) was deleted outright and replaced by a comment (main.cpp:1593-1597); only the ADC read (main.cpp:1591) and the timer update (main.cpp:1599) remain. git log -L1587,1601:src/main.cpp confirms the exact hunk removal and shows no later re-introduction.

ORIGINAL BUG IS DEAD. There is no Serial/USB-CDC call left in the POWER_SUPPLY_SENSE block, so the flag can no longer drive core 1 into TinyUSB. BOTH TARGETS covered: this is unconditional shared code in src/main.cpp with no per-board #ifdef, and platformio.ini [env:jumperless_og] (line 186) extends [env:jumperless_v5] with the same build_src_filter (line 257-258), so V5/RP2350 and OG/RP2040 get the identical loop1().

ATTACK RESULTS: no boundary problem (millis()-timer unsigned compare unchanged); no early-exit state left behind (the timer stamp is still inside the same if, so the 1000 ms cadence is unchanged and no partial state exists); no reentrancy concern (loop1/core2stuff are core-1-only, verified by caller grep); readAdcVoltage() itself does no Serial I/O and its lock path (Peripherals.cpp:2760-2806) is core-safe, so the surviving half of the block is clean. The only caller/consumer the fix forgot is the debug menu that still exposes the now-inert toggle.

COLLATERAL: default behavior (flag false) is byte-for-byte identical to old code. But with the flag ON, old code printed 'supplySense = <v>' once per second and new code prints nothing, while the menu still offers the switch — a user-noticeable capability loss plus a lying UI, and supplySense became a write-only global. That is why fix_correct=false. Minimal completion: have a core-0 service (PortHousekeeping / the existing core-0 debug output) print supplySense when printPowerSupplySense is set, or delete the flag, the menu entry, and the now-pointless ADC read together.

Not build-verified (pio is gated on this machine per project memory), but the change is a pure deletion of statements and printPowerSupplySense is still referenced from Debugs.cpp, so there is no unused-symbol/link risk.

Files: /Users/kevinsanto/Documents/GitHub/JumperlOS/src/main.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/src/selfreflection/Debugs.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/src/Peripherals.cpp, /Users/kevinsanto/Documents/GitHub/JumperlOS/platformio.ini

</details>

## #36 — INCOMPLETE — Duplicate '_' trigger: the history-status command is silently replaced by the micros-per-byte debug 

- **[medium]** `src/selfreflection/HelpDocs.cpp:244` — Stale help for the sibling trigger the same fix moved. `case '^'` (HelpDocs.cpp:240-252) still prints "Set DAC voltage output" / "Usage: ^3.3 (sets DAC 1 voltage to 3.3V)", but the fix moved cmd_setDAC to ':' (SingleCharCommands.cpp:544) and '^' is now cmd_undo (SingleCharCommands.cpp:578). Typing `^?` at the bench yields instructions that, if followed, run an undo instead: cmd_undo (SingleCharCommands.cpp:2994) ignores `line`, so `^3.3` reverts the user's last connection/DAC transaction and never sets any DAC, then '3','.','3' fall through the dispatcher. This is the exact harm class of bug #36 (advertised command != what the key does), left half-fixed — the fix corrected the command table's shortDesc but not the help corpus.
- **[medium]** `src/selfreflection/HelpDocs.cpp:995` — Second stale mirror of the same '^' remap: `help voltage` (showCategoryHelp, HelpDocs.cpp:988-995) prints "^  - Set DAC voltage output (^3.3 for 3.3V)". Same consequence as HelpDocs.cpp:244 — the documented DAC command now runs undo. Both targets (no #if guards; src/ is shared by env:jumperless_v5 and env:jumperless_og).
- **[low]** `src/selfreflection/HelpDocs.cpp:824` — Neither relocated trigger got a help entry. There is no `case ','` and no `case ':'` in showCommandHelp (':' is commented out at HelpDocs.cpp:813), so `,?` and `:?` hit the default branch and print "No specific help available for command". Discoverability gap only — the command table's shortDesc/helpText are correct and the Ser3 manifest (Ser3Backchannel.cpp:439/559/588/635) enumerates the live table, so the app sees both commands.
- **[low]** `src/SingleCharCommands.cpp:119` — The new duplicate-trigger guard logs via raw `Serial.print` from static-initialization time: `SingleCharCommands singleCharCommands;` (SingleCharCommands.cpp:63) is a global whose ctor runs initializeCommands() -> registerCommand() before setup()/Serial.begin(). A future collision would emit its only diagnostic into an unopened USB CDC, i.e. the guard is silent exactly when it is needed. It also bypasses Jerial, which every other command output uses. Unreachable today (0 duplicates), so latent.
- **[low]** `src/tubes/Ser3Backchannel.cpp:785` — Stale trigger argument left behind by the move: the ':histstatus' verb still calls `cmd_historyStatus('_', "_")` after '_' was reassigned to cmd_printMicrosPerByte. Harmless in effect — cmd_historyStatus (SingleCharCommands.cpp:3039) ignores both `c` and `line` — but it is a stale mirror that will mislead the next reader; should be `(',', ",")`, matching the adjacent `cmd_historyList('(', "(")` at Ser3Backchannel.cpp:781.

<details><summary>checker notes</summary>

BUG #36 IS DEAD. Fix: cmd_historyStatus moved '_' -> ',' at src/SingleCharCommands.cpp:586-589 (with an explanatory comment), cmd_printMicrosPerByte keeps '_' at src/SingleCharCommands.cpp:720-722, and registerCommand() now prints a "[cmd] trigger 'X' re-registered" line instead of silently overwriting (src/SingleCharCommands.cpp:113-127). Note the fix landed in 0152d5f, not the four audit commits named in the task.

ATTACK RESULTS (all clean):
- Duplicate sweep: parsed every registerCommand() char-literal in src/SingleCharCommands.cpp with comment lines stripped — 72 registrations, 72 unique triggers, zero collisions. ('%' appears twice only because src/SingleCharCommands.cpp:512 is commented out.)
- Both targets: src/SingleCharCommands.cpp:586-589 and the cmd_historyStatus definition at :3039 sit under no preprocessor guard, and only one non-worktree copy of the file exists, so env:jumperless_v5 and env:jumperless_og get identical tables.
- Boundary: MAX_COMMANDS = 128 (src/SingleCharCommands.h:123); count is 72, and the fix adds a net +1 entry (the old '_' pair collapsed into one). No overflow, no silent `return false`.
- Reachability of ',': nothing intercepts it before dispatch. main.cpp:1355 (interactive) and main.cpp:1178 (CommandBuffer/UART relay) pass the raw char to executeCommand(); the only early-outs are ' ', '\n', '\r' (main.cpp:1342). Every other ',' in src/ is an argument/CSV delimiter inside handlers, never a top-level trigger.
- registerCommand overwrite path: does not call sortCommands(), but the trigger is unchanged on overwrite so sort order stays valid for findCommandIndex (src/SingleCharCommands.cpp:378).
- '_' help doc is correct: HelpDocs.cpp:594 still describes micros-per-byte, matching the surviving registration.

COLLATERAL: the same edit remapped the companion shadowed pair ('^' setDAC -> ':'), and the help corpus was not carried along — see the two medium issues. That is a sibling path left open, hence fix_correct=false. Everything else is preserved: menu text comes from the live table, printMenu's hardcoded lines are untouched, and menuItemCounts[] = {14,22,37,46} (src/SingleCharCommands.cpp:~259) only feeds terminal color stepping, so the +1 command count is cosmetic.

</details>

## #37 — INCOMPLETE — LED-show request taken but never completed on frame-hold abort wedges the ledShowIdle-gated pipeline

- **[medium]** `src/main.cpp:1845` — Re-post uses raw core1req::post() (OR) instead of postMode(). take() at :1781 empties the slot, so a concurrent core-0 requestLedShow() sees no pending mode and its keep-rule (Commands.cpp:97) cannot fire; the abort then ORs the taken mode back in, producing a multi-mode bit set (e.g. LED_NETS|LED_MENU) that postMode is specifically designed to make unreachable. The decode at :1784-1785 gives MENU priority over NETS, so the taken full render is downgraded to a menu flush - the exact 'a MENU flush must not downgrade a pending NETS/GFX' failure documented at Commands.cpp:87-96. The comment's promise that 'the dropped frame renders once the hold releases' is therefore not kept. Fix: re-post via postMode(REQ_SHOW_LEDS, LED_MODE_MASK, takenMode, takenFlags, takenMode==LED_MENU ? (LED_NETS|LED_GFX) : 0).
- **[medium]** `src/main.cpp:1911` — Same raw-post defect on the menu abort path: post(taken) ORs LED_MENU on top of a NETS/GFX that core 0 posted during the render window (lightUpRail + logoSwirl at :1791-1792), and MENU wins the :1784 priority, dropping the full render.
- **[low]** `src/main.cpp:1830` — LED_CLEAR is re-posted after it was already consumed: clearLEDsExceptRails() ran at :1830 and clearBeforeSend was zeroed at :1832, then the :1836 abort re-posts the whole taken word including LED_CLEAR. Combined with the mode-OR above, the next pass can take rails==2 with inClickMenu==0, fall to :1919 needsLedShow=true, and leds.show() the nets-blank buffer - a visibly blank breadboard until some later NETS post. Standalone (no MENU race) it is only a harmless redundant clear.
- **[low]** `src/coredination/CoreMailbox.cpp:24` — core1req::post is NOT __not_in_flash_func, unlike take/complete (:62, :74). Confirmed in both built ELFs: post at 0x1008bad4 (jumperless_v5) / 0x1007eb20 (jumperless_og) = XIP, while take/complete sit at 0x2000xxxx = RAM. The new abort paths (main.cpp:1845, :1911) call it from core 1 while a core-1 frame hold is up - the one path whose entire purpose is escaping an imminent flash op, and the reason CoreMailbox.cpp:21-22 keeps the core-1 side RAM-resident. rp2040.idleOtherCore() (externVars.cpp:224-228) is the hard park so a crash is unlikely, but mark post/postMode __not_in_flash_func to keep the discipline (postMode is also called from core 1 at main.cpp:1748).

<details><summary>checker notes</summary>

Fix found at src/main.cpp:1843-1846 and :1909-1912 (commit 47325d3), both guarded by `if (taken)`, complete() before post(). Original wedge is genuinely dead: take() at :1781 and complete() at :1977 bracket exactly two returns (:1849, :1914), both patched; core2stuff() has one caller (loop1, main.cpp:1622) which spins at :1544 while the hold is up, so no re-entry leak; ledShowIdle() (Commands.cpp:101) recovers, the :1747 menu pacer un-sticks, and waitForBlockingDisplay() (Probing.cpp:1780) no longer burns its 100ms timeout every call. Both targets covered - shared src/main.cpp, jumperless_og inherits build_src_filter (platformio.ini:257); CoreMailbox/externVars use no board-conditional code.

Attack results: LED_GFX (rails==3) never reaches either abort (gated out at :1810/:1891/:1919), so ownership state is unaffected. core2busy is already false at :1907 (the :1789-1794 block resets it), so the missing `core2busy = false` there is correct, not an omission. ledFrameAbortsPause is core-1-only. No flags-without-mode requestLedShow values exist in the tree (only 0/1/2/3/-1/-2/-3/12), so `taken` always carries a mode bit. The commented-out abort at :1939-1943 takes nothing, so it is not a sibling leak.

Remaining gap: the fix restores the gate but not reliably the frame, because it bypasses the mode-exclusivity invariant that postMode enforces. That is an incomplete fix rather than a regression (pre-fix the frame was lost unconditionally), but the fix's own comment claims the frame renders, and the MENU-over-NETS downgrade plus the already-consumed LED_CLEAR can put a nets-blank frame on the strip. Plus the flash-residency slip on post().

</details>

## #4 — INCOMPLETE — DisplayService::routeDataPins writes phantom transactions into the user's undo history; one undo pre

- **[medium]** `src/displays/DisplayService.cpp:57` — Incomplete fix: the fix_sketch's second half was never implemented. The 'already routed' early-return still trusts the persisted connect fields alone: `if (p.pins[sdaIdx].connect == sdaNode && p.pins[sclIdx].connect == sclNode) return true;` with no globalState.hasConnection() verify. JumperlessState::clearAllConnections (src/routing/States.cpp:719-737) wipes connections.bridges but never touches parts.parts[] - and it is reached by the everyday `x` / clearNodeFile path (src/remembering/FileParsing.cpp:1276,1295,1301, clearActiveContext at :1276 even saves the cleared slot). After it, the display part is still placed with connect==131/132, every bridge is gone, and nothing re-routes: pollParts returns at DisplayService.cpp:243 because inst.partIdx>=0 and the identity check passes, so attach()/routeDataPins() is never re-entered; and if it were, line 57 returns true. The panel drops to ROUTED and beacons into a dead bus forever with no message. Part bridges are only rebuilt by expandPartsToBridges, which runs solely on slot load (src/routing/States.cpp:3402,3656) - so the only in-session recovery is a slot reload or removing/re-placing the part. That is exactly the 'no obvious recovery' half of finding #4's title; only the undo trigger was closed.
- **[low]** `src/displays/DisplayService.cpp:92` — The dirty-gate added alongside the guard is inert. `if (priorSda != -1 || priorScl != -1) st.markDirty();` cannot stop the slot rewrite it claims to stop: the refusal path has already run removePartPlacement + applyPartPlacement x2, and JumperlessState::addConnection/removeConnection call markDirty() unconditionally (src/routing/States.cpp:653 and :700) before the gate is ever evaluated. So a standing refusal still dirties state on every retry; only the routeFails>=5 backoff to 5 s (line 160) actually reduces the rewrite rate. Not user-visible harm, but the comment overstates what the code does.
- **[low]** `src/displays/DisplayService.cpp:65` — New (narrow) side effect of putting the guard here: undoEndIngest() resets g_lastOpMs = millis() when the outermost region closes (src/remembering/Undo.cpp:1955). routeDataPins now runs that reset from an ambient 750 ms-polled service. g_lastOpMs feeds DAC slide coalescing (Undo.cpp:2244, DAC_SLIDE_COALESCE_MS) and transaction auto-close (Undo.cpp:2074,2303), so a display attach that lands right after a user DAC slide can extend the coalescing window and fold the user's next DAC set on the same channel into the previous undo step instead of giving it its own. Only fires on an attach transition (the line-57 early return covers steady state), so the window is rare - but it is behavior the old code did not have.

<details><summary>checker notes</summary>

FIX FOUND. src/displays/DisplayService.cpp:65 `UndoIngestGuard undoGuard;` plus the include at :14, landed in 57cf63d (only commit touching the symbol on this file). Scope covers the whole mutation block - RAII fires on all three exits (:71 remove failure, :93 refusal, :97 success), including refreshConnections(-1) at :96.

ORIGINAL SCENARIO IS DEAD. recordOneAction short-circuits on g_ingestDepth>0 (src/remembering/Undo.cpp:2134), and the churn's only recorders are JumperlessState::addConnection/removeConnection (undoRecordConnect at States.cpp:659, undoRecordDisconnect at :703). Under the guard the ~6 phantom txns (DISCONNECT vcc-rail, DISCONNECT gnd-GND, CONNECT vcc/gnd/sda/scl) never enter the ring, so one undo press after placing the 0.91in OLED now reverts the user's own last action.

BOTH TARGETS. V5 (jumperless_v5): UNDO_ENABLED=1, caps.breadboardDisplays=true (src/boards/v5/board_v5.cpp:117) - guard is live. OG (jumperless_og): the bug cannot exist twice over - DisplayService::service() returns IDLE at DisplayService.cpp:298 on !caps.breadboardDisplays (src/boards/og/board_og.cpp:114 = false), and UNDO_ENABLED=0 (src/JumperlessDefines.h:69) compiles the whole subsystem to stubs including undoBeginIngest/undoEndIngest (Undo.cpp:2822-2823), so the guard still links and is a no-op. No #ifdef needed at the call site.

SIBLING PATHS CHECKED (clean).
- applyPartPlacement/removePartPlacement callers are exactly three: PartsApp.cpp:465/472 and :593 (both already guarded, PartsApp.cpp:463 and :590), DisplayService.cpp:71/76/88 (now guarded), and the MicroPython API JumperlessMicroPythonAPI.cpp:2044/2101 (jl_place_part / jl_remove_part, exposed as place_part/remove_part - src/snakes/micropythonExamples.h:4025-4026). The MP pair is UNGUARDED, so `place_part()` bridges do land in undo with no matching parts-table op. But it cannot reproduce finding #4: place_part takes no partId/driverKey (signature at JumperlessMicroPythonAPI.cpp:1919), and partdbResolveDriver returns 0 when both are empty (src/partdb/PartDb.cpp:148-160), so a place_part part never resolves to a display driver and DisplayService never attaches to it. Worth a separate finding for placement/undo desync generally; out of scope for #4.
- Slot load does not call applyPartPlacement (it uses expandPartsToBridges), and the load region is already bracketed by main.cpp:1396/1490 ingest.

CONCURRENCY / REENTRANCY (clean). g_ingestDepth is a plain int (Undo.cpp:226) with no atomics, but services run on core 0 only (jOS dispatch from loop(); refreshConnections hard-refuses core 1 at Commands.cpp:179-183), and routeDataPins is unreachable under a modal loop (pollParts returns at DisplayService.cpp:224 on jOS.inModalContext()). refreshConnections does not pump the service set, so the guard cannot swallow a concurrent user action. No cross-core race.

COLLATERAL. No user-visible behavior change from the guard itself beyond removing the phantom txns - it suppresses only records, never bridge state, so the fabric ends identical. Two low-grade items are listed above (inert dirty-gate at :92, g_lastOpMs reset at :65). Pre-existing and NOT introduced by this fix, so not listed: the refusal path at :85-88 calls applyPartPlacement a second time without an intervening remove after a partial apply, and it wipes user-authored connect values to -1 instead of restoring priorSda/priorScl (the fix captures those two values but only uses them for the dirty gate).

VERDICT. fix_present=true - the undo-pollution trigger is genuinely closed on both targets. fix_correct=false - the sketch's second half (hasConnection re-verify on the already-routed early return) is missing, and the finding's terminal symptom (display permanently dead, no in-session recovery) is still reachable through the common `x` clear-all path.

</details>

## #40 — INCOMPLETE — Host-initiated USB eject silently discards dirty breadboard state (device-side disable path saves it

- **[medium]** `src/remembering/USBfs.cpp:741` — New save block has no `fs_mutex_held_by_this_core()` guard, unlike the remount immediately above it (line 722). That guard exists because usbUnplugCallback can be re-entered from a tud_task pump inside an in-flight firmware FS operation; in that case the FatFS remount at 723-727 is deliberately SKIPPED, so FatFS's in-RAM FAT window / free-cluster hints are known-stale after the host rewrote the FAT. The fix then calls saveActiveSlot() -> f_write through exactly that stale state, and fs_mutex is recursive per-core (externVars.cpp:152), so the nested acquire silently succeeds instead of blocking. Pre-fix this window only did READS (the reload, recoverable via loadSlotFromPath's restore contract); the fix turns it into a committed WRITE — cross-linked clusters / a nested write inside another in-flight FS op. Correct shape: `if (!fs_mutex_held_by_this_core() && ...isDirty())` and leave dirty for the deferred auto-save (States.cpp:4856 already defers on purpose).
- **[medium]** `src/remembering/USBfs.cpp:1261` — Sibling path left open: there is a mount EDGE (hostDiskIOAllowed calls usbPlugCallback on the first READ10/WRITE10 because 'most hosts never send START STOP UNIT at mount') but no unmount edge. usbMountedByHost is cleared in exactly two places — usbUnplugCallback:706 and disableUSBMassStorage:848 — and usbUnplugCallback is only reachable from SCSI START_STOP_UNIT(loej=1,start=0) at line 1207. A host that unmounts without that command (Linux `umount`, cable yank, host suspend/bus reset — the only tud_umount_cb/tud_suspend_cb in the tree is USBAudio.cpp:629/635 and touches audio state only) leaves usbMountedByHost stuck true forever: every firmware write stays blocked (FilesystemStuff.cpp:3355/4229/4467/4604/4644/4682, externVars.cpp:270), auto-save stays deferred, and the dirty breadboard state is lost on the next power cycle — the exact harm of finding #40, via the sibling door. Only a manual `u`/menu disable or reboot clears it.
- **[low]** `src/remembering/USBfs.cpp:901` — The fix was pasted into disableUSBMassStorage as well, where an identical isDirty()/saveActiveSlot() block already existed at 884-894. saveStateToActivePath/saveSlot clear dirty on success (States.cpp:3853, 3771), so the second block is dead on the success path; on failure it retries the same doomed save and prints '◆ Saving pending changes...' / '⚠ Warning: Could not save pending changes' twice. Dead duplication, and it makes the two paths look intentionally different when they are not.
- **[low]** `src/remembering/USBfs.cpp:744` — On the host-eject path the new write is issued BEFORE tud_disconnect(). Sequence: tud_msc_start_stop_cb:1207 FatFSUSB.unplug() -> usbUnplugCallback -> saveActiveSlot (flash write, host still enumerated and MSC still live, mscModeEnabled still true) -> then 1211 disableUSBMassStorage -> tud_disconnect at 837. That inverts the ordering the same function documents at 830-832 ('Force disconnect from host perspective FIRST - writing to the filesystem while the host still holds a cached FAT is the two-writer corruption bug. Any pending dirty state is saved below, after the host is gone.'). Pre-fix, the only firmware flash write on eject happened after the disconnect. Practical risk is bounded (the host has flushed and disableUSBMassStorage re-enumerates), but it also means the SCSI eject command's completion is now delayed by a full slot write + core2 pause inside the callback.
- **[low]** `src/remembering/USBfs.cpp:757` — Incomplete on the save-refused path: when saveActiveSlot legitimately returns false the code only prints a warning and falls straight into the reload at 757-782, which overwrites activeState from the file and discards the dirty state anyway. This is deterministic for a project TEMPLATE active context (/projects/<dir>/wiring*.yaml — States.h:749-756: saveActiveSlot refuses to write it) reached via isPathContext() -> loadSlotFromPath. The fix converts a silent loss into a warned loss there rather than preventing it; skipping the reload when the save failed would preserve it.

<details><summary>checker notes</summary>

FIX LOCATION: src/remembering/USBfs.cpp:736-750 (usbUnplugCallback), added by 47325d3 (`git log -p -S"Dirty in-RAM state must be saved BEFORE"`). Same block also pasted at 896-910 in disableUSBMassStorage.

ORIGINAL SCENARIO IS DEAD for the reported trigger. Mechanism confirmed end-to-end: while mounted, auto-save is deferred, not dropped (States.cpp:4856 "we'll save when USB is unmounted"; dirty survives). Pre-fix the loss was purely ordering — usbUnplugCallback cleared usbMountedByHost (706) and then reloaded the active context (757-782), wiping dirty RAM; by the time disableUSBMassStorage's pre-existing save block (884-894) ran, isDirty() was already false. The new block runs after usbMountedByHost=false (writes legal, gates are usbMountedByHost-only, not mscModeEnabled) and before the reload, so a host eject now persists the wiring. saveActiveSlot is the correct path-aware entry (States.cpp:3866) and clears dirty on success (3853/3771).

BOTH TARGETS COVERED: USBfs.cpp has no board #ifdefs and is not excluded by either build_src_filter — [env:jumperless_og] extends jumperless_v5 and only strips boards/v5/ and routing/NetsToChipConnections.cpp (platformio.ini:186-262). Identical code on RP2350 and RP2040. No target gap.

ATTACK RESULTS: two real gaps (issues 1 and 2). The eject callback's own re-entrancy guard (722) was not applied to the new write, and the fix only covers the SCSI-eject door — there is a mount edge (1261) but no unmount edge, so `umount`/unplug/suspend still strands usbMountedByHost=true with the state unsaved. Note also that tud_msc_start_stop_cb's "temporary stop" else-branch (1212-1227) is unreachable dead code (`load_eject && !start` is always true there) — pre-existing, not fix-related.

COLLATERAL: new "◆ Saving pending changes..." / "✓ Pending changes saved" lines on every host eject; a flash write now occurs inside the SCSI eject callback before tud_disconnect (issue 4). The double slot reload on the eject path (usbUnplugCallback 757 + disableUSBMassStorage 917) is pre-existing, unchanged by this fix.

NOT BUILT: no compile run (pio is gated under py3.14 per project memory). The added code is a verbatim copy of a block that already compiles in the same TU, with States.h included at line 30, so build risk is nil.

</details>

## #41 — INCOMPLETE — OLED rotation menu setting is never persisted — reverts on every reboot, and a rotation-only save no

- **[medium]** `src/configManager.cpp:1474` — MIGRATION GAP - original bug reproduces verbatim on any existing config.txt. configFileIsComplete()'s requiredKeys[] (1474-1492) is the ONLY mechanism that upgrades a config.txt to include newly-added keys (its own comment at 1472-1473: "Add new keys here when they're added to config.h"), and the fix did not add "rotation". No shipped firmware ever wrote a rotation line, and the incremental rebuild only substitutes values for keys ALREADY present - lines 2197-2202 keep unknown lines as-is and there is no key-insertion path anywhere after the loop (MARK_KEY_SEEN at 1670-1671 is a stub; seenKeys is never read). So on a config.txt that has every requiredKey but no rotation line: menu sets rotation (Menus.cpp:4798) -> configChanged=true -> saveConfig -> configHasChanges true (1398) -> completeness passes -> rebuild emits nothing new -> contentChanged false (2233) -> early return true (2235-2243) with updateShadowConfig() (2241, memcpy into lastSavedConfig at 1246) -> ConfigSaveService clears configChanged (129-134). Setting laundered away, save reports success, reverts on reboot - the exact pre-fix behavior. Verified no boot path full-rewrites: 3547/3719 are `~ reset` terminal commands; 912 and 963 (firmware-version-bump and major-version-diff re-saves) and checkAndHandleFirmwareUpdate:2660 all call saveConfig() -> incremental. Exposure: any unit whose config.txt was written between 6711653 (2026-08-19, added the last requiredKey "encoder_pio") and the fix - i.e. current dev/bench boards - plus any hand-edited or restored config that keeps the required keys. Pre-2026-08-19 units heal only by accident (missing encoder_pio forces the full writer). One line fixes it.
- **[medium]** `src/Menus.cpp:4790` — OOB read still live on the rotation path (incomplete sibling fix from 57cf63d). The `previousMenuPositions[2] >= 0` guard added at 4782 covers only the Width branch; when it is -1 (initialized to -1 at Menus.cpp:641, reachable from a depth-1 row per the code's own comment at 4779-4781) the condition is false and control falls straight into the else-if at 4790, which evaluates `menuLines[-1].indexOf("Height")` on a String array - the guard just moved the out-of-bounds read down one line. Line 4797 (the Rotation branch this fix touches) sits in the same unguarded chain. Fix: hoist the >=0 test around the whole if/else-if chain.
- **[low]** `src/configManager.cpp:1173` — Sibling field the fix left open, explicitly named in the finding's fix_sketch. top_oled.display_type is parsed (770-area:767, 4310) and echoed (4084) but is written by NEITHER saver - absent from saveConfigToFile's [top_oled] block (1171-1188) and from saveConfigIncremental's key chain (2109-2159) - and has no comparison in configHasChanges (1394-1410). `~ top_oled display_type SH1106` prints a setting-changed confirmation, then reverts on reboot. 47325d3 fixed only the dangling-stack-pointer half (oledDisplayTypeLiteral, 19-25). Harm is cosmetic only: grep confirms display_type has zero consumers outside config echo (src/config.h:389 is its sole other mention), so nothing on the display behaves differently.

<details><summary>checker notes</summary>

FIX FOUND (commit 47325d3, all three sites present in tree): saveConfigToFile writes `rotation = N;` at src/configManager.cpp:1176; configHasChanges compares it at 1398; saveConfigIncremental's [top_oled] chain updates it at 2121-2123. Parser was already there (770). For a config.txt that CONTAINS a rotation line the original scenario is dead - the value round-trips and the rotation-only save no longer no-ops. It is NOT dead for files lacking the line (issue 1), which is every currently-flashed bench unit.

BOTH TARGETS: no gap. env:jumperless_og extends env:jumperless_v5 and its build_src_filter only drops boards/v5/ and routing/NetsToChipConnections.cpp (platformio.ini:186-260); configManager.cpp, Menus.cpp and oled.cpp compile identically on RP2040, and none of the three fix sites sits behind an OG_JUMPERLESS or PICO_RP2350 guard.

BOUNDARIES: rotation is `int` (config.h:389). Out-of-range values persist verbatim but cannot crash - Adafruit_GFX::setRotation takes uint8_t and masks `& 3`, at both the live-apply (Menus.cpp:4801) and boot-apply (oled.cpp:652) sites.

COLLATERAL: none that breaks old behavior. config.txt grows one line; the parser is key-keyed so ordering is irrelevant; configHasChanges only becomes MORE willing to save. One inherent consequence of the fix working: a rotation of 1 or 3 is now sticky across reboots, and oled.cpp:580-581 keeps displayWidth/displayHeight from config (unrotated) while GFX swaps its own width/height - so a 90/270 UI mislay no longer self-heals on a power cycle. Pre-existing inconsistency, recoverable via `~ top_oled rotation 0`, not a regression in the fix.

OUT OF SCOPE, spotted while tracing (different finding, same audit batch): the needsReset restore at configManager.cpp:948-959 omits usb_cdc and usb_audio, which is precisely the loss the audit's own comment at 901-908 documents and guards against in the sibling branch at 889-908.

</details>

## #42 — INCOMPLETE — usbMountedByHost write-guard missing from safeFileDelete/safeFileRename/safeMkdir — firmware can del

- **[medium]** `src/remembering/FilesystemStuff.cpp:3247` — deleteDirectoryContents() still has NO usbMountedByHost guard — the exact second half of the finding's fix_sketch was not implemented. Its FatFS.rmdir(fullPath) at :3247 is a raw FAT/directory write with no guard, no fs_mutex, and no pauseCore2ForFlash. Reachable while mounted via `config clear_filesystem` / `factory_reset` (src/configManager.cpp:3581, :3601, :3755, :3776) — none of those callers check the mount flag either. Trace: a populated subdir now fails (safeFileDelete refuses → success=false → `continue` skips rmdir), but an EMPTY subdir makes the recursion return true and rmdir executes, removing the directory entry behind the host's cached FAT. Nested empty trees are removed wholesale. Same original corruption class, narrower reach.
- **[medium]** `src/JumperlessMicroPythonAPI.cpp:3572` — Sibling raw paths the fix forgot: the MicroPython os.* bindings jl_fs_mkdir (:3548 FatFS.mkdir), jl_fs_rmdir (:3572 FatFS.rmdir), jl_fs_remove (:3587 FatFS.remove), jl_fs_rename (:3602 FatFS.rename) all mutate the FAT with no usbMountedByHost check. This same file already applies the rule for write-mode opens at :3214-3215 (`extern bool usbMountedByHost; if (usbMountedByHost && strpbrk(mode,"wa+"))`), so the inconsistency is internal to one file. A script doing os.remove/os.rename/os.mkdir from the REPL (CDC) while MSC is mounted reproduces the original bug verbatim — the safe* wrappers are bypassed entirely.
- **[medium]** `src/remembering/FilesystemStuff.cpp:4671` — Guard placed at the *Raw* layer instead of the public wrapper, so the FileCache is mutated before the refusal — state left behind on the new early-exit path. safeFileDelete (:4669-4673) calls fileCacheDelete(path) first; that is fileCacheInvalidate → resetEntry (src/remembering/FileCache.cpp:1143-1152), which discards a DIRTY entry unconditionally. While mounted the raw delete then refuses: the newest (unflushed) content is destroyed, the stale file survives on flash, and the caller gets false. Pre-fix the delete succeeded so cache and flash agreed. Contrast the established pattern in safeFileWriteAll (:4460-4472), where the guard runs before fileCacheWrite. Fix: hoist the check into safeFileDelete/safeFileRename above the cache calls (keep it in the Raw functions too for FileCache.cpp:897's direct safeFileDeleteRaw use).
- **[medium]** `src/remembering/FilesystemStuff.cpp:4708` — Same ordering defect on rename, with a worse residue: safeFileRename calls fileCacheRename(pathFrom,pathTo) (src/remembering/FileCache.cpp:1259-1283) which retargets the entry's path and deliberately KEEPS dirty set, then safeFileRenameRaw refuses at :4682 and returns false. Result while mounted: safeFileExists(pathTo) returns true (:4563-4566 consults fileCacheExists first) and safeFileSize(pathTo) returns a cached size for a file that does not exist on flash, while pathFrom now reads through to its old flash content — a caller that verifies a rename via exists() gets a false success. After unmount the flush service writes the dirty entry under pathTo, leaving pathFrom behind as a duplicate. Concrete path: writeConfigFile's atomic replace (src/configManager.cpp:1226-1231) does safeFileDelete(filename) then safeFileRename(temp,filename).
- **[low]** `src/remembering/FilesystemStuff.cpp:4621` — Collateral: safeMkdir's already-exists early-return (`if (FatFS.exists(path)) { ... return true; }`) now sits AFTER the new guard at :4604, so an idempotent 'ensure directory exists' call that previously performed no flash write and returned true now returns false while mounted. Callers that hard-abort on it: createSlots (src/remembering/FileParsing.cpp:400, :408 — returns early), src/routing/States.cpp:3002, :3270, src/remembering/FilesystemStuff.cpp:3546, :3571, :3596, src/Python_Proper.cpp:2985. Mostly benign because those flows need writes that are already refused, but it is a behavior change beyond removing the bug; moving the exists() check above the guard (a read, allowed while mounted) restores old-code behavior.
- **[low]** `src/remembering/FilesystemStuff.cpp:3601` — Remaining raw FAT mutators outside the wrappers, all unguarded: FatFS.rmdir("/micropython_examples") here in initializeMicroPythonExamples; src/remembering/Undo.cpp:1766-1767 FatFS.remove("/undo.log")/("/undo.snap") legacy cleanup; src/unused/TuiPopUpFileManager.cpp:531 FatFS.rmdir for the TUI file manager's directory delete (src/unused is not excluded by either env's build_src_filter, so it compiles). The first two run at boot (main.cpp:354 / undoInit) before a host mount, so exposure is low, but they are the same 'TODO: create safeRmdir' class the fix left open.
- **[low]** `src/remembering/FileCache.cpp:378` — Adjacent write hole the guard does not cover: flushEntryChunked writes raw — FatFS.mkdir(buf) at :378 to build /.bak, FatFS.open(path,"r+"/"w") at :409/:414 — with no usbMountedByHost check. The periodic flush *service* is gated at :1387, and the mount edge drains first (src/remembering/USBfs.cpp:672-679), but explicit big-event drains can fire while mounted: src/routing/States.cpp:737, :3313, :3544, src/ProjectsApp.cpp:569, :622, src/SingleCharCommands.cpp:1318, and fileCacheFlushNow("/config.txt") at src/configManager.cpp:2394. Out of the finding's literal scope (writes, not delete/rename) but the same two-writer corruption.

<details><summary>checker notes</summary>

FIX FOUND — commit 47325d3, one hunk per function, guards live at src/remembering/FilesystemStuff.cpp:4604 (safeMkdir), :4644 (safeFileDeleteRaw), :4682 (safeFileRenameRaw), each an `if (usbMountedByHost) { Serial.println(...); return false; }` before any mutex/Core2 work. The literal scenario in finding #42 is dead: the device file-manager delete (deleteFile → safeFileDelete, :1332), undoPersistHistory's safeFileDelete (src/remembering/Undo.cpp:1883, :1885), removeNetColorFile (src/remembering/FileParsing.cpp:2895), and the config atomic replace (src/configManager.cpp:1226-1227) all now refuse while mounted.

BOTH TARGETS: covered. The guards are unconditional C++ with no #ifdef; usbMountedByHost is defined unconditionally at src/remembering/USBfs.cpp:33; env:jumperless_og inherits build_src_filter from env:jumperless_v5 (platformio.ini:257-260) and only subtracts boards/v5/ and routing/NetsToChipConnections.cpp, so src/remembering/ compiles identically on RP2040 and RP2350. No target-specific gap.

WHY NOT CORRECT: (1) the fix_sketch's own second clause — "make deleteDirectoryContents refuse outright when mounted since its FatFS.rmdir calls are raw" — was not implemented; empty directories are still rmdir'd behind a mounted host from `clear_filesystem`/`factory_reset`. (2) The MicroPython os.* bindings are a wide-open sibling of the identical class in a file that already knows the rule. (3) The guard sits one layer too low, so both public wrappers commit their FileCache side effects before returning false, producing cache/flash divergence (rename) and dirty-data loss (delete) that the pre-fix code did not have as a guaranteed outcome.

NO REGRESSION when unmounted: usbMountedByHost is false, the added blocks are skipped, and nothing else in the hunks changed. The only unmounted-path behavior change is none; the mounted-path changes are the intended refusals plus the safeMkdir already-exists case noted above. Not built — pio is broken under Python 3.14 in this env and the change is a plain if-statement over an already-included extern (USBfs.h at FilesystemStuff.cpp:13, same symbol already used at :3355/:4229/:4467).

</details>

## #43 — OK+notes — Slot /.bak recovery reads mirrors that nothing writes anymore; on upgraded units it silently restore

- **[medium]** `src/routing/States.cpp:3386` — loadSlot's parse-failure early return is unguarded, and the fix makes it the ONLY outcome of a corrupt slot on every unit (previously masked on upgraded units by the mirror rescue). fromYAML() clear()s then parses in place (1453), and clearDirty() runs only on success (1688) - so a partial parse that got as far as any addConnection (markDirty at 653/700) returns false with activeState holding partial/foreign content AND dirty=true, while activeSlotNumber still names the PREVIOUS context because it is only assigned on success (3435). SlotManager::service()'s idle auto-save (4626 -> 4819) then writes that partial state over the previous slot's file. loadSlotFromPath has the restore + terminal-NO-ACTIVE-CONTEXT machinery for exactly this hazard (3547-3628, and its own comment at 3473-3477 names it); loadSlot has none. Reachable via USBfs.cpp:772/932 (MSC-eject reload calls mgr.loadSlot(netSlot) and only logs on failure - no restore, no clearDirty), MicroPythonAPI:1800, SingleCharCommands:1694. Boot (main.cpp:1450-1459) is safe: it calls clearActiveSlot() which clear()s dirty. Pre-existing, not introduced - but the fix removed the thing that was hiding it.
- **[low]** `src/routing/States.cpp:3600` — Comments across the tree still assert a rescue that can no longer run, and one of them is now materially wrong about behavior: 3599-3601 says the terminal NO-ACTIVE-CONTEXT state is 'genuinely rare' for numbered contexts BECAUSE 'loadSlot has a /.bak mirror rescue' - with the rescue gated off, a corrupt numbered slot reaches the terminal state deterministically, same as the FILE case. Same drift at States.cpp:3337-3340 (the pre-fix 'the ABA flusher mirrors every save' comment still sits directly above the gate that contradicts it), 3515-3516, 3585-3590 ('NUANCE, deliberate' describes a dead code path), States.h:800, ProjectsApp.cpp:489.
- **[low]** `src/routing/States.cpp:3387` — Cosmetic collateral: the wrapper that named the slot ('Failed to parse YAML slot N (no usable mirror): ...') lives only in the #else branch (3382), so in the shipping build errorMsg is fromYAML's raw text (e.g. 'Missing opening brace in bridge entry') with no slot number. Callers surface it verbatim - USBfs.cpp:779-780 prints 'Failed to reload slot: <msg>' without the number, main.cpp:1452-1455 likewise. Old-code message content not preserved.
- **[low]** `src/routing/States.cpp:3349` — Incompleteness against the finding's consequence (a), acknowledged by the chosen option rather than fixed: no /.bak writer was added, so slot YAMLs now have zero power-loss/corruption recovery on both targets, and no migration deletes the stale tree - the fix only prints 'delete /.bak if you like'. The tree keeps occupying flash and is only removable by the user via MicroPython os/jl_fs_rmdir (JumperlessMicroPythonAPI.cpp:3565) or MSC, and /.bak is a dot-directory so it may not appear in the file browser. The finding's fix_sketch explicitly permitted this either/or, so not a defect - a documented gap.

<details><summary>checker notes</summary>

Original bug is truly dead on both targets. src/routing/States.cpp:3343 wraps the mirror read in #else of #if !USE_FILE_CACHE; USE_FILE_CACHE defaults 0 at src/remembering/FileCache.h:46, States.cpp includes FileCache.h at line 22 (macro genuinely defined, not an undefined-token fallthrough), and platformio.ini defines it in no env - jumperless_og inherits ${env:jumperless_v5.build_flags} verbatim (platformio.ini:237-238) and only swaps boards/ + NetsToChipConnections. Both objects were rebuilt after the edit (States.cpp mtime 21:57:14; og .o 21:57:24, v5 .o 22:20), so 'both targets build green' checks out. activeState.markDirty() after a mirror restore (3375) is now unreachable, killing the save-stale-over-canonical consequence. No sibling door: the only other /.bak code in the tree is FileCache.cpp, entirely inside #if USE_FILE_CACHE (76 / #else 1542 / #endif 1616), and the pass-through stubs touch no mirror; main.cpp:578 gates the flush service identically. Gated build has no unused-var churn (bakPath read at 3349, recovered read at 3386) and a -DUSE_FILE_CACHE=1 build restores the old rescue intact. The new safeFileExists+print only fires on an actual parse failure with a mirror present, so fresh units see no new output. Real remaining exposure is the failure path the fix now routes everything into (issue 1).

</details>

## #6 — INCOMPLETE — pollParts identity check is descriptor-equality only - parts-table compaction (jl_remove_part) can s

- **[medium]** `src/routing/PartPlacement.cpp:722` — The fix's premise — "part names are unique" (stated at src/JumperlessMicroPythonAPI.cpp:2087) — is enforced only on the two CREATION paths (jl_place_part refuses a dup at JumperlessMicroPythonAPI.cpp:1929; partsCommitPlacement auto-suffixes at src/PartsApp.cpp:425-439). commitPart() never checks findByName before `st.parts.parts[st.parts.numParts++] = p;`, so a slot YAML carrying two parts named SSD1306 loads both. The ORIGINAL scenario then reproduces verbatim: jl_remove_part("SSD1306") resolves findByName to the first entry, removePartPlacement pulls its bridges, the shift at JumperlessMicroPythonAPI.cpp:2104 slides the duplicate into inst.partIdx, and the DisplayService.cpp:229-232 triple (index / desc / name) all match -> gone=false -> beaconing forever against torn-down bridges. Slot files are hand-editable text and default names are non-unique by construction (partdbInstantiate, src/partdb/PartDb.cpp:169-179, names every SSD1306 record "SSD1306"), so the collision is one hand-edit or one imported file away.
- **[medium]** `src/displays/DisplayService.cpp:229` — Sibling table-mutation path left open: a slot load. States.cpp:1619 -> deserializeParts (PartPlacement.cpp:845) sets numParts=0 and rebuilds the parts table, and the bridge table is replaced wholesale from the file (parts are NOT re-expanded on load — applyPartPlacement has no load-time caller). Nothing notifies DisplayService (grep: displayService is referenced only from NetVoltageScan.cpp:794/811/1147), and inst.partIdx / inst.desc / attachedName all survive the load. Because partdbInstantiate names every panel after its record id, "SSD1306" at index 0 is the ordinary name in EVERY slot that holds one — so load a second slot whose SSD1306 sits at the same index and was never routed (connect fields -1, no GP20/21 bridges in its netlist) and the identity check passes: the service stays ALIVE, keeps flushing a bus with no bridges, and routeDataPins never re-runs because it is reachable only from attach(). Same harm as finding #6, different trigger — exactly what the fix_sketch's "placement generation counter bumped by any table compaction" would have caught and what name-equality does not. Pre-existing rather than introduced, but the fix left the door open.
- **[low]** `src/displays/DisplayService.cpp:229` — The identity check is asymmetric with the attach predicate: attach()'s scan requires p.placed (DisplayService.cpp:249) but pollParts' `gone` test does not. A same-named, same-driver part that comes back with `placed: false` (slot YAML authored or reloaded that way — removePartPlacement sets placed=false at PartPlacement.cpp:414) keeps the service bound to a part it would never have attached to, with no bridges. Adding `|| !globalState.parts.parts[inst.partIdx].placed` to the disjunction closes it in one term.
- **[low]** `src/displays/DisplayService.cpp:168` — New reentrancy window the old desc-only check did not have. attach() publishes inst.partIdx at line 102 but writes attachedName only at line 168, AFTER routeDataPins()->applyPartPlacement()->refreshConnections(). A pollParts re-entered inside that call would see partIdx>=0 with a stale/empty attachedName -> mismatch -> detach(), which frees inst.fb; attach() then resumes into `memset(inst.fb, 0, ...)` at line 164 on a null pointer. Under the old code displayResolveForPart still matched inst.desc during that window (removePartPlacement only clears `placed`), so no detach fired. Today it is unreachable: pollParts stamps nextPartsPollMs at line 225 BEFORE the check and bails at line 220 on re-entry, plus the inModalContext gate at 224. Latent only — but the safety now rests entirely on that stamp ordering, which is undocumented. Setting attachedName next to `inst.partIdx = partIdx` at line 102 removes the window outright.

<details><summary>checker notes</summary>

FIX LOCATION (commit 57cf63d, only commit touching the symbol): new member `char attachedName[16]` at src/displays/DisplayService.h:66; written at src/displays/DisplayService.cpp:168-169; cleared at :215 (detach); compared at :229-232 (pollParts).

1. ORIGINAL SCENARIO IS DEAD (as stated). Two same-model panels: PartsApp.cpp:425-439 uniquifies the second to SSD1306_2 (MAX_PARTS suffix cap), and jl_place_part refuses an exact dup outright (JumperlessMicroPythonAPI.cpp:1929-1934). Remove the attached one -> the compaction shift (JumperlessMicroPythonAPI.cpp:2102-2106) slides SSD1306_2 into inst.partIdx -> strncmp != 0 -> gone -> detach(). Next poll re-enters the scan at :246-255, attaches the survivor, and routeDataPins runs from attach() and actually routes it (its connect fields are -1, so it takes the full remove/mutate/reapply path rather than the "already routed" early-return at :58-60). The dead-forever beacon is gone.

BUFFER/BOUNDS: attachedName[16] vs PartDefinition::name[16] — every writer bounds the name to <=15 chars + NUL (jl_place_part:1921, parsePartLine PartPlacement.cpp:736 rejects >15, partdbInstantiate caps at sizeof-1, PartsApp truncates base to 12 for "_16"), so strncmp over sizeof(attachedName)=16 reads exactly the 16-byte field and can never mis-compare a truncated prefix. Short-circuit order is correct: `inst.partIdx >= numParts` guards the array read that follows.

2. OTHER TARGET: OG is unaffected by construction, not by luck — DisplayService::service() returns IDLE at DisplayService.cpp:298 on !caps.breadboardDisplays, which is false at src/boards/og/board_og.cpp:114 (true at board_v5.cpp:117); displayBusAcquire has the same gate at DisplayBus.cpp:141. Shared code, no #ifdef around the fix, so it compiles identically for jumperless_og. Not an incomplete-target case.

3. COLLATERAL: none found. The name term adds exactly one new detach trigger — a different part occupying inst.partIdx — which is the bug case. No in-place rename path exists (audited every write to parts.parts[] and to PartDefinition::name across src/): names are only assigned at construction. Removing a LOWER-index part while the display survives still behaves as before (index slides, name mismatches, detach -> re-attach, and routeDataPins early-returns at :58-60 because connect already equals the bus nodes, so no fabric churn and no extra dirty). Cost is 16 bytes of service RAM. attachedName is reset in detach() (:215) and on every early-exit path partIdx is -1, so no stale value is ever read.

WHY fix_correct=false: the fix substitutes NAME equality for a real identity token, and name uniqueness is a creation-path invariant only. Both residuals above (load-path duplicates, slot swap) are the same failure the finding describes, reached through a table mutation the fix does not observe. The fix_sketch's alternative — a placement generation counter bumped by ANY table mutation (compaction, clear, deserializeParts) — closes all three in one term.

</details>

## #7 — INCOMPLETE — PWM and the display bus are mutually invisible - each silently kills the other on the same pin

- **[medium]** `lib/micropython/port/machine_pwm_jl.c:171` — Forgotten caller — the original symptom is reproducible verbatim. `machine.PWM(24, freq=1000, duty_u16=32768)` calls gpio_set_function(24, GPIO_FUNC_PWM) and registers NO claim: it never touches globalState.config.gpioPwmEnabled (only Peripherals.cpp setupPWM does) and never calls jl_gpio_claim_pin. mp_hal_get_pin_obj (lib/micropython/port/mphalport.h:50) accepts a bare int, so no Pin object is constructed. displayBusUserClaimed (src/displays/DisplayBus.cpp:200) reads only those two config arrays, so it returns false, the service never yields, and the bit-bang keeps writing SIO into a PWM-muxed pad: 8 phantom bus errors -> "DISPLAY lost (8 bus errors - re-beaconing)" (src/displays/DisplayService.cpp:421) -> endless beaconing, with the PWM waveform on the panel's SDA row. Note `machine.PWM(machine.Pin(24))` IS covered (machine_pin_jl.c:295 claims); the raw-int form is the hole. mpconfigport.h:389-390 enables machine.PWM and points the INCLUDEFILE at this copy; lib/micropython/micropython_embed/port/machine_pwm_jl.c:171 is the mirror.
- **[medium]** `src/displays/DisplayBus.cpp:183` — NEW defect introduced by this fix. The early-return added to displayBusRelease is per-INSTANCE, not per-PIN: displayBusUserClaimed is true if EITHER GP24(idx4)/GP25(idx5) is claimed, so a claim on only one pin makes release forget BOTH. Repro: display ALIVE (acquire set gpioState[4]=gpioState[5]=6, both pads SIO outputs) -> jumperless.pwm(5,...) claims idx4 only -> service yields (DisplayService.cpp:304) -> user removes the part -> pollParts detach() -> displayBusRelease early-returns. GP25 is left as a driven SIO output parked at whatever level the last transfer ended on, with gpioState[5] still 6 and gpio_function_map[5] still SIO. setGPIO() then skips index 5 forever (src/Peripherals.cpp:796) so the persisted direction/pulls are never re-applied, and readGPIO takes the raw-read branch (src/Peripherals.cpp:1362) so the UI shows a constant level with no floating detection. Nothing clears it — stopPWM only resets gpioState[4] (Peripherals.cpp:3233ish) — so the pin stays stuck until reboot or a full display attach/release cycle with nothing claimed. Pre-fix, release always cleared both pins. Fix: test and clear each pin independently.
- **[medium]** `src/displays/DisplayBus.cpp:204` — NEW behavior regression from making a persisted flag load-bearing. config.gpioPwmEnabled is serialized into the slot YAML (src/routing/States.cpp:2245) and restored on every load (States.cpp:2381 via deserializeConfig, reached from JumperlessState::fromYAML at States.cpp:1435), and setupPWM (the only thing that sets it) is never re-run at boot — nothing re-establishes the PWM hardware. So after any jumperless.pwm(5|6, ...) call followed by a power cycle or slot load, the flag is true with no PWM running, displayBusAcquire refuses at DisplayBus.cpp:158, and the display is permanently dead with "DISPLAY paused: GPIO claimed by your script" — misleading precisely here, since no script is running and the user has no obvious way back except calling pwm stop on a pin they think is idle. Pre-fix the display attached normally in this state.
- **[low]** `src/routing/InfraPaths.cpp:276` — Sibling claimant the fix sketch named ("and probe-assigned GPIO") and the fix omitted, in BOTH directions. rpGpio's candidate walk skips python-owned, PWM-enabled and OLED pins but never checks gpioState[i]==6, and displayBusAcquire never checks the infra claim. Unguarded ordering: probe takes GPIO 5/6 as the buffer feed while no display exists (so nonInfraBridgeTouches is false) -> probeGpioPowerHwClaim (src/Probing.cpp:6544-6559) overwrites gpioState[idx] and drives the pad output-high at 12 mA -> a display part is then placed -> displayBusAcquire sees neither claim, acquires, and bit-bangs onto the probe's live 12 mA feed. When infra re-evaluates and releases, probeGpioPowerHwRelease restores the STALE saved gpioState (src/Probing.cpp:6573) and sets the pad to input, stripping the 6 mark off a live bus — which puts setGPIO back to re-asserting config pulls mid-transaction, the exact failure the mark exists to prevent (Peripherals.cpp:790-797). Reachability is narrow (GPIO feed is the DAC0 fallback, walks 8->1 so GP24/25 are reached last), and the reverse order self-heals via nonInfraBridgeTouches once the part is routed.
- **[low]** `src/Peripherals.cpp:3081` — Incomplete fix: the sketch's second half ("make setupPWM refuse or warn on gpioState==6 pins") was not implemented. setupPWM validates only pin range and frequency, and setupSlowPWM (Peripherals.cpp:2921) only range/frequency/duty; neither consults gpioState[] or gpio_function_map[]. Residual harm is much smaller than the original because the display now yields before the flush, but the PWM side is still silent about the conflict, the waveform still drives the paused panel's SDA/SCL rows, and setupSlowPWM additionally arms a repeating-timer callback (Peripherals.cpp:2898-2916) that gpio_puts the pin from IRQ, which can corrupt the in-flight burst during the up-to-one-service-period window before the yield.

<details><summary>checker notes</summary>

FIX FOUND, commit 57cf63d, src/displays/DisplayBus.cpp:200-205: displayBusUserClaimed now ORs globalState.config.gpioPwmEnabled[] for both bus pins alongside gpioPythonOwned[]. Two consumers make it bite: displayBusAcquire refuses at DisplayBus.cpp:158 (reverse direction — placing a display no longer flips funcsel back to SIO under a running PWM), and DisplayService.cpp:304 yields BEFORE any bus traffic (forward direction — no 8 phantom bus errors, no "DISPLAY lost", no endless beaconing, one explanatory line). I verified DisplayService::service() is the ONLY path to the wire: every displayI2c*/flushChunk call is inside DisplayDrivers.cpp ops invoked from service() (lines 335/356/397/414), and there is no MP display API that writes directly, so the yield gate has no bypass. Indexing is sound (pins 24/25 -> idx 4/5, matches gpioDef[][2]; the <20 guard covers sdaPin==-1). ORIGINAL SCENARIO: dead for the named jumperless.pwm()/setupPWM path, alive verbatim for machine.PWM(int).

BOARD TARGETS: fix is target-complete. OG cannot hit bug #7 at all — caps.breadboardDisplays is false (src/boards/og/board_og.cpp:114), so DisplayService::service() returns IDLE at DisplayService.cpp:298 and displayBusAcquire refuses at DisplayBus.cpp:141 (that caps belt was added by the same commit). Separately and PRE-EXISTING, not attributable to this fix: setupPWM has no board guard, so on OG jumperless.pwm(6,...) maps to RP pin 25 = the WS2812 breadboard-LED data line and pwm(5,...) to pin 24, exactly the pins initGPIO refuses to touch on OG (Peripherals.cpp:423-435). Worth a separate finding.

COLLATERAL: two user-visible behavior changes beyond removing the bug, both listed above — the per-pin release leak (issue 2) and the stale persisted-flag lockout (issue 3). Deliberately NOT reported: the sub-one-service-period race between enabling PWM and the yield check (a few corrupted bytes, self-heals via displayBusUnstick + retry); the "claimed by your script" wording for setupPWM (accurate — setupPWM is reachable only from the MP API at JumperlessMicroPythonAPI.cpp:2740, there is no serial/menu PWM command); setGPIO's PWM skip at Peripherals.cpp:778, which predates the audit (commit 19ee950).

</details>

## #8 — INCOMPLETE — All three modal gates key on getBlockingService(), which is always nullptr inside the synchronous mo

- **[medium]** `src/displays/DisplayService.cpp:323` — Over-broad gate: innerDepth is also raised outside modal UI. mp_hal_delay_ms pumps jOS.serviceInner() every 50 ms (src/snakes/Python_Proper.cpp:277), and MpRemoteService::service() executes mpremote/ViperIDE raw-REPL scripts INLINE from inside serviceInner (src/snakes/MpRemoteService.cpp:101-125), so innerDepth >= 1 for a whole script's lifetime. During a foreground script serviceAll() does not run at all (comment at src/snakes/Python_Proper.cpp:4982: 'services keep ticking... because jOS.serviceAll() runs inside MicroPython's delay hook' - it is serviceInner), so DisplayService is dispatched ONLY under innerDepth>0. Result: the beacon/init gate here and the routing gate at :224 return early for the entire script. A display part placed, or a panel powered up, while a long script runs never attaches and never initializes - it sits in ROUTED, dark, indefinitely. Pre-fix (dead gate) it came alive. Self-heals at script exit (neither nextBeaconMs nor nextPartsPollMs is advanced while gated), so it is starvation, not a wedge. V5-only; DisplayService returns IDLE on OG at :293. Suggested narrowing: exclude the MicroPython delay-hook path (e.g. a separate depth/flag for UI modal loops, or skip the increment when jl_vm_exec_depth>0 / the caller is mp_hal_delay_ms).
- **[low]** `src/displays/DisplayService.cpp:388` — Same over-breadth on the throughput path: `bool modal = jOS.inModalContext()` now also latches true for the whole of any MicroPython script (same serviceInner sources as above), so chunkBytes is halved (:390) and the burst budget drops 1500us -> 700us (:393) for every flush during a script, not just during menu/probe UI. Roughly 2x slower panel refresh whenever Python is running. Currently cosmetic only - it affects just the startup animation, because userOwnsContent (src/displays/DisplayTypes.h:96) is never set true anywhere in the tree, i.e. the MP blit path is not wired yet - but it becomes a visible regression the moment scripts can draw.

<details><summary>checker notes</summary>

FIX FOUND, ORIGINAL SCENARIO DEAD.
Mechanism: new member `uint8_t innerDepth` (src/JumperlOS.h:353), bumped around the serviceInner dispatch loop (src/JumperlOS.cpp:318 `innerDepth++`, :340 `innerDepth--`), exposed as `bool inModalContext() const { return innerDepth > 0 || blockingService != nullptr; }` (src/JumperlOS.h:328). All three DisplayService gates rekeyed off it in 57cf63d: routing/attach gate src/displays/DisplayService.cpp:224, beacon+init gate :323, flush halving :388. DisplayService overrides inInnerSet()=true (src/displays/DisplayService.h:36), so it IS dispatched by serviceInner -> depth>0 -> all three gates now engage inside probeMode/click menus/pad menus/app loops. The ~45 ms monoOledInit burst and every routeDataPins/applyPartPlacement rebuild are genuinely deferred now. Matches the fix_sketch exactly.

ATTACK RESULTS (all clean except the two issues above):
- Balance: no early return, break, or return statement between ++ and -- (src/JumperlOS.cpp:318-340); loop only uses `continue`. No leak path.
- Longjmp/NLR: MicroPython's raise points are OUTSIDE the serviceInner call (mp_hal_check_interrupt at src/snakes/Python_Proper.cpp:252, mp_handle_pending at :264, both above the serviceInner at :277), and MpRemoteService's pyexec_event_repl_process_char pushes its own NLR landing pad inside the frame. No unwind skips the decrement, so innerDepth cannot leak to a permanently-true inModalContext().
- Nesting: script sleep inside a modal loop nests to depth 2 and unwinds symmetrically. uint8_t overflow needs 255 levels - unreachable.
- Cross-core: single manager (`static jOSmanager inst(1)` at src/JumperlOS.cpp:79-82, `jOSmanager& jOS` at :41); loop1() (src/main.cpp:1538+) never calls jOS.* - grep confirms zero `jOS.` uses past 1538. So the non-atomic, non-volatile uint8_t is single-core-owned. No race.
- Reentrancy of the chunkBytes save/restore (:391-395): nothing in src/displays/ calls serviceInner or delay(), and runService (src/JumperlOS.cpp) has no guard, so re-entry would corrupt `saved` - but it is unreachable. Safe.
- Forgotten siblings: src/eyecandy/StepViewer.cpp:256 still uses getBlockingService(), but StepViewer is HIGH with no inInnerSet override (src/eyecandy/StepViewer.h:42) so serviceInner never reaches it, and it has its own inClickMenu/inPadMenu/probeActive guards. Not a hole. forceServiceByName/ByIndex do not bump innerDepth, but their only callers are the MP API (src/JumperlessMicroPythonAPI.cpp:2760, 2767) - an explicit user call, not a modal loop. No modal loop pumps serviceAll() instead of serviceInner (only main.cpp's USB-MSC mode loop, a separate mode).
- All routing is behind the pollParts gate: routeDataPins is called only from attach (src/displays/DisplayService.cpp:146), attach only from pollParts (:241, :252). No sibling route path.
- BOTH TARGETS: the mechanism lives in shared src/JumperlOS.{h,cpp}, board-agnostic. OG never had exposure - DisplayService::service() returns IDLE at src/displays/DisplayService.cpp:293 on !caps.breadboardDisplays. OG env compiled after the fix (.pio/build/jumperless_og/firmware.elf mtime 2026-08-26 00:56 vs 57cf63d at 2026-08-25 21:48), so no build break on RP2040.

COLLATERAL BEYOND THE TWO ISSUES: none. Gated returns happen before nextBeaconMs/nextPartsPollMs are advanced, so the panel comes up on the first ungated tick after the modal context ends - matches the code's own promise. Other changes in the same hunks (UndoIngestGuard, alloc-before-route, routeFails backoff, attachedName identity, activeDataNodes YIELDED) are separate audit findings, not side effects of this one.

</details>

## #9 — STILL-LIVE — Power auto-route rowNetHas guard judges only the pre-placement netlist, so a placement-created confl

- **[medium]** `src/PartsApp.cpp:403` — ORIGINAL BUG STILL LIVE. The landed change only broadened the guard from "opposing special node" to "any power node" (403-404) and added the role-VCC gate (411). The loop at 393-418 still decides EVERY pin's `connect` against the pre-placement netlist and never compares sibling pins that resolve to the same net, which is the finding's headline scenario. Repro in the current tree: leftover user bridge 5-35; place a 555 (pinout 53, src/partdb/PartDbData.h:902-910 - pin1 GND class2, pin8 VCC class1 role1) with the DIP anchor tap on row 35. partPinNode (src/routing/PartPlacement.cpp:69-78) gives GND->35, VCC->5. rowNetHas finds no power node in net {5,35}, so rowPowered is false for BOTH pins: 35->GND and 5->TOP_RAIL are both assigned. Both st.addConnection calls succeed (connectionAllowed, src/routing/MatrixState.cpp:421, only refuses direct special-special pairs), err stays empty, and PartsApp prints `PARTDB place ok`. Nothing downstream catches it: partGeometryOk (src/routing/PartPlacement.cpp:175-268) never looks at nets, and expandOnePart (src/routing/PartPlacement.cpp:345-368) only dedupes exact bridges. The fix_sketch's part (a) (class-1 also testing the opposing rail) landed; part (b) (per-part simulated net merge) did not.
- **[medium]** `src/routing/NetManager.cpp:461` — Consequence of the above, and why it is silent rather than a hard short: at rebuild the GND bridge merges {5,35} into net 1 (combineNets swaps so the <=5 special net absorbs, NetManager.cpp:557-561). The 5-TOP_RAIL bridge then sees net1 vs net2, both <=5, so line 461 skips combineNets and returns 2 WITHOUT ever assigning paths[newBridgeIndex].net (contrast 455, 480, 501, 539, 554, 565 - every other branch sets it). Net effect: the auto-routed rail bridge is committed to bridges[], persisted to the slot file, listed in the bridge table, and never routes; the parts table reports the VCC pin as `connect: TOP_RAIL` while the chip's VCC actually sits on GND. Order-dependent - if the rail bridge merges first, the GND bridge is the dropped one instead. Mitigating: PartLabels does fire PARTWARN vcc_to_gnd (src/eyecandy/PartLabels.cpp:233-237), so the "or PartLabels warns" half of the guard's promise at src/PartsApp.cpp:380-382 holds; the "no self-made conflict" half does not. Identical on both targets - PartsApp.cpp is in the OG build (platformio.ini:186-188, jumperless_og extends jumperless_v5 incl. build_src_filter), the code is board-agnostic, and the <=5 special-net rule is shared.
- **[low]** `src/PartsApp.cpp:412` — COLLATERAL (latent, not a live regression). A class-1 VCC pin whose row is already tied to a rail, and a class-2 pin whose row already reaches GND indirectly, now keep connect = -1 where the old code set TOP_RAIL/GND. That is the intended no-adoption fix, but pin.connect is ALSO the geometry authority for compact placement: partPinNode returns pin.connect for a compact-eligible leg (src/routing/PartPlacement.cpp:136-157, 205-208), so -1 turns the leg into an open leg that follows its partner or falls back to the footprint row. Unreachable from this path today because partdbInstantiate memsets placement to EXPANDED (src/partdb/PartDb.cpp:163), so it only bites if placement mode is ever chosen before this loop runs.
- **[low]** `src/PartsApp.cpp:388` — Pre-existing weakness the fix made load-bearing three times per pin instead of once: rowNetHas's scan stops at the first zero in net.nodes[]. Any hole in a net's node list (the same table the audit had to bound in NetManager for the 0x7f fallback) makes rowPowered read false and re-opens the pre-existing-power-net case the fix was meant to close. A count-bounded scan, or reusing the netlist's own membership helper, would be immune.

<details><summary>checker notes</summary>

Fix located: src/PartsApp.cpp:383-418 (only touched by 57cf63d; PartsApp.cpp is unmodified in the working tree). The audit ledger CodeDocs/RELEASE_AUDIT_2026-08-25.md:70 claims it under "pre-placement netlist / bridge-adoption guards" — that claim covers only the sub-case where the row's net ALREADY holds a power node, which is genuinely closed now (a second part landing a GND pin on a row a first part put on the top rail is correctly skipped). The finding's actual scenario — the conflict CREATED by the placement, two of the same part's own pins resolving into one pre-existing non-power net — is untouched. fix_present=false.

Attacks that came back clean, so the partial fix introduces nothing new: (1) po.pins[j] <-> tmp.pins[j] index alignment for the role gate at 411 is sound — partdbInstantiate copies pins 1:1 in order with numPins = min(po.numPins, MAX_PART_PINS) (src/partdb/PartDb.cpp:194-206); the `j >= po.numPins` half is defensive-only. (2) OG's MAX_PART_PINS 16 vs V5's 24 (src/routing/States.h:63,66) does not misalign it — partdbPlaceableHere refuses over-wide records first (src/partdb/PartDb.cpp:100-104). (3) Role-gate collateral is exactly the intended set: the whole DB has only 6 class-1 pins with role != VCC and all are 405x VEE / regulator VOUT (src/partdb/PartDbData.h:576, 791, 808, 937, 942, 945) — no legitimate VCC pin lost auto-routing. (4) No sibling auto-route path was forgotten: 413/416 are the only sites in the tree that ever assign a power connect; DisplayService::routeDataPins touches SDA/SCL only, and the MicroPython place_part path (src/JumperlessMicroPythonAPI.cpp:2044) never auto-routes power. (5) The rollback added in the same hunk (src/PartsApp.cpp:466-473) leaves no state behind — removePartPlacement drops the bridges and addConnection/removeConnection invalidate the cache themselves (src/routing/States.cpp:650, 697).

Minimal completion, matching the sketch: inside the assignment loop keep a per-part tally of which special node each already-assigned sibling pin's row resolves to (findNodeInNet(rowA) == findNodeInNet(rowB)), and refuse the second half of an opposing pair with the same leave-unrouted policy — plus fold rowPowered into that tally so a pin that resolves onto a net a sibling just claimed is caught too.

</details>

## #0 — OK+notes — Power auto-routing keys on pinClass alone, wiring regulator outputs and mux VEE pins to a positive r

- **[low]** `src/PartsApp.cpp:380` — Stale comment introduced by the fix: the block comment at 376-382 still says a row is skipped only when its net holds "the OPPOSING special node", but the code at 403-404 now skips on ANY power node (GND, TOP_RAIL, or BOTTOM_RAIL) for BOTH pin classes. The inline comment at 398-402 states the real rule; the two contradict each other and the next reader hits the stale one first. Doc-only, no behavioral defect.
- **[low]** `src/PartsApp.cpp:472` — Rollback asymmetry on the refusal path (new code in this fix). expandOnePart skips a pin whose bridge ALREADY exists (PartPlacement.cpp:360 hasConnection), but removePartPlacement removes any pin bridge that exists (PartPlacement.cpp:407), so a rollback deletes a pre-existing USER bridge that happens to be identical to one of the part's pin bridges. Only reachable when applyPartPlacement returns err text (bridge table full) AND the user already jumpered exactly node->connect for a pin with an explicit DB binding; the new rowPowered gate at 403-404 precludes the coincidence for auto-power pins. Same class as the acknowledged "adopting it would delete their wire" hazard.

<details><summary>checker notes</summary>

FIX: src/PartsApp.cpp:411 `if (j >= po.numPins || po.pins[j].role != PARTDB_ROLE_VCC) continue;` (57cf63d, untouched since). Rails now require role VCC, not just pinClass==1.

BUG DEAD. Parsed every pin row of src/partdb/PartDbData.h (30-1127): class1+role!=VCC is exactly the six pins the finding named - 4051 VEE (:571), 4052 VEE (:791), 4053 VEE (:808), LM317 VOUT (:936), L7805 VOUT (:941), AMS1117 VOUT (:946). All six now fall through unrouted. Zero role-VCC pins with class!=1 and zero class-2 pins with role!=GND, so no inverse hole. The 73 railed pins are only V+/VCC/VDD/VIN/VS. AMS1117 per-signal: VIN->rail, VOUT->nothing, GND->GND - VIN/VOUT no longer share a rail.

DURABLE: scripts/generate_partdb.py:140 maps VOUT/VEE -> (PINCLASS_POWER, "NONE"), so regenerating from data/partdb/*.yaml cannot reintroduce it. The optional YAML reclass in the fix sketch was not needed.

ATTACKS, all clean:
- Index alignment. partdbInstantiate (PartDb.cpp:199-206) copies po.pins[j] -> out.pins[j] 1:1 with n = min(po.numPins, MAX_PART_PINS), so tmp.pins[j].role IS po.pins[j].role. The launcher taps EVERY listed signal in order (PartsApp.cpp:677-690), so rows[j] maps to pin j on the SIP path; DIP takes one anchor; AXIAL2 rewrites only pinNumber. The `j >= po.numPins` guard is dead but harmless.
- Sibling paths. Only auto-power site in the tree. expandOnePart (PartPlacement.cpp:350) bridges only `connect >= 0` - no class-based rail logic anywhere downstream. DisplayService::routeDataPins touches SDA/SCL only ("VCC/GND stay untouched"). No partdb call sites in Commands.cpp, ProjectsApp.cpp, or JumperlessMicroPythonAPI.cpp. File-parsed / custom-labeled parts (PartPlacement.cpp:586-602) carry explicit connect and no role, and never reach this loop.
- BOTH TARGETS. jumperless_og inherits jumperless_v5's build_src_filter (platformio.ini:257-258), so PartsApp.cpp compiles for both; GND/TOP_RAIL/BOTTOM_RAIL are unconditional (JumperlessDefines.h:414-416); no board #ifdef in the changed lines. The only V5/OG delta is MAX_PART_PINS 24/16 (States.h:63/66), gated by partdbPlaceableHere (PartDb.cpp:102) before any placement - a dip16 4051 fits both.
- Early exit / reentrancy. placement is EXPANDED (memset 0 == PART_PLACEMENT_EXPANDED, States.h:72), so partPinNode uses footprint math - no compact partner surprise. rowNetHas's `!= 0` terminator matches the dominant NetManager convention. Rollback at 470-476 verified: applyPartPlacement sets placed=true regardless of err text (PartPlacement.cpp:388-391) and removePartPlacement does not gate on placed and is hasConnection-guarded (:402-414), so a partial apply unwinds exactly. No refreshConnections needed on that path since the fabric was never reprogrammed between add and remove. `reason` is copied before err is reused (:471).

DELIBERATE COLLATERAL (intended, not defects): (1) the six pins above are now left for the user to wire; (2) the skip broadened from "opposing node" to "any power node", so a class-1 pin on a row already on a rail no longer adds a duplicate bridge and a class-1 pin on a row holding the OPPOSITE rail no longer shorts rail-to-rail; (3) consequence of (2): the auto-route is one-shot at placement, so if the user later pulls the jumper that made the row rowPowered, the part's VCC pin stays floating with no PartLabels warning (PartLabels only warns on VCC_TO_GND / SELF_SHORT / GND_TO_HOT, eyecandy/PartLabels.cpp:234-259). Old-code behavior is otherwise preserved.

NO MIGRATION GAP: 57cf63d (fix) precedes 486eb28 (v5.7.6 "the Parts release"), so no shipped slot file carries the bad VOUT/VEE rail bridges.

</details>

## #1 — OK+notes — No board gate on OG: display bus drives GPIO 24 = CH446Q RESET and GPIO 25 = WS2812 data (fix exists

- **[low]** `src/boards/board.cpp:120` — boardCapabilitiesJson emits every other BoardCaps bool (rails_firmware_controlled, probe_pads, scanning_probe, rotary_encoder, oled, breadboard_text, psram, startup_animation, spi_dac) but omits breadboard_displays. board.h:57-61 documents this JSON as the enumerated contract an LLM/MicroPython tool reads to discover what the board can do, so an agent on either target cannot learn whether the display service exists. Not a hazard path (it cannot drive GPIO 24/25) and not a regression (the field did not exist before), but it leaves the fix's new capability invisible to the discovery surface. Insert an a.b("breadboard_displays", b.caps.breadboardDisplays) + a.s(",") pair between startup_animation and spi_dac; the Appender cap checks already guard overflow.
- **[low]** `test/test_boards/test_boards.cpp:117` — testCapabilities() pins OG/V5 expectations for railsFirmwareControlled, hasOled, hasRotaryEncoder, hasProbePads, scanningProbe, spiDac, hasPsram, hasStartupAnimation, ledsPerRow and ledCount, but has no assertion for breadboardDisplays. Both board descriptors use positional aggregate initializers (board_og.cpp:106-119, board_v5.cpp:108-122), so a future field insertion or reorder in BoardCaps could silently flip OG to true and re-open the GPIO 24/25 hazard with the host test suite still green. Add CHECK(ogBoardTopology.caps.breadboardDisplays == false) and CHECK(v5BoardTopology.caps.breadboardDisplays == true).

<details><summary>checker notes</summary>

FIX FOUND, COMMITTED (57cf63d - the finding's "uncommitted working tree" caveat is resolved), AND EFFECTIVE ON BOTH TARGETS.

Gate mechanism: new cap BoardCaps::breadboardDisplays (src/boards/board.h:91-93), false at src/boards/og/board_og.cpp:114, true at src/boards/v5/board_v5.cpp:117. Enforced at src/displays/DisplayService.cpp:298 (first statement of service(), before pollParts) and belt-and-suspenders at src/displays/DisplayBus.cpp:141-144 (inside displayBusAcquire, before d.sdaPin/d.sclPin are assigned at :156-157). The fix_sketch's requested comment naming OG GPIO 24 = CH446Q RESET / GPIO 25 = WS2812 data is present at DisplayBus.cpp:138-140.

ORIGINAL SCENARIO IS DEAD. service() is the sole work entry: Service (src/JumperlOS.h:118-172) declares only service() pure-virtual plus getName/getPriority - no init/begin hook that registration could call. pollParts (DisplayService.cpp:219) is called only from service() at :300, attach() only from pollParts (:241, :252), displayBusAcquire only from attach (:106). With the :298 return, a placed part resolving to a display driver on OG never reaches acquire, never routes, never bit-bangs 24/25.

ATTACKS RUN, NOTHING SURVIVED:
- Sibling front doors: displayBusAcquire/displayBusRelease/displayBusUnstick/displayI2cWrite/displayI2cWriteRaw/displayI2cPing/displayI2cCountGhosts have zero callers outside src/displays/ (grep over src/ and lib/). No MicroPython surface exists for this layer - every "display" symbol in modules/jumperless/modjumperless.c is the separate onboard-OLED stack (jl_oled_*), gated independently by caps.hasOled.
- Callers the fix might have forgotten: the only cross-module users are src/sensing/NetVoltageScan.cpp:794, :811 (activeDataNodes) and :1147 (printStats). activeDataNodes (DisplayService.cpp:445-452) returns 0 on inst.partIdx < 0 and additionally guards inst.sdaPin < 20 before nodeForRpPin; printStats (:434) returns on partIdx < 0. On OG both stay at their EMPTY defaults forever (DisplayTypes.h:75 sdaPin=sclPin=-1), so both are no-ops - no bogus node ids, no [disp] line.
- Alternate reach vector implied by the original scenario (a V5-authored slot/project YAML loaded on OG whose part already carries connect: 135/136 on SDA/SCL, bypassing DisplayService entirely): nodes 131-138 = RP_GPIO_20..27 (JumperlessDefines.h:468-470) do not appear in the OG topology's gpio table (board_og.cpp:73-78 enumerates exactly RP_GPIO_0=114, RP_UART_TX=116, RP_UART_RX=117) nor in its crossbar X map (board_og.cpp:38). Routing refuses; no crosspoint closes toward GPIO 24/25 regardless of the display layer. Hazard is dead through that door too.
- Board-target coverage: currentBoard() (board.cpp:10-16) is a compile-time #if on OG_JUMPERLESS (-DOG_JUMPERLESS, platformio.ini:240), returning a const static descriptor - no runtime init-order window where the gate could read the wrong board before board setup. The OG env inherits the V5 build_src_filter (platformio.ini:257-261 only subtracts boards/v5/ and routing/NetsToChipConnections.cpp), so src/displays/ IS compiled on OG; .pio/build/jumperless_og/src/displays/*.o (Aug 25 18:06) postdate the fixed sources (mtime 18:04, content == HEAD), so the OG target did compile with the gate in place.
- Struct-layout hazard from inserting a field mid-BoardCaps: only two aggregate initializers of BoardCaps exist in the tree (board_og.cpp, board_v5.cpp) and both got the new field in the correct slot (after hasStartupAnimation, before spiDac), matching board.h order - no silent shift of spiDac/ledsPerRow/ledCount/usbCdcCount. No mock/host-test descriptor to update.
- Early-exit state: the new branch at DisplayBus.cpp:141 returns before assigning d.sdaPin/d.sclPin, unlike the user-claimed branch at :159-163 which explicitly resets them to -1. Not reachable with stale pins - attach() is only entered from the inst.partIdx < 0 arm of pollParts, and every path into that state ran detach() -> displayBusRelease(), which sets sdaPin=sclPin=-1 (:196-199, :265... i.e. DisplayBus.cpp:191). attach()'s failure path (DisplayService.cpp:116-119) also clears desc/partIdx/state. Style asymmetry only.
- Concurrency: gate reads an immutable const descriptor; no shared mutable state added. Core 1's NetVoltageScan reads only the two int8 pin fields it already read.

COLLATERAL: none on V5. breadboardDisplays == true makes both gates straight pass-throughs; every pre-fix V5 code path (soft-bus pin choice, acquire marks gpio_function_map/gpioState[pin-20]=6, beacon/init/animate/flush, YIELDED handling) is byte-identical. On OG the only user-visible change is the intended one: a placed OLED part is now silently ignored instead of driving RESET/LED-data. Minor UX note (not a defect, not a regression): because the gate sits in service() rather than attach(), the OG user gets NO message at all - the DisplayBus.cpp:141 belt's "no breadboard displays on this board" string is unreachable on OG in the current call graph, so PartsApp.cpp:695-697's promise that "DisplayService routes a display's data pins the moment it polls" is quietly unfulfilled there.

NOT DONE, and correctly so: the fix_sketch's longer-term item (derive the soft-bus pins from a board define instead of hardcoding SOFT_SDA_PIN=24 / SOFT_SCL_PIN=25 at DisplayBus.cpp:19-20). The pins remain hardcoded, which is safe now because only V5 can reach them, and the OG meaning of those pins is documented in the fix comment as requested.

</details>

## #12 — INCOMPLETE — netSemanticName truncates in the five 16-byte call-site buffers - the 128x32 OLED (this arc's flagsh

- **[low]** `src/eyecandy/Highlighting.cpp:69` — NEW defect the fix introduced: dedup suffixes are now eaten, so identical parts become indistinguishable on the readout. PartsApp.cpp:427-433 truncates the base name to 12 chars before appending "_N", so a second ssd1306_32_i2c is named "SSD1306_32_I_2". With outLen=16 and pin "SDA", nameBudget=11, and BOTH "SSD1306_32_I2C" and "SSD1306_32_I_2"/"_I_3" render as the same string "SSD1306_32_ SDA" (verified by running the exact expression on the host). Pre-fix these rendered distinctly ("SSD1306_32_I2C " vs "SSD1306_32_I_2 "), so old-code behavior was not preserved: the fix traded instance identity for pin identity. At the bench, probing OLED #2's SDA shows a label identical to OLED #1's SDA.
- **[low]** `src/eyecandy/Highlighting.cpp:1444` — Incomplete: the five call-site buffers were left at 16 bytes (also 1460, 1478, 1613, 1632), so the part name is still mangled when nothing downstream requires it. The finding's primary remedy (28-32 byte buffers) is unblocked: ReadingDisplay::show copies into nameBuf[48] (ReadingDisplay.cpp:224) and the header renders at Andale Mono 5pt left-aligned on 128px, which fits ~30 chars. With outLen=28 the same code emits the full "SSD1306_32_I2C SDA" and both issues disappear. Only the truncation half of the sketch was applied.

<details><summary>checker notes</summary>

FIX FOUND (commit 57cf63d, src/eyecandy/Highlighting.cpp:65-69): netSemanticName now computes nameBudget = outLen-1-pinLen-1 (clamped >=1) and formats "%.*s %s", truncating the part NAME and always preserving the pin suffix. Buffers stayed 16 bytes. All five callers (1445, 1461, 1480, 1614, 1633) route through this single chokepoint, so no caller was forgotten.

ORIGINAL SCENARIO IS DEAD. Verified by compiling and running the exact expression on the host: outLen=16, name "SSD1306_32_I2C", pin "SDA"/"SCL" -> "SSD1306_32_ SDA" / "SSD1306_32_ SCL" (15 chars). SDA vs SCL are distinguishable again; the pin identity survives.

BOUNDARIES CLEAN. PartPin.name[12] caps pinLen at 11; worst real case (name 15 + pin 11) yields "ABC ABCDEFGHIJK" (15) with the pin intact. The pin only starts truncating at pinLen>=14, unreachable. outLen=1 writes just NUL; outLen=2 writes "S" — safe, no overflow. Names that already fit are byte-identical to pre-fix output (verified: "NE555 OUT", "R1 A"), so no collateral in the common case. %.*s is bounded by precision so an unterminated p.name cannot over-read; strlen(pinName) is safe because every writer memsets first (PartDb.cpp:163, PartPlacement.cpp:559/653, States.cpp:27) then strncpy's with sizeof-1.

BOTH TARGETS COVERED. Single shared src/ file, no OG variant, no target guards in the function. Compiled objects exist for both envs and postdate the source: .pio/build/jumperless_og/src/eyecandy/Highlighting.cpp.o (2026-08-25 18:06:40) and .pio/build/jumperless_v5/... (22:20:06) vs source 18:06:16. gpio_get_function/GPIO_FUNC_I2C exist on RP2040 and RP2350 alike.

SIBLING PATHS CHECKED, none open. netDisplayName (Highlighting.cpp:880) returns the net's name POINTER, never copying into the 16-byte buf except for the "Net %d" fallback, so lines 1212/1764/1811 do not truncate. makePinNetName (PartPlacement.cpp:277-289) writes into char[32] and fits "SSD1306_32_I2C_SDA" (18). PartLabels.cpp:185 passes name and pin as separate ReadingDisplay fields. No other "%s %s" name+pin composer exists.

COLLATERAL IN THE SAME HUNK: the commit also added the specialFunction<=0 guard at line 53-54 (a separate sweep finding, so GND/rails/DACs keep authoritative names). Verified benign: regular nets are initialized specialFunction=-1 (NetManager.cpp:701,775), matching PartPlacement.cpp:325's own >0 predicate, so ordinary part nets still get semantic names.

VERDICT: the reported harm is genuinely gone on both targets; fix_correct=false only because the truncation approach lost the dedup suffix (a distinction old code had) when the sanctioned 28-byte-buffer alternative would have kept both.

</details>

## #13 — INCOMPLETE — Serial typed-row twin of partsTapForRow bypasses the usedRows refusal the probe path enforces; dupli

- **[low]** `src/PartsApp.cpp:264` — NEW defect introduced by the fix: the `if (rowUsed) {...}` block at :251-256 is not terminal, so control falls through to the unguarded `Serial.println("  row must be 1-60")` at :264. Failure scenario: per-signal SIP flow over serial, user types `12<enter>` for VCC then `12<enter>` for GND. The refusal itself is correct (row is rejected, digits reset), but the console emits BOTH `  row 12 is already one of this part's pins` AND `  row must be 1-60` for a row that IS in 1-60. Pre-fix, that second line printed only for genuinely out-of-range input, so this is new misleading output; automation parsing the refusal reason (the Large Breadboard Model MCP drives this exact grammar) reads the wrong reason. The redundant `nDigits = 0; digits[0] = '\0';` at :255-256, duplicating :265-266, is evidence the author intended the dup branch to be terminal. Fix: make :264-266 an `else { ... }` on the `row >= 1 && row <= 60` test, or `continue`/guard after the rowUsed branch.

<details><summary>checker notes</summary>

FIX FOUND (commit 57cf63d, src/PartsApp.cpp:247-256): the typed-row branch now runs the same usedRows scan the probe branch enforces at :282-299, printing the identical nudge and resetting the digit buffer instead of returning the row. Original scenario is dead: typing a previously-tapped row is refused, so two pins can no longer resolve to one node via the serial twin.

BOTH TARGETS: covered. src/PartsApp.cpp has no #ifdef/caps board gates; jumperless_og extends jumperless_v5 and inherits its build_src_filter (platformio.ini:186-188, 257-258), so RP2040 and RP2350 compile the identical guard.

ATTACK RESULTS (all clean): callers at :673 (DIP anchor, nUsed=0, single row - no dup possible) and :682-683 (per-signal, passes the accumulating rows/nRows) both feed the guard; nullptr usedRows with nUsed=0 never dereferences; the axial2 branch (:341-353) additionally requires bottom==top+30 so it is doubly distinct; `digits[4]`/`nDigits<3` bounds are safe (max index 3); leading zeros ("012" -> 12) match the scan since usedRows only ever holds 1-60; probe and serial branches share the loop without interfering; no state is left behind on the refusal path (LEDs for used rows were already painted at :176-178). The 4th-digit -> `return -2` exit-app behavior at :268 is pre-existing and untouched by this fix.

SIBLING PATH (note only, not counted as an issue): partGeometryOk (src/routing/PartPlacement.cpp:176-270) still has no duplicate-node/offset check, so jl_place_part (src/JumperlessMicroPythonAPI.cpp:2028) accepts two listed pins at the same offset. This was the fix_sketch's explicitly optional half and is pre-existing, not regressed. The original harm chain cannot complete there: pinClass->rail/GND auto-routing exists ONLY in partsCommitPlacement (src/PartsApp.cpp:413 and :416); applyPartPlacement does not auto-route power, and PartPlacement.cpp:596 is the parser storing an explicit user-supplied `connect:` field, not a pinClass derivation. So the API route yields a degenerate part (two pin names on one node, confusing labels), not the rail-to-GND short. Worth closing eventually; does not reopen finding 13.

COLLATERAL: the only behavior change beyond the refusal is the spurious ":264" message above. The 400 ms `openMs` deadtime added in the same hunk (:213, :281) is a separate finding and is correctly scoped to the probe branch only - the typed path deliberately skips it, matching the comment at :209-212. No LED, OLED, undo, or state-machine behavior changed. Not built (pio is broken under Python 3.14 in this env); verification is line-level reading of the current tree.

</details>

## #14 — OK+notes — fb/shadow allocation failure in attach() is completely silent - retries every 750 ms forever (rebuil

- **[low]** `src/displays/DisplayService.cpp:435` — Residual half of the finding's title ("nothing in the i! stats"): printStats() still early-outs on inst.partIdx < 0, so an OOM episode leaves the i! dump with no [disp] line at all. Serial now warns once, but a user diagnosing after the fact via i! still sees nothing. Symmetric with the acquire-failure path (equally invisible), and the audit's fix_sketch only prescribed warn+reorder, so this does not flip correctness.
- **[low]** `src/displays/DisplayService.cpp:144` — allocWarned is a once-per-boot latch, not once-per-episode as its header comment (DisplayService.h:65) claims. It clears only on a successful alloc (line 144) or in detach() (line 214), and detach() is unreachable while partIdx == -1 (pollParts:228 skips the attached branch). So: OOM warns once -> user deletes the display part -> later places a different display -> second, unrelated OOM episode is completely silent again. Mirrors the pre-existing acquireWarned pattern the sketch asked for, so low.

<details><summary>checker notes</summary>

FIX FOUND, bug is dead. src/displays/DisplayService.cpp:123-144 (introduced in 57cf63d). Both prescribed remedies landed: (a) fb/shadow now allocate at :126/:130 BEFORE routeDataPins(:146), so an OOM retry never touches the fabric; (b) a one-shot warn at :138-141 ("DISPLAY paused: not enough free memory for a framebuffer") mirroring acquireWarned (:111-115), cleared at :144 on success and :214 in detach().

RETRY COST NOW: pollParts(750 ms) -> attach -> displayBusAcquire (DisplayBus.cpp:136-179 = 2x pinMode + gpio_function_map/gpioState marks, no routing, no flash) -> 2 failed new[] -> displayBusRelease (:181-198) -> return. No removePartPlacement/applyPartPlacement/refreshConnections. The expensive-retry half of the finding is gone.

EARLY-EXIT STATE: OOM path frees both buffers with delete[]+nullptr (:132-133), releases the bus, resets desc/partIdx/state. activeDataNodes (:448) reads 0 at partIdx=-1, so the net-voltage scan is not left excluding nodes. No path sets DispState::EMPTY while fb is allocated (only :118/:137/:155/:205, all of which free or never allocated), so attach() overwriting inst.fb cannot leak.

BOTH TARGETS: DisplayService is board-gated three ways - service() returns IDLE at :298 before pollParts, displayBusAcquire refuses at DisplayBus.cpp:141, and caps.breadboardDisplays is false in boards/og/board_og.cpp:114, true in boards/v5/board_v5.cpp:117. On OG attach() is unreachable (only external refs are NetVoltageScan.cpp:794/811/1147 + the registration at main.cpp:571), so the fix is V5-live/OG-dormant, which is correct for a V5-only feature. Compile-verified on both: .pio/build/jumperless_og/src/displays/DisplayService.cpp.o (Aug 25 18:06) is newer than the source (18:04, unmodified since per git status), and "not enough free memory for a framebuffer" is present in both jumperless_og and jumperless_v5 firmware.elf.

SIBLING PATHS: :126/:130 are the only heap allocations in the whole displays subsystem - grep for nothrow/malloc/new across DisplayDrivers.cpp, DisplayBus.cpp, DisplayDrivers.h returns nothing. No silent-OOM twin in the detect-driver path.

BOUNDARIES: fbBytes is uint16_t (DisplayTypes.h:58), max panel 128x64 = 1024 B; the pageFlip doubling at :128 promotes to int before the uint16_t cast, so no overflow at any real driver size. fbBytes==0 would yield a valid non-null new[] - not a null-deref.

COLLATERAL (old behavior preserved): success path is byte-identical in end state and Serial output ("DISPLAY routed ..."). Pre-fix the OOM path never unrouted what routeDataPins had just persisted, so retry #2 onward hit the "already routed" early return at :57 (cheap) but left routed-looking connect fields with a dead panel; post-fix an OOM leaves the pins honestly unrouted - strictly more truthful, and a slot already routed from disk still short-circuits at :57 unchanged. Two marginal trades, both inherent to the prescribed reorder: routeDataPins now runs with ~1.5 KB less heap headroom, and a routing refusal now allocs+frees 1.5 KB per round (immediate LIFO free, no fragmentation risk).

OUT OF SCOPE, noted: routeFails (uint8_t, DisplayService.h:64) increments unbounded at :156 - after 255 standing refusals (~21 min at the 5 s backoff) it wraps to 0, dropping the backoff for ~3.75 s and re-printing the "routing keeps failing" line. That is a different finding's fix sharing the same hunk, not #14's path.

</details>

## #15 — INCOMPLETE — detach() resets flushFails/yieldNoted but not initFails or acquireWarned - stale strike count and 2s

- **[low]** `src/displays/DisplayService.cpp:213` — The `acquireWarned = false;` added to detach() is provably dead code, so the finding's second half is still live in the tree. acquireWarned is set true only on attach()'s acquire-fail early exit (:111-118), which sets inst.partIdx = -1 before returning; it is cleared at :121 the instant an acquire succeeds, i.e. before partIdx can survive an attach. detach() is reachable only from pollParts :235 / :240, both inside `if (inst.partIdx >= 0)` (:228), and dispatch is single-core (jOS services run on core 0; loop1 does not dispatch them). Therefore acquireWarned is guaranteed already false at every detach() call, and the flag stays true across a part swap: script owns GP24/25 -> place display part A -> attach fails, one 'DISPLAY paused: GPIO claimed by your script' line -> remove part A (no detach: partIdx is already -1) -> place part B -> attach(B) fails at acquire and prints NOTHING. New episode, zero feedback, exactly the scenario text ('a second display part attached while the user's script still owns GP24 pauses with no message at all'). The fix_sketch's alternative placement -- reset at the top of attach() -- would have closed it; the detach() placement does not. Same dead-by-construction argument applies to allocWarned (:214, cleared at :144) and routeFails (:212, cleared at :163) -- harmless there, since both are per-attempt state that is already correct, but they are not fixes either.
- **[low]** `src/displays/DisplayService.cpp:357` — The uint8_t wrap named verbatim in the finding's scenario ('The counter also wraps at 255 (uint8_t), briefly re-arming the fast beacon and reprinting the message mid-episode') was not addressed. `if (++initFails == 5)` / `if (initFails >= 5)` (:357/:362) never saturate. A panel that ACKs its address but fails init continuously holds the 2 s backoff (:362) for 250 more strikes = ~500 s, then initFails wraps 255->0: the beacon returns to DISP_BEACON_MS 300 ms (:324) for five ~45 ms full-init bursts and 'DISPLAY init failing' reprints, repeating every ~8.5 min for as long as the panel stays sick. Fix is a one-liner: `if (initFails < 255) ++initFails;` then test `== 5` / `>= 5`, or clamp after the increment.
- **[low]** `src/displays/DisplayService.cpp:156` — Same unsaturated pattern in routeFails, which is NEW code added by the same fix hunk in 57cf63d. `if (++routeFails == 5)` (:156) with `if (routeFails >= 5) nextPartsPollMs = millis() + 5000;` (:160) wraps at 255: a standing routing refusal (bridge table full) backs off to a 5 s poll for 250 attempts = ~21 min, then routeFails wraps to 0 and the service returns to 750 ms polling -- five more remove/reapply fabric-rebuild attempts per cycle (routeDataPins :65-94) plus a reprinted 'DISPLAY routing keeps failing' line, forever, on that period. Same one-line clamp.

<details><summary>checker notes</summary>

FIX LOCATED: commit 57cf63d, src/displays/DisplayService.cpp:209-215 (detach() now zeroes yieldNoted, flushFails, initFails, routeFails, acquireWarned, allocWarned, attachedName). Header counters at DisplayService.h:60-66 (routeFails/allocWarned added by the same commit).

PRIMARY SCENARIO IS DEAD. `initFails = 0;` at :211 is the one added reset that is actually live -- initFails can be >0 while attached (it only increments in the ROUTED beacon at :357 and only clears on a successful init at :341), so a part leaving the table -> pollParts :235 `gone` -> detach() now clears it. A later, healthy panel gets a fresh 5-strike budget, the ==5 warning prints for its own episode, and no 2 s backoff is inherited (attach also re-arms inst.nextBeaconMs = 0 at :175). Panel-A-punishes-panel-B is gone.

BOTH TARGETS. The fix is board-independent member assignment, and OG can't reach it anyway: service() returns IDLE before pollParts on !caps.breadboardDisplays (:298), with a second belt in displayBusAcquire (DisplayBus.cpp:141); caps false for OG (boards/og/board_og.cpp:114), true for V5 (boards/v5/board_v5.cpp:117). Registration in main.cpp:571 is unconditional but inert on OG. Complete for V5 + OG.

ATTACK RESULTS. (a) Sibling exits: attach()'s three early-exit paths (:116-119 acquire, :132-142 alloc, :150-161 route) unwind by hand and never call detach() -- verified each leaves only the counter it is supposed to leave. (b) Alternate re-entry to ROUTED without detach: the 8-error lost path (:415-424) keeps initFails at 0 because :341 zeroed it on the prior successful init -- no stale strikes there. (c) Physical panel swapped under an unchanged part definition never detaches, but costs only up to 2 s of extra beacon latency and prints nothing wrong -- intended backoff, not a gap. (d) Concurrency: single-core (jOS services on core 0; loop1 in main.cpp:1538 runs core-1 work only), and no cross-core reader touches the strike counters -- printStats (:434) reads only the cumulative totals, activeDataNodes (:445) only partIdx/sdaPin. No race.

COLLATERAL: none. Three of the four added resets (:212 routeFails, :213 acquireWarned, :214 allocWarned) are no-ops in every reachable path -- each is already cleared inside attach() (:163, :121, :144) before partIdx can survive, and detach() is guarded by partIdx >= 0 (:228). They cannot re-arm a warning or shorten a backoff, so there is no new console spam and no user-visible change beyond the intended one. attachedName[0]='\0' at :215 is hygiene (the strncmp identity check at :231 is unreachable with partIdx < 0). Old-code behavior otherwise preserved.

VERDICT: fix_present=true (core cross-episode bleed closed at :211); fix_correct=false -- the acquireWarned half of the finding is untouched because the reset landed where it can never execute, and the uint8_t wrap called out in the scenario text still stands. All three residuals are low: log/latency noise on an already-broken panel, no crash, no corrupted routing, no misleading reading.

</details>

## #17 — OK+notes — MeasureMode still runs wait-for-5-stable against the once-per-500ms cache contract; works only while

- **[low]** `src/sensing/MeasureMode.cpp:158` — Fix-introduced rate amplification on the failed-tap path. When startMeasurement() fails, measurementActive stays false and lastProbeReading is left at the tapped node, so the next service pass re-satisfies `stableReadingCount >= 1` and retries immediately -- old threshold 5 spaced retries 5 passes apart. It also retries through the 15ms PROBE_GUARD window, because probeReadingStable (:116) is evaluated outside the `if (safeToReadProbe)` block (:85-106) that freezes the counter. Failure sources: findUnusedADC() == -1 (:344-349, emitLiveSerialLine + a full oled.clearPrintShow frame) and addEphemeralConnection failure (:361-372, 'Connection failed' line + an Arduino String errorMsg heap alloc per attempt). On a board with the ADC pool exhausted or MAX_BRIDGES reached this is ~5x the failed route attempts / heap churn / reading-row repaints per held tap. No state corruption: States.cpp:786-834 validates every failure case before mutating connections. Wants an 'already tried this node' latch (PartLabels' lastInspectNode + LBL_LIFT_MS is the precedent at PartLabels.cpp:165-172).
- **[low]** `src/sensing/MeasureMode.h:134` — `static constexpr int STABLE_READINGS_REQUIRED = 5;` is now dead -- the fix inlined a literal `1` at MeasureMode.cpp:116 rather than retiring the constant. grep over src/ and lib/ shows zero remaining references. It is a live trap: the constant still reads as the service's documented contract, so a future reader restoring `stableReadingCount >= STABLE_READINGS_REQUIRED` silently resurrects finding #17 with no compiler complaint.

<details><summary>checker notes</summary>

FIX: dec1b45 changed one line at src/sensing/MeasureMode.cpp:116, `stableReadingCount >= STABLE_READINGS_REQUIRED` -> `stableReadingCount >= 1`, keeping the counter block (:89-105) for probe-lift bookkeeping.

BUG DEAD: yes. One positive cached reading from probing.getLastProbeReading() (:86) satisfies the gate on the first pass of the ~10ms Probing cache window (Probing.cpp:1660-1684 refreshes at 100Hz), so the tap no longer depends on >=5 sub-2ms scheduler passes landing inside one window. The DisplayService-burst / safeToReadProbe-blanking margin the finding worried about is gone.

NO SIBLING PATH: grep over src/ + lib/ finds stableReadingCount / STABLE_READINGS_REQUIRED only in MeasureMode. PartLabels.cpp:159-172 and PartsApp.cpp:272-281 already carry the single-reading contract with matching comments. MeasureMode was genuinely the last consumer.

BOTH TARGETS: OG is a no-op twice -- MeasureMode.cpp:54-62 returns before any of this under #if defined(OG_JUMPERLESS) (-DOG_JUMPERLESS is set in platformio.ini [env:jumperless_og]), and main.cpp:556-565 gates measureModeService registration on board::currentBoard().caps.hasProbePads. Only one live source copy exists (the .claude/worktrees/ copy is an unrelated pre-folder-move worktree, not in any build_src_filter).

ATTACKS THAT HELD: same-node re-trigger blocked by `stableProbeReading != measuredNode` (:138) + startMeasurement's early return (:238-240). stopMeasurement (:287-288) and the switch-exit branch (:196-197) both zero counter + lastProbeReading, so no stale latch across a mode change or an external Python_Proper.cpp:5016 stop. Stale positive state frozen across the PROBE_GUARD window can only replay a node the tap already consumed. Highlighting (registered immediately before, same priority, Highlighting.cpp:150-158) still gets its one highlightNets() pass before isMeasurementActive() flips -- the measurement now starts ~4 passes (<2ms) earlier, not visible. Counter overflow needs 2^31 passes in one 10ms window. Single-core (core 0) consumer of a non-volatile int written by the same core: no race.

DEVIATION FROM THE SKETCH (harmless): the sketch said drop stableReadingCount entirely; the fix kept it at threshold 1. Consequence is that during the count's decay (cache back to -1, count still >0) stableProbeReading returns the STALE node rather than -1. Traced every branch -- it either equals measuredNode (no-op) or is filtered by isValidMeasurableNode. Preserves the old lift-tolerance behavior, which the sketch's version would have removed.

PRE-EXISTING, NOT INTRODUCED (amplified in frequency by issue 1, so worth a look): connectADCToNode returns at MeasureMode.cpp:369-371 without infraReleaseAdc(INFRA_ADC_MEASURE) when addEphemeralConnection fails, and stopMeasurement() early-returns at :269-271 when !measurementActive so the switch-exit teardown at :184-193 never releases it either -- the pool slot stays owned by INFRA_ADC_MEASURE until a measurement successfully starts and stops. infraAcquireAdc's keep-what-you-own rule (InfraPaths.cpp:808-814) makes it idempotent rather than cumulative.

</details>

## #18 — STILL-LIVE — The last-touched row stays armed for a single-decode accept forever; one glancing re-contact reaches

- **[low]** `src/Probing.cpp:7676` — ORIGINAL BUG UNFIXED. The duplicate branch still accepts the row EQUAL to lastRowProbed from a single decode whenever millis()-lastDuplicateTime >= 500 (gate at 7678, returns at 7696/7698), bypassing the two-consecutive-decode pendingNewRow confirm that every OTHER row must pass (7710-7722). lastRowProbed (decl 7598, written only 7722/7733) and lastDuplicateTime (decl 7600, written only 7692/7720) never expire - grep across src/lib/include finds no other writer. The probeRead<=0 early return at 7649-7652, where the fix sketch called for a quiet-timeout expiry, is unchanged. Chain to harm intact: Probing.cpp:1672/1683 caches one justReadProbe(true) per 10ms; PartLabels.cpp:165, Highlighting.cpp:158, MeasureMode.cpp:86 each treat one positive cached reading as a tap, and PartLabels.cpp:167-169 re-arms after LBL_LIFT_MS so the re-announce fires. Affects V5 and OG identically (platformio.ini:186 jumperless_og extends jumperless_v5, same src filter, no board gate).
- **[low]** `src/Probing.cpp:7733` — SIBLING PATH the eventual fix must also close. The allowDuplicates==false path arms lastRowProbed but does NOT refresh lastDuplicateTime. A menu/picker read or JumperlessMicroPythonAPI.cpp:2629 (justReadProbe(false,1)) therefore leaves the row armed against an already-expired 500ms timer, so the very next allowDuplicates decode of that row emits instantly with zero confirm - the single-decode accept without even the 500ms wait. Any expiry added only at the probeRead<=0 return would still need to cover this arming site.

<details><summary>checker notes</summary>

Searched by symbol (lastRowProbed, lastDuplicateTime, pendingNewRow, smoothProbeReading) and via git log -S: pendingNewRow predates the audit (09391b7); lastDuplicateTime last touched in c18f8d2. The only Probing.cpp changes in the four fix commits are 57cf63d (node1Name char[12] stack smash -> char[24] snprintf at 3724-3820; chooseGPIO index base at 5471) and dec1b45 (double-tap kDblConfirmMinMs at 251-256/534-540). 47325d3, 4e5b0d0, 486eb28 touch Probing.cpp not at all. CodeDocs/RELEASE_AUDIT_2026-08-25.md does not list this finding as fixed; it falls in the "50 verified-pending" work-list. git diff HEAD -- src/ is empty, so the tree equals HEAD.

Attack surface if a fix is written later: (a) expiry must key on decode-level quiet at 7649, not on service cadence, or a HELD row (which decodes every ~10ms tick) would lose its 500ms re-emission that PartsApp.cpp:280 and PartLabels rely on; (b) the 7733 non-allowDuplicates arming site must be covered too; (c) pendingNewRow/pendingNewRowSeq are function-local statics shared across all callers (Menus.cpp:3031, Peripherals.cpp:3451, LEDs.cpp:1160, PartsApp.cpp:280, MicroPython API 2636) - a modal picker polling justReadProbe(true) interleaved with the 100Hz service already voids each other's pending confirm via the callSeq check at 7712-7713; that is pre-existing, not fix collateral.

Collateral from what DID land: dec1b45 changed MeasureMode.cpp:113 from stableReadingCount >= STABLE_READINGS_REQUIRED to >= 1. Not a real widening of this bug (periodUs=0 means the service sampled the same 10ms cache window enough times to reach 5 from one emission anyway), but the nominal guard that would have absorbed a one-decode false positive under a future scheduling change is gone. No other user-visible behavior change found in the audited commits' Probing.cpp hunks.

</details>

## #19 — OK+notes — netSemanticName has no special-net guard - probing GND shows the first placed part's power-pin label

- **[low]** `src/eyecandy/Highlighting.cpp:1446` — Incomplete relative to the fix's own stated rule ("special-function nets keep their authoritative names"), though the reported part-label leak IS closed here. On the four GPIO call sites (1445/1446, 1461/1462, 1614/1615, 1633/1634) netSemanticName now returns false for a special net, but the fallback is the numeric "GPIO N input"/"GPIO N out", not netDisplayName(). So a GPIO tied to the GND rail still never reads "GND" — it reads "GPIO 5 input". This is pre-existing behavior (identical before parts existed), not a regression the fix introduced, and it shows no part label, so the audited scenario does not reopen. Only the plain-net branch at 1480-1483 falls back to netDisplayName().

<details><summary>checker notes</summary>

FIX LOCATION. src/eyecandy/Highlighting.cpp:48-83. Guard added at 52-54: the part-name loop now runs only when `nets[netNum].number == netNum && nets[netNum].specialFunction <= 0`. Landed in 57cf63d; `git log --oneline -- src/eyecandy/Highlighting.cpp` shows 57cf63d is the newest touch and git status shows the file clean, so tree == HEAD.

ORIGINAL SCENARIO IS DEAD. Probe path: netHighlighted==1 sets brightenedRail at 1049-1056 then falls into `default:` at 1126 (the switch has cases 0,2,3,4,5 only — no case 1), past the isCurrentSenseNet/UART/ADC/GPIO branches to the else at 1468, reaching netSemanticName(1,-1,...) at 1480. nets[1].specialFunction == GND == 100 (src/routing/MatrixState.cpp:21; src/JumperlessDefines.h:414), so 100 > 0 → part loop skipped → userGpioIdx==-1 → returns false without writing `out` → 1483 netDisplayName(1) → nets[1].name "GND". Confirmed.

GUARD DURABILITY. specialFunction survives every mutation path: initNets (MatrixState.cpp:628-633) seeds nets[0..5]; both routing engines re-stamp nets[0..5] literally on each full rebuild — src/routing/NetsToChipConnections.cpp:1346-1352 (V5) and src/routing/NetsToChipConnections_OG.cpp:1334-1340 (OG). combineNets swaps so the <=5 net is always the survivor (NetManager.cpp:557-561) and deleteNet is called only on the non-special side (623); deleteNet's shift-down (692-696) only moves plain nets (all -1, set at 701/775), so no plain net can inherit specialFunction>0 and silently lose its part label. deleteNet has exactly one caller.

PREDICATE PARITY. Matches partsReassertNetNames' `specialFunction > 0 continue` (src/routing/PartPlacement.cpp:325) and its `number != netNum` validity check (323), so readout naming and net naming now agree on authority.

BOTH TARGETS. Highlighting.cpp is compiled by both envs: platformio.ini:256-259 — jumperless_og inherits jumperless_v5's build_src_filter and subtracts only boards/v5/ and routing/NetsToChipConnections.cpp. No #ifdef OG_JUMPERLESS / board gating anywhere in netSemanticName or its callers. The special-net table is identical in the OG engine. Fix covers V5 and OG identically.

ATTACKS THAT FAILED. (a) netNum bounds: 0 excluded by `>0`, MAX_NETS=60 upper bound; nets[0].number==127 so the added number==netNum check also rejects it. (b) `number == netNum` adds no new restriction — netHasNode (41-42) already required it, so pre-fix a mismatched entry already produced no match; behavior identical. (c) specialFunction==0: falls on the permissive side (<=0), same direction as partsReassertNetNames, so a zero-inited plain net keeps its part label. (d) Early-exit state: the false path writes nothing to `out`; the snprintf at 69 only precedes `return true`, so the 1480/1483 nameBuffer reuse is safe. (e) Truncation math at 66-69: pin.name[12] (States.h:432) caps pinLen at 11, outLen is 16 at all five call sites → nameBudget >= 3, the `<1` clamp at 68 is unreachable, worst case is 3+1+11=15+NUL = exact fit. No overflow, no lost pin suffix. (f) Reentrancy: reads the same globalState.parts/connections netHasNode already read; no new shared-state exposure.

COLLATERAL. Rails/DACs (nets 2-5) also lose part labels, but they never reach netSemanticName on the probe path (cases 2-5 at 1071-1125 handle them), so the visible change is confined to GND on the plain-net branch and to special nets in the live updater's GPIO branches (1614/1633). PartLabels' node-level tap-to-inspect (src/eyecandy/PartLabels.cpp:172-198) is unaffected — probing the actual part-pin row still announces the part, so no information is lost. isCurrentSenseNet is checked at 1190 before the netSemanticName branch, so "I Sense +/-" precedence is intact on the probe path.

</details>

## #26 — OK+notes — USB→UART bridge has no backpressure: sustained host writes to the Arduino port silently drop bytes

- **[low]** `src/tubes/AsyncPassthrough.cpp:1326` — Residual silent-drop window inside the fixed function. `ringFree` is computed once at line 1276 and reserves space only for the forward bytes, but `process_runtime_command(buf[i])` at line 1326 runs inside the same byte loop and can push up to ~22 bytes ("\r\nTag parsing enabled\r\n") into the SAME TX ring at line 930 before the forward pushes at 1354-1356 execute. If the ring was within ~22 bytes of full, that many forward bytes still drop via tx_ring_push_byte with no error. Also note the response push at 929-931 ignores its own return value. Narrow: requires a literal "tag parsing =" line in-band AND a near-full ring, and line 1325 skips the scanner entirely while flashingArduino. The original fix_sketch explicitly called for reserving this margin ("minus the tag-parser's possible expansion") and it was not reserved. Strictly better than pre-fix (which dropped everything), so low.
- **[low]** `src/tubes/AsyncPassthrough.cpp:1277` — State left behind on the new early-exit path: `if (ringFree == 0) return;` skips the `s_last_usb_to_uart_data_time` refreshes at lines 1289 and 1295, which the pre-fix code always performed (it read and dropped). Staleness is bounded by one TX-DMA span, since tx_dma_pump (line 529-537) retires the tail only on span completion: worst case ~1023 bytes = 89 ms at 115200, 178 ms at 57600, 533 ms at 19200, ~1.07 s at 9600. Compared against FLASH_INACTIVITY_MS=1500 (line 119, used at 2376) and the 2000 ms tag-parsing inactivity re-enable (set at line 2240, checked at 1945), a sustained host stream below ~4800 baud can now false-trip "flash done" / re-enable tag parsing mid-transfer, which fires uart_force_receiver_resync + ring_clear (1956-1970). Not reachable at any real flashing baud. Cheap hardening: refresh both timestamps before the early return.

<details><summary>checker notes</summary>

FIX FOUND & ORIGINAL BUG IS DEAD. src/tubes/AsyncPassthrough.cpp:1275-1280 (commit 47325d3, 11+/2-, the only change to this file): the unconditional `tud_cdc_n_read(itf, buf, sizeof(buf))` is gone; the read is now capped to `room = min(64, ringFree)` where `ringFree = (uartToSendTail - uartToSendHead - 1) & UART_TOSEND_MASK` (line 1276), with an early return at 1277 when the ring is full.

Boundary math is right: tail==head -> free=1023 (true capacity of the 1024-byte ring, line 385-388); head==tail-1 -> free=0. No zero-length read. forward_idx <= rd always (the tag parser at 1321-1338 emits at most one byte per input byte; no expansion), so every push at 1354-1356 fits.

BACKPRESSURE IS REAL, not just moved. Verified in the vendored stack: /Users/kevinsanto/.platformio/packages/framework-arduinopico/libraries/Adafruit_TinyUSB_Arduino/src/tusb.c:444-451 - tu_edpt_stream_read_xfer refuses to arm the OUT transfer unless `tu_fifo_remaining(&s->ff) >= s->mps`, so unread bytes leave the endpoint unarmed and the controller NAKs the host. tu_edpt_stream_read (tusb.c:468-472) re-arms after every read, and CFG_TUD_CDC_RX_BUFSIZE=256 (arduino/ports/rp2040/tusb_config_rp2040.h:110) is 4x mps, so progress is guaranteed and the endpoint can always re-arm once the ring drains.

NO DEADLOCK from the early return: tx_dma_pump() is called unconditionally every task() iteration at line 2115 (independent of the bridge), and the sole early return in task() (line 2008, s_uart_suspended_by_mpy) sits ABOVE the bridge call at 2034, so it never strands a full ring. `s_usb_rx_pending = false` at 2035 after an early-exit bridge call is harmless because the re-entry condition at 2028 also tests tud_cdc_n_available().

BOTH TARGETS COVERED. One shared implementation, no target guard: uartToSend[1024]/UART_TOSEND_MASK at 385-388 have no OG_JUMPERLESS variant (unlike the RX ring at 351-356, which does), and platformio.ini:186-260 (env:jumperless_og) extends jumperless_v5 and subtracts only boards/v5/ and routing/NetsToChipConnections.cpp, so AsyncPassthrough.cpp compiles identically for RP2040. Did not attempt a build (pio broken under py3.14).

NO SIBLING PATH LEFT OPEN. Only two tud_cdc_n_read call sites exist in src/ (line 660 in the dead, uncalled flush_cdc_buffers; line 1280 the fixed bridge) - no second raw-CDC->UART bridge. The other TX-ring producers are lossless-on-full by construction: sendPendingUARTResponses leaves the byte queued and resumes next call (2526-2530, 2550-2553). The legacy ISR TX-drain that also pops the ring (713-726) is inside `#if 0` (672/843), so no concurrent tail mutation; the 16-bit volatile tail read at 1276 is atomic on ARM and any staleness only underestimates free space (conservative). Not reachable from IRQ/core 1: bridge_usb_to_uart has exactly one caller (2034) under the s_in_task re-entrancy guard (1794-1796).

COLLATERAL (expected, this is the fix working): host writes to the Arduino CDC port now block at USB instead of silently vanishing when the UART cannot keep up. At a host-selected low baud a paste that previously "worked" (with missing bytes) now stalls the host writer for seconds. That is correct bridge semantics and matches the commit's stated intent. uartToSendOverflowCount (389) should now stay 0 for the bridge path - any diagnostic reading it will under-report drops that no longer happen. Cosmetic leftovers, not defects: dead empty `else {}` at 1360-1364, uncalled flush_cdc_buffers at 649, `#if 0` ISR at 672-843.

</details>

## #35 — OK+notes — Custom App demo leaves the device stuck in temp slot 8 when it runs to completion

- **[low]** `src/SingleCharCommands.cpp:1371` — Residual (amplifier not hardened, not a fix regression): cmd_cycleSlots still wraps only on `else if ( netSlot == 7 )`, so any netSlot > 7 increments instead of wrapping. Finding #35's producer is dead, but the follow-on blast radius it described is untouched. RotaryEncoder.h:18 documents netSlot == NUM_SLOTS (8) as a live attract-mode sentinel, so a stray 8 here still walks to 9 -> loadSlot fails -> clearActiveSlot. A `netSlot >= 7 || netSlot < 0 -> 0` guard would close it. Pre-existing in both targets.
- **[low]** `src/Apps.cpp:1352` — Pre-existing OG-target hazard in the same function the fix touched (NOT introduced by the fix; identical on all 9 early-exit paths). customApp does pinMode(GPIO_1_PIN=20, OUTPUT)/pinMode(GPIO_2_PIN=21, INPUT) + digitalWrite(20, HIGH/LOW). Per States.cpp:2925-2928 those pins are the CH446Q chip selects for chips I/J on OG, not routable GPIO. Nothing restores them: the GPIO half of applyStateToHardware() that leaveApp()->exitTemporarySlot() invokes is wrapped in `#if !defined(OG_JUMPERLESS)` (States.cpp:2923), so on OG the demo leaves CS lines re-muxed and every SF-side connection silently fails to program until reboot. The slot restore the fix added works on both targets; this GPIO leak does not.

<details><summary>checker notes</summary>

FIX FOUND — src/Apps.cpp:1580-1582, landed in 47325d3 (`git log -L 1575,1585:src/Apps.cpp`), unmodified in the working tree:
  1580  // Every early exit above calls leaveApp(); running to completion did not,
  1581  // so the demo left the device stuck in temp slot 8 (sweep finding).
  1582  leaveApp( );
placed after the blink-loop crosspoint teardown (sendXYraw at 1577-1579), before the closing brace.

1. ORIGINAL SCENARIO IS DEAD. Exhaustive scan of the temp-slot region (Apps.cpp:1338-1583) shows every `return` is preceded by leaveApp() (1386/1419/1449/1476/1497/1525/1541/1551/1566) and the sole fallthrough now ends at 1582. No `break`, `goto`, or exception path leaves the region otherwise. src/Apps.cpp:139-202 (runApp) does zero slot bookkeeping around apps[i].action(), so customApp's own call is the only restore — the fix is necessary and it is sufficient. leaveApp (Apps.cpp:247-249) -> SlotManager::exitTemporarySlot (States.cpp:3985-4034) restores netSlot + activeSlotNumber + activeSlotPath, clears temporarySlotActive, applyStateToHardware(), refreshConnections(-1,0,1).

2. ATTACK RESULTS.
- Both targets: platformio.ini:257-260 — jumperless_og extends jumperless_v5 and only subtracts `boards/v5/` and `routing/NetsToChipConnections.cpp`; Apps.cpp is compiled for both. customApp has no board guard (the only `#if defined(OG_JUMPERLESS)` in Apps.cpp is 1960, inside calibrateDacs). leaveApp/exitTemporarySlot are board-independent. Fix covers V5 and OG equally.
- Sibling enterTemporarySlot(8) sites all restore on every path: Apps.cpp:422 -> 624 (unconditional `done:` label, all exits goto it); Apps.cpp:728 -> 1317 (loop is `while (done == false)` at 825, leaveApp sits in `if (done)` at 1270 inside the final iteration; the breaks at 1143-1165/1257/1265 are switch-breaks, not loop exits — verified by reading each); Apps.cpp:1981 -> 2730 (OG returns at 1961 and the audio-yield abort returns at 1976, both BEFORE entering); SelfTest.cpp:1171 -> 1218 (no return/break between). No sibling leak.
- Boundary: enterTemporarySlot(8) cannot fail here — NUM_SLOTS is 8 (RotaryEncoder.h:6) so `tempSlot >= NUM_SLOTS` trips, but States.cpp:3946-3949 explicitly whitelists 8 and 99.
- Reentrancy/nesting: enterTemporarySlot returns false when temporarySlotActive (States.cpp:3953), and customApp ignores the return, so a nested launch would make the new leaveApp() pop the OUTER app's temp slot. No reachable nesting path exists — runApp's only callers (Menus.cpp:4576/4613, SingleCharCommands.cpp:2532/4285, JumperlessMicroPythonAPI.cpp:2598) all run outside a temp slot, and none of the 5 temp-slot apps invoke runApp or the menu. Also identical risk on the 9 pre-existing early exits, so not fix-introduced.
- Cross-core: exitTemporarySlot's refreshConnections is core0-guarded (Commands.cpp:180) and customApp runs on core0; the added call is on the same core as the 9 existing ones.

3. COLLATERAL. The completion path now runs applyStateToHardware() + refreshConnections(-1,0,1), so the demo's leftovers (slot-8 bridges, topRail 4.20 V from 1454, DAC1 5.35 V from 1373, V5 GPIO directions) revert to the user's original slot instead of persisting. That IS the fix, and it is byte-identical to what every early exit already did before the fix, so a user who aborted the demo saw this exact behavior already — no new divergence, no old behavior lost. Nothing else in customApp changed; the sendXYraw teardown at 1577-1579 still runs first, so the K/A raw crosspoints are dropped before the rebuild.

</details>

## #38 — OK+notes — top_oled.display_type stores a dangling pointer to a stack buffer; later read is an unbounded sprint

- **[low]** `src/configManager.cpp:4395` — New (minor) collateral from the coercion: printSettingChange() echoes the caller's raw `value` as the new setting, but oledDisplayTypeLiteral() silently coerced anything outside {SSD1306,SH1106,SSD1309,SSD1315} to "SSD1306". `[top_oled] display_type = SH1107;` prints "display_type: SSD1306 -> SH1107" while the struct holds SSD1306. Pre-fix the echo matched what was stored (a pointer to that same buffer), so this mismatch is introduced by the fix. Impact is cosmetic only - display_type has zero consumers (oled.cpp always constructs Adafruit_SSD1306, src/oled.cpp:156), so no display behavior changes. Cheap fix: echo jumperlessConfig.top_oled.display_type instead of value, or print a 'not a known display type' line in the fallback branch.
- **[low]** `src/configManager.cpp:4084` — fix_sketch's second half ('bound the oldValue read with snprintf regardless') was not applied - still `sprintf(oldValue, "%s", jumperlessConfig.top_oled.display_type)` into char oldValue[64]. Safe today only by construction: the two writers (767, 4310) are the only assignment sites and both funnel through oledDisplayTypeLiteral(), which returns one of four <=7-char flash literals and never NULL, and config.h:389 defaults to a literal. Latent hardening gap, not a live defect - any future direct assignment reopens the overflow.
- **[low]** `src/configManager.cpp:1173` — Pre-existing, NOT introduced by the fix, but adjacent and left open by the same commit: saveConfigToFile()'s [top_oled] block (1171-1188) never writes display_type, and configHasChanges() (1394-1410) never compares it. So `[top_oled] display_type = SH1106;` is parsed, sets configChanged, then is dropped from config.txt on the next save and reverts to SSD1306 on reboot - and a display_type-only change does not even mark the config dirty. This is exactly the class 47325d3 fixed for top_oled.rotation (line 1176 added in that commit) but did not fix for display_type. Harmless while the field drives nothing; becomes a real revert-on-reboot bug the moment a driver switch is wired to it.

<details><summary>checker notes</summary>

FIX FOUND. Landed in 47325d3. Helper src/configManager.cpp:19-25 `static const char* oledDisplayTypeLiteral(const char*)` maps the parsed token (strcasecmp) onto flash string literals, NULL-safe, defaulting to "SSD1306". Both former dangling-store sites now call it: src/configManager.cpp:767 (updateConfigFromFile boot parser, was `= value` into the loop's `char value[64]` at :562) and src/configManager.cpp:4310 (updateConfigValue interactive setter, was `= value` into the caller's stack buffer).

ORIGINAL BUG IS DEAD. jumperlessConfig.top_oled.display_type can now only ever hold: the config.h:389 default literal, or one of the four helper literals - all in flash, valid for program lifetime. The only reader, src/configManager.cpp:4084, therefore reads live memory, and the string it copies is <=7 bytes into oldValue[64].

COMPLETENESS / ATTACK:
- Writers: grep across the whole tree finds exactly two assignment sites (767, 4310), both fixed. No Python/MicroPython, menu, or app path touches top_oled.display_type (repo-wide grep: only config.h:389, the comment at 1247, and 767/4084/4310).
- Sibling fields: display_type is the ONLY `const char*` in `struct config` (src/config.h). Every other string field is a char array (startup_message, last_version) already strncpy'd. No sibling instance of this bug class exists.
- BOTH TARGETS COVERED. platformio.ini:186 `[env:jumperless_og] extends = env:jumperless_v5` and shares build_src_filter/src tree, so one source change serves both. Verified empirically: `strings .pio/build/{jumperless_v5,jumperless_og}/src/configManager.cpp.o` both contain SSD1306/SH1106/SSD1309/SSD1315; both .o timestamped Aug 26 00:56, after the source edit (Aug 25 21:52). Compiles on both - strcasecmp resolves (already used at src/oled.cpp:436, src/Menus.cpp:1879, src/displays/DisplayService.cpp:50).
- Value hygiene both paths: boot parser trims and strips the trailing ';' before the compare (src/configManager.cpp:598-604), and the serial path trims trailing whitespace/';' (src/configManager.cpp:4489-4493, 4537-4543), so a legitimate `display_type = SH1106;` still matches the whitelist and is not coerced.
- Concurrency/reentrancy: strictly improved. The store is a single aligned 32-bit pointer write to a flash literal, so any core0/core1 or nested-call interleaving now yields a valid string; pre-fix any interleaving could publish a pointer to another frame's dead buffer.
- No binary persistence of the struct: the only bulk copy is the in-RAM shadow `memcpy(&lastSavedConfig, &jumperlessConfig, sizeof(struct config))` at src/configManager.cpp:1246, which copies a pointer to a flash literal - valid, and the comment at 1247 is now actually true (it was a false reassurance pre-fix).
- Missing `else` at src/configManager.cpp:4308 (`if (enabled) ...; if (i2c_address) ...`) is pre-existing - present at 47325d3^:4292 - not collateral, and harmless (distinct keys).

COLLATERAL: one user-visible change - unrecognized display_type values are now silently coerced to SSD1306 instead of stored verbatim, with the echo still showing the raw input (issue 1). Since nothing in the firmware branches on display_type (always Adafruit_SSD1306, src/oled.cpp:58/156), no display, geometry, or routing behavior changes. Case is also normalized ("sh1106" -> "SH1106"). No HIL test references display_type (grep of test/ is empty). Note: .claude/worktrees/serene-burnell-b7797c/ still holds the pre-fix source; it is outside build_src_filter and not compiled.

</details>

## #45 — OK+notes — Background-callback ticks bypass the jl_vm_exec_depth guard, re-enabling nested raw-REPL execution

- **[low]** `modules/jumperless/modjumperless.c:2838` — One-strike error path prints the traceback without first quiescing interrupts: mp_embed_exec_str (lib/micropython/micropython_embed/port/micropython_embed.c:230-231, the LINKED copy) deliberately calls mp_hal_set_interrupt_char(-1) + mp_handle_pending(false) before mp_obj_print_exception because the cooked-output path re-enters mp_hal_check_interrupt and can re-schedule a KeyboardInterrupt with no NLR frame. jl_bg_service_tick's else branch (2838-2840) omits both. Pre-existing (added in 1a69b37, not by the fix), but the fix widens the blast radius by one notch: an nlr escape from mp_obj_print_exception now also skips jl_vm_exec_depth-- at 2842, leaving the counter permanently >0. In practice the escape itself is nlr_jump_fail (no outer NLR anywhere on loop() -> serviceAll -> MpBackgroundService::service()), so the board parks before the leaked counter can be observed - hence low.

<details><summary>checker notes</summary>

FIX PRESENT (commit 47325d3). modules/jumperless/modjumperless.c:2831-2832 (`extern volatile int jl_vm_exec_depth; jl_vm_exec_depth++;`) before nlr_push, 2842 decrement after both branches. Diff is exactly +5/+1 lines, nothing else touched.

ORIGINAL SCENARIO DEAD, end to end: bg callback -> time.sleep -> mp_hal_delay_ms (src/snakes/Python_Proper.cpp:278-281) -> jOS.serviceInner() -> MpRemoteService::service() -> guard at src/snakes/MpRemoteService.cpp:121 now sees depth==1 and returns before any pyexec_event_repl_process_char / jl_soft_reboot. The guard sits at the top of service() itself, so it is caller-agnostic: serviceAll (src/JumperlOS.cpp:260), serviceInner (src/JumperlOS.cpp:335) and the force* paths all funnel through line 121 - that also closes the finding's "the jOS CRITICAL-priority loop can reach us the same way" clause.

BOTH TARGETS: shared C, no board macro. `nm` shows `U jl_vm_exec_depth` in .pio/build/jumperless_v5/modules/jumperless/modjumperless.c.o AND .pio/build/jumperless_og/modules/jumperless/modjumperless.c.o, both objects + both firmware.elf built 2026-08-26 00:56 (after the 08-25 21:54 fix commit). mpRemoteService and mpBackgroundService are registered unconditionally for both boards (src/main.cpp:565, 570; the nearest OG #ifdef closes at main.cpp:504).

ATTACK RESULTS (all negative):
- Forgotten callers: only two C->VM call sites exist repo-wide (modjumperless.c:2834 and lib/micropython/port/rp2_pio_jl.c:782); the latter runs inside VM execution already. No sibling C entry point lacks the counter.
- Nested-exec siblings in the inner set: inner set = CRITICAL + explicit inInnerSet overrides (tinyUSB, asyncPassthrough, probeButton, MpRemote, peripherals, display, ledDump). MpRemote is the only VM-entering member; pyexec_event_repl_process_char appears nowhere else.
- Reverse nesting (bg tick landing inside a foreground script) is not reachable: jl_bg_service_tick runs only from serviceAll, and the two jl_service_python()->serviceAll() sites (Python_Proper.cpp:1200-1207, 1252-1255) both execute post-unwind with no script on the C stack; mp_hal_delay_ms uses serviceInner, and MpBackground is not in the inner set.
- Reentrancy/early exit: jl_bg_in_call (2804/2812/2844) blocks recursion; both nlr branches converge on the decrement at 2842 and the stack_top restore at 2843; the pre-increment early returns (2805, 2809) leave no state. Only escape between ++ and -- is the traceback printer (issue above).
- Boundary: counter is `volatile int`, matching definitions in micropython_embed.c:185 (C) and MpRemoteService.cpp:99 (extern "C"); single-core-0 writers; nothing resets it on soft reboot, and nothing needs to.

COLLATERAL: only MpRemoteService reads the counter (repo-wide grep), so the sole behavior delta is USBSer2 raw-REPL processing deferring for the duration of each bg tick - identical to how a foreground script has always behaved, and ticks are interval-gated. No interrupt wedge: a runaway callback is still breakable because mp_hal_check_interrupt peeks Serial and USBSer2 directly (Python_Proper.cpp:708-715), independent of service(). Return value stays m_in_raw_repl ? BUSY : IDLE, unchanged shape; jl_in_raw_repl_mode is untouched on the guard path.

HYGIENE (unrelated to this fix): lib/micropython/port/micropython_embed.c (Aug 18) and lib/micropython/micropython_embed/port/micropython_embed.c both compile to objects under .pio/build/*/; ELF strings prove only the latter is linked. Read the linked copy when citing that file.

</details>

## #46 — OK+notes — Quiet MicroPython init never mounts the VFS or sets sys.path — and the broken state persists into la

- **[low]** `src/snakes/Python_Proper.cpp:4556` — Incomplete-fix residue (same family, beyond #46's literal scope): quiet init still diverges from initMicroPythonProper in ways that stay sticky for the whole VM lifetime, because every later loud entry early-returns on mp_initialized. runBootPyIfPresent() (:910, guarded by a static boot_py_executed) never runs, so a user's boot.py is silently skipped for the entire session whenever the first Python use is a file-manager run / app / single command. addMicroPythonModules() (:907 -> time/os/math/gc preimports) is also skipped, and enterMicroPythonREPLWithFile only re-runs addJumperlessPythonFunctions() (:1141), not the module preimports. On V5 the rp2.py provisioning block (:882-892) is skipped too, so a first-ever quiet init leaves 'import rp2' unresolvable until a loud init self-heals it. Interrupt-char divergence is NOT an issue: setGlobalStreamWithInterrupt (:1137, :451-469) repairs it on REPL entry.
- **[low]** `src/snakes/Python_Proper.cpp:5302` — Collateral: setupFilesystemAndPaths() opens with changeTerminalColor(replColors[13], true, global_mp_stream), and changeTerminalColor (src/Graphics.cpp:444-454) IGNORES the stream argument and always writes \033[38;5;Nm to Jerial. A function documented 'without any output' therefore now emits an ANSI color escape and leaves the terminal foreground set to replColors[13] where old code emitted nothing (only the pre-existing Serial.write("\x1b[0 q") at :4572). Cosmetic only in practice (Jerial defaults to a single output stream, broadcast off, and disableTerminalColors gates it), but it is a user-visible behavior change. Related: the new comment at :4553-4554 ('the stream is restored above, because setupFilesystemAndPaths prints through it') is factually wrong - changeTerminalColor bypasses global_mp_stream and the sys.path exec prints nothing. The ordering is harmless, the stated rationale is not, and it will mislead the next reader.

<details><summary>checker notes</summary>

FIX FOUND: src/snakes/Python_Proper.cpp:4549-4556 (commit dec1b45) adds jl_vfs_mount_root() + setupFilesystemAndPaths() to initMicroPythonQuiet(), placed after the stream restore at :4546-4547 and before the inline 'from jumperless import *' exec at :4560.\n\nORIGINAL BUG DEAD: yes. All quiet-init entry points now get a mounted root VFS and populated sys.path: executeSinglePythonCommand :4908, executePythonFileContent :5045, executeSinglePythonCommandFormatted :5089, plus :5136/:5179/:5246 - i.e. FilesystemStuff.cpp:1163/1240, Apps.cpp:231, ProjectsApp.cpp:924, SingleCharCommands.cpp:2492/2501. The sticky consequence (enterMicroPythonREPLWithFile :1126 and MpRemoteService::ensureMicroPythonInitialized MpRemoteService.cpp:412-421 both early-return on mp_initialized) no longer matters for open()/imports.\n\nBOTH TARGETS: covered. Python_Proper.cpp is one TU compiled by both envs (platformio.ini:186 jumperless_og extends jumperless_v5; build_src_filter only drops boards/v5 and routing/NetsToChipConnections.cpp). MICROPY_VFS is (1) in lib/micropython/port/mpconfigport.h:145 and lib/micropython/micropython_embed/port/mpconfigport.h:145, so jl_vfs_mount_root (modjumperless.c:6245, inside #if MICROPY_VFS) links on RP2040 too - and initMicroPythonProper:855 already called it unconditionally, proving the symbol exists on both. New calls carry no #ifdef, unlike the V5-only rp2.py block at :882.\n\nATTACK: (a) double-mount -> impossible: jl_vfs_mount_root has exactly 3 call sites (:855 loud, :4555 quiet behind the mp_initialized guard at :4515, JumperlessMicroPythonAPI.cpp:1530 immediately after a fresh mp_embed_init at :1522); each is once per VM lifecycle, so mp_vfs_mount's EPERM 'mount point in use' raise is unreachable. (b) unguarded NLR -> jl_vfs's mount method is a pure no-op (modjumperless.c:5968-5973) and mp_vfs_chdir('/') resolves against the just-inserted root, so no exception escapes into a context without nlr_push. (c) early exit -> mpAllocHeap() failure at :4529 restores global_mp_stream/_ptr and returns false before reaching the new calls; mp_initialized stays false. (d) ordering -> mp_initialized=true is set at :4542 before the mount at :4555, a ~2-exec window where isMicroPythonInitialized() reads true with an unmounted VFS; harmless because all quiet-init callers are core-0 UI/command paths (no core-1 Python execution) and nothing runs between the two statements. (e) siblings -> grep confirms mp_embed_init appears only at :852, :4536 and JumperlessMicroPythonAPI.cpp:1522, and all three now mount + setup paths; no fourth init path exists. (f) dead param -> preserve_interrupt_char is still ignored by quiet init (:4538-4540 commented out), pre-existing and repaired on REPL entry by setGlobalStreamWithInterrupt.\n\nCOLLATERAL: one real change - the ANSI color escape described in issue 2. Nothing else: setupFilesystemAndPaths' only other effect is the silent sys.path script (:5306-5323, all prints commented out), identical to what the loud init has always run.

</details>

## #21 — OK — help() with a section name over 31 chars smashes the core-0 stack (unbounded strcpy)


<details><summary>checker notes</summary>

FIX LOCATED — /Users/kevinsanto/Documents/GitHub/JumperlOS/modules/jumperless/modjumperless.c:4806-4811, landed in 57cf63d (`git log -p -S"section_upper"` shows the exact hunk: `strcpy(section_upper, section)` -> `strncpy(...)` + explicit NUL). Working tree is clean for this file, so what's committed is what's on disk.

Current code:
  4806  char section_upper[ 32 ];
  4810  strncpy( section_upper, section, sizeof( section_upper ) - 1 );
  4811  section_upper[ sizeof( section_upper ) - 1 ] = '\0';

1. BUG DEAD. Boundary math: n=31, so strncpy writes at most indices 0..30 (zero-padding the tail when src is shorter) and never touches index 31; line 4811 then writes index 31, the last in-bounds byte. Max write index 31 == sizeof-1. The toupper loop at 4812-4813 stops at the guaranteed NUL, max i=30. help("x"*40), help("x"*31), help("x"*32) are all in-bounds. No off-by-one.

2. ATTACK — nothing survives:
- Both targets covered by one edit. platformio.ini:257-261: [env:jumperless_og] inherits `${env:jumperless_v5.build_src_filter}` and only subtracts `boards/v5/` and `routing/NetsToChipConnections.cpp`; `+<../modules/jumperless/>` (platformio.ini:104) applies to both, so V5 and OG compile this same TU. No OG-specific duplicate of the function exists.
- Callers: only two, both in this file. modjumperless.c:4771-4786 pass string literals (all <=10 chars); modjumperless.c:4790 is the sole user-supplied path, and it now feeds the bounded copy. `grep -rn jl_help_section` over the tree hits nothing outside modjumperless.c (the only other hits are a stale copy under .claude/worktrees/, not built).
- No sibling uppercase-into-fixed-buffer path: grep for `toupper` / `_upper[` across src, modules, lib/micropython/port matches only modjumperless.c:1878 and 4813. Line 1878 (jl wavegen name parse, `char up[24]`) was already correctly clamped (`if (n > 23) n = 23;`).
- Other strcpy/strcat in the file (modjumperless.c:1536/1537, 1553/1554, 1571/1572, ProbePad concat) are into `m_new(char, lhs_len+rhs_len+1)` — correctly sized, not siblings.
- Reentrancy/concurrency: buffer is a plain stack local, no shared state. The only static here is `color_index` (4799), pre-existing and untouched; the `color` it computes is dead. Early-exit: the function has no early returns and leaves nothing behind — the truncated name simply matches no section and falls through to the unknown-section branch at 5054-5065.
- Non-str args (help(5), help(node)) still raise a Python TypeError from mp_obj_str_get_str at 4789 — same as before, no crash.

3. COLLATERAL — none observable. For every name <=31 chars the result is byte-identical to the old strcpy (strncpy copies the same bytes; the explicit NUL at [31] duplicates what strcpy already wrote or lands in already-zeroed padding). Only inputs >=32 chars change, and those previously smashed the stack, so there is no old behavior to preserve. Notably the error message at modjumperless.c:5064 prints the ORIGINAL `section`, not the truncated copy, so a user who types a long name still sees exactly what they typed rather than a confusing 31-char stub. Worth knowing but not a defect: that path now echoes an arbitrarily long user string to the terminal (bounded by MP heap) where it used to hard-fault — noisy at worst, and it is the user's own input.

</details>

## #28 — OK — Uninitialized node1Name read in clear-mode miss path: garbage strcpy + garbage on OLED


<details><summary>checker notes</summary>

FIX LOCATION: /Users/kevinsanto/Documents/GitHub/JumperlOS/src/Probing.cpp:3736-3742 (declaration) and :3831-3832 (the rowsRemoved>0 format). Landed in 57cf63d, unchanged by 47325d3/dec1b45/4e5b0d0/486eb28.

WHAT CHANGED: the clear branch (`else if (s.setOrClear == 0)` at :3734) now declares `char node1Name[24]` and immediately initializes it with `snprintf(node1Name, sizeof(node1Name), "%s", definesToChar(nodesToConnect[0]))` at :3741. The dead `node2Name[12]` / `bothNames[25]` strcpy+strcat block that read node1Name uninitialized was deleted. The later `sprintf(node1Name, "%s cleared", ...)` became `snprintf(..., sizeof(node1Name), ...)` at :3831.

ORIGINAL SCENARIO DEAD: yes. The miss path `else { oled.clearPrintShow(node1Name, ...) }` at :3840 now reads a fully initialized string; there is no uninitialized read and no strcpy of uninitialized stack anywhere in :3734-3842 (verified: only node1Name appears in that range, all other mentions are comments).

ATTACK RESULTS (all negative):
- Buffer sizing: definesToChar(x,0) short names max 8 chars (GPIO_PAD / NANO_3V3 / BLDG_TOP), EMPTY_NET=9, and the itoa fallback writes into `same[12]` (src/routing/NetManager.cpp:2118,2146) so worst case is 11 chars. "%s cleared" worst case = 19+NUL < 24. No truncation for any real node, and snprintf is bounded regardless. The old sprintf into [12] overflowed for any name >=4 chars ("TOP_GND cleared"=15) — that second stack smash is also gone.
- NULL deref: definesToChar never returns nullptr — every path returns a literal, a table entry, emptyNet[0], or `same` (NetManager.cpp:2119-2152). snprintf %s is safe.
- Unguarded node value: the clear branch has no `nodesToConnect[0] > 0` guard (unlike the connect branch at :3614), but definesToChar(-1) falls to itoa -> "-1", so the fixed path is safe for any int. (Pre-existing `s.deleteMisses[0] = nodesToConnect[0]` -> `printRawRow(..., -2, ...)` at :3759/:3773 is untouched by this fix and out of scope.)
- Adjacent array: `s.deleteMisses` is [20] (:2647), so the `for (i=12; i>0; i--)` shift at :3752 is in bounds — not an OOB neighbor that the removed locals were masking.
- Sibling paths: the other two node1Name sites (:3534-3542 single-node display, :3622-3633 connect commit) both strcpy from definesToChar before any read — no uninitialized-read sibling remains. Their `node1Name[12]` exactly fits the 11-char worst case; `bothNames[25]` at :3630 would need two simultaneous 11-char itoa fallbacks (26 bytes) to overflow, unreachable with real node values (realistic max 8+3+8+1=20). Not this bug's family, not introduced here.
- Concurrency: node1Name is a stack copy taken at :3741, so a concurrent core-2 definesToChar() overwriting the shared static `same[12]` cannot corrupt what :3840 prints.
- BOTH TARGETS: src/Probing.cpp is shared by env jumperless_v5 and jumperless_og (OG build_src_filter inherits V5's, platformio.ini:257-258); the region :3734-3842 sits inside no #if (nearest OG_JUMPERLESS guard is :6175), so the fix is identical on RP2350 and RP2040. Both .pio/build/jumperless_v5/src/Probing.cpp.o and .pio/build/jumperless_og/src/Probing.cpp.o are dated after the fix commit, so both envs compiled it.

COLLATERAL: none user-visible. The removed node2Name/bothNames were never read in the clear branch (bothNames was only ever a copy of the uninitialized buffer). Success path at :3831-3834 still shows "<node> cleared"; miss path at :3840 now shows the bare node name where it previously showed stack garbage — that is the bug removal, not a behavior change. Serial output, LED fades, s.fadeClear/s.fadeTimer, and deleteMisses bookkeeping are byte-identical to the old code.

</details>

## #29 — OK — couldntFindPath overflows unconnectablePaths[10] - no bounds check on numberOfUnconnectablePaths


<details><summary>checker notes</summary>

FIX FOUND (commit 57cf63d, still present on dev). V5: src/routing/NetsToChipConnections.cpp:4611-4615 — the two writes in couldntFindPath() are now wrapped in `if (numberOfUnconnectablePaths < 10)`; the `++` stays outside so the printed total still counts all failures. OG: src/routing/NetsToChipConnections_OG.cpp:4634-4638 — identical guard on the sibling site the finding flagged. Both targets covered: platformio.ini:257-260 shows env:jumperless_og compiles _OG.cpp and excludes NetsToChipConnections.cpp, and _OG.cpp's body is #ifdef OG_JUMPERLESS so V5 never compiles it. Original scenario (11th unroutable path writing past int[10][2] into adjacent .data) is dead on both.

ATTACK — all writers and readers checked, no sibling path left open:
- Every write site into unconnectablePaths[] is guarded: V5 4611 (fixed), 5462, 5574; OG 4634 (fixed), 5487, 5601; shared RouteSafety.cpp:425. Grep for "unconnectable" repo-wide found no other writer.
- Only indexed reader is routeRefused() at src/guiding/GuideChecks.cpp:463-466, which clamps `if (n > 10) n = 10` before iterating — so the deliberately-unbounded counter at 4615/4638 cannot drive an OOB read. Remaining counter consumers are Serial prints (NetsToChipConnections.cpp:5603, _OG.cpp:5632) and one commented-out line (LEDs.cpp:2786). Clear loops (NetsToChipConnections.cpp:1371, _OG.cpp:1359) are fixed-bound `i < 10`.
- Boundaries: at count==9 last valid slot [9] is written; at 10 writes stop, count keeps rising; no decrement path, no signed wrap in any realistic run (clearAllNTCC()/refreshConnections() Commands.cpp:218 and fastRefresh() Commands.cpp:611 reset it every pass). Brace balance verified: couldntFindPath spans 4549-4635 (V5) and 4588-4647 (OG); checkForOverlappingPaths 5274-5637 (OG).
- Early-exit state: `paths[i].skip = true` (V5:4616) is still outside the guard, exactly as before, so an 11th failure is still skipped even though it isn't listed. No half-state introduced.

COLLATERAL — none. The `++` was already unconditional at V5 4615 / OG 4638 pre-fix, and the pre-existing guarded sites already had their `++` inside the guard, so in-bounds array contents and the printed "total unconnectable paths" are bit-identical to old code; the only delta is that indices >=10 no longer scribble past the array.

Cosmetic, not defects (pre-existing or no-behavior-change, so not listed as issues): OG 5488 and 5602 now nest a redundant second `< 10` inside the existing outer `< 10` (dead code from the sweep); GuideChecks.cpp:464's comment "the producer has one unguarded push site" is now stale though the clamp itself is correct defense; OG's couldntFindPath lacks the `numberOfUnconnectablePaths = 0` entry-reset that V5 has at 4553, so repeated standalone couldntFindPath(1) diagnostic calls (SingleCharCommands.cpp:1762/1998, Debugs.cpp:1057) append duplicates and inflate the count between refreshes — unchanged by this fix and harmless now that writes are bounded. Also, .claude/worktrees/serene-burnell-b7797c/ holds an untracked pre-fix copy with the unguarded write; no env compiles it.

</details>

## #44 — OK — undoDeserialize blob-length pointer-wrap: hostile/corrupt /undo_history.txt causes a ~4GB memcpy at 


<details><summary>checker notes</summary>

FIX FOUND — src/remembering/Undo.cpp:1339, landed in 57cf63d (`git log -S"blen > (size_t)"` confirms the single hunk; diff shows `- if (p + blen > end) break;` / `+ if (blen > (size_t)(end - p)) break;`).

1) ORIGINAL SCENARIO DEAD. The wrapping additive check is gone. Invariant p <= end holds everywhere: `end = p + len` (1227); nextLine early-returns at `p >= end` and sets `p = nl + 1` (nl found by memchr inside [p,end), so nl+1 <= end) or `p = end` (1234-1241); the only other advance, `p += blen` (1341), now runs after the check. So `end - p` is a non-negative ptrdiff_t and `(size_t)(end - p)` cannot wrap. `blen` is `unsigned` from `sscanf %u` (1335); on both targets (32-bit ARM) unsigned int and size_t have equal rank, so the comparison is unsigned-vs-unsigned with no promotion trap. "@0 4294967295" and "@0 -1" (newlib %u negates to 0xFFFFFFFF) both hit `break`.

Downstream wraps are closed as a consequence, not by luck: blen <= end-p <= len <= file size, and undoRestore rejects `sz > bufBytes` (1901-1906, bufBytes ~5.3 KB no-PSRAM / ~70 KB fallback), so `total = 16 + blen` (1349, sizeof(BlobHeader)==16 per static_assert at 299), `aligned = (total+3)&~3` (1350) and `g_blobUsed + aligned > g_blobCap` (1351) are all far from UINT_MAX. The fix_sketch's extra "reject blen > bufBytes" is implied by blen <= end-p, so its absence is not a gap.

2) ATTACK — no surviving hole.
- Boundaries: blen == end-p accepted (body runs to EOF, p becomes end, the `p < end` CR/LF probes at 1342-1343 short-circuit); blen == end-p+1 breaks; blen == 0 takes the poolSet-empty branch at 1345 before any memcpy.
- Both targets: no #ifdef in the parse path; on OG the question is moot by construction — `UNDO_ENABLED` is 0 for OG_JUMPERLESS (src/JumperlessDefines.h:65-73), so undoDeserialize/undoRestore are compiled out entirely and replaced by stubs (`undoRestore(void) { return false; }`, src/remembering/Undo.cpp:2819). V5 gets the fixed parser; OG never reads /undo_history.txt at all. jumperless_og extends env:jumperless_v5 (platformio.ini:186-187) and adds -DOG_JUMPERLESS, same sources.
- Forgotten callers: undoDeserialize has exactly one call site (Undo.cpp:1925, inside undoRestore), and undoRestore has exactly one caller (Undo.cpp:1720, inside undoInit). Legacy /undo.hist is only deleted (1884-1885), never parsed. No CLI/MicroPython path re-enters the parser.
- `end` is trustworthy: safeFileReadAll bounds readBytes by buffer_size via fileCacheReadInto/fileReadAllRawImpl (src/remembering/FilesystemStuff.cpp:4440-4457), and the buffer is sized sz+1 (Undo.cpp:1915-1921), so `len` never exceeds the allocation.
- Truncated-header edge: a >191-char "@" line is truncated into line[192] but p still advances past the real newline, so a mis-parsed blen is still bounded by the same check.
- State on early exit: `finalizeTxn()` runs at the top of the '@' branch (1333) before the break, so haveTxn is false when the loop exits; the post-loop `finalizeTxn()` (1443) is a no-op and txnCount/opCount/cursors commit a partial-but-consistent ring — identical to the pre-fix truncated-file path.
- Reentrancy: not a concern — restore runs once from undoInit before core1/apps touch the ring.

3) COLLATERAL — none. In the non-wrapping regime `p + blen > end` and `blen > end - p` are semantically identical, so every valid file parses byte-for-byte as before; the only inputs whose behavior changed are the ones that previously invoked UB/hard-faulted. Same `break` (not `continue`), same partial-restore semantics.

Non-blocking observations (pre-existing, not introduced by this fix, not reported as issues): src/remembering/Undo.cpp:1611-1616 keeps an `#if defined(OG_JUMPERLESS) return;` inside undoInit that is now unreachable dead code, since the whole block lives under `#if UNDO_ENABLED` which is 0 on OG. Separately, lib/micropython/micropython_embed/py/modstruct.c:251 has the same additive `p + sz > end_p` shape, but sz there derives from the format string (bounded small), and it is vendored upstream — different hazard class, out of scope for this finding.

</details>
