# Bench verification ledger (5.7.11.0, 2026-09-09 03:15)

Totals - steps by category: {'literal': 308, 'substitute': 73, 'needs_hand': 116, 'could_not_observe': 121}; matches: {'yes': 216, 'no': 39, 'partial': 64, 'n/a': 299}

## gs01_what_s_in_the_box - What's in the box (01-getting-started.md)

steps: {'literal': 1, 'could_not_observe': 5, 'needs_hand': 2, 'substitute': 1}  
summary: This section is a packing list with no commands or Try-it steps, so nothing on it can fail on the board and no discrepancies were found. What the tool could confirm is consistent with the page: the board identifies itself as a Jumperless V5 on an rp2350b running firmware 5.7.11.0; the firmware accepts probe switch positions and probe taps (a tap alone, with no Connect press, lights nothing and connects nothing, as expected); and an OLED is present and rendering readable text ('0.0 mA' at idle). The probe buttons, the physical switch, the breakout board, the feet, the OLED mounting and the crossbar count all need hands or eyes. The one claim a new user might want to see demonstrated - 'the OLED copies everything the breadboard LEDs say into actual text' - could not be exercised from this page alone because nothing here makes the LEDs say anything; the idle OLED shows a current readout the LEDs do not, which is not a contradiction. Harness steps: bench.py reset was run first and last; switch positions were simulated.

needs a hand: Pressing the probe's Connect (front) and Remove (rear) buttons - no button simulation on this rig; Plugging the probe into the socket on the right side of the board / confirming the socket location; Moving the physical Select / Measure slide switch (the tool only simulates the position, and the board shows no visible feedback for it); Seeing the breakout board and ribbon cable and clipping a scope or meter to its headers; Seeing the sheet of rubber feet; Seeing where the OLED is mounted (rev 7 pin headers vs rev 5 breakout) and the board revision; Counting the 12 crossbar switch chips underneath the board

screenshots: gs01_what_s_in_the_box_1_board.png, gs01_what_s_in_the_box_1_oled.png, gs01_what_s_in_the_box_2_board.png, gs01_what_s_in_the_box_2_oled.png, gs01_what_s_in_the_box_3_board.png, gs01_what_s_in_the_box_3_oled.png

## gs02_plug_it_in - Plug it in (01-getting-started.md)

steps: {'needs_hand': 2, 'literal': 5, 'could_not_observe': 3}  
summary: Every testable claim on 'Plug it in' holds: the V5 enumerates as exactly four serial ports (JLV5port1/3/5/7), the first is the terminal, `m` prints the menu and `?` prints 'Jumperless firmware version: 5.7.11.0' (matching `ver`). The startup animation and slot-reload-on-boot need a power cycle the rig cannot do; the 'LEDs mirrored to the terminal' and 'baud rate doesn't matter' claims were not observable here. No discrepancies.

needs a hand: Plug in / power cycle the board and watch the startup animation (intro sentence and Try it step 1). The rig cannot reboot the board.

screenshots: gs02_plug_it_in_0_board.png, gs02_plug_it_in_0_oled.png, gs02_plug_it_in_1_board.png, gs02_plug_it_in_1_oled.png

## gs03_the_probe - The probe (01-getting-started.md)

steps: {'substitute': 4, 'needs_hand': 1, 'could_not_observe': 1}  
summary: Short section, almost entirely hand-dependent. Every step depended on the simulated slide switch or on a simulated tap, so no literal claim could be proven. What the rig showed: Select = stable blue logo, quiet terminal, '0.0 mA' OLED; simulated Measure = logo cycling through many colours (purple only on 1-2 LEDs), terminal redrawing the text-art board picture ~3x/s with no voltage in it (even with no tap), and a simulated row tap showing nothing on the OLED or terminal. The only voltage ever shown was the DAC 0 pad card ('DAC 0 / 3.37 V / 1.6 mA / DAC_0 / adjust?'), and the identical card appears for that pad tap in Select, so it is pad-tap behaviour, not the voltmeter. The nets never changed (consistent with 'never gets saved', but unproven since no measurement was seen). The click wheel menu could not be opened (click did nothing in 4 tries, hold unavailable on this rig), so Calibration > Probe Pads is unverified, and the page does not say how to open the menu. Harness note: after each bench.py reset the OLED stayed on the DAC 0 card and the logo kept cycling; the board was left in that state.

needs a hand: Set the real probe slide switch to Select and to Measure (only simulated here) and confirm the logo colour in each position by eye.; In Measure, touch a real row with the probe tip and check whether a voltage appears on the OLED and in the terminal.; Put fingers on the probe pads / the backs of the 4 risers and see the 'weird readings'.; Open the click wheel menu (a click did nothing here; a hold is not available on this rig), find Calibration > Probe Pads, and read the prompts.; Check whether the OLED being stuck on the 'DAC 0 / 3.37 V / 1.6 mA' card after a Measure-mode DAC pad tap (surviving a reset) is real on the physical board or an artefact of the simulated switch/tap.; The board was left (after the final bench.py reset) with the OLED on 'DAC 0 / 3.37 V / 1.6 mA / DAC_0 / adjust?' and the logo cycling; a click or Enter may be needed to dismiss it before the next item.

- **[major] unchecked**: page says: Measure: touching any row reads its voltage on the OLED and in the terminal.  
  observed: With the switch simulated to Measure, a ~1.2 s simulated tap on empty row 5 (twice) changed nothing on the OLED and printed no voltage on the terminal; the terminal instead redrew the text-art board picture ~3 times a second (also with no tap at all), with no voltage number in it. Evidence the mode and tap were live: the logo went from a stable pattern to cycling colours and the terminal began redrawing exactly at `switch measure` (neither happened in the Select listen before it), and the simulated tap registered in that mode (the DAC 0 pad produced a 'DAC 0 / 3.37 V / 1.6 mA / DAC_0 / adjust?' card). That pad card is not a voltmeter reading: the identical card appears for the same pad tap in Select. So nothing voltmeter-like was shown for a row or a pad in Measure. Observed via simulated switch and tap; needs a hand with the real probe to confirm.  
  repro: bench.py reset; bench.py switch measure; run `bench.py listen 7` in the background, then `bench.py tap 5` and `bench.py oled` during the hold; inspect the listen output for any voltage text (none).

- **[minor] unchecked**: page says: Measure: the logo goes purple.  
  observed: bench.py logo (the only logo readout on this firmware) showed the logo cycling through blue/yellow/green/cyan/orange/red/purple in Measure, with purple on only 1-2 of ~8 logo LEDs per sample; in Select at the start it was a stable blue x2/blue x2/yellow x2/cyan x2 pattern. After returning to Select the cycling never stopped. Observed via the simulated switch; needs a hand to confirm.  
  repro: bench.py reset; bench.py logo (x3, stable); bench.py switch measure; bench.py logo (x6, cycling colours); bench.py switch select; bench.py logo (still cycling).

- **[minor] unchecked**: page says: run `Calibration` > `Probe Pads` from the click wheel menu  
  observed: The page never says how to open the click wheel menu, and a wheel click did nothing here (4 attempts, no OLED change, no terminal output). Whether the menu needs a hold (unavailable on this rig) or the board was stuck after the Measure-mode pad tap could not be told apart.  
  repro: bench.py wheel click; bench.py oled; bench.py listen 3.

- **[minor] unchecked**: page says: (implicit) switching the probe to Measure and back leaves the board as it was  
  observed: After the Measure-mode DAC 0 pad tap the OLED stayed on the 'DAC 0 / 3.37 V / 1.6 mA' card (the 'adjust?' prompt cleared only after an Enter on the terminal), the card survived a harness `bench.py reset`, and the click wheel had no visible effect afterwards. End state after the final reset: the OLED shows the full 'DAC 0 / 3.37 V / 1.6 mA / DAC_0 / adjust?' card again (the Select-mode discriminator tap re-raised it; an Enter typed before the reset did not clear it this time) and the logo is still cycling colours instead of the stable blue pattern seen at the start.  
  repro: bench.py switch measure; bench.py tap dac0; bench.py switch select; bench.py term ""; bench.py reset; bench.py oled (still the DAC 0 card); bench.py wheel click; bench.py oled (unchanged).

screenshots: gs03_the_probe_0_board.png, gs03_the_probe_0_oled.png, gs03_the_probe_1_board.png, gs03_the_probe_1_oled.png, gs03_the_probe_2_board.png, gs03_the_probe_2_oled.png, gs03_the_probe_3_board.png, gs03_the_probe_3_oled.png, gs03_the_probe_4_board.png, gs03_the_probe_4_oled.png, gs03_the_probe_5_board.png, gs03_the_probe_5_oled.png, gs03_the_probe_6_board.png, gs03_the_probe_6_oled.png

## gs04_connecting_rows - Connecting rows (01-getting-started.md)

steps: {'needs_hand': 12, 'substitute': 4}  
summary: Firmware 5.7.11.0. Every step on this page that a user does with the probe button or the click wheel needs a hand on this rig, so no literal pass/fail could be recorded for those. What could be exercised was the outcome side of the Try-it flow via the terminal's own `+` command (listed in the `m` menu): `+ 1-5` made one net [1,5] and drew a pink wire on the LEDs (rows 1 and 5 fully lit, one LED on rows 2-4 between them); `+ 5-GND` merged it into the GND net [GND,5,1] and recolored rows 1-5 to a single green matching the rail GND strips - exactly the outcome Try-it steps 2 and 3 describe. 'Anything can be an end of a bridge' held for a rail (GND), a special-function pad (ADC0) and a Nano pin (A0) through the terminal. A probe tap in idle (no button press) does nothing, confirming the page's ordering (button first). Idle logo reads as a cycling rainbow, matching the page's idle description. No discrepancies found in what was observable. Doc gap: the page never says which position the probe's slide switch must be in. Aside, not a page claim: an ADC net lights a green very close to the GND green; and a bare Enter on the terminal reprints the board picture many times.

needs a hand: Probe `Connect` button click: enter Connect mode (logo blue, probe LEDs change), drop a held row, exit back to idle, Try-it steps 1 and 4.; Probe `Remove` double-tap to undo a connection.; The 'holding' feedback: logo and probe `Connect` text brightening after the first tap (probe LEDs not readable by the tool either).; Everything under 'Connecting with just the click wheel': Click > Connect > Add/Remove, turning to scroll the node cursor, click to select, hold to exit, 5-second cursor auto-hide (bench.py refuses wheel input on this rig).

screenshots: gs04_connecting_rows_0_board.png, gs04_connecting_rows_0_oled.png, gs04_connecting_rows_1_board.png, gs04_connecting_rows_1_oled.png, gs04_connecting_rows_2_board.png, gs04_connecting_rows_2_oled.png, gs04_connecting_rows_3_board.png, gs04_connecting_rows_3_oled.png, gs04_connecting_rows_4_board.png, gs04_connecting_rows_4_oled.png

## gs05_removing_rows - Removing rows (01-getting-started.md)

steps: {'substitute': 3, 'needs_hand': 3, 'could_not_observe': 3}  
summary: Every removal claim on this page hinges on the probe's Remove button, which needs a hand: this rig has no way to press it (checked bench.py, the board's Python API - readers only, probe_tap(1 arg) - and the terminal menu). So the literal Try-it could not be run. What I could show: (1) idle taps on row 1 and on the bottom - pad remove nothing - they give a measurement readout ('GND  1  -0.01 V  0.1-0.3 mA' on the terminal, 'GND / 1 / -0.01 V / 0.1 mA' on the OLED; the - pad gives 'GND / -0.01 V / 0.0 mA'), which is what a reader sees if the button press did not take; (2) the page's node-not-net semantics hold at the API level - disconnect(1,-1) removed only the row 1-5 wire and left row 5 on GND (only row 5 lit green), and disconnect(GND,-1) then took row 5 off GND (board dark), matching the page's end state; (3) the idle logo is an animated multi-colour cycle, so 'goes red' would be a visible change if it works, but I could not trigger it. No command the page names was missing and nothing contradicted the page; no discrepancies filed. Note for the doc: the Try-it depends on the state left by the previous sections (row 1 wired to row 5, row 5 on GND) which this section does not restate; I recreated it via the Python API as a harness step. The board was reset before and after.

needs a hand: Pressing the probe's Remove button (Try it 1 and 4, and the 'logo goes red' claim) - no simulation exists on this rig (no bench command; the board's Python API only reads buttons; probe_tap takes one argument; no terminal key enters probe Remove mode).; Swiping the probe along several rows in Remove mode.; Special-function pads in Remove mode: tap the pad and 'pick the function' (click-wheel step).

screenshots: gs05_removing_rows_0_board.png, gs05_removing_rows_0_oled.png, gs05_removing_rows_1_board.png, gs05_removing_rows_1_oled.png, gs05_removing_rows_2_board.png, gs05_removing_rows_2_oled.png, gs05_removing_rows_3_board.png, gs05_removing_rows_3_oled.png, gs05_removing_rows_4_board.png, gs05_removing_rows_4_oled.png

## gs06_undo_redo - Undo / Redo (01-getting-started.md)

steps: {'literal': 3, 'substitute': 3, 'could_not_observe': 2, 'needs_hand': 2}  
summary: Firmware 5.7.11.0, slot 3. The terminal half of the page is solid: `^` undoes and `&` redoes exactly as written, one change at a time in order (15-20 first, then 10-15), the breadboard LEDs follow (rows go dark/pink), `status` confirms the net, the OLED pops a transient 'Undo'/'Redo' + 'connect 15-20' card naming the reverted change (caught with live OLED reads; the OLED PNGs were taken after it cleared), and redoing past the end says 'nothing to redo'. Everything hands-on could not be tested on this rig: the probe double-taps (substituted with `^`/`&`, so the 'last wire disappears / comes back' outcomes were seen but not through the gesture), the yellow logo flash, the whole wheel History scrub (two detents a step, '3/12' readout, click/hold exits), and the power-cycle persistence. No discrepancies found in what was observable. Side notes for the doc owner: simulated tip taps in Select made no wire on this rig (harness/switch limitation, not a doc fault; the wires were made with the terminal `+` command); the terminal reports history position as 'position -1 / 32' rather than the '3/12' style the page describes for the OLED; and the `m` menu does not list `^`/`&`, so a terminal user only learns them from this page.

needs a hand: Double-tap the probe's Remove button (Try it 2) - substituted the terminal `^`.; Double-tap the probe's Connect button (Try it 3) - substituted the terminal `&`.; Click wheel -> History -> turn the wheel to scrub -> hold/click to exit (paragraph 3 and Try it 4) - no wheel input reaches the firmware on this rig, no substitute.; Slide the probe's physical switch to Select so tip taps make wires (Try it 1) - the simulated switch did not hold; the wires were made with the terminal `+` command instead.; Watch the logo flash yellow on undo/redo - the logo LED is not readable through the tool.; Power-cycle the board to confirm the history survives - reboots are forbidden for this run.

screenshots: gs06_undo_redo_1_board.png, gs06_undo_redo_1_oled.png, gs06_undo_redo_2_board.png, gs06_undo_redo_2_oled.png, gs06_undo_redo_3_board.png, gs06_undo_redo_3_oled.png, gs06_undo_redo_4_board.png, gs06_undo_redo_4_oled.png, gs06_undo_redo_5_board.png, gs06_undo_redo_5_oled.png

## gs07_the_click_wheel - The click wheel (01-getting-started.md)

steps: {'needs_hand': 4, 'could_not_observe': 2, 'substitute': 1}  
summary: This page could not be exercised on this rig: every instruction on it (click to open the menu, turn to scroll, click into a submenu, hold to back out, set Display Options > Bright > Menu) is a click-wheel action, and bench.py reports that simulated wheel input (turn, click, hold) does not reach the firmware's menus on this build and the debug probe is on the other board. The page names no terminal route into the menu, and typing '?' on the terminal printed only the firmware version and board redraws, so a new user has no substitute. Nothing was falsified and nothing was confirmed; discrepancies are empty. The only substitute taken was an Enter on the terminal in place of the final 'hold to get out', which was a no-op because no menu was ever open. Harness reset ran first and last. Baseline captured after the first reset: firmware 5.7.11.0, slot 3, no rows lit, OLED showing 'GND / 0.0 mA', GND rail strips green, + rails dark at 0.0 V. Environment note: the terminal on this rig redraws the ASCII board about 3 times a second with no input, even right after a reset; the page makes no claim about that, so it is not listed as a discrepancy. All six wheel checks are handed back as a two-minute checklist in needs_hand.

needs a hand: Click the wheel once (a diagonal push toward the center of the board): does a menu appear on the breadboard LEDs, and is it mirrored on the terminal and the OLED?; Turn the wheel: do the top-level entries read exactly 'Rails', 'Connect', 'Parts', 'GPIO', 'Output', 'Show', 'Apps' (in that order) followed by more entries?; Scroll to an entry spelled 'Display Options', click, then 'Bright', click, then 'Menu', click: does a brightness level list appear, and does turning change the menu text brightness live?; Click on a brightness level: does the menu text stay at that brightness afterwards?; Hold the wheel: does each hold back out exactly one level, and do two or three holds return the board to the plain circuit view (rows dark, OLED back to its readout)?; While a submenu is open, hold once: confirm it backs out ONE level rather than closing the whole menu.

screenshots: gs07_the_click_wheel_0_board.png, gs07_the_click_wheel_0_oled.png, gs07_the_click_wheel_1_board.png, gs07_the_click_wheel_1_oled.png

## gs08_power_rails_and_dacs - Power: rails and DACs (01-getting-started.md)

steps: {'needs_hand': 4, 'substitute': 4, 'could_not_observe': 3, 'literal': 4}  
summary: The page's factual claims that the tool can see hold up: DAC 0 is pre-occupied powering the probe (net DAC_0/BUF_IN at 3.367 V), the rails and DAC 1 accept the full -8..+8 V range, the settings are written to the slot file (power: topRail 3.30 ...), an idle tap on the top rail pad or DAC 0 shows an OLED card ending in the exact 'adjust?' hint, and once a rail-to-row-10 connection exists the row lights red on a 3.3 V 'Top Rail' net. Every hands-on step (wheel menus for Rails and Output > Voltage, the Connect button, the physical Measure switch plus a real tip on row 10) needs a hand on this rig, so the three 'Try it' steps were substituted (py dac_set / py connect; the simulated Measure switch did not hold) and their literal wording is unverified; the rail strip LEDs are not readable here either. One real problem: tapping DAC 1 - the DAC the page tells you to reach for - in idle mode highlights nothing while it is unrouted (its default state), even at 3 V; it only gets its 'DAC 1 ... adjust?' card once connected to a row, unlike the rail pad which highlights unrouted. Minor: the config snippet has a stray backtick and a 'jython' fence tag.

needs a hand: Click wheel: click, turn and hold for the Rails > Top/Bottom/Both menu (Try it 1) - wheel input is NOT AVAILABLE on this rig; substituted py dac_set(TOP_RAIL, 3.3).; Click wheel: Output > Voltage > DAC 0 / DAC 1 menu and 'pick a row'.; Click wheel: the shortcut's 'click the wheel to adjust that voltage right there' after tapping a rail pad / DAC.; Probe Connect button (Try it 2) - taps alone formed no net; substituted py connect(TOP_RAIL, 10).; Physical probe switch flip to Measure and back to Select, plus a real tip on row 10 for the 3.3 V reading (Try it 3) - the simulated switch was overridden by the physical one within 0.5 s, so the step effectively ran in Select.; 'Tap the DAC pad by the logo while probing' - needs the Connect button first; in idle mode it did nothing visible.; Seeing the real rail strip LEDs (not readable on this firmware; only the tool's voltage-derived rendering was available).; Unplugging the OLED to check that the shortcut is off by default without one.

- **[major] CONFIRMED**: page says: Shortcut: in idle mode, tap a rail pad or a DAC to highlight it, then click the wheel to adjust that voltage right there - with DAC 1 named as the DAC to reach for.  
  observed: An idle tap on DAC 1 highlights nothing while DAC 1 is unrouted (the default state): the OLED kept its previous card at 0 V (two taps) and at 3.0 V (one tap), no LED changed. The same tap shows 'DAC 1 0.00 V 0.0 mA DAC_1 adjust?' once DAC 1 is connected to a row (connect(DAC1, 10)). The top rail pad highlights even while unrouted ('Top Rail 3.30 V 0.0 mA TOP_R adjust?'), and DAC 0 highlights because it is already in the DAC_0/BUF_IN net. An idle tap on unrouted row 10 also showed nothing, so this is 'unrouted nodes don't get a card', which the page's DAC wording doesn't say.  
  repro: reset; switch select; tap top_rail (OLED shows the Top Rail card); tap dac1; oled -> unchanged. py "dac_set(DAC1, 3.0)"; tap dac1; oled -> unchanged. py "connect(DAC1, 10)"; tap dac1; oled -> 'DAC 1 0.00 V 0.0 mA DAC_1 adjust?'.  
  suggested: Shortcut: in idle mode, tap a rail `+` pad to highlight it, then `click` the wheel to adjust that voltage right there. The same works for a `DAC` once it is connected to a row — tap that row; an unconnected `DAC 1` has nothing to tap, so set it from `Output` > `Voltage` first.

- **[minor] unchecked**: page says: Config snippet: put this in your config: `[clickwheel] rail_click_adjust = always;` (in a code fence tagged 'jython')  
  observed: The fenced line starts with a stray backtick and the fence language tag is 'jython', so the copyable text is '`[clickwheel] rail_click_adjust = always;'.  
  repro: Read lines 13-15 of the page excerpt.

screenshots: gs08_power_rails_and_dacs_0_board.png, gs08_power_rails_and_dacs_0_oled.png, gs08_power_rails_and_dacs_1_board.png, gs08_power_rails_and_dacs_1_oled.png, gs08_power_rails_and_dacs_2a_board.png, gs08_power_rails_and_dacs_2a_oled.png, gs08_power_rails_and_dacs_2_board.png, gs08_power_rails_and_dacs_2_oled.png, gs08_power_rails_and_dacs_2b_board.png, gs08_power_rails_and_dacs_2b_oled.png, gs08_power_rails_and_dacs_3_board.png, gs08_power_rails_and_dacs_3_oled.png, gs08_power_rails_and_dacs_4_board.png, gs08_power_rails_and_dacs_4_oled.png, gs08_power_rails_and_dacs_5_board.png, gs08_power_rails_and_dacs_5_oled.png, gs08_power_rails_and_dacs_6_board.png, gs08_power_rails_and_dacs_6_oled.png, gs08_power_rails_and_dacs_7_board.png, gs08_power_rails_and_dacs_7_oled.png, gs08_power_rails_and_dacs_8_board.png, gs08_power_rails_and_dacs_8_oled.png, gs08_power_rails_and_dacs_9_board.png, gs08_power_rails_and_dacs_9_oled.png, gs08_power_rails_and_dacs_10_board.png, gs08_power_rails_and_dacs_10_oled.png, gs08_power_rails_and_dacs_11_board.png, gs08_power_rails_and_dacs_11_oled.png, gs08_power_rails_and_dacs_12_board.png, gs08_power_rails_and_dacs_12_oled.png

## gs09_special_functions - Special functions (01-getting-started.md)

steps: {'substitute': 6, 'needs_hand': 6, 'literal': 1}  
summary: This section is almost entirely gated on the probe's Connect button, which this rig cannot press and which has no terminal or Python substitute, so no chooser (DAC/GPIO/ADC pads, user pads) was ever seen: pad taps from idle do nothing on the LEDs, OLED or terminal, which is consistent with the page's 'while probing'. What could be verified: (1) a GPIO 1 - row 20 connection made from the terminal is an ordinary net (row 20 lights orange, status lists GP_1+20, and a terminal remove clears it); (2) tapping row 20 from idle highlights it - the OLED switches to the card 'GPIO 1 in  20 / FLOAT', and the board PNG shows row 20 change from uniform pale to an orange gradient (the leds text readout did not change) - matching Try-it step 3's 'tap row 20 to highlight it'; (3) config.txt has the [logo_pads] section with top_guy = uart_tx, bottom_guy = uart_rx, building_pad_top = isense_pos, building_pad_bottom = isense_neg, matching the page's user-pad mapping. No discrepancies found in what was observable; the 'GPIO 1 is now driving row 20', high/low toggle, Remove mode, undo, hold-then-tap, and all three chooser trees need a hand. Post-reset state note: the OLED starts on a leftover 'DAC 1 / 0.00 V / 0.0 mA / adjust?' card and a DAC 0 - BUF_IN net exists; neither is from this page. Firmware 5.7.11.0. Disclosure: to look for a Connect-button substitute I consulted the board's built-in terminal help ('help', 'help probe', '+?', the 'h' menu) and its Python dir()/help() listing, and read config.txt over the Python channel; none of that was used to judge the page's claims, only to find the '+'/'-' terminal substitute and to check that [logo_pads] exists.

needs a hand: Press the probe's Connect button to enter connect mode - this gates every chooser on the page (DAC, GPIO, ADC pads and the four user pads); this rig has no probe button and the board offers no terminal command for it; In the GPIO chooser: tap the right side of the '1' (output) and then tap row 20 (Try it 2); With row 20 highlighted, click Connect and watch the row toggle high/low (Try it 3c); Probe Remove mode on the GPIO 1 - row 20 connection, and undo (Try it 4; the page does not say how undo is triggered); Hold a row with the probe tip, then tap a pad ('if you were already holding a row, it connects there'); DAC chooser: pick 0/1, pick a voltage by the bottom pads or the clickwheel, click Connect to confirm, tap a row; ADC chooser: pick 0-4 or P, tap a row, confirm its voltage shows on the LEDs; Tap the two guys and the two buildings in connect mode and confirm they come up as UART Tx, UART Rx, I+ and I-

screenshots: gs09_special_functions_1_board.png, gs09_special_functions_1_oled.png, gs09_special_functions_2_board.png, gs09_special_functions_2_oled.png, gs09_special_functions_3_board.png, gs09_special_functions_3_oled.png, gs09_special_functions_4_board.png, gs09_special_functions_4_oled.png

## gs10_measuring_things - Measuring things (01-getting-started.md)

steps: {'substitute': 10, 'needs_hand': 6, 'could_not_observe': 4}  
summary: Firmware 5.7.11.0. No step of this section can be done literally without hands: the Measure switch needs real tip contact, every connection needs the probe's Connect button (idle taps did nothing), and the Show menu and row highlight need the wheel, which this rig cannot deliver. Substituting the connections through the board's Python (connect()) confirmed the page's underlying claims: the top rail, I+ and I- land on their rows as nets 'Top Rail', 'I Sense +' and 'I Sense -'; an ADC attached to a row stays there and recolours the row by voltage (3.26 V orange, 0 V green); I+ reads 0 V when I- is on GND, confirming the shunt-short warning; and the OLED shows the shunt current ('I Sense -', '0.00 V', '-0.58 mA'). Two things differ from the page in minor ways: the OLED shows the current without any highlight while the terminal shows none in idle, and the board calls the pads 'I Sense +/-' rather than I+/I-. The section also assumes the top rail is already at 3.3 V. Marching ants and 'the LED lights' were unobservable because no part could be inserted, so no current flowed.

needs a hand: Probe Measure switch plus real tip contact on a row (bullet 1) - simulated taps make no electrical contact; Probe Connect button (Try it 2) - without it every tap (ADC pad, rail, building pads, rows) does nothing; Click wheel: highlight a row (ADC bullet; Try it 3) - wheel input does not reach the firmware on this rig; Click wheel: Show > Voltage / Digital / Current menu; Push an LED and a resistor between rows 10 and 12 (Try it 1) - needed for 'the LED lights' and the marching ants

- **[minor] unchecked**: page says: Highlight row 12 in idle mode to read the current on the OLED / terminal.  
  observed: The OLED shows the current readout automatically as soon as I+/I- are connected (titled 'I Sense -', '0.00 V', '-0.58 mA'); no highlight is needed. The terminal shows no current figure in idle at all - only the board picture. The highlight path itself could not be tested (wheel needs a hand).  
  repro: bench.py reset; bench.py py "dac_set(TOP_RAIL,3.3); connect(TOP_RAIL,10); connect(ISENSE_PLUS,12); connect(ISENSE_MINUS,GND)"; bench.py oled; bench.py listen 4

- **[minor] unchecked**: page says: Tap the building pads in Connect mode to get I+ (top) and I- (bottom).  
  observed: The board names them 'I Sense +' and 'I Sense -' (net names in status and the OLED title), not I+ / I-.  
  repro: Same connections as above; bench.py status (nets) and bench.py oled

- **[minor] unchecked**: page says: Try it 1: With the top rail at 3.3 V ...  
  observed: After the harness reset the top rail is 0.000 V and this section gives no instruction for setting it (it assumes an earlier section); a reader starting here is stuck. I set it via Python.  
  repro: bench.py reset; bench.py status -> power.top_rail 0.000

screenshots: gs10_measuring_things_0_board.png, gs10_measuring_things_0_oled.png, gs10_measuring_things_1_board.png, gs10_measuring_things_1_oled.png, gs10_measuring_things_2_board.png, gs10_measuring_things_2_oled.png, gs10_measuring_things_3_board.png, gs10_measuring_things_3_oled.png, gs10_measuring_things_4_board.png, gs10_measuring_things_4_oled.png, gs10_measuring_things_5_board.png, gs10_measuring_things_5_oled.png

## gs11_idle_mode - Idle mode (01-getting-started.md)

steps: {'literal': 5, 'needs_hand': 8, 'could_not_observe': 1}  
summary: Everything on this page that a hands-free rig can reach checks out. Tapping a connected row lights the whole net and gives the tapped row a distinctly brighter treatment than the rest of it (row 10 = 502a47 vs row 15 = 14000a on a 10-15 net; the bright row follows the tap both ways, on both breadboard halves) - "a slightly different animation" is accurate. The net's identity and live readings land on both the OLED and the terminal: `Net 6 / -0.02 V / 0.00 mA` for a plain net, `ADC 0  30  -0.00 V  0.0 mA` for an ADC net, `GPIO 1 in  25  FLOAT` for a GPIO net, and `Top Rail  TOP_R  0.00 V  0.0 mA  adjust?` / `DAC 0  DAC_0  3.37 V  1.7 mA  adjust?` for a rail and a DAC - the last two carrying exactly the click-the-wheel-to-adjust invitation the page promises. The idle logo is a shifting multi-colour mix, matching "rainbow". Three softer findings: the LED highlight lasts only while the tip is on the row (a second later the board shows nothing selected, though the OLED card stays up), which sits awkwardly under a section that keeps referring to "the highlighted net" as a state you then act on; the measurement bullet is written as conditional but a plain net prints V and mA too; and one net latched a partial white pulse onto two of its three rows for 12+ seconds, unreproducible. Most of the section's interaction surface is out of reach on this rig: every click-wheel turn and click is refused (needs a hand), and both probe buttons, `Connect` and `Remove`, have no bench command at all - so Try-it steps 2 and 3, the Connect/Remove bullets, the GPIO-output toggle, the rail/DAC voltage adjust, the GPIO options screen and the placed-part pin name are all untested.

needs a hand: Turning the click wheel (both the bullet "Turn the click wheel to walk the highlight along the rows" and Try-it step 2) - bench.py refuses wheel turns on this rig: simulated wheel input does not reach the firmware's highlighter and there is no debug probe on the V5.; Clicking the click wheel to adjust a rail or DAC voltage - same refusal. The board did show the `adjust?` invitation on both a rail and a DAC.; Clicking the click wheel to open a GPIO pin's options - same refusal.; Pressing the `Connect` probe button (Connect mode holding the tapped row; Connect toggling a GPIO output; Try-it step 3) - bench.py has no probe-button command.; Pressing the `Remove` probe button (the one-second double-press into Remove mode).; Placing a part so a part pin can be highlighted - could not be set up without hands, so the part/pin-name OLED claim is untested.

- **[minor] NOT_REPRODUCED**: page says: The section as a whole talks about "the highlighted net" as a state you are in - turn the wheel to walk it, press `Connect` on it, `Remove` flashes a warning on it, click the wheel if it is a rail.  
  observed: On the LEDs the highlight does not survive the tap. While the tip is on row 10 of net 10-15, row 10 reads 502a47 and row 15 reads 14000a; in the next `leds` sample about a second later both rows are back to the plain net colour 0a0007, with nothing on the board showing which net is "the highlighted one". The OLED and terminal card (`Net 6 / -0.02 V / 0.00 mA`) does stay up, and stayed up even after I tapped an unconnected row (45) - so the state exists, it just has no LED indication once you lift the probe. A reader who taps, looks away at the OLED, and then looks back at the board has no idea what is selected.  
  repro: bench.py reset; bench.py term "+ 10-15"; bench.py tap 10; then run `bench.py leds` three times in a row - the first shows row 10 = 502a47 / row 15 = 14000a, the second and third show both rows at 0a0007. `bench.py oled` still shows the net card.  
  suggested: No page change is required - the board does what this section says. If anything is added, one optional clause on the first bullet: "the tapped row's brighter animation fades after a few seconds; the OLED keeps showing the net's name." (The fact that the OLED card lingers even after the net is deleted is a firmware quirk, not something the page should describe.)

- **[minor] CONFIRMED**: page says: "If the highlighted net has a measurement on it (a `GPIO` input, an `ADC`, current sense), the reading prints on the OLED and the terminal." - phrased as conditional on a measurement being present.  
  observed: A plain net with no measurement device on it also prints a voltage and a current on both surfaces: tapping row 5 of a bare 5/20/35 net gave `Net 6  5  0.06 V  2.1 mA` on the terminal and `Net 6` + V + mA on the OLED. So the readout is unconditional; the page's `if` makes a new user expect a blank screen for a plain net.  
  repro: bench.py term "+ 40-50"; run `bench.py listen 6` in the background, then `bench.py tap 40`; read the captured terminal text and `bench.py oled`.  
  suggested: Replace the bullet with: "The highlighted net's voltage and current show on the OLED and the terminal, whatever is on it. If there's a `GPIO` on the net, you get the pin's state (`HIGH` / `LOW` / `FLOAT`) instead; an `ADC` or current sense just names the card." (Wording for the current-sense half should be checked on a board with an INA net - that case was not tested.)

- **[minor] NOT_REPRODUCED**: page says: Implicit in "Tap a row to highlight its net": the highlight belongs to one net and clears when you move on.  
  observed: Once, on a net of rows 5/20/35, a sustained white pulse (1a1a16 / 1b1d17 alternating with 0a0007) latched onto rows 5 and 20 and stayed there for at least 12 s of idle, through a tap on row 35 and a tap on unconnected row 45. Row 35, a full member of the same net, never joined it. I could not reproduce it on three later nets (10-15, 40/50/58, 45-55), which all returned cleanly to 0a0007. Low confidence, but a stuck highlight on part of a net is worth an eye on hardware.  
  repro: bench.py term "+ 5-20"; bench.py term "+ 20-35"; bench.py tap 5; sample `bench.py leds | grep -E 'row (5|20|35):'` repeatedly for ~15 s. Seen once out of one attempt on that net; not seen on later nets.  
  suggested: No change. The page is correct as written: a tap lights the whole net including row 35, and the highlight clears when you move on. The reported pulse is the wire animation between rows 5 and 20, present before any tap. Elsewhere in the excerpt: n/a - nothing confirmed.

screenshots: gs11_idle_mode_0_board.png, gs11_idle_mode_0_oled.png, gs11_idle_mode_1_board.png, gs11_idle_mode_1_oled.png, gs11_idle_mode_2_board.png, gs11_idle_mode_2_oled.png, gs11_idle_mode_3_board.png, gs11_idle_mode_3_oled.png, gs11_idle_mode_4_board.png, gs11_idle_mode_4_oled.png, gs11_idle_mode_5_board.png, gs11_idle_mode_5_oled.png, gs11_idle_mode_6_board.png, gs11_idle_mode_6_oled.png, gs11_idle_mode_7_board.png, gs11_idle_mode_7_oled.png

## gs12_slots - Slots (01-getting-started.md)

steps: {'literal': 6, 'substitute': 3, 'needs_hand': 4, 'could_not_observe': 2}  
summary: Nothing on the Slots section is contradicted by the board; the one finding is an omission, not an error. The terminal half is fully confirmed on firmware 5.7.11.0: `<` cycles (3->4->5, and 7 wraps round to 0), `<N` jumps (`<3`, `<2`, `<1`, `<7`, `<0` all worked), each printing the new slot name, and `<8` is refused with 'Invalid slot: 8' - so '8 slots (0-7)' is exact. The core promise holds: with two connections and a 3.3 V top rail on slot 3, I switched away and back without ever saving, and the connections, the pink/blue row colours and the rail voltage all came back, while slots 4, 2 and 1 stayed empty - 'saves automatically' and 'switching slots switches the whole circuit' are both true. I could not prove the DAC-voltage and GPIO parts of that list switch per slot (DAC0 read the same in every slot and I never changed a GPIO); they do appear as stored fields in the slot file. /slots/slot3.yaml is plain readable YAML holding bridges, nets with colours, power and GPIO config, and /config.txt really has a [slots] section with boot_mode and boot_slot spelled as the page spells them. What I could NOT test: every click-wheel path (Load, Save to, Clear, and therefore Try-it step 1) - the wheel is not drivable on this rig, so the entire menu half of the section is unverified and needs a hand; making the connections with the probe (simulated taps produced no net, so I made them over the harness python channel instead); and the boot behaviour, since rebooting is forbidden. A new user following the terminal bullet would succeed; a user following the Try-it steps as written would be on a surface I could not reach. Housekeeping: the harness starts on the owner's slot 3, so I deliberately did not run the page's 'Save to 1', and I removed my test connections and rail voltage before the final reset.

needs a hand: Click > Slots > Load - opening the Click menu needs a real click wheel; bench.py wheel is refused on this rig (turn, click and hold all do not reach the firmware's menus on this build).; Click > Slots > Save to - same; this is Try it step 1, so the 1 -> 2 -> 1 round trip on the page was never run as written.; Click > Slots > Clear - same.; Making connections with the probe - simulated taps do not create a connection, so the 'everything you do with the probe auto-saves' sentence needs a real probe on a real row.; Rebooting to confirm 'on boot it loads the last slot you had active', and setting boot_mode = fixed_slot and rebooting to confirm that value works.

- **[minor] unchecked**: page says: The page describes `<` as 'cycles to the next slot' and never says what happens at the ends of the range.  
  observed: Omission rather than an error: a bare `<` on slot 7 wraps round to slot 0, and `<8` is refused with 'Invalid slot: 8' leaving the slot unchanged. Both behaviours are sensible, but the wrap is worth one clause since anyone cycling with `<` will hit it.  
  repro: bench.py term "<7" then bench.py term "<" -> 'Slot 0'; bench.py term "<8" -> 'Invalid slot: 8'.

screenshots: gs12_slots_1_board.png, gs12_slots_1_oled.png, gs12_slots_2_board.png, gs12_slots_2_oled.png, gs12_slots_3_board.png, gs12_slots_3_oled.png, gs12_slots_4_board.png, gs12_slots_4_oled.png, gs12_slots_5_board.png, gs12_slots_5_oled.png, gs12_slots_6_board.png, gs12_slots_6_oled.png, gs12_slots_7_board.png, gs12_slots_7_oled.png

## gs13_from_the_terminal - From the terminal (01-getting-started.md)

steps: {'literal': 16, 'could_not_observe': 1}  
summary: The page holds up. Every command it names exists and does what it says on firmware 5.7.11.0: m prints the menu, n prints the net list, `+ 1-5,10-gnd` makes both connections (10 folds into GND, 1-5 becomes a pink Net 6), `- 1-5` removes just that one, x prints "Cleared all connections" and blanks the board, `<` steps slot 3 to 4 and `<3` jumps straight back, p opens the MicroPython REPL at a >>> prompt, / opens the File Manager TUI, and `/python_scripts/examples/gpio_basics.py` runs the named script to completion. help, `[command]?` (tried +?, n?, <?) and e all work as described, e adding nine more menu entries. The Try it block passes literally end to end: `+ 1-5` lights rows 1 and 5 fully pink with a single-LED trail through rows 2-4 - the wire visibly appearing - n shows "Net 6 pink 1,5", and `- 1-5` puts the board back to no rows lit. Three minor gaps, all about what the page leaves out rather than what it gets wrong: the menu did not print by itself when the serial port was opened (only `m` produced it), the gpio_basics.py example silently leaves a GP_1,3,13,GP_2 net wired up afterwards, and neither p nor / has a stated way out (Ctrl+Q, learned only from the board's own banners). U was not tested - the harness forbids mass-storage commands - though the menu lists it as "U/u = enable/disable USB Mass Storage", consistent with the page.

- **[minor] unchecked**: page says: "Open the first serial port ... and the menu prints."  
  observed: Opening CDC port 1 and reading for 3 s, then again for 6 s, printed nothing at all; a bare Enter printed nothing either. The menu only appeared after typing `m` (or automatically after a `<` slot change). A brand-new user who opens a serial monitor and waits, as the sentence invites, sees a blank window and has no on-screen hint that `m` is the way in. Caveat on the grade: bench.py's port open may not assert DTR the way a GUI serial monitor does, so I could not rule out that a real serial monitor triggers a print this harness does not - if it also shows nothing, this is major.  
  repro: bench.py reset; bench.py listen 6  ->  empty. bench.py term ""  ->  empty. bench.py term "m"  ->  the menu.

- **[minor] unchecked**: page says: "/  file manager (or /python_scripts/examples/gpio_basics.py to run a script)" - presented as a harmless way to run an example.  
  observed: The named example rewires the board and leaves the wiring in place. After it printed "--- script finished ---", `n` showed a new net: "Net 6  GP_1,3,13,GP_2  input - low". The page gives no warning and no cleanup step, so a new user following it ends up with connections they did not ask for.  
  repro: bench.py term "x"; bench.py term "/python_scripts/examples/gpio_basics.py" 10; bench.py listen 6; bench.py term "n"  ->  Net 6 GP_1,3,13,GP_2.

- **[minor] unchecked**: page says: "p   MicroPython REPL" (and "/  file manager") - listed as commands to try, with no exit stated.  
  observed: Both drop you into a mode the page never tells you how to leave. Ctrl+Q is the way out of each, but the page does not say so - you only learn it from the REPL's own banner or the file manager's footer bar. Nothing on the page gets a reader back to the menu.  
  repro: bench.py term "p"  ->  >>> prompt, no exit hint on the page; bench.py raw "\x11"  ->  menu returns.

screenshots: gs13_from_the_terminal_1_board.png, gs13_from_the_terminal_1_oled.png, gs13_from_the_terminal_2_board.png, gs13_from_the_terminal_2_oled.png, gs13_from_the_terminal_3_board.png, gs13_from_the_terminal_3_oled.png, gs13_from_the_terminal_4_board.png, gs13_from_the_terminal_4_oled.png

## pr00_guided_projects_flow - Guided projects (the flow) (08.5-examples.md)

steps: {'literal': 20, 'needs_hand': 4, 'substitute': 2, 'could_not_observe': 2}  
summary: The page describes a flow that genuinely works - the first time you open a project. On a project with no saved run file (nand00, i2cscrn, eeprom) every promise lands: opening sets the top rail to 3.3 V, the part rows light one outer-edge LED each and fade after a few seconds exactly as written, the OLED shows "Logic Gates 101 2/7" in precisely the documented Name N/M format, each step lights its own part's rows (colour-coding LED anode red vs cathode blue), a probe tap on row 35 puts "U1 | A1 | 35   ic 74HC00, 14 pins" on the OLED, and z check prints a resistor value, an LED forward voltage and a chip presence result, with z check step 3 resolving to that step's part. z steps / next / prev / 3 / off / on all exist and behave as named, opening really does leave the parts unconnected, and the tip box's 13-pair + line lands every connection verbatim and survives switching projects and coming back. Three things break. (1) Major, and it is what a new user hits: reopening a saved run file drops every part. I reproduced it from scratch - nand00's first open gave PARTS n=3 / steps=7, and reopening the run file that same open wrote gave "unknown part 'U1'/'LED1'/'R1' (skipped)", PARTS n=0, steps=4. The shipped 555_run.yaml is already in that state, so following the page on the 555 gives "555 LED Flasher 1/3" instead of 3/9, no part rows lit, and "CHECK error unknown part 'R1'". Connections persist; parts do not. (2) Major: Ctrl+Q did not stop the 555's main.py after three tries plus Ctrl+C - it only reaches a script sitting at an input prompt (it stopped nand00's instantly), and the 555's own banner tells you to hold the clickwheel, which needs a hand. (3) Major: "Nothing fails you for using a different value" versus the firmware printing result=fail ... "wrong part?". Minor: the 555's top rail never left 0 V (probably the same degraded-file cause, since nand00 powers its rail at open despite also having a confirm step), and z steps off leaves the card on an otherwise-idle OLED. Untestable without hands: the whole wheel route (Files browser, turning, holding, the power-on confirm), pushing parts in, a real probe tap and probe wiring, z 555 new, and the open/short resistor wordings.

needs a hand: The wheel route to open a project: Files > projects > the project's folder > click wiring.yaml. bench.py wheel up/down/click is not available on this rig, so that whole alternative was untested.; Turning the wheel to move between steps (I substituted z steps next / z steps prev).; Holding the wheel to put the steps away (I substituted z steps off), and holding it to stop a running script - which is what the 555's own script banner tells you to do.; Clicking the wheel to confirm the 555's step 2 "Confirm to power up (5V)".; Pushing parts into the board, so every z check reading here is whatever the owner already has in those rows.; A real probe tap with electrical contact. bench.py tap only tells the firmware where the tip is, so the OLED pin card is verified but nothing measured through the tip is.; Making connections with the probe in select mode (button presses) - only the + command half of that sentence was tested.; z 555 new, and clicking wiring.yaml in Files, to start over from the shipped wiring - the harness refuses `z <project> new` because it overwrites the owner's run file. That also means the 555 could never be seen in its shipped state.; A physically mis-seated resistor, to see the documented `open` and `short` readings.; SIDE EFFECT THE OWNER SHOULD KNOW: following the page wrote to the board - the documented + line was applied and saved into /projects/555/555_run.yaml, and opening the other three projects created /projects/nand00/nand00_run.yaml, /projects/i2cscrn/i2cscrn_run.yaml and /projects/eeprom/eeprom_run.yaml.

- **[major] CONFIRMED**: page says: What you build is saved in the project's own run file, so it's still there next time you open it - and with it, bullets 1-3 of the flow (the OLED shows 555 LED Flasher 3/9, the rows for that step's part light up, z check R1 measures the part).  
  observed: Reopening any saved run file loses every part. This is reproducible from scratch, not owner-specific: a first open of nand00 read RUNFILE action=new, PARTS n=3, VIEWER steps=7, part rows lit; reopening the run file that same open had just written printed "guide parse warnings: guide step 2: unknown part 'U1' (skipped); ... 'LED1' ... 'R1' (skipped)", PARTS n=0, VIEWER steps=4. The pre-existing 555 file shows the identical failure with its six parts (U1, R1, R2, C1, LED1, R3), leaving VIEWER steps=3. Consequences for a reader following the page on the 555: the OLED reads "555 LED Flasher 1/3" instead of 3/9, no part rows ever light (ROWS LIT: none), and z check R1 answers "CHECK error unknown part 'R1'". Connections do survive - only parts and their guide steps are dropped.  
  repro: bench.py term "z nand00 noscript" on a project with no run file -> PARTS n=3, VIEWER steps=7. Then bench.py term "z nand00 noscript" again -> guide parse warnings, PARTS n=0, VIEWER steps=4. Same shape for "z 555 noscript" against the shipped 555_run.yaml.  
  suggested: Line 5: "The first time you open one, the top rail gets set (5 V for the 555, 3.3 V for the others), the parts light up along the board edge for a few seconds so you can see where they go, and the OLED walks you through them one at a time. After that the project comes back the way you left it, rail included." The same unconditional "on open" phrasing carries the parts-light-up and OLED-walkthrough halves of line 5, so fixing line 5 covers all three (line 14 already says saved state returns). The tip box is not wrong, but a reader who reopens a saved 555 wires rows 5, 10 and 38 to a rail that is still at 0 V until they power it, so it is worth a clause there.

- **[major] HARNESS_ARTIFACT**: page says: Hold the wheel for a few seconds (or Ctrl+Q) to stop it.  
  observed: Ctrl+Q (\x11 on the terminal) did not stop the 555's main.py. Three sends plus one Ctrl+C left it looping "measured: 0.00 Hz, 0% high   cap 0.0x V", and while it ran it also blocked the read-only port so the OLED and LEDs were unreadable. Only the harness reset ended it. The same Ctrl+Q stopped nand00's main.py instantly ("bye / --- script finished ---") because that script sits at an input prompt. So the page's terminal escape hatch works for some project scripts and not others, and the 555's own banner points at the wheel hold instead.  
  repro: bench.py term "z 555" then bench.py raw "\x11" and bench.py listen 6 - the measured line keeps arriving. Compare: bench.py seq "z nand00" "<WAIT:3>" "<CTRL-Q>" -> "bye".  
  suggested: No change needed - the page's "`Hold` the wheel for a few seconds (or `Ctrl+Q`) to stop it" is correct as written; Ctrl+Q stopped the 555's main.py immediately in both runs. The same wording does not appear wrong elsewhere in the excerpt: the `hold` in the steps bullet is a different action (putting the step cards away), and the "Hold the clickwheel to exit." line the tester quoted is the 555 script's own banner, not the page - if anything is worth changing it is that banner, which could say "or Ctrl+Q" too.

- **[major] unchecked**: page says: Nothing fails you for using a different value.  
  observed: z check R1 on a 330-ohm step that measured 591 ohm printed CHECK part=R1 result=fail val=591 detail="R1: 591 (band 264-396, 3.84mA @ 3.3V)" hint="reads 591, expected 330 +/-20% - wrong part?". Nothing is blocked, but the firmware's word for a different value is literally "fail" plus "wrong part?", which is the opposite of the reassurance the page gives.  
  repro: bench.py term "z nand00 noscript" (first open, so parts exist) then bench.py term "z check R1".

- **[minor] unchecked**: page says: the top rail gets set (5 V for the 555, ...)  
  observed: Every open of the 555 left the top rail at 0.000 V, and it stayed there all session - including after the documented + line connected rows 5, 10 and 38 to top_rail. The 555 could only ever be loaded from its degraded saved run file here (`z 555 new` is forbidden on this rig), so this may be the same file degradation as discrepancy 1 rather than a separate bug. It is not the power_on confirm step: nand00 has one too and still powered its rail to 3.3 V at open. The "3.3 V for the others" half is correct - nand00 and i2cscrn both read top 3.3 V immediately.  
  repro: bench.py term "z 555 noscript" then bench.py leds -> RAILS: top 0.0 V. Compare bench.py term "z nand00 noscript" (first open) -> top 3.3 V.

- **[minor] unchecked**: page says: hold to put the steps away / z steps off does what it says  
  observed: On the 555 the terminal answered "VIEWER off" but the OLED kept displaying the step card ("555 LED Flasher 2/3 / Confirm to power up (5V).") indefinitely, because nothing else redrew the screen. On nand00 the same command cleared both the part LEDs and the OLED. So on a project whose screen is otherwise idle, "put the steps away" leaves them visibly on screen.  
  repro: bench.py term "z 555 noscript"; "z steps 2"; "z steps off"; bench.py oled.

screenshots: pr00_guided_projects_flow_1_board.png, pr00_guided_projects_flow_1_oled.png, pr00_guided_projects_flow_2_board.png, pr00_guided_projects_flow_2_oled.png, pr00_guided_projects_flow_3_board.png, pr00_guided_projects_flow_3_oled.png, pr00_guided_projects_flow_4_board.png, pr00_guided_projects_flow_4_oled.png, pr00_guided_projects_flow_5_board.png, pr00_guided_projects_flow_5_oled.png, pr00_guided_projects_flow_6_board.png, pr00_guided_projects_flow_6_oled.png, pr00_guided_projects_flow_7_board.png, pr00_guided_projects_flow_7_oled.png, pr00_guided_projects_flow_7b_board.png, pr00_guided_projects_flow_7b_oled.png, pr00_guided_projects_flow_8_board.png, pr00_guided_projects_flow_8_oled.png, pr00_guided_projects_flow_9_board.png, pr00_guided_projects_flow_9_oled.png, pr00_guided_projects_flow_10_board.png, pr00_guided_projects_flow_10_oled.png, pr00_guided_projects_flow_11_board.png, pr00_guided_projects_flow_11_oled.png

## pr_555_led_flasher - Project: 555 LED Flasher (08.5-examples.md)

steps: {'literal': 4, 'needs_hand': 3, 'could_not_observe': 11}  
summary: The page's opening command works: `z 555` is recognised, loads /projects/555/555_run.yaml (the run file path the page names), and starts main.py immediately, exactly as written. Everything after that is blocked by one major failure - the page says `Hold` the wheel for a few seconds (or `Ctrl+Q`) to stop it, and Ctrl+Q does not stop it. I sent it five ways plus Ctrl+C over more than two minutes; main.py kept printing and the terminal stopped echoing altogether. While it runs it owns all three channels: no terminal command is answered, the read-only OLED/LED/status channel returns nothing, and the MicroPython REPL cannot be opened, so even the harness reset fails. Because `z 555` is the page's own first instruction and it auto-runs the script, a user who follows the page top to bottom and cannot physically hold the wheel is locked out of every later step on it - `z steps`, the probe tap, `z check R1`, the `+ 35-gnd,...` line and `z 555 noscript` are all unreachable. Two smaller things surfaced before the lockout: the project loaded as 'VIEWER steps=3' with 'PARTS n=0' and six 'unknown part ... (skipped)' warnings for U1/R1/R2/C1/LED1/R3, against the page's `555 LED Flasher 3/9` example (caveat: this was the owner's persisted run file and `z 555 new` is forbidden here), and the script's own exit hint reads 'Hold the clickwheel to exit.' without mentioning Ctrl+Q. No screenshots exist for this run - `shot` wrote no files because the board was already captured. The board is still running main.py.

needs a hand: Hold the click wheel for a few seconds to stop main.py - the board is LEFT RUNNING /projects/555/main.py at the end of this session and no software channel (terminal, read-only, MicroPython REPL) responds, so the harness reset cannot run either. Someone has to hold the wheel or power-cycle the board.; Turn the wheel to move between steps, and hold the wheel to put the steps away (wheel turn and hold are both unavailable on this rig).; The wheel route into the project: Files > projects > 555 > click wiring.yaml, and running main.py yourself from the Files browser.; `z 555 new` - refused by the harness because it would overwrite the owner's run file, so 'starts over from the shipped wiring' is untested.; Push the parts in (NE555 at row 35, the two resistors, the cap, the LED, the 330 ohm) - without them `z check` readings and main.py's predicted-vs-measured blink rate cannot be exercised.

- **[major] unchecked**: page says: "`Hold` the wheel for a few seconds (or `Ctrl+Q`) to stop it."  
  observed: Ctrl+Q does not stop main.py. Sent five different ways (single, triple-burst, seq token, followed by Enter) plus Ctrl+C; the script kept printing 'measured: 0.00 Hz...' for over two minutes and the terminal stopped echoing typed characters entirely. The script's own on-screen line says only 'Hold the clickwheel to exit.' and does not mention Ctrl+Q.  
  repro: bench.py term "z 555" 6 ; bench.py raw "\x11" 8 ; bench.py listen 10  -> the measured: lines keep coming.

- **[major] unchecked**: page says: "`z 555` runs `main.py` as soon as the project opens" - and the page then tells you to type `z steps`, `z check R1`, the `+ 35-gnd,...` line and `z 555 noscript` in the same terminal.  
  observed: Downstream of the Ctrl+Q failure above. While main.py runs it owns everything: the terminal echoes nothing and answers no command, the read-only status/OLED/LED channel returns empty, and the MicroPython REPL cannot be entered ('could not open the raw REPL: TimeoutError'), so even the harness reset fails. Because `z 555` starts the script automatically and Ctrl+Q does not stop it, a user who follows the page top to bottom and cannot physically hold the wheel is locked out of every later step the page lists.  
  repro: bench.py term "z 555" 6 ; bench.py term "z steps" 5 (no echo, no answer) ; bench.py status (empty) ; bench.py reset (REPL timeout).

- **[minor] unchecked**: page says: "The OLED shows the step (`555 LED Flasher 3/9`)" - i.e. the 555 project walks through nine steps.  
  observed: Opening the project printed 'VIEWER steps=3 cursor=1' - three steps - together with 'PARTS n=0' and six warnings 'guide parse warnings: guide step 2: unknown part U1 (skipped)' for U1, R1, R2, C1, LED1 and R3, i.e. the guide could not resolve any of the parts the page names. The same open also reported 'PARTS n=0', so there were no parts to light along the board edge either.  
  repro: bench.py term "z 555" 6 and read the RUNFILE / VIEWER / PARTS lines. Caveat: this loaded the owner's persisted 555_run.yaml, not the shipped wiring; `z 555 new` (the page's way back to the shipped wiring) is forbidden on this rig, so I could not check the shipped file.

- **[minor] unchecked**: page says: The page tells the reader to stop the script with the wheel hold or Ctrl+Q.  
  observed: The running script itself prints a different, narrower instruction - 'Hold the clickwheel to exit.' - with no mention of Ctrl+Q. The text a stranded user actually reads on their terminal contradicts the doc by omission.  
  repro: bench.py term "z 555" 6 and read the first lines of the script output.

## pr_eeprom_dumper - Project: EEPROM Dumper (08.5-examples.md)

steps: {'could_not_observe': 9, 'literal': 20, 'needs_hand': 3, 'substitute': 3}  
summary: Firmware 5.7.11.0. The half of the page about the terminal step viewer is accurate and pleasant: `z eeprom noscript` opened the project and said so ('now your active circuit', SCRIPT action=skip), the OLED showed 'EEPROM Dumper 1/4' in exactly the '<name> N/M' shape the page promises, and `z steps`, `z steps next`, `z steps prev`, `z steps 3`, `z steps off` and `z steps on` each did what the page says (off disarmed the viewer and cleared its row LED; on came back at the same step). `z check step 3` ran and printed an I2C scan in the page's own vocabulary ('No I2C devices found' - the owner has no EEPROM seated). 'Opening a project doesn't make the connections' held exactly: after opening, the only net was the built-in DAC 0. The `+` command works with the page's syntax (`+ 38-gnd` created net GND [GND, 38] and drew it), and the persistence promise is real - that bridge survived a re-open of /projects/eeprom/eeprom_run.yaml. I removed it with `- 38-gnd` and re-opened again to confirm the file no longer carries it; the only trace left is the step cursor sitting at 4/4.

The part-facing half did not work. Opening printed 'guide parse warnings: unknown part U1 (skipped); unknown part R1 (skipped); unknown part R2 (skipped)' and 'PARTS n=0' - the project came up with no parts. Three of the page's promises fall out of that one hole: no rows lit at open (the page says the parts light up along the edge for a few seconds; my first LED read was ~4 s in, so a shorter flash could have been missed, but PARTS n=0 says there was nothing to flash), `z check R1` answers 'CHECK error unknown part R1' (the page says any part name works), and a tap on row 35 - where the page puts the EEPROM's pin 1 - leaves the OLED reading 'Found =' with nothing after the equals sign instead of naming a pin. I re-ran that tap after `switch select` and it changed nothing, and tapping the GND pad did produce 'GND / 0.0 mA', so it is not a switch-position artifact or a dead readout path. Separately, the top rail never left 0.000 V, though the page says opening sets it to 3.3 V; step 2's text ('Confirm to power the chip up (3.3V)') suggests that is behind a wheel click, which I could not deliver. Big caveat on those rail/parts findings: this open loaded an existing eeprom_run.yaml (a previously saved state), and `z eeprom new` - the documented way back to the shipped wiring - is refused by this harness, so I could not test a first-ever open. If a saved run file legitimately drops the parts and the rail, the page should say so, because a returning user hits exactly what I hit.

Two smaller things. `z steps off` disarms the viewer but leaves the step card sitting on the OLED. And the EEPROM section's 'write-protect pin held by the board's own wiring' reads as a contradiction of the bullet four lines above it - opening makes no connections at all; WP is held only after you confirm step 2. Also worth a writer's glance: 'Same note as above about the top OLED' has no antecedent anywhere in this section, so a reader who lands on EEPROM Dumper has nothing to refer back to. Not tested at all (no hands / harness-forbidden): bare `z eeprom` and therefore main.py and the hex dump itself, making a connection with the probe, the wheel, the Files browser, `z eeprom new`, and pushing the parts in.

needs a hand: Opening the project from the wheel: Files > projects > eeprom > click wiring.yaml (and the same click to start over from the shipped wiring).; Turning the wheel to move between steps (substituted `z steps next` / `z steps prev`).; Holding the wheel to put the steps away (substituted `z steps off`).; Clicking the wheel to confirm step 2 - the click that actually powers the chip to 3.3 V and holds WP high.; Pushing the EEPROM and the two 4.7k resistors into the board.; A real probe touch on a row (only a tip-position tap could be simulated; it makes no electrical contact).; Running main.py from the Files browser, and holding the wheel to stop it. Bare `z eeprom` (which would run it) is refused by this harness.; `z eeprom new` - refused by the harness because it overwrites the owner's run file; this is the one step that would show whether a fresh open sets the rail and lights the parts.; Making a connection with the probe (the page's other route for the wiring file's connect: lines) - `tap 38` then `tap gnd` created no net; a real tip contact / probe button is required.

- **[major] unchecked**: page says: "Opening one loads it as your circuit: ... the parts light up along the board edge for a few seconds so you can see where they go" - and the EEPROM section's "Parts: the EEPROM (DIP-8, pin 1 at row 35) and two 4.7k pull-up resistors."  
  observed: Nothing lit. `leds` said 'ROWS LIT: none' as soon as the open returned (~4 s after the keystroke) and again ~8 s later - so a sub-4-second flash could have been missed, but the real evidence is what the open itself printed: 'guide parse warnings: guide step 2: unknown part U1 (skipped); unknown part R1 (skipped); unknown part R2 (skipped)' and 'PARTS n=0'. The eeprom project loaded with no parts at all, so there was nothing to light, nothing to check and nothing to name on a tap. Caveat: the open loaded an existing /projects/eeprom/eeprom_run.yaml (RUNS n=1), and `z eeprom new` (the documented way back to the shipped wiring) is refused by this harness, so a first-ever open was not testable.  
  repro: bench.py term "z eeprom noscript" 3 ; bench.py leds ; wait 8 s ; bench.py leds

- **[major] unchecked**: page says: "the top rail gets set (5 V for the 555, 3.3 V for the others)" on opening a project  
  observed: The top rail stayed at 0.000 V from the moment the project opened until the end of the session (status: top_rail 0.000, bottom_rail 0.000). Step 2's text - 'Confirm to power the chip up (3.3V)' - suggests the 3.3 V is behind a wheel click, but I could not click, so that part is inference from the step text. Same run-file caveat as above.  
  repro: bench.py term "z eeprom noscript" 3 ; bench.py status | grep top_rail

- **[major] unchecked**: page says: "`z check R1` (any part name, or `z check step 3`) measures the part and prints what it read"  
  observed: `z check R1` printed 'CHECK error unknown part R1'. No measurement, no value, no open/short. (`z check step 3` did work - it ran an I2C scan and printed 'No I2C devices found'.) Downstream of the PARTS n=0 problem above.  
  repro: bench.py term "z eeprom noscript" 3 ; bench.py term "z check R1" 5

- **[major] unchecked**: page says: "Tap one of its rows with the probe and the OLED tells you which pin that is."  
  observed: A tap on row 35 - where the page puts the EEPROM's pin 1 - left the OLED reading 'Found =': a label with an empty right-hand side, no pin name, no part name, no row number. A tap on row 8 (the row step 3 lights) gave the identical card, which stayed up across three reads. Repeating the tap after `switch select` changed nothing at all - the OLED just kept the step card. This is not a Measure-mode artifact and not a dead readout path: tapping the GND pad immediately put 'GND / 0.0 mA' on the screen. It is downstream of PARTS n=0 - with no parts loaded there is no pin to name - but 'Found =' with nothing after the equals sign is what the user is actually shown.  
  repro: bench.py term "z eeprom noscript" 3 ; bench.py tap 35 ; bench.py oled ; bench.py switch select ; bench.py tap 35 ; bench.py oled ; (contrast) bench.py tap gnd ; bench.py oled

- **[minor] unchecked**: page says: "`z steps off` ... do what they say" (i.e. put the steps away, the terminal twin of `hold`)  
  observed: The viewer really did disarm - terminal said 'VIEWER off', row 8 went dark, and a following `z steps` answered 'VIEWER error not armed'. But the OLED went on displaying the 'EEPROM Dumper 3/4 / Scanning the bus...' card. The screen only changed later when something else painted it, so 'put the steps away' leaves the step still on the screen.  
  repro: bench.py term "z steps 3" ; bench.py term "z steps off" ; bench.py oled

- **[minor] unchecked**: page says: EEPROM Dumper: "with the write-protect pin held by the board's own wiring"  
  observed: Right after opening, status has no nets at all beyond the built-in DAC 0 - nothing is holding WP. The hold is gated behind step 2 ('Confirm to power the chip up (3.3V). WP is held high - reads only.'), which needs a wheel click, and it also reads as a contradiction of the bullet four lines earlier: 'Opening a project doesn't make the connections between the parts.'  
  repro: bench.py term "z eeprom noscript" 3 ; bench.py status ; bench.py term "z steps 2"

screenshots: pr_eeprom_dumper_1_board.png, pr_eeprom_dumper_1_oled.png, pr_eeprom_dumper_2_board.png, pr_eeprom_dumper_2_oled.png, pr_eeprom_dumper_3_board.png, pr_eeprom_dumper_3_oled.png, pr_eeprom_dumper_4_board.png, pr_eeprom_dumper_4_oled.png, pr_eeprom_dumper_5_board.png, pr_eeprom_dumper_5_oled.png, pr_eeprom_dumper_6_board.png, pr_eeprom_dumper_6_oled.png, pr_eeprom_dumper_7_board.png, pr_eeprom_dumper_7_oled.png, pr_eeprom_dumper_8_board.png, pr_eeprom_dumper_8_oled.png, pr_eeprom_dumper_9_board.png, pr_eeprom_dumper_9_oled.png, pr_eeprom_dumper_10_board.png, pr_eeprom_dumper_10_oled.png, pr_eeprom_dumper_11_board.png, pr_eeprom_dumper_11_oled.png, pr_eeprom_dumper_12_board.png, pr_eeprom_dumper_12_oled.png, pr_eeprom_dumper_13_board.png, pr_eeprom_dumper_13_oled.png

## pr_logic_gates_101 - Project: Logic Gates 101 (08.5-examples.md)

steps: {'substitute': 3, 'literal': 17, 'needs_hand': 3, 'could_not_observe': 6}  
summary: The scaffolding of the page is real and the terminal half of it works: `z nand00 noscript` opens the project, the OLED shows a step card in exactly the page's format (`Logic Gates 101 1/4`), the whole `z steps` family (bare, next, prev, <n>, off, on) exists and does what the page says, `+` makes connections, opening genuinely does NOT make the part connections, and what you build persists in `/projects/nand00/nand00_run.yaml` across a reopen (verified by making `+ 35-gpio_1`, reopening, seeing it survive, then removing it). What is broken is the part layer, and it breaks four of the page's promises at once. Opening nand00 prints `guide step 2: unknown part 'U1' (skipped); ... 'LED1' (skipped); ... 'R1' (skipped)` and `PARTS n=0`. So: no parts light up along the edge (ROWS LIT: none, ever - only step 3 lights row 37 for its verify), a probe tap on row 35 names no pin, and `z check R1` - the page's own example command - answers `CHECK error unknown part 'R1'`, as do U1 and LED1. Separately, the top rail is never set: it reads 0.000 V from open to finish, because the project makes powering up a wheel CLICK on step 2 ("Confirm to power the gate up (3.3V).") rather than something opening does, which the page states the opposite of. I also re-opened the project at the end to confirm the owner's run file was left as found (only the DAC 0 net). Two smaller things: `z steps off` disarms the viewer but leaves the step card on the OLED forever, and the Logic Gates 101 paragraph gives a reader no connect: lines and no rows for the LED or resistor, so at the page's own "you make those yourself" step a nand00 user has nothing to type. main.py could not be exercised - a bare `z nand00` is refused by this rig and running it over the MicroPython port timed out with no output - so the truth-table / ok / WRONG / type-input-pairs claims are untested rather than contradicted. Everything involving the wheel (turn between steps, hold to exit, click to confirm/power up) and seating the actual parts needed a hand.

needs a hand: Turning the wheel to move between steps (the page's primary way to walk the guide) - bench.py wheel is unavailable on this rig; I substituted `z steps next/prev/<n>`.; Holding the wheel to put the steps away, and to stop main.py - I substituted `z steps off` and `bench.py stop` (Ctrl+Q).; Clicking the wheel to confirm step 2, which is what actually powers the top rail to 3.3 V. Without a hand the rail never comes up, so every downstream electrical claim is untestable.; Pushing the 74HC00, the LED and the 330 ohm resistor into the board.; The Files browser route (Files > projects > nand00 > click wiring.yaml, and running main.py from the folder).; A bare `z nand00` (the page's own command, which runs main.py) - refused by the rig because only a wheel hold can stop the resulting script.; `z nand00 new` - forbidden by the rig, it would overwrite the owner's run file.

- **[major] unchecked**: page says: "Opening one loads it as your circuit: the top rail gets set (5 V for the 555, 3.3 V for the others)."  
  observed: After `z nand00 noscript` the top rail is 0.000 V and stays 0.000 V for the whole session. The project's own step 2 reads "Confirm to power the gate up (3.3V)." and the OLED's step-1 footer reads `Turn=prev/next, click=confirm, hold=exit`, which together imply the rail is powered by confirming step 2 with a wheel click - something the page never mentions (it describes only turn and hold). I could not click, so that is an inference; what I directly observed is that opening the project does not set the rail.  
  repro: bench.py reset; bench.py term "z nand00 noscript" 6; bench.py status  ->  "top_rail": 0.000

- **[major] unchecked**: page says: "the parts light up along the board edge for a few seconds so you can see where they go" and "the rows for that step's part light up".  
  observed: No part rows ever light. The open prints `guide step 2: unknown part 'U1' (skipped); ... 'LED1' (skipped); ... 'R1' (skipped)` and `PARTS n=0`, so nand00 has no parts registered at all. Of the four steps only step 3 lights anything (row 37, blue), and that is the verify step, not a part step. A new user is told to "push the part in where the step says" but the step never says where.  
  repro: bench.py term "z nand00 noscript" 6 (read the guide parse warnings and PARTS n=0); bench.py leds  ->  ROWS LIT: none; then z steps next through all four steps, leds after each.

- **[major] unchecked**: page says: "`z check R1` (any part name ...) measures the part and prints what it read."  
  observed: `z check R1` prints `CHECK error unknown part 'R1'`. So do `z check U1` and `z check LED1` - i.e. every part name in this project. Only the `z check step <k>` form works.  
  repro: bench.py term "z check R1" 5  ->  CHECK error unknown part 'R1'

- **[major] unchecked**: page says: "Tap one of its rows with the probe and the OLED tells you which pin that is."  
  observed: A simulated probe tap held ~1.2 s on row 35 (the page's 74HC00 pin 1) and on row 37 changed nothing on the OLED - it kept showing the step card - and printed nothing on the terminal. Repeated with the probe switch explicitly set to select: still no OLED change, no row lit, no net created. With PARTS n=0 there is no part for the firmware to name a pin from. Caveat: the tap is simulated (tip location only, no electrical contact), so this does not strictly disprove a real probe tap - but pin naming is a lookup, not a measurement.  
  repro: bench.py term "z nand00 noscript" 6; bench.py switch select; bench.py tap 35; bench.py oled  ->  unchanged step card

- **[minor] unchecked**: page says: "`z steps off` ... do what they say" (paired with "`hold` to put the steps away").  
  observed: `z steps off` disarms the viewer (terminal prints `VIEWER off`, a following `z steps` prints `VIEWER error not armed`) but the OLED keeps showing the step card `Logic Gates 101 2/4 / Confirm to power the gate up (3.3V).` indefinitely. The steps are not actually put away on screen.  
  repro: bench.py term "z steps off" 3; bench.py shot X  ->  X_oled.png still shows the step card

- **[minor] unchecked**: page says: "prints what it read: a resistor's value, an LED's forward voltage, whether a chip answers ... `open` ... `short`".  
  observed: The only working form, `z check step <k>`, prints a different vocabulary: `result=pass val=norows hint="no power/gnd-class pins to verify"` and `result=fail val=float hint="row reads floating (undriven?)"`. Neither `open` nor `short` nor any value/forward-voltage reading ever appeared. (Nothing is seated here, so this may be partly the bench's fault - but a user gets no vocabulary the page prepared them for.)  
  repro: bench.py term "z check step 3" 5

- **[minor] unchecked**: page says: "you make those yourself, with the probe or the `+` command, from the wiring file's `connect:` lines. The 555's are all in the box below."  
  observed: The `+` command itself works (`+ 35-gpio_1` created net `["35","GP_1"]`). But the page supplies connect lines only for the 555 and names no way to read nand00's wiring file, so a reader following the Logic Gates 101 section reaches this step with nothing to type. The Logic Gates 101 paragraph also gives no row numbers for the LED or the resistor - only the 74HC00's pin 1 at row 35.  
  repro: Read the page's Logic Gates 101 paragraph and try to find the connections; then note no terminal command on the page lists them.

screenshots: pr_logic_gates_101_1_board.png, pr_logic_gates_101_1_oled.png, pr_logic_gates_101_2_board.png, pr_logic_gates_101_2_oled.png, pr_logic_gates_101_3_board.png, pr_logic_gates_101_3_oled.png, pr_logic_gates_101_4_board.png, pr_logic_gates_101_4_oled.png, pr_logic_gates_101_5_board.png, pr_logic_gates_101_5_oled.png, pr_logic_gates_101_6_board.png, pr_logic_gates_101_6_oled.png, pr_logic_gates_101_7_board.png, pr_logic_gates_101_7_oled.png, pr_logic_gates_101_8_board.png, pr_logic_gates_101_8_oled.png, pr_logic_gates_101_9_board.png, pr_logic_gates_101_9_oled.png

## pr_type_to_screen - Project: Type to Screen (08.5-examples.md)

steps: {'literal': 16, 'substitute': 3, 'needs_hand': 6, 'could_not_observe': 8}  
summary: The page's project machinery works. `z i2cscrn noscript` opened the project, set the top rail to 3.3 V ("3.3 V for the others"), left the wiring unconnected ("opening a project doesn't make the connections"), pointed the run file at /projects/i2cscrn/i2cscrn_run.yaml, and put a step card on the OLED in exactly the shape the page shows - "Type to Screen 2/5" alongside "555 LED Flasher 3/9". All five terminal step verbs exist and behave: `z steps` prints the current step, `next`/`prev`/`3` move the cursor (next wraps 5 -> 1), and `on` restores the viewer at the step it left. Part rows light per step (step 2 lit rows 5/6/7/8 in green/red/blue/blue for GND/VCC/SCL/SDA), and a probe tap on row 5 put a part card on the OLED naming the pin - "DISP  ic SSD1306 / |GND| VCC SCL SDA / |5 | 6 7 8" - exactly as the page promises. `z check DISP` and `z check step 2` both run and report. Four minor problems: (1) `z steps off` clears the row LEDs and prints VIEWER off but leaves the step card on the OLED, so the steps are not actually "put away"; (2) I never saw "the parts light up along the board edge for a few seconds" on opening - the board was dark except the rails; (3) `z check` on this project's chip returns result=skipped val=ic_unverified rather than the reading the page describes, and the page gives no hint you must power the project first; (4) "Turn the Jumperless's own top OLED off" names no command or menu path anywhere on the page. The Type-to-Screen paragraph's own flow is only half-testable here: launching main.py by hand did produce "Tap GND or row 5" on the OLED, confirming it asks where each signal is and wants probe taps, but my simulated tap did not advance it and that attempt is confounded (the background run died of a MemoryError under my exec() substitute, and the tap tool shares the same CDC port as the script), so the driver picker and the type-in-the-terminal behaviour were never reached - and no display module is plugged into this board anyway. Board was reset before and after; it stayed responsive throughout.

needs a hand: Opening a project from the wheel: Files > projects > the project's folder > click wiring.yaml (no wheel turn or click on this rig).; Turning the wheel to move between steps, and holding the wheel to put the steps away.; Holding the wheel for a few seconds to stop a running main.py (I could only send Ctrl+Q on the terminal plus Ctrl+C on the Python port together).; Pushing the SSD1306/SH1106 module (or any part) into the breadboard.; Answering main.py's "Tap GND or row 5" prompts with real probe taps - my one attempt with the simulated tap is confounded (the background run died of a MemoryError under my exec() substitute, and the tap tool shares CDC port 5 with the script), so the four-signal flow was never carried through.; Running main.py from the Files browser.; The bare `z i2cscrn` (runs main.py on open) and `z i2cscrn new` - both refused by the harness because they run an unstoppable script / overwrite the owner's run file.; Making the wiring connections with the probe or the `+` command, including the 555 tip box line - they would be written into the owner's project run file.; Verifying that what you type in the terminal shows up on the module (needs a real SSD1306/SH1106 wired in).

- **[minor] unchecked**: page says: z steps off ... does what it says / hold to put the steps away  
  observed: `z steps off` prints `VIEWER off` and clears the step's row LEDs, but the OLED keeps showing the step card ("Type to Screen 2/5 / Header in rows 5-8: ...") indefinitely - checked immediately and again 5 s later. The steps are only half put away.  
  repro: z i2cscrn noscript ; z steps 3 ; z steps prev ; z steps off ; then look at the OLED (bench.py oled / shot).

- **[minor] unchecked**: page says: the parts light up along the board edge for a few seconds so you can see where they go  
  observed: Nothing lit on opening i2cscrn. Immediately after `z i2cscrn noscript`, `leds` reported "ROWS LIT: none" and the board PNG showed only the rail strips (top rail orange 3.3 V, GND green). Part rows only light once you are on a part step (step 2 lights rows 5-8) - not at open.  
  repro: bench.py reset ; bench.py term "z i2cscrn noscript" 1 ; bench.py leds ; bench.py shot X and look at X_board.png.

- **[minor] unchecked**: page says: z check R1 (any part name ...) measures the part and prints what it read: ... whether a chip answers  
  observed: For i2cscrn's only part the check declines instead of reading: `CHECK part=DISP result=skipped val=ic_unverified hint="unpowered IC can't be electrically confirmed - real verification happens at power_on"`. Same for `z check step 2`. A reader of this page expects a measurement and gets a skip, with no hint on the page that the project must be powered first (that lives in step 3's text on the OLED).  
  repro: z i2cscrn noscript ; z check DISP ; z check step 2

- **[minor] unchecked**: page says: Turn the Jumperless's own top OLED off while you use this, they share the I2C pins.  
  observed: The page names no command, menu path or gesture for turning the Jumperless's OLED off. The instruction cannot be followed from this page alone.  
  repro: Read the Type to Screen paragraph and try to find how to turn the OLED off.

screenshots: pr_type_to_screen_1_board.png, pr_type_to_screen_1_oled.png, pr_type_to_screen_edge_board.png, pr_type_to_screen_edge_oled.png, pr_type_to_screen_2_board.png, pr_type_to_screen_2_oled.png, pr_type_to_screen_3_off_board.png, pr_type_to_screen_3_off_oled.png, pr_type_to_screen_3b_off_board.png, pr_type_to_screen_3b_off_oled.png, pr_type_to_screen_4_tap5_board.png, pr_type_to_screen_4_tap5_oled.png, pr_type_to_screen_5_mainpy_board.png, pr_type_to_screen_5_mainpy_oled.png, pr_type_to_screen_6_tapgnd_board.png, pr_type_to_screen_6_tapgnd_oled.png, pr_type_to_screen_7_board.png, pr_type_to_screen_7_oled.png

## gpio1_set_up_a_pin - GPIO: Set up a pin (05.7-gpio.md)

steps: {'literal': 1, 'could_not_observe': 3, 'substitute': 2, 'needs_hand': 7}  
summary: This page is almost entirely a click-wheel-and-OLED page, and neither surface is drivable from this rig, so it came out mostly untested rather than passing or failing. Firmware 5.7.11.0, slot 3. The one thing I could check straight from the page - that there are ten routable GPIOs - holds: status reports exactly ten GPIO slots (all unrouted, net -1). Everything else stalled at Try it step 1. bench.py wheel refuses turn/click/hold on this build, and there is no probe-button simulation, so the two documented ways in and Try it steps 2-4 are needs_hand outright. The probe substitute failed too: bench.py tap gpio_pad, tap 20 and tap 25 all ran cleanly but changed nothing - no highlight, no OLED change, no lit rows, and no net (a control tap 20 / tap 25, which should have made a wire, made none either), so GPIO 1 was never connected to row 20 and no options? tag was ever on screen. The OLED compounded it: through the entire session it read back a frozen part card (DISP / ic SSD1306 / GND VCC SCL SDA / 5 6 7 8) that contradicted the terminal's own view of the board (no nets, no rows lit) and never updated once. I filed no major discrepancies because I never observed the page's behavior; the one minor finding is a doc gap I hit as a reader, not on the board - Try it step 1 tells you to use "the GPIO pad, right side of 1" to get an output but this page never explains that the pad has per-pin sides or that the right side means output, deferring to a different page for the only step the rest of the flow depends on. I ran bench.py reset first and last; the board answered on the terminal and read-only ports throughout and never wedged. Someone with hands needs to rerun Try it 1-4 to actually judge this page.

needs a hand: Every click-wheel action - turn, click, hold. bench.py wheel prints NOT AVAILABLE on this rig, so both documented ways in ("Highlight it and click" and "Click > GPIO > Set Pins") and Try it steps 2, 3 and 4 are entirely unreachable.; The probe's Connect / Remove buttons - the page's stated alternative to click/hold. bench.py has no probe-button simulation at all.; A probe tap that the firmware actually registers. bench.py tap ran without error but produced zero observable effect (no highlight, no OLED change, no LEDs, no net) - even a control tap 20 / tap 25, which should have made a wire, made none. Try it steps 1 and 2 need a real hand on the probe.; Touching a specific side of the GPIO pad ("right side of 1") - the tool can only tap the pad as a single node.; Confirming what the OLED actually shows. Through the whole session the OLED read back a frozen, stale part card (DISP / ic SSD1306 / GND VCC SCL SDA / 5 6 7 8) that did not match the terminal's own breadboard view (no nets, no rows lit) and never changed, so every OLED claim on this page - "GPIO 3 out" / "HIGH", the "options?" tag, the "GPIO 7" / "OLED" in-use card - is unverified.; Watching row 20's LED brightness track the PWM Freq while the wheel turns.

- **[minor] unchecked**: page says: Try it 1: "Connect GPIO 1 to row 20 as an output (GPIO pad, right side of 1, tap row 20)."  
  observed: As a new user following only this page, I cannot tell what the parenthetical means. The page never says the GPIO pad is divided into per-pin left/right halves or that the right side selects an output - the only pointer is a link to a different page (Getting Started, Special Functions) earlier in the section. The step that establishes the whole Try it flow is the one step the page does not explain on its own.  
  repro: Read the page top to bottom with no prior Jumperless knowledge and try to perform Try it step 1; the pad's side-to-direction mapping is never stated on this page.

screenshots: gpio1_set_up_a_pin_0_board.png, gpio1_set_up_a_pin_0_oled.png, gpio1_set_up_a_pin_1_board.png, gpio1_set_up_a_pin_1_oled.png

## gpio2_counter_and_rest - GPIO: The counter, OLED chooser, what's saved (05.7-gpio.md)

steps: {'needs_hand': 9, 'literal': 3, 'substitute': 1, 'could_not_observe': 1}  
summary: Firmware 5.7.11.0, slot 3. Nothing on this page contradicted the board, but most of it is out of reach from this rig, so the page is largely unverified rather than verified. What I could test passed cleanly: the two config lines work exactly as printed, leading backtick and all - `[logo_pads] top_guy_idle = bcd_up; answered 'Changed [logo_pads] top_guy_idle from off to bcd_up / Config updated', and bottom_guy_idle = bcd_down did the same. The parenthetical is true too: building_pad_top_idle and building_pad_bottom_idle accept the same bcd_up/bcd_down values. The 'What's saved' sentence is half-confirmed from the slot file itself: /slots/slot3.yaml carries gpio direction, pulls, pwmFrequency, pwmDutyCycle, pwmEnabled and bcd: {pins: 0, value: 0}, and the direction array had already absorbed a live change I made - but I had no way to load a slot back, so the 'puts the pins back' half is could_not_observe. Try-it step 1 works, though I had to do it by MicroPython instead of the probe: GPIO 1-4 connected to rows 10-13 as outputs, all four rows lit green for LOW, so rows do follow their GPIO level. Everything else needs a hand. The wheel is simply not available on this rig, which takes out the whole Counting section, the whole Range section and Try-it steps 2 and 3. Both probe sections - the logo-pad counting and 'Choosing a GPIO on the OLED' - were blocked by a subtler thing: the simulated Select switch is overridden by the physical switch about twice a second while a tap is held for 1.2 s, so no Select-mode tap ever lands. I confirmed the taps themselves reach the firmware by tapping row 10 with the switch on Measure, which printed 'GPIO 1 out  10  LOW' on the terminal. So four gpio_pad taps yielded no chooser and four logo_pad_top taps yielded no counting, but neither of those is evidence against the page - it needs a hand on the switch. Two rig notes worth passing on: bench.py rejected the node name 'gpio_1' even though its own help lists gpio_1..gpio_8, and after `reset` the OLED sat on the owner's part card (DISP / ic SSD1306, rows 5-8), not a neutral screen, so the starting state a new reader would assume is not quite what the board shows. I restored the owner's four [logo_pads] *_idle keys to 'off' and verified them by reading /config.txt, then ran the closing reset.

needs a hand: Wheel turn, click and hold: bench.py declares these NOT AVAILABLE on this rig, so the whole Counting section, the whole Range section, 'pick BCD from a pin's options' and Try-it steps 2 and 3 are untestable.; The probe's physical slide switch must be moved to Select by hand. The simulated Select is overwritten by the physical switch about every 0.5 s while a simulated tap is held ~1.2 s, so no Select-mode tap ever completes. This blocks both the logo-pad counting section and the whole 'Choosing a GPIO on the OLED' section. Evidence that taps themselves work: with the switch on Measure, tap 10 made the terminal print 'GPIO 1 out  10  LOW'.; Loading a slot (to check that saved directions/pulls/PWM/counter come back) needs a hand or a slot-load verb this rig does not offer.

screenshots: gpio2_counter_and_rest_01_board.png, gpio2_counter_and_rest_01_oled.png, gpio2_counter_and_rest_02_board.png, gpio2_counter_and_rest_02_oled.png, gpio2_counter_and_rest_03_board.png, gpio2_counter_and_rest_03_oled.png

## ex00_running_an_example - Running an example (08.5-examples.md)

steps: {'needs_hand': 1, 'literal': 6, 'substitute': 1, 'could_not_observe': 1}  
summary: Most of the page holds up. All three launch routes exist and are named correctly where they could be reached: the terminal accepts a bare script path, `/` opens a real file manager, `p` gives a MicroPython REPL where `files` opens that same manager and Ctrl+P runs the open file. Try it 3 is exact - adc_basics.py prints four ADC voltages (ADC0-ADC3) at precisely 2 Hz, 20 blocks in a 10 s listen, and a bare Ctrl+Q stops it and hands the terminal back. Try it 1's click-wheel route could not be exercised at all: simulated wheel input does not reach this firmware's menus, so it needs a hand. The real problem is node_connections.py on the page's own terminal route: it prints 'Node Connections Demo' / 'Cleared all connections' and then the terminal goes quiet forever - the 'Final status:' dump and 'Node Connections complete!' that the Ctrl+P and MicroPython-port routes both print never appear. The script is not wedged (with no Ctrl+Q sent, the Python port and then `ver` on the terminal both answered), so it finished and its output was simply lost on that route - but to a beginner it reads as a demo that hung right after wiping their connections. Whether the promised four pairs of rows light up could not be settled: one LED read costs ~4.8 s on this rig against a ~4 s script, so that claim needs eyes on the breadboard. Two smaller wording issues: 'pick one' in the file manager opens the editor rather than running the script (Ctrl+P runs it, and saves it to flash on the way), and Ctrl+Q unwinds one layer at a time, so from the REPL route you land back in the file manager and need a second Ctrl+Q before the terminal listens again.

needs a hand: Try it 1 exactly as written: `click` the wheel, navigate Files > examples > node_connections.py, and `click`. Simulated wheel turns and clicks do not reach this firmware's menus, so the whole click-wheel route - including whether the menu really says `Files` and `examples`, and whether that route prints more to the terminal than the bare-path route does - is untested.; Try it 2: whether four pairs of rows actually light up one after another. One LED read costs ~4.8 s on this rig while the script completes in ~4 s, so it can neither be confirmed nor ruled out; someone has to watch the breadboard while node_connections.py runs. Worth checking at the same time whether all four pairs read as 'pairs of rows' - two of them involve DAC0 and GPIO_1 rather than two breadboard rows.; Whether the OLED text the script writes is what the page intends - it was read only as pixel art ('Is connected' above a large 'DISCONNECTED') and transcribed by eye.

- **[major] unchecked**: page says: Terminal bullet: 'It runs, and the output goes to the terminal' - applied to Try it 1's node_connections.py.  
  observed: Launched the way the page's Terminal bullet says (typing the path `/python_scripts/examples/node_connections.py`), the terminal shows 'Node Connections Demo' and 'Cleared all connections' and then nothing else, ever. The rest of the script's terminal output - the 'Final status:' path/net dump and the closing 'Node Connections complete!' - never arrives on this route, although the identical file prints all of it within seconds when run from the editor with Ctrl+P or exec'd on the MicroPython port. The script is not stuck: with no Ctrl+Q sent, `bench.py py "print('alive-probe')"` and then `bench.py ver` both answered normally, so it had already finished. To a new user it reads as a demo that hangs after clearing their board, with no completion signal and no error. Reproduced on four separate launches, one of them with nothing else touching the board.  
  repro: python3 bench.py term "/python_scripts/examples/node_connections.py" 45 ; python3 bench.py listen 30  -> only the two startup lines. Then, WITHOUT sending Ctrl+Q, python3 bench.py ver -> the terminal answers, proving the script already ended. Compare with: term "/" -> arrow to /python_scripts/examples/node_connections.py -> Enter -> raw "\x10" (Ctrl+P), which prints the net dump and 'Node Connections complete!'.

- **[minor] unchecked**: page says: Terminal bullet: 'type `/` to open the file manager and pick one' - reads as though picking a script runs it.  
  observed: `/` does open the file manager, but Enter on a .py file opens it in the eKilo editor showing its source, footer 'Ctrl-S = save | Ctrl-P = save & REPL | Ctrl-Q = quit'. It runs only after a further Ctrl+P, which also writes the file back to flash ('973 bytes written to flash (low-mem)'). A user following that bullet lands in an editor and has to guess the next keystroke; the page only mentions Ctrl+P in the REPL bullet below it.  
  repro: python3 bench.py term "/" ; arrow to python_scripts ; Enter ; arrow to examples ; Enter ; arrow to any .py ; Enter

- **[minor] unchecked**: page says: 'To stop one that loops forever, press `Ctrl+Q` in the terminal' - written as though one Ctrl+Q returns you to the terminal.  
  observed: True for a script started from the terminal. Coming from the file-manager/REPL route the presses stack one layer at a time: after node_connections.py finished I was left at a '>>>' REPL prompt, one Ctrl+Q dropped me back into the file manager that had launched it, and a second Ctrl+Q was needed to reach the normal terminal. Nothing on the page hints you can be several layers deep, and until the last Ctrl+Q the terminal ignores typed commands (`ver` returned nothing).  
  repro: term "p" ; term "files" ; Enter on a script ; raw "\x10" ; then Ctrl+Q -> file manager reappears ; raw "\x11" -> terminal

screenshots: ex00_running_an_example_0_board.png, ex00_running_an_example_0_oled.png, ex00_running_an_example_1_board.png, ex00_running_an_example_1_oled.png, ex00_running_an_example_3_board.png, ex00_running_an_example_3_oled.png, ex00_running_an_example_4_board.png, ex00_running_an_example_4_oled.png

## ex01_dac_basics_adc_basics_gpio_basics - Example scripts (The basics): dac_basics.py, adc_basics.py, gpio_basics.py (08.5-examples.md)

steps: {'needs_hand': 1, 'literal': 16, 'could_not_observe': 2, 'substitute': 1}  
summary: Firmware 5.7.11.0, slot 3. The three scripts this section is about all do what the page says. dac_basics.py steps DAC_1, TOP_RAIL and BOTTOM_RAIL through 0.0/1.5/3.3 V with dac_get() readbacks, never touches DAC 0, runs once and ends at 0 V. adc_basics.py prints exactly ADC0-ADC3 at exactly twice a second (12 blocks in a 6 s window), connects nothing, and Ctrl+Q stops it. gpio_basics.py clears the board, wires GP_1 to row 3 and GP_2 to row 13 (both rows light full green with the wire trail between them), blinks, cycles pull none/up/down, bridges the rows so Input tracks Output, and mirrors its text onto the OLED. Both terminal launch methods work (`/` opens the file manager, and a direct `/python_scripts/examples/...` path runs a script), and slots stay untouched. The one thing that fails is the first thing the page tells a new user to try: node_connections.py never connects anything. Across five launches it prints 'Node Connections Demo' and 'Cleared all connections' and then goes quiet - no rows light, no nets appear, and it never prints the '--- script finished ---' line the other two scripts do. It does reach a connection test (the OLED ends on 'Is connected: DISCONNECTED'), but the four pairs of rows the page promises never appear, and the read-only channel was free throughout, so the script was not still working in the background. A new user following the Try it box would conclude their board is broken. Two smaller things: in the REPL flow, Ctrl+P loads the script into the REPL rather than running it (an Enter runs it) and `p` opens with a redirect notice to viper-ide.org; and `connect(ADC0, 20)` alone does not cure the ADC noise the page says it cures, because nothing is driving row 20 - add a source and ADC0 reads a steady 3.2 V. I could not check the wheel launch path (no hands), Ctrl+C from the REPL (time box), or the rail LEDs changing during dac_basics (the read-only channel is blocked while a terminal script runs). Board left reset and clean: rails 0 V, only the DAC 0 net, ADCs back at noise.

needs a hand: The click-wheel launch path (`click` > `Files` > `examples` > pick a script > `click`) - the rig cannot turn or click the wheel, so neither the existence of a `Files` menu entry nor the wheel launch of node_connections.py was verified. I substituted the terminal path for the script itself.; Stopping a script with `Ctrl+C` from the REPL, and anything involving JumperIDE (a web app this rig cannot reach).; Watching the rail LEDs change during dac_basics.py - the read-only LED/status channel is blocked while a terminal script runs, so a human needs to look at the board while the script steps the rails.

- **[major] unchecked**: page says: Try it step 2: 'Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them.' (node_connections.py)  
  observed: No rows ever light and no net is ever created. Across five launches, node_connections.py prints 'Node Connections Demo' and 'Cleared all connections' on the terminal and then nothing more - not even the '--- script finished ---' line that dac_basics.py and gpio_basics.py both print. It does get past the clear: the OLED ends on 'Is connected:' / 'DISCONNECTED'. But 20 consecutive `leds` samples read 'ROWS LIT: none' and 8 consecutive net samples showed only the untouched DAC 0 net, and the read-only channel answered normally on every one of those samples (it goes silent while a terminal script is running, proven by a control test), so the script was not still working through connections while I watched. Whatever it does, it is over before a user could see it and it leaves the board exactly as it found it.  
  repro: python3 bench.py reset; python3 bench.py term "/python_scripts/examples/node_connections.py" 50  -> terminal output ends at 'Cleared all connections'. Then python3 bench.py leds and python3 bench.py status: no rows lit, no new nets; python3 bench.py shot x -> OLED reads 'Is connected: DISCONNECTED'. This is the very first thing the page's 'Try it' box tells a new user to do.

- **[minor] unchecked**: page says: 'type `p` to get into the REPL, then `files`, open a script, and press `Ctrl+P` to run it.'  
  observed: Ctrl+P does not run the script - it saves and pastes the file into the REPL as continuation lines and stops at the '...' prompt. You must then press Enter on the empty line to execute it. The editor's own help bar says 'Ctrl-P = save & REPL' and 'CTRL-P = Save and load', never 'run'. The page also skips how you 'open a script' (arrow keys / wheel to the file, then Enter). Separately, `p` greets you with 'There's now a better way to do this! Go to: https://viper-ide.org/' before the REPL banner, which reads like a redirect away from the step you were just told to take; the REPL does still work.  
  repro: python3 bench.py term "p"; python3 bench.py term "files"; arrow down to a script and Enter; python3 bench.py raw "\x10" 20  -> the file is echoed into the REPL as '...' lines and nothing runs until an Enter is sent.

- **[minor] unchecked**: page says: adc_basics.py: 'route a voltage to an ADC first (the ADC pad on the probe, or `connect(ADC0, 20)`) or you'll just see noise.'  
  observed: `connect(ADC0, 20)` on its own does not stop the noise - it wires ADC0 to row 20, and if row 20 is empty ADC0 still reads -0.002 V. It only works once something is actually driving row 20; the page presents the connect() call as the whole fix.  
  repro: python3 bench.py reset; p; connect(ADC0, 20); run /python_scripts/examples/adc_basics.py -> ADC0 still reads ~0 V noise. Put 3.3 V on row 20 (dac_set(2,3.3); connect(TOP_RAIL,20)) and re-run -> ADC0 reads a steady 3.21-3.23 V.

screenshots: ex01_dac_basics_adc_basics_gpio_basics_1_board.png, ex01_dac_basics_adc_basics_gpio_basics_1_oled.png, ex01_dac_basics_adc_basics_gpio_basics_2_board.png, ex01_dac_basics_adc_basics_gpio_basics_2_oled.png, ex01_dac_basics_adc_basics_gpio_basics_3_board.png, ex01_dac_basics_adc_basics_gpio_basics_3_oled.png, ex01_dac_basics_adc_basics_gpio_basics_4_board.png, ex01_dac_basics_adc_basics_gpio_basics_4_oled.png, ex01_dac_basics_adc_basics_gpio_basics_5_board.png, ex01_dac_basics_adc_basics_gpio_basics_5_oled.png, ex01_dac_basics_adc_basics_gpio_basics_6_board.png, ex01_dac_basics_adc_basics_gpio_basics_6_oled.png

## ex02_node_connections_machine_pin_basics_file_io_basics - Example scripts (The basics): node_connections.py, machine_pin_basics.py, file_io_basics.py (08.5-examples.md)

steps: {'substitute': 2, 'literal': 5, 'could_not_observe': 5}  
summary: Half the page holds up and half of it was blocked by one hard failure. What works: launching an example by typing its path on the terminal; node_connections.py runs once, says 'Cleared all connections' at the start, ends with 'Node Connections complete!' and does clear the board first and last (a planted 10-11 net was gone afterwards); print_bridges() output is there, though the screen labels it 'Final status:'; adc_basics.py does print the four ADC voltages 1.9 times a second (20 blocks in 10.62 s). What fails, and it is major: the page's stop instruction does not work. Ctrl+Q in the terminal was delivered about fifteen times through two independent paths, plus Ctrl+C on the terminal and Ctrl+C on the Python port, and adc_basics.py kept printing at ~2 blocks/s with live varying values throughout. That captured the board: the Python port now times out opening the raw REPL, `status` returns empty, and the harness reset fails. The board is not silent, it is flooding - CDC 1 is still streaming 'ADC Readings:' blocks - and it needs a hand (wheel hold or power cycle) to recover. Because of that, machine_pin_basics.py and file_io_basics.py were never run, and the file-manager (`/`) and REPL (`p` > `files` > Ctrl+P) launch routes were never reached. The LED show that the page calls 'the show here' was also unobservable: every read-only sample costs 2.7-4.8 s (a shot ~15 s) against a claimed half-second per pair, and the read-only port stops answering while a script runs - one sample at t=4.8 s came back with 'None' rail voltages. Ten polls and four board captures across five runs showed nothing lit, but the same instrument correctly reported two colored nets for a static connect(10,11)/connect(25,26), so that null is a sampling limit, not a dead sensor. One caveat on the major finding: I relaunched adc_basics once without first confirming the preceding stop had failed, so the first instance's fate is undetermined - every attempt after that is unambiguous. The final harness reset was attempted and failed with: could not open the raw REPL: TimeoutError("raw REPL: no b'raw REPL; CTRL-B to exit\r\n' within 2.0s (got b'')").

needs a hand: Try it 1 and Running-an-example way 1: turning and clicking the wheel to walk `click` > `Files` > `examples` > `node_connections.py`. This rig cannot deliver wheel steps or clicks.; Recovering the board: adc_basics.py is still running and flooding CDC 1 with ADC readings; Ctrl+Q, Ctrl+C and the harness reset all failed. It needs a wheel hold or a power cycle.; Running-an-example way 2's file manager (`/`) and way 3's REPL (`p` > `files` > open a file > Ctrl+P): both need list navigation this rig cannot deliver, and both were unreachable once the board was captured.; machine_pin_basics.py and file_io_basics.py were never run - they need the board freed first.

- **[major] unchecked**: page says: "To stop one that loops forever, press `Ctrl+Q` in the terminal (or `Ctrl+C` from the REPL or JumperIDE)." and Try it 3's "Press `Ctrl+Q` to stop it."  
  observed: Ctrl+Q does not stop adc_basics.py when it was launched by typing its path on the terminal. A bare Ctrl+Q was delivered about fifteen times through two independent paths (`bench.py stop`, which sends a bare Ctrl+Q on CDC 1 and then a bare Ctrl+C on CDC 5, roughly ten times; and `bench.py raw "\x11"` directly, including a five-Ctrl+Q burst). A bare Ctrl+C on the terminal did nothing either. The script kept printing at ~2 blocks/s with live, varying values the whole time. Afterwards the Python port stopped answering ('could not open the raw REPL: TimeoutError("raw REPL: no b\'raw REPL; CTRL-B to exit\\r\\n\' within 2.0s")'), `status` returns empty, and the harness `reset` fails. The board is left running adc_basics.py and flooding the terminal; it needs a hand (wheel hold or power cycle).  
  repro: python3 bench.py term "/python_scripts/examples/adc_basics.py" 6 ; python3 bench.py stop ; python3 bench.py listen 5   -> still prints 'ADC Readings:' blocks. Repeat `stop` or `raw "\x11"` any number of times; it keeps printing.

- **[minor] unchecked**: page says: node_connections.py bullet: "`connect()`, `is_connected()`, `disconnect()` and `print_bridges()` on four pairs of nodes".  
  observed: Nothing about the four pairs reaches the terminal. The whole run prints only: 'Node Connections Demo', 'Cleared all connections', then a long silence, then 'Final status:' + the bridges table + 'Node Connections complete!'. There is no per-pair line, no is_connected() result, and no node names, so a terminal user gets no confirmation the four pairs happened. Also, `print_bridges()` output is labelled 'Final status:' on screen, which does not match the name the page gives it.  
  repro: python3 bench.py seq --secs 20 "/python_scripts/examples/node_connections.py" "<WAIT:12>"

- **[minor] unchecked**: page says: Try it 2 says "four pairs of rows light up one after another", while the bullet says the four pairs are "rows, `DAC0`, `GPIO_1`".  
  observed: The two sentences disagree about what the pairs are: 'four pairs of rows' vs pairs that include DAC0 and GPIO_1 (which are not breadboard rows and light pads/headers, not rows). Which is right could not be checked here, because the LED show was not observable through this rig.  
  repro: Read the page: Try it step 2 against the node_connections.py bullet.

- **[minor] unchecked**: page says: The bullets link to GitHub at `scripts/ex/node_connections.py`, while the page's own terminal instruction uses `/python_scripts/examples/...`.  
  observed: The on-device path that works is `/python_scripts/examples/node_connections.py`. The GitHub links point at `scripts/ex/`, so a new user matching link to device path has to guess the translation. The page never says the two are the same file.  
  repro: Compare the bullet link targets with the Try it / Terminal instruction on the same page.

screenshots: ex02_node_connections_machine_pin_basics_file_io_basics_run1_board.png, ex02_node_connections_machine_pin_basics_file_io_basics_run1_oled.png, ex02_node_connections_machine_pin_basics_file_io_basics_run2_board.png, ex02_node_connections_machine_pin_basics_file_io_basics_run2_oled.png, ex02_node_connections_machine_pin_basics_file_io_basics_run3_board.png, ex02_node_connections_machine_pin_basics_file_io_basics_run3_oled.png, ex02_node_connections_machine_pin_basics_file_io_basics_run4_board.png, ex02_node_connections_machine_pin_basics_file_io_basics_run4_oled.png

## ex03_uart_basics_uart_loopback_voltage_monitor - Example scripts (The basics): uart_basics.py, uart_loopback.py, voltage_monitor.py (08.5-examples.md)

steps: {'needs_hand': 2, 'literal': 17, 'could_not_observe': 2, 'substitute': 3}  
summary: Firmware 5.7.11.0. The plumbing on this page is accurate and the scripts are where it says: all three exist under /python_scripts/examples/, running by path works, `p` then `files` works, Ctrl+C from the REPL works, and adc_basics.py prints exactly four ADC voltages twice a second as promised. uart_basics.py routes exactly as described (UART_Tx+D0, UART_Rx+D1) and is silent after its banner; uart_loopback.py really does put Tx and Rx on one node; voltage_monitor.py really does connect ADC_0 to row 12 and paint the value on both the OLED and the terminal. What fails is the payoff in three places. The page's first Try it sends a new user to node_connections.py to watch four pairs of rows light up one after another - across three runs and ~40 samples no row ever lit and no row net ever appeared, so the breadboard stays dark. uart_loopback.py, sold as a no-parts 'is the UART alive' check, reads back `None` (with the odd 0xff) rather than the message on a freshly reset board - though its loopback node is D0, a Nano-header pin I cannot inspect from here. And voltage_monitor.py disconnects a software source already on row 12 when it starts, so following the page's own advice to put a DAC there yields a steady ~0 V readout. Smaller snags: picking a file in the `/` file manager opens the editor instead of running it (and re-saves the file on the way out, unchanged at 614 B), Ctrl+P needs an extra Enter, Ctrl+Q sometimes needs a second press, and the GitHub links say scripts/ex/ while the board says /python_scripts/examples/. Rig limits worth knowing: the read-only port went silent for the whole time uart_basics.py, uart_loopback.py and voltage_monitor.py were running, so no OLED/LED/board capture exists from during those runs; and the click wheel route, a listener in the Nano header, a hand-wired source on row 12, and JumperIDE could not be tested at all.

needs a hand: The click wheel route (`click` > `Files` > `examples` > pick > `click`) - the rig cannot turn or click the wheel, so the page's first and most prominent way of running an example was never exercised.; uart_basics.py's "sends a message every 1.5 seconds" - nothing is plugged into the Nano header, so there is no listener and no way to see the traffic. Needs a device on D0/D1 or a scope.; voltage_monitor.py with a physically wired source (a pot wiper) on row 12 - I could only try a software DAC connection, which the script disconnects.; The JumperIDE route (ide.jumperless.org, open from the device browser, hit run) - no browser on this rig.

- **[major] HARNESS_ARTIFACT**: page says: Try it, step 2: "Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them."  
  observed: No rows ever light and no row-to-row net ever appears. Across three runs of node_connections.py (two by terminal path, one from the REPL) and ~40 read-only samples, `leds` reported `ROWS LIT: none` every time and `status` never showed anything but the board's default DAC 0 net. The script does run (4.375 s measured) and prints `Node Connections Demo / Cleared all connections / Final status: / Node Connections complete!` - but with nothing listed under `Final status:`. A new user doing the page's very first Try it sees a dark breadboard.  
  repro: bench.py run /python_scripts/examples/node_connections.py 22, polling `bench.py leds` and `bench.py status` throughout; or bench.py py "exec(open('/python_scripts/examples/node_connections.py').read())" while polling.  
  suggested: The current wording is not wrong, only loose, so this is a minor tightening and not the major fix the report asks for: only two of the four pairs are row-to-row (1-30 and 15-45), the other two are DAC 0 to row 20 and GPIO 1 to row 25, and each connection is only up for about half a second, which is worth saying so nobody thinks they missed it. Suggested: "Watch the breadboard: four connections flash in turn, about half a second each - rows 1 and 30, rows 15 and 45, then DAC 0 to row 20 and GPIO 1 to row 25. It's over in about four seconds and the board goes dark again at the end." Nothing else in the excerpt repeats this claim; the three bullets in "The basics" make no LED claims.

- **[major] CONFIRMED**: page says: uart_loopback.py "prints what it reads back every half second. A good 'is the UART alive' check with no parts."  
  observed: It prints on time (8 lines in 4 s) but what it reads back is `None` on nearly every line, with an occasional `b'\xff'` / `b'\xff\xff'`. The message it sent never comes back, on a freshly reset board with Tx and Rx confirmed on the same node ([UART_Tx, D0, UART_Rx]). The check the page recommends reports the UART as not alive. Confound: the loopback node is D0, a Nano-header pin. I cannot see the header from this rig, and a seated Nano would drive D0/D1 and could swamp the loopback. Flagged major anyway because the page sells this as a no-parts check and never mentions that anything in the Nano header will interfere.  
  repro: bench.py reset; bench.py run /python_scripts/examples/uart_loopback.py 9; read the output.  
  suggested: **uart_loopback.py** - Connects `UART Tx` and `Rx` to the same node (D0) and prints whatever `uart.read()` returns every half second. On current firmware the read comes back `None`, so the loop-back does not actually work yet and this is not a "is the UART alive" check. `Ctrl+Q` to stop.

The same wording is not wrong elsewhere in the excerpt: the uart_basics.py bullet makes no readback claim (it correctly says the script prints nothing after the banner), and voltage_monitor.py is unrelated. Only this one bullet carries the aliveness claim.

- **[major] CONFIRMED**: page says: voltage_monitor.py: "Put anything from 0 to 3.3 V on row 12 (a DAC, a pot wiper, a rail)."  
  observed: The script removes a software connection already on row 12 when it starts. With DAC 0 (3.367 V) connected to row 12 beforehand, the net after starting is `[ADC_0, 12]` alone, DAC 0 is back on its default net, and the readout sits at ~0 V on both the OLED and the terminal. Repeated with the net pre-merged as [DAC_0, 12, ADC_0] - same result. Untested branches: placing the source AFTER the script starts (the rig forbids sending commands while a script runs) and a hand-wired pot wiper (needs a hand; it is not a net, so the script could not tear it off). Note this is not a full nodes_clear() - an unrelated UART net survived.  
  repro: bench.py py "connect(DAC0, 12)"; confirm `[DAC_0, 12]` in bench.py status; bench.py run /python_scripts/examples/voltage_monitor.py 8; the terminal shows `Voltage: -0.006V` and status shows `[ADC_0, 12]`.  
  suggested: voltage_monitor.py clears row 12 before it wires ADC 0 there, so a DAC or a rail you connected in software beforehand is dropped and the reading sits at 0 V. Wire your source to row 12 by hand (a pot wiper, a jumper from a rail), or connect it once the script is running - that is what its own "Connect voltage source to row 12" prompt is asking for.

- **[minor] CONFIRMED**: page says: "Terminal: type `/` to open the file manager and pick one" - presented as a way to run an example.  
  observed: Picking a file (enter) opens it in the eKilo editor rather than running it; you have to notice the editor's own help line and press Ctrl+P, which the page only mentions under the REPL bullet. Backing out of the editor with Ctrl+Q also prints `File saved as /python_scripts/examples/voltage_monitor.py` even with no edits made.  
  repro: bench.py term "/", arrow down to python_scripts, enter, down to examples, enter, arrow to any .py, press enter.  
  suggested: **Terminal:** run one directly with its path, like `/python_scripts/examples/gpio_basics.py`. Typing `/` alone opens the file manager; Enter on a script opens it in the editor, and Ctrl+P then Enter runs it from there (Ctrl+Q backs out and prints `File saved as ...` even when you changed nothing - it leaves the file as it was).

- **[minor] CONFIRMED**: page says: "...open a script, and press `Ctrl+P` to run it."  
  observed: Ctrl+P pastes the script into the REPL and stops at a `...` continuation prompt; it does not run until you press Enter as well.  
  repro: p, files, examples, adc_basics.py, enter, then raw \x10 - the code echoes as `... ` lines and nothing executes until raw \r.  
  suggested: Line 9 should say: "**MicroPython REPL:** type `p` to get into the REPL, then `files`, open a script, and press `Ctrl+P` to load it into the REPL, then `Enter` to run it." That matches the firmware's own labels (the editor says `Ctrl-P = save & REPL`, the REPL prints `Loaded: <path>` and its help says `Enter - Execute (on an empty line)`). Line 9 is the only place in the excerpt that mentions `Ctrl+P`, so nothing else needs the same edit; line 8's `/` file manager route lands in the same editor and inherits the same load-then-Enter path, but the page makes no Ctrl+P claim there (the tester filed that separately). Line 11's `Ctrl+C` from the REPL is correct as written - I confirmed it interrupts cleanly.

- **[minor] HARNESS_ARTIFACT**: page says: "To stop one that loops forever, press `Ctrl+Q` in the terminal."  
  observed: Works for adc_basics.py (one trailing print), but uart_loopback.py ignored the first bare Ctrl+Q for ~6 seconds and needed a second one.  
  repro: Run uart_loopback.py, send raw \x11, then listen - `None` keeps printing; send raw \x11 again.  
  suggested: No change. Line 11 is correct as written - with one terminal open, a single Ctrl+Q stops uart_loopback.py and adc_basics.py alike, and the same wording on the uart_basics.py and uart_loopback.py bullets (lines 26 and 27) and in Try it step 3 (line 18) holds on this evidence. Separately and not part of this ruling: line 28 tells you to stop voltage_monitor.py with "Ctrl+C from the REPL" while every other script on the page says Ctrl+Q - an inconsistency worth its own look, since voltage_monitor.py is run the same way as the others. Aside for the firmware, not the docs: the terminal stops accepting Ctrl+Q if the host closes and reopens the CDC port while a script is running, which a reconnecting serial monitor or IDE could hit.

- **[minor] NOT_REPRODUCED**: page says: The three bullets link to `github.com/.../blob/main/scripts/ex/uart_basics.py` etc.  
  observed: On the board the same scripts live at `/python_scripts/examples/` (confirmed in the file manager: uart_basics.py 400 B, uart_loopback.py 538 B, voltage_monitor.py 614 B). A reader who copies the linked path into the terminal will not find them; the page's own run instruction uses the right prefix, but the two spellings sit a few lines apart.  
  repro: Open the file manager (`/`) and compare the on-board tree with the GitHub paths in the bullets.  
  suggested: No change required - the page is correct as written. If the maintainer wants belt and braces, one optional clause under "The basics" would do it: "these live on the board in `/python_scripts/examples/`; the links go to the source on GitHub" - but line 8 already gives the on-board path, so it is redundant.

screenshots: ex03_uart_basics_uart_loopback_voltage_monitor_1_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_1_oled.png, ex03_uart_basics_uart_loopback_voltage_monitor_2_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_2_oled.png, ex03_uart_basics_uart_loopback_voltage_monitor_3_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_3_oled.png, ex03_uart_basics_uart_loopback_voltage_monitor_4_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_4_oled.png, ex03_uart_basics_uart_loopback_voltage_monitor_5_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_5_oled.png, ex03_uart_basics_uart_loopback_voltage_monitor_6_board.png, ex03_uart_basics_uart_loopback_voltage_monitor_6_oled.png

## ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game - Example scripts (Interrupts): pin_irq_basics.py, pin_irq_freq_counter.py, pin_irq_reaction_game.py (08.5-examples.md)

steps: {'substitute': 3, 'literal': 9, 'could_not_observe': 4, 'needs_hand': 3}  
summary: The Interrupts section itself is mostly accurate. pin_irq_basics.py is exactly what the page describes: it routes GPIO_1 -> GPIO_2, pulses five times, prints running rising/falling counts and a 'PASS - caught 5 rising and 5 falling edges' line, runs once, needs no parts, and tears its route back down. pin_irq_freq_counter.py runs, PWMs GPIO 1 into GPIO 2 and reports 'set: 1000 Hz  measured: 996-1000 Hz (249 edges in 250 ms)' about four times a second - the hard-interrupt edge counting works; its 'row 20' routing could not be checked because the read-only status port goes completely silent for the whole run, and the two wheel interactions (turn to change frequency, click to quit) need a hand, though Ctrl+Q quits it cleanly. pin_irq_reaction_game.py starts correctly and self-checks its own wiring ('wiring check: GPIO_2-28: CONNECTED  GND-30: CONNECTED  idle reads: 1 (want 1)'), and the round sequence reaches five, but the tact switch and taps need a hand so the green block, the microsecond timestamps and the best/average were never seen - with no taps each round times out at 3 s as a miss and the script exits with no summary, neither of which the page mentions. The two real failures are in the shared preamble the section depends on: the page's third way to run an example (REPL > files > open > Ctrl+P) re-enumerates the board's USB instead of running the script - reproduced three times, with an Enter-only control run proving Ctrl+P is the trigger; and the page's own Try it 2 (node_connections.py, 'four pairs of rows light up one after another') shows nothing - the script prints two header lines and then stalls, never lighting a row and never finishing, with the LED reader independently verified working. Everything else - '/' opening the file manager, running a script by full path, adc_basics.py printing four ADC voltages exactly twice a second, and Ctrl+Q stopping a looping script - matched the page word for word.

needs a hand: The click-wheel route in 'Running an example' and Try it step 1 (click > Files > examples > node_connections.py > click) - no wheel turn or click can be delivered on this rig; I substituted the terminal file manager and a run-by-path.; pin_irq_freq_counter.py: turning the wheel to change the frequency (and watching the measurement follow) - no substitute exists; the frequency stayed at its 1000 Hz default the whole time.; pin_irq_freq_counter.py: clicking the wheel to quit - I substituted Ctrl+Q, which does quit it cleanly.; pin_irq_reaction_game.py: inserting a tact switch across rows 28 and 30 (or the jumper-into-row-28 trick), and the five physical taps. No substitute: `tap` uses the Python port, which must not be touched while a script runs, and a software wire between 28 and 30 is a stuck-closed switch, not a tap.; pin_irq_reaction_game.py: seeing the green block on the LEDs and the microsecond tap timestamps / best / average - all downstream of a real tap.; The JumperIDE (ide.jumperless.org) route - no browser available through this tool.

- **[major] NEEDS_HAND**: page says: MicroPython REPL: type `p` to get into the REPL, then `files`, open a script, and press `Ctrl+P` to run it.  
  observed: Ctrl+P on an opened script does not run it - it re-enumerates the board's USB. All three attempts raised 'OSError: [Errno 6] Device not configured', and with a once-per-second port watcher running, all four /dev/cu.usbmodemJLV5* ports went 4 -> 0 -> 4 within about two seconds of the keypress. A control run that opened the same script with Enter and then waited 20 s WITHOUT pressing Ctrl+P saw no drop at all (port count stayed 4), so the fault is Ctrl+P's, not the open step's. The board always came back (firmware answered `?` with 5.7.11.0) but pin_irq_basics.py never printed a line, so the third of the page's three 'ways to run an example' does not work as written. Secondary: Enter on a .py in that file manager prints nothing whatsoever on the terminal, so the reader has no sign a script was 'opened'.  
  repro: On the terminal: type `p`, then `files`. Arrow down to `examples`, Enter; arrow down to `pin_irq_basics.py` (item 15 of 25), Enter to open it; press Ctrl+P. The USB device re-enumerates and the script never runs. Reproduced three times.  
  suggested: Only one wording gap is actually proven, and it is the one all three testers hit: the page skips the editor step. Change the REPL bullet to "type `p` to get into the REPL, then `files`, Enter on a script opens it in the editor, and `Ctrl+P` there saves it back to flash and runs it." Do not add a Ctrl+P caution until a hand settles the reboot. The same defect appears elsewhere in the excerpt: the Terminal bullet's "type `/` to open the file manager and pick one" - picking a .py does not run it either, it opens the same editor, and Ctrl+P is still needed (flagged independently in the ex00 and ex09 runs).

- **[major] NEEDS_HAND**: page says: Try it 2: Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them.  
  observed: node_connections.py prints 'Node Connections Demo' and 'Cleared all connections' and then does nothing more - no connect/disconnect lines, no rows lit ('ROWS LIT: none' on every poll), and it never reaches '--- script finished ---' even after 25 s across two runs. It has to be killed with Ctrl+Q. The LED reader was verified good immediately afterwards (connect(1, 2) lit rows 1 and 2 pink), so the rows genuinely never lit. The page's own Try it therefore shows the reader nothing.  
  repro: Terminal: `/python_scripts/examples/node_connections.py`, then watch for 15+ s. Only the two header lines ever appear.  
  suggested: Conditional on the hand-test: if rows never light on the wheel route, step 2 is wrong and should be replaced with what the board actually does. If rows do light but the terminal stays quiet, add one sentence - "The script prints nothing while it runs, so watch the board, not the terminal" - and do not state a duration nobody has measured.

- **[minor] NOT_REPRODUCED**: page says: The three interrupt scripts are linked as scripts/ex/pin_irq_basics.py etc.  
  observed: That is the GitHub repo path; on the device they are at /python_scripts/examples/<name>.py. The page shows the runnable path pattern only once, for gpio_basics.py in the preamble - the Interrupts bullets give no path, so a reader has to infer it.  
  repro: Type `/` and look: there is no /scripts directory at the filesystem root.  
  suggested: No change required - the page does not claim a device path of scripts/ex, and the path it does give works. (Optional convenience, not a fix: the Interrupts intro could open with "All three live under `/python_scripts/examples/`.") The same GitHub-link convention is used identically on all three bullets in the excerpt and is consistently a source link rather than a path, so there is nothing else to flag here.

- **[minor] NEEDS_HAND**: page says: pin_irq_reaction_game.py - Five rounds, then a best / average.  
  observed: Five rounds ran, but with no taps each round printed '  no tap within 3 seconds - round counts as a miss' and the script exited straight after round 5 with no best/average line at all. The page doesn't mention the 3-second per-round timeout or that the summary needs at least one valid tap, so a reader who hasn't got a switch in yet sees the game silently score itself out.  
  repro: Run /python_scripts/examples/pin_irq_reaction_game.py with rows 28 and 30 empty and watch: five rounds, five misses, then '--- script finished ---'.  
  suggested: No change needed - the bullet already tells the reader a tact switch across rows 28 and 30 is required, and the script says "no tap within 3 seconds - round counts as a miss" out loud each round. If a hedge is wanted anyway, one clause at the end of that bullet: "With nothing in rows 28 and 30, each round times out after 3 seconds as a miss and no summary is printed." Nothing else in the excerpt shares this wording; the two script bullets above it (pin_irq_basics.py, pin_irq_freq_counter.py) make no conditional-summary claim.

- **[minor] HARNESS_ARTIFACT**: page says: pin_irq_freq_counter.py - routed ... to `row 20` so you can scope it (implying you can inspect it while it runs).  
  observed: Unverified here, and the board is hard to inspect while these two IRQ scripts run: the read-only status port stopped answering entirely for the whole duration of pin_irq_freq_counter.py and pin_irq_reaction_game.py (no LED snapshot, no OLED, no status, no screenshot), recovering the moment each script stopped. Not a wording bug, but it means neither the row-20 route nor the reaction game's green block can be confirmed from software.  
  repro: Run /python_scripts/examples/pin_irq_freq_counter.py, then ask for `leds` / `status` / `oled` on the read-only port - all empty until the script is stopped.  
  suggested: No change. The page says "routed ... to `row 20` so you can scope it", which is a physical instruction (scope probe or the row-20 LED) and is not contradicted by anything in the report; the silent read-only port is the bench harness's own backchannel, which a reader does not have and the page never promises. The same "can't see it through the tool" limitation is behind the reaction game's "green block on the LEDs" bullet, and it is not a wording problem there either.

screenshots: ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_1_board.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_1_oled.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_2_board.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_2_oled.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_4_board.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_4_oled.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_5_board.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_5_oled.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_6_board.png, ex04_pin_irq_basics_pin_irq_freq_counter_pin_irq_reaction_game_6_oled.png

## ex05_interaction_demo_led_brightness_control_stylophone - Example scripts (Interactive): interaction_demo.py, led_brightness_control.py, stylophone.py (08.5-examples.md)

steps: {'needs_hand': 8, 'literal': 15, 'substitute': 1, 'could_not_observe': 3}  
summary: The launch machinery on this page is solid and the Try it's terminal step is exact, but one of the three Interactive scripts is dead on arrival and the Try it's headline demo does nothing. Verified literally: `/` opens the file manager; running a script by its path works; `p` then `files` gives a browser rooted at /python_scripts; a Ctrl+Q-then-Ctrl+C stopped every looping script and left the board responsive; and adc_basics.py prints four ADC voltages at exactly twice a second (8 blocks in 4 s). Two majors. led_brightness_control.py crashes about a second in with "NameError: name 'voltage' isn't defined" at line 39 (reproduced twice) - it prints its hardware-setup lines and connects DAC_0 to row 15, then dies, so the pad-to-voltage behaviour, the dimming and the OLED voltage/current readout the bullet promises never happen. And node_connections.py, the script the Try it tells a brand-new user to run first, prints 'Cleared all connections' and then stalls: no rows lit on six `leds` samples over ~25 s of a clean run, no nets created, no further output - the promised 'four pairs of rows light up one after another' does not occur. stylophone.py is healthy: it prints its wiring line, shows 'Touch a pad!' on the OLED, and really does connect GP_1 to row 25 and GND to row 55 exactly as the bullet says; the tone itself is not observable through this rig. interaction_demo.py runs and lays down a bridge on rows 1-5 (pink) with no parts as claimed, but prints nothing at all to the terminal, and simulated tip-position taps on pads 30 and 45 did not move that bridge - flagged minor rather than major because the tap is injected on the python port while the script owns the VM and the read-only port was intermittently dead during the run, so it wants a sanity-check against the script. The wheel slide, the probe-button colour shift, the Measure-mode walk, the probe-button sustain and every physical part all need a hand. Minor doc issues: stylophone's stop key is given as Ctrl+C-from-the-REPL while the page's own recommended launch route is the terminal, and the three bullets link to GitHub `scripts/ex/` paths without ever naming the on-device `/python_scripts/examples/` path.

needs a hand: Click wheel route: `click` > `Files` > `examples` > pick a script > `click` - wheel turns and clicks cannot be delivered on this rig, so the page's first launch route was never exercised.; REPL route's last two steps: selecting a script in the `files` browser (wheel/arrow navigation) and pressing Ctrl+P to run it.; interaction_demo.py: turning the wheel to slide the bridge, and pressing the probe buttons to shift its color.; interaction_demo.py: physically flipping the probe slide switch to Measure - the simulated switch is overwritten by the firmware's re-read of the real one about twice a second, so 'walks up the board on its own' is unproven either way.; led_brightness_control.py: plugging an LED and resistor from row 15 to GND (moot until the NameError is fixed).; stylophone.py: plugging a speaker or piezo between row 25 and row 55, hearing the tone (audio is not observable through this rig at all), and the probe buttons for sustain.

- **[major] NEEDS_HAND**: page says: led_brightness_control.py - 'Touch a pad and DAC 0 goes to that pad number's share of 5 V (pad 30 is 2.5 V), dimming an LED on row 15. The OLED shows the voltage and the current it draws.'  
  observed: The script crashes about a second after it starts, before any of that: 'File "<stdin>", line 39, in <module>' / "NameError: name 'voltage' isn't defined", then '--- script finished ---'. Reproduced twice. It gets as far as printing its hardware-setup lines and connecting DAC_0 to row 15; the pad-to-voltage loop, the dimming and the OLED voltage/current readout never happen, and the OLED is left showing only a title banner.  
  repro: From the terminal, run /python_scripts/examples/led_brightness_control.py  
  suggested: If a hand-run confirms the crash, the bullet should say plainly what happens: "led_brightness_control.py currently exits with an error about a second after it starts, right after connecting DAC 0 to row 15 - it does not react to pads and the OLED readout never appears; the script needs fixing before this description is true." Nowhere else in the excerpt describes this script, so no other wording carries the same error (the other two Interactive bullets describe different scripts and were not in question here).

- **[major] NEEDS_HAND**: page says: Try it step 2 - 'Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them.' (node_connections.py)  
  observed: Nothing lights. On a clean first run the script printed 'Node Connections Demo' and 'Cleared all connections' and then produced no further output; six `leds` samples over about 25 s all reported 'ROWS LIT: none' on a port that was answering normally, and `status` showed only the default DAC 0 net. No pair of rows was ever connected or lit. Two further runs (one held open 20 s) behaved the same, though I did not stop between them.  
  repro: From the terminal, run /python_scripts/examples/node_connections.py and watch the board / `leds` for 20 s  
  suggested: Conditional, since the claim is not confirmed: if a human recheck reproduces the stall, Try-it steps 1-2 should stop promising that four pairs of rows light up one after another - either drop node_connections.py from the Try it, or say what actually happens after "Cleared all connections", including any button, tap or click the script waits for. The same wording does not appear elsewhere in the excerpt: node_connections.py is named only in Try-it step 2.

- **[minor] HARNESS_ARTIFACT**: page says: interaction_demo.py - 'tap a pad to move a bridge there'.  
  observed: A simulated tip-position tap on pad 30 and again on pad 45 moved nothing: the bridge stayed at rows 1-5 across both runs and no net appeared at 30 or 45. Stated as minor because two things could mask a working script - the tap is injected on the python port while the script owns the MicroPython VM, and the read-only port was intermittently dead during the run. Worth a sanity-check against interaction_demo.py: the rig's tap sets tip position, which is all this action should need; if the script reads the probe by another path this is a rig artifact, otherwise the tap-to-move flow named in the bullet is broken.  
  repro: Run /python_scripts/examples/interaction_demo.py, tap a pad well away from rows 1-5, and watch which rows are lit  
  suggested: No page change warranted from this item. The bullet's claim ("tap a pad to move a bridge there") is untested, not contradicted - verifying it needs a real probe tap (a hand), or a rig that injects tip position outside the MicroPython VM. The same wording is not wrong elsewhere in the excerpt; the stylophone tap and the Measure-switch observations are the same rig artifact, not doc errors.

- **[minor] CONFIRMED**: page says: stylophone.py - '`Ctrl+C` from the REPL to stop.'  
  observed: This is the only one of the three bullets naming a REPL-only stop key; the other two say Ctrl+Q and the preamble says Ctrl+Q in the terminal stops any looping script. A user who launched stylophone.py the way the same page describes two lines earlier - terminal, by path - is told a key for a surface they are not on.  
  repro: Read the three Interactive bullets side by side, then run stylophone.py from the terminal and look for the stop key the page gives.  
  suggested: The stylophone.py bullet should end "`Ctrl+Q` to stop." - the same as the other two Interactive bullets - since the page's own launch instructions send the reader to the terminal, not the REPL. The same wording is not wrong anywhere else in the excerpt: lines 11, 18, 24 and 25 all say Ctrl+Q for the terminal, and line 26 is the only one that differs.

- **[minor] NEEDS_HAND**: page says: interaction_demo.py - 'Reads every control at once from one loop.'  
  observed: The script prints absolutely nothing to the terminal while it runs - no banner, no state, no acknowledgement - so a user has no sign it is alive or what it is reading. It silently creates a bridge between rows 1 and 5 (rows 1-5 lit pink), which the page never mentions, so it is not obvious that the pink thing on the board is the bridge you are meant to move.  
  repro: Run /python_scripts/examples/interaction_demo.py from the terminal and watch the terminal and the board.  
  suggested: Only if a hand confirms the terminal and OLED are both silent, add to the interaction_demo.py bullet: "It starts with a bridge on rows 1 to 5 (lit pink) and prints nothing to the terminal - everything happens on the board." If the OLED does show state, the bullet needs only the first half: "It starts with a bridge on rows 1 to 5, lit pink." The same wording is not wrong elsewhere in the excerpt: neither of the other two bullets claims terminal output either, and per the tester's ledger both of those scripts do print a banner.

- **[minor] CONFIRMED**: page says: The three Interactive bullets link to `scripts/ex/interaction_demo.py` etc.  
  observed: That is the GitHub repo path, not the on-device path. On the board these live at /python_scripts/examples/interaction_demo.py (and led_brightness_control.py, stylophone.py). The page shows the on-device convention only once, in the preamble's gpio_basics.py example, and never for these three, so a reader following the links has to infer the translation.  
  repro: Compare the bullet links with os.listdir('/python_scripts/examples') on the board.  
  suggested: Add one sentence above the Interactive list (or at the end of "Running an example"): "These links open each script's source on GitHub; on the board they are at `/python_scripts/examples/<name>.py`." No existing sentence needs changing. Within this excerpt the same gap covers all three Interactive bullets - they are the only GitHub links here - so the one sentence fixes all of them; I cannot vouch for bullets outside the excerpt, which likely follow the same pattern and would be covered by the same line if the list continues below.

screenshots: ex05_interaction_demo_led_brightness_control_stylophone_1_board.png, ex05_interaction_demo_led_brightness_control_stylophone_1_oled.png, ex05_interaction_demo_led_brightness_control_stylophone_2_board.png, ex05_interaction_demo_led_brightness_control_stylophone_2_oled.png, ex05_interaction_demo_led_brightness_control_stylophone_3_board.png, ex05_interaction_demo_led_brightness_control_stylophone_3_oled.png, ex05_interaction_demo_led_brightness_control_stylophone_4_board.png, ex05_interaction_demo_led_brightness_control_stylophone_4_oled.png, ex05_interaction_demo_led_brightness_control_stylophone_5_board.png, ex05_interaction_demo_led_brightness_control_stylophone_5_oled.png

## ex06_oled_layout_editor - Example scripts (Interactive): oled_layout_editor.py (08.5-examples.md)

steps: {'needs_hand': 2, 'literal': 10, 'could_not_observe': 7, 'substitute': 1}  
summary: The mechanical parts of the page hold up: `/` opens the file manager, `p` then `files` gets you the REPL file browser, running a script by its full path works verbatim, adc_basics.py prints exactly four ADC voltages twice a second, and oled_layout_editor.py exists at /python_scripts/examples/, starts, owns the OLED and quits on a literal Ctrl+C in the terminal (verified: bench.py writes the exact 0x03 byte to port 1). Two things do not. First, the 'Try it' step 2 claim is simply false on this board: node_connections.py goes dead after printing 'Cleared all connections' - no rows ever light, no nets are ever created, and the terminal goes silent for 30 s, so a new user gets a dark breadboard where the page promises four pairs of rows blinking in sequence (major). Second, the Interactive bullet mis-assigns a control: it says the wheel changes 'font and size', but the editor's own legend says 'wheel=size ... CONNECT=font', i.e. font is on the probe's CONNECT button (minor). Ctrl+Q was also flaky - two sends were ignored before it took (minor). Most of the editor's actual interaction (pad taps, rail nudges, typing the text, and the REMOVE-button save that creates /screens/layout.json) could not be exercised: probe taps and terminal typing are both forbidden while a script runs on this rig, and the wheel and probe buttons need a hand. Worth noting for a re-test: while the editor runs, the board's read-only status/screenshot channel stops answering entirely, so its OLED output could not be observed at all.

needs a hand: Click-wheel navigation for way 1 (`click` > Files > examples > pick a script > `click`) - no wheel turns or clicks can be delivered on this rig.; Turning the wheel inside oled_layout_editor.py to change size (and, per the page, font).; Pressing the probe button ('press=next box') to move between the top and bottom text boxes.; The probe's CONNECT button ('CONNECT=font') - the control that actually changes the font.; The probe's REMOVE button ('REMOVE=save') - the only way to write /screens/layout.json.; Probe taps on rows 1-30 / 31-60 and on the rail pads while the editor is running (bench.py tap goes through the MicroPython port the script occupies, and the harness forbids it mid-script).; Typing the layout text on the terminal while the editor is running (terminal input mid-script is forbidden by the harness).; Opening a script in the REPL file manager and pressing Ctrl+P (needs arrow/wheel navigation), and anything via JumperIDE.

- **[major] NEEDS_HAND**: page says: Try it 2: 'Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them.'  
  observed: Nothing ever lit. Across two runs of /python_scripts/examples/node_connections.py (14 s and 30 s) and 18 `leds` polls, every poll read 'ROWS LIT: none', and `status` never showed any net beyond the pre-existing 'DAC 0' net. The script printed 'Node Connections Demo' and 'Cleared all connections' and then produced no further output for 30 s. Stopping it produced no '^Q' echo and no KeyboardInterrupt traceback, unlike every other script I stopped, so whether it hangs after the clear or exits silently cannot be told from the terminal. Either way, a new user following this step sees a dark board and a silent terminal, not four pairs of rows lighting in sequence.  
  repro: bench.py run /python_scripts/examples/node_connections.py 30, and poll bench.py leds / bench.py status throughout.  
  suggested: Conditional on the human confirming it on a reset board: Try it 2 should describe what the script actually does - if node_connections.py only clears connections and exits, replace the sentence with something like "It clears every connection and prints what it did; it stops on its own with no further message." If it is meant to light rows, the step should name a script that does. Either way the step should not promise four pairs of rows lighting in sequence unless that is what a user sees. The claim appears only once in the excerpt; no other sentence in it repeats the promise.

- **[minor] NEEDS_HAND**: page says: 'the wheel to change font and size'  
  observed: The script's own legend, printed on the terminal when it starts, reads 'wheel=size  press=next box  CONNECT=font  REMOVE=save'. The wheel only changes size; font is on the probe's CONNECT button. A user turning the wheel expecting a font change will not get one.  
  repro: bench.py run /python_scripts/examples/oled_layout_editor.py 6 and read the legend it prints.  
  suggested: Conditional on a hand confirming the legend: replace "the wheel to change font and size" with "the wheel to change the text size and the probe's CONNECT button to change the font" - and do not assert what a wheel press does until someone checks whether "press=next box" is the wheel click or a probe button. The wheel/font wording does not recur anywhere else in the excerpt; the only other wheel mention is menu navigation under "Running an example" (line 7), which is unrelated and fine.

- **[minor] NEEDS_HAND**: page says: 'Saves to /screens/layout.json so you can read the numbers off for your own scripts.'  
  observed: The page does not say how to save. The save is bound to the probe's REMOVE button ('REMOVE=save' in the script's legend). /screens did not exist before or after two runs of the editor, so nothing is written unless the user knows to press REMOVE.  
  repro: bench.py py "import os; print(os.listdir('/screens'))" before and after running the editor - ENOENT both times.  
  suggested: Only if a hand confirms the save actually writes the file: "...the wheel to change size, the probe's CONNECT button to change font, and type the text in the terminal. The probe's REMOVE button saves the layout to `/screens/layout.json` so you can read the numbers off for your own scripts." The same sentence carries a second, already-filed defect — "the wheel to change font and size" contradicts the legend's "wheel=size ... CONNECT=font"; the wording above fixes both at once, so the two should be applied together rather than separately.

- **[minor] NEEDS_HAND**: page says: 'To stop one that loops forever, press Ctrl+Q in the terminal' / Try it 3 'Press Ctrl+Q to stop it.'  
  observed: Intermittent. Two consecutive bare Ctrl+Q (0x11) bytes sent on the terminal while adc_basics.py was looping had no effect at all; the script kept printing and only stopped once a Ctrl+C was also sent on the Python port. On a later clean run one Ctrl+Q did stop it (echo '^Q' + KeyboardInterrupt). So the documented key does work, but not reliably on the first press.  
  repro: bench.py run /python_scripts/examples/adc_basics.py 6, then bench.py raw '\x11' 5 - repeat; the first attempts were ignored.  
  suggested: No change to the Ctrl+Q sentence is warranted by this report - on a healthy board, in four independent same-firmware sessions, one Ctrl+Q stops adc_basics.py first press, and the failures line up with a board-wide wedge in which no key on any port works. The same wording appears twice in the excerpt (line 11, "To stop one that loops forever, press Ctrl+Q in the terminal", and Try it 3, "Press Ctrl+Q to stop it"); they stand or fall together, so leave both alone. Line 24's "Ctrl+C to stop" for oled_layout_editor.py is a separate claim and is not covered by this ruling.

- **[minor] NEEDS_HAND**: page says: BOARD OBSERVATION, not a page error: the page gives no hint that oled_layout_editor.py takes the board over completely (the section above only says output goes 'to the terminal (and the OLED where the script uses it)').  
  observed: While oled_layout_editor.py runs, the board's read-only status/screenshot channel stops responding entirely - `oled`, `leds`, `shot` and `status` all return nothing until the script is stopped. Anything else that reads board state (an app, a status page) will appear frozen for as long as the editor is open. The page gives no hint that the editor takes the board over this completely.  
  repro: bench.py run /python_scripts/examples/oled_layout_editor.py 6, then bench.py oled / bench.py status - both empty; they return as soon as the script is stopped.  
  suggested: No page change is warranted on this evidence. If a later test on a healthy board shows the read-only channel dies under any running script, the note belongs in "Running an example" as a general remark about running scripts, not on the oled_layout_editor.py bullet - and it should not be written as if the editor were special. Nothing in the rest of the excerpt has this wording problem: the Interactive bullet already lists the surfaces the editor uses, and the "Running an example" line about output going "to the terminal (and the OLED where the script uses it)" is about where output appears, not a promise that other channels keep answering.

- **[minor] NEEDS_HAND**: page says: 'Ctrl+C to stop.'  
  observed: It stops, but the terminal is left with a raw MicroPython traceback ('Traceback (most recent call last): File "<stdin>", line 226 ... File "/python_scripts/lib/oledgui.py", line 284, in free / KeyboardInterrupt:'), which reads like a crash to a new user rather than a clean quit.  
  repro: bench.py run /python_scripts/examples/oled_layout_editor.py 6, then bench.py raw '\x03' 4.  
  suggested: Not settled as a doc bug, so no wording change is justified on the traceback itself. Separately, and independent of hardware, line 24 contradicts lines 11 and 18 on which key stops a script from the terminal; align it: "Ctrl+Q in the terminal to stop (or Ctrl+C from the REPL or JumperIDE)." The same wording is not wrong elsewhere in the excerpt - the inconsistency runs the other way, line 24 is the outlier.

screenshots: ex06_oled_layout_editor_1_board.png, ex06_oled_layout_editor_1_oled.png, ex06_oled_layout_editor_3_board.png, ex06_oled_layout_editor_3_oled.png, ex06_oled_layout_editor_4_board.png, ex06_oled_layout_editor_4_oled.png

## ex07_oled_stats_page_test_oled_features_oledgui - Example scripts (OLED): oled_stats_page.py, test_oled_features.py, oledgui.py (08.5-examples.md)

steps: {'needs_hand': 1, 'literal': 15, 'substitute': 3, 'could_not_observe': 4}  
summary: The OLED section holds up well. oled_stats_page.py does exactly what it promises: a JUMPERLESS-headed screen showing the four ADC voltages and 'uptime MM:SS', it stays up as the idle screen once the script is gone, its numbers keep updating on their own (uptime 50:04 -> 50:40, ADC0 2.54V -> -0.00V), it survived a file-manager session and was back afterwards, and `import oledgui; oledgui.hide()` blanks it instantly. oledgui.py is exactly where the page says, imports cleanly, and exposes Screen, Text, Line and Rect. test_oled_features.py runs all six named sections and takes about the promised 30 seconds. The one real failure is its ending: the terminal never prints `ALL TESTS PASSED!` - output stops at '=== Testing Small Text Scrolling ===' around t+24 and never resumes, verified with a sub-second-gap 25 s listen straddling the actual finish, plus 45 s and 60 s listens on two other runs. The only pass message is on the OLED and reads 'All tests passed!'. Smaller frictions in the shared 'Running an example' intro: `/` needs an Enter before the file manager opens, Ctrl+P in the editor loads the script into the REPL rather than running it (one more Enter needed), and Ctrl+Q/Ctrl+C stop scripts with a raw KeyboardInterrupt traceback the page never mentions. Also: the OLED bullets give only GitHub scripts/ex/ paths while the device path is /python_scripts/examples/, the bitmap test warns that /jogo32h.bin is missing, and the stats page wraps the fourth voltage's 'V' onto the uptime line when values are negative. Not testable without hands: everything on the click wheel (including Try it step 2's four pairs of rows - each LED read takes ~5 s and the script finishes inside that), whether the stats page steps aside for a wheel menu, and whether the idle screen is normally the logo.

needs a hand: The click-wheel route in 'Running an example' and Try it step 1 (click > Files > examples > pick a script > click) - this rig cannot turn or click the wheel.; Try it step 2: whether four pairs of rows light up one after another during node_connections.py. Each `leds` read takes ~5 s and the script's whole sequence finishes inside that, so every sample I got was after the fact ('ROWS LIT: none'). Eyes on the breadboard for one run would settle it.; Whether oled_stats_page.py's idle page steps ASIDE for a wheel menu (the read-only port goes silent while a menu or file manager owns the UI, so the OLED cannot be read mid-menu). It does come back afterwards.; Whether the OLED idle screen is normally the Jumperless logo, i.e. what the stats page displaces - this board idled blank both before the stats page and after oledgui.hide(), possibly because of the harness reset.; The JumperIDE route (open the file from the device browser and hit run) - the web IDE is not reachable from this rig.

- **[major] NEEDS_HAND**: page says: test_oled_features.py: 'About 30 seconds, then `ALL TESTS PASSED!`'  
  observed: The terminal never prints ALL TESTS PASSED! Its output stops dead at '=== Testing Small Text Scrolling ===' about 24 s in and never resumes - a `listen 25` started with a sub-second gap after the run, straddling the moment the script actually finishes (t+29), returned zero bytes; two earlier runs listened a further 45 s and 60 s with the same result. Completion is only visible on the OLED, spelled 'All tests passed!' (mixed case), so a user running this from the terminal as the page suggests never sees a pass message at all.  
  repro: In one shell line: `bench.py run /python_scripts/examples/test_oled_features.py 45; bench.py listen 25; bench.py oled`. The listen is empty; the OLED reads 'All tests passed!'.  
  suggested: No change to the page on this evidence - the tester's captures never covered the moment the script ends, so their "never prints" observation is unsupported. If a clean run later shows the terminal really does stay silent, the bullet should read: "About 30 seconds. The terminal goes quiet after the scrolling section; when it finishes the OLED shows `All tests passed!`." The caps string appears only once in the excerpt, so no other wording here is affected either way.

- **[minor] NEEDS_HAND**: page says: 'press `Ctrl+P` to run it' (the REPL route)  
  observed: Ctrl+P in the eKilo editor pastes the script into the REPL and stops there; it only runs after an extra Enter. The editor's own help calls Ctrl-P 'save & REPL' / 'Save and load', never 'run'.  
  repro: p -> files -> down-arrow, Enter into examples -> down-arrow, Enter on adc_basics.py -> raw \x10 (script appears as `...` lines, nothing runs) -> raw \r (now it runs).  
  suggested: Contingent on the rerun above (safe either way, since an extra Enter after an already-running script is harmless): "**MicroPython REPL:** type `p` to get into the REPL, then `files`, open a script, and press `Ctrl+P` to load it into the REPL, then `Enter` to run it."

- **[minor] NEEDS_HAND**: page says: 'type `/` to open the file manager'  
  observed: Typing `/` alone only echoes it; the file manager opens on the following Enter.  
  repro: bench.py raw "/" 3 (nothing happens), then bench.py raw "\r" 4 (file manager appears).  
  suggested: Only if a rerun shows `p` firing on a bare keystroke while `/` does not: change the bullet to "Terminal: type `/` and press Enter to open the file manager and pick one, or run one directly with its path, like `/python_scripts/examples/gpio_basics.py`." If `p` also needs Enter, leave the page alone — the whole terminal is line-based and "type" already implies Enter throughout. The neighbouring "type `p` to get into the REPL" bullet uses the identical construction, so whichever way the `p` test goes, the two bullets should be worded the same way as each other.

- **[minor] NEEDS_HAND**: page says: 'To stop one that loops forever, press `Ctrl+Q` in the terminal (or `Ctrl+C` from the REPL or JumperIDE).'  
  observed: Both work, but each prints a raw Python traceback ('Traceback (most recent call last): File "<stdin>", line 22, in <module> KeyboardInterrupt:'). The page gives no hint that the documented way to stop a script looks like a crash.  
  repro: Run adc_basics.py, then send Ctrl+Q on the terminal, or Ctrl+C from the REPL.  
  suggested: If confirmed, extend the existing sentence: "To stop one that loops forever, press `Ctrl+Q` in the terminal (or `Ctrl+C` from the REPL or JumperIDE). It stops with a `KeyboardInterrupt` traceback followed by `--- script finished ---` - that's just how a Python script gets interrupted, not a crash." Try it step 3 carries the same omission and can stay as-is once the intro says this; no wording change is needed in the OLED bullets.

- **[minor] NOT_REPRODUCED**: page says: test_oled_features.py 'Runs through the OLED functions one section at a time: ... bitmaps ...'  
  observed: The bitmap section prints a warning a new user has to interpret: 'Attempting to load bitmap: /jogo32h.bin' / '⚠ Bitmap file not found: /jogo32h.bin' / '  (This is OK if the file doesn't exist)'. The page does not mention that this section has nothing to load on a stock board.  
  repro: bench.py run /python_scripts/examples/test_oled_features.py 45 and read the Bitmap Functions section.  
  suggested: No page change is needed - the page is correct as written. If a caveat is wanted anyway, one clause on the test_oled_features.py bullet would do it: "The bitmap section looks for a /jogo32h.bin you probably don't have and says so; the check still passes." The same wording is not wrong anywhere else in the excerpt - the other items in that list (text sizes, print mirroring, fonts, framebuffer, scrolling) are not affected by this.

- **[minor] CONFIRMED**: page says: The two OLED bullets link the scripts as GitHub paths scripts/ex/oled_stats_page.py and scripts/ex/test_oled_features.py, but never give the on-device path.  
  observed: On the board they live at /python_scripts/examples/oled_stats_page.py and /python_scripts/examples/test_oled_features.py; there is no /scripts/ex/ on the device. The 'Running an example' section above uses the /python_scripts/examples/ form, so the reader has to translate between the two.  
  repro: bench.py py "import os; print(sorted(os.listdir('/python_scripts/examples')))" - both files are listed there.  
  suggested: Give the two script bullets the same shape as the oledgui.py bullet, keeping the GitHub link on the filename: "**[oled_stats_page.py](...)** (in `/python_scripts/examples/`) - Builds a screen with the four ADC voltages and the uptime..." and the same for test_oled_features.py. Before doing that, check that the `scripts/ex/` path in both GitHub URLs is still correct in the repo.

- **[minor] CONFIRMED**: page says: oled_stats_page.py 'Builds a screen with the four ADC voltages and the uptime'  
  observed: It does, but the layout overflows once a reading carries a minus sign: the fourth value renders as '-0.00' with its 'V' wrapped onto the next line, colliding with the 'uptime' row.  
  repro: Run oled_stats_page.py with nothing driving the ADC inputs, then read the OLED (shot _5).  
  suggested: The page is not wrong - "Builds a screen with the four ADC voltages and the uptime" is exactly what happens, and the defect is the example script's row width, so the real fix belongs in oled_stats_page.py (shorten the row: drop the V, or the space ahead of a sign). If a caveat is wanted anyway, one clause after the claim is enough: "with nothing on the inputs the readings hover around zero, and if three come up negative the row runs one character past the edge and the last V lands on the uptime line - cosmetic." This wording appears nowhere else in the excerpt; the other two OLED bullets make no layout claim.

screenshots: ex07_oled_stats_page_test_oled_features_oledgui_0_board.png, ex07_oled_stats_page_test_oled_features_oledgui_0_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_1_board.png, ex07_oled_stats_page_test_oled_features_oledgui_1_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_2_board.png, ex07_oled_stats_page_test_oled_features_oledgui_2_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_4_board.png, ex07_oled_stats_page_test_oled_features_oledgui_4_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_5_board.png, ex07_oled_stats_page_test_oled_features_oledgui_5_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_6_board.png, ex07_oled_stats_page_test_oled_features_oledgui_6_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_8_board.png, ex07_oled_stats_page_test_oled_features_oledgui_8_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_9_board.png, ex07_oled_stats_page_test_oled_features_oledgui_9_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_10_board.png, ex07_oled_stats_page_test_oled_features_oledgui_10_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_11_board.png, ex07_oled_stats_page_test_oled_features_oledgui_11_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_12_board.png, ex07_oled_stats_page_test_oled_features_oledgui_12_oled.png, ex07_oled_stats_page_test_oled_features_oledgui_13_board.png, ex07_oled_stats_page_test_oled_features_oledgui_13_oled.png

## ex08_oscilloscope_usb_audio_mic_excel_listener - Example scripts (Tools): oscilloscope.py, usb_audio_mic.py, excel_listener.py (08.5-examples.md)

steps: {'substitute': 2, 'literal': 13, 'could_not_observe': 7, 'needs_hand': 1}  
summary: Firmware 5.7.11.0, slot 3; harness reset first and last, board never wedged. The page's plumbing is all real: `/` opens the file manager, running a script by its path works, `p` -> `files` -> Enter -> Ctrl+P works, and Ctrl+Q stops things reliably every time. Try it step 3 is exactly right - adc_basics.py prints four ADC voltages at precisely 2.0 blocks per second and Ctrl+Q stops it. usb_audio_mic.py is the one Tools script that fully delivers: the serial port drops and returns in about 8 s, 'JL Audio In' appears as a 2-channel USB input, rows 20 and 40 light green wired to ADC_0 and ADC_1 exactly as documented, a second run scrolls live L/R levels, and Ctrl+Q ends it - only the '48 kHz' is off (macOS reports 16 kHz) and the page never says how to turn the device back off. The two bigger Tools scripts do not run on this board at all: oscilloscope.py (28192 bytes) and excel_listener.py (15307 bytes) both die at load with 'Failed to read ... (out of memory?)' with 41-45 KB free, so a new user gets none of what the page describes - no scope on the OLED, no ignore_dtr hint, no board clear. Everything the page says about the scope's controls (wheel, click, probe buttons, probe tap) and about recording or the Excel workbook needs a hand. One thing I could not judge: Try it 2's four pairs of rows - the demo runs for 4363 ms and disconnects what it connects, faster than this rig's ~3-5 s per LED/net sample, so I saw no rows lit but cannot call that a discrepancy.

needs a hand: The wheel itself: `click` > `Files` > `examples` > pick a script > `click` (Try it step 1 and the first Running-an-example bullet). bench.py wheel is not available on this rig.; oscilloscope.py interaction: turning the wheel to change the selected setting and clicking to cycle timebase / volts-per-division / trigger level / trigger edge / auto-range.; oscilloscope.py probe buttons: the front button to reset settings and the rear button to exit.; oscilloscope.py signal source: tapping a row with the probe to move ADC 0 there, and feeding it a real 0-3.3 V signal to see a trace - and eyes on the OLED, which this rig cannot read while a heavy script runs.; Try it 2: eyes on the breadboard while node_connections.py runs - the demo lasts 4.4 s and cleans up after itself, faster than this rig can sample.; usb_audio_mic.py: recording from 'JL Audio In' in Audacity / QuickTime / a browser, and confirming the +/-8 V full-scale figure with a real signal on rows 20 and 40.; excel_listener.py: the Excel GUI workbook on the host side, and adding `[usb_cdc] ignore_dtr = 1;` to the config (config edit commands are off-limits to this rig).

- **[major] NEEDS_HAND**: page says: Tools: oscilloscope.py - 'A scope on the OLED.' The page presents it as a script you run like any other ('run one directly with its path').  
  observed: Launching it by path from the terminal fails outright: 'Failed to read /python_scripts/examples/oscilloscope.py (out of memory?)'. Reproduced twice, with 45760 and 42224 bytes free reported by gc.mem_free(); the file is 28192 bytes. Importing it as a module from the MicroPython port raises 'MemoryError: memory allocation failed, allocating 18 bytes' (exec(open().read()) also fails, at 28193 bytes, but that method loads the file into a string so it is only supporting evidence). Only the file-manager + Ctrl+P route gets past loading, and the OLED was unreadable through the rig while it ran, so whether a trace ever draws is unknown.  
  repro: bench.py reset; bench.py run /python_scripts/examples/oscilloscope.py 8  
  suggested: Conditional on a fresh-boot confirmation - if it fails on a freshly powered board, add to the Tools intro: "oscilloscope.py and excel_listener.py are big. If launching one by its path says `out of memory`, reboot and try again, or open it from the file manager instead (`p`, then `files`, pick it, `Ctrl+P`) - that route loaded it on the same board." The same wording is at issue for both scripts, because both hang off the one line in Running an example: "or run one directly with its path" - that is the claim that failed, not anything specific to the scope's own description.

- **[major] NEEDS_HAND**: page says: Tools: excel_listener.py - 'The board side of the Excel GUI workbook ... Needs [usb_cdc] ignore_dtr = 1 ... and it tells you so if it's missing. Clears the board first.'  
  observed: The script never loads: 'Failed to read /python_scripts/examples/excel_listener.py (out of memory?)' (file is 15307 bytes, 41120 bytes free). No ignore_dtr message, no measurements, and the board is not cleared - rows 1-5 and 10-20 I had connected beforehand were still connected afterwards.  
  repro: bench.py py "jumperless.connect(1,5)"; bench.py run /python_scripts/examples/excel_listener.py 8; bench.py status  
  suggested: No edit yet - the claim is unsettled. If a fresh-boot run reproduces it, add one line near the Tools list: "oscilloscope.py and excel_listener.py are the two biggest examples; if other scripts have run since power-on they can fail to load with an out-of-memory message, so power-cycle the board and run them first." If a fresh-boot run loads them fine, leave the page as it is - the ignore_dtr and "clears the board first" wording was never actually tested, only blocked by the load failure.

- **[minor] CONFIRMED**: page says: Tools: usb_audio_mic.py - 'a 2-channel 48 kHz microphone called JL Audio In'  
  observed: macOS reports 'JL Audio In:' with 'Input Channels: 2' and 'Manufacturer: Architeuthis Flux' - but 'Current SampleRate: 16000'. Name and channel count are right; the rate the page promises is not what the host shows.  
  repro: bench.py run /python_scripts/examples/usb_audio_mic.py 10; sleep 8; system_profiler SPAudioDataType | grep -A6 'JL Audio In'  
  suggested: Replace "a 2-channel 48 kHz microphone called `JL Audio In`" with "a 2-channel 16 kHz microphone called `JL Audio In`". If the firmware's descriptor turns out to offer 48 kHz as well, the page can say "up to 48 kHz" instead — but on arrival macOS shows 16000, and that is the number a reader following this page will see.

- **[minor] CONFIRMED**: page says: Tools: usb_audio_mic.py - 'Run it once to turn the device on ... Ctrl+Q to stop.' The page never says how to turn the audio device back off.  
  observed: 'JL Audio In' is still listed as a Mac audio input after Ctrl+Q stopped the script and after the harness reset (no reboot was performed, so this does not prove it survives a power cycle). A user who tries this once is left with an extra microphone on their Mac and no documented way back.  
  repro: Run usb_audio_mic.py, Ctrl+Q, bench.py reset, then system_profiler SPAudioDataType  
  suggested: Add to the usb_audio_mic.py bullet, after "`Ctrl+Q` to stop.": "That stops the script, but `JL Audio In` stays on your computer - it goes away when you unplug the board or reboot it." (If a power-cycled board shows the script saves `enabled = true` into the config's `[usb_audio]` section, the sentence should name `[usb_audio] enabled = false;` as the way off instead - that is the one thing the wedged rig could not settle.) The same wording problem does not appear elsewhere in the excerpt: line 11's "press `Ctrl+Q`" is about stopping scripts and is correct, and no other Tools script leaves anything behind on the host. One flag for the author: the excerpt links 06-config.md, which may already document `[usb_audio]` - out of scope for me to check.

- **[minor] NEEDS_HAND**: page says: Running an example: 'A few of these call nodes_clear() when they start, which wipes whatever you had connected. They're marked below.' - in this excerpt only excel_listener.py carries the mark.  
  observed: node_connections.py, the script the Try it tells a brand-new user to run first, prints 'Cleared all connections' on startup and wiped the rows I had connected. Caveat: this excerpt begins at the Tools list, so the mark for node_connections.py may well appear in the Basics list the excerpt omits - I could not check.  
  repro: bench.py py "jumperless.connect(1,5)"; bench.py run /python_scripts/examples/node_connections.py 5 - watch for 'Cleared all connections'  
  suggested: Conditional on the board half being confirmed after a power cycle: end Try it step 1 with "(this one clears the board first)", so the first script a new user is told to run carries its own warning. If the Basics list does not already mark node_connections.py with "*Clears the board first.*", add it there too.

screenshots: ex08_oscilloscope_usb_audio_mic_excel_listener_0_board.png, ex08_oscilloscope_usb_audio_mic_excel_listener_0_oled.png, ex08_oscilloscope_usb_audio_mic_excel_listener_1_board.png, ex08_oscilloscope_usb_audio_mic_excel_listener_1_oled.png, ex08_oscilloscope_usb_audio_mic_excel_listener_2_board.png, ex08_oscilloscope_usb_audio_mic_excel_listener_2_oled.png, ex08_oscilloscope_usb_audio_mic_excel_listener_4_board.png, ex08_oscilloscope_usb_audio_mic_excel_listener_4_oled.png, ex08_oscilloscope_usb_audio_mic_excel_listener_6_board.png, ex08_oscilloscope_usb_audio_mic_excel_listener_6_oled.png

## ex09_async_read_test_neopixel_viperide_reinit - Example scripts (Utilities): async_read.py, test_neopixel.py, viperide_reinit.py (08.5-examples.md)

steps: {'substitute': 2, 'literal': 12, 'needs_hand': 2, 'could_not_observe': 3}  
summary: Two of the three Utilities scripts behave exactly as the page says. viperide_reinit.py is spot on - board info, flash usage and a pipe-delimited recursive file listing, all machine-readable. async_read.py starts, echoes what you type ('hello' came back as 'hello') and stops on Ctrl+Q, though its banner says 'Ctrl+C to exit' where the page says Ctrl+Q, and it prints an undocumented 'ack' before each echo. test_neopixel.py is honest about its one testable claim - import neopixel does fail without the ViperIDE install - but everything else about it (GPIO 1, six animations, rows 14/15/16, the wheel and button controls, even the 'clears the board first' warning) sits behind a module install this rig cannot do, and running the script shows nothing about how to do it. The real damage is in the shared 'Running an example' prose the excerpt carries. node_connections.py, the page's own headline Try-it, never lights a single row: three runs, up to 40 s each, and all you get is 'Cleared all connections' on the terminal and 'Is connected: / DISCONNECTED' on the OLED, with no nets, no rows and no finish - the opposite of what step 2 promises. And 'type / to open the file manager and pick one' drops you in the eKilo editor rather than running anything; the required Ctrl+P is mentioned only in a different bullet, and when I pressed it from that editor it did not run the script - it knocked the terminal port over (serial 'Device not configured', OLED splash, board back at a bare prompt). The same key from the `p` > `files` editor instead pasted the script into the REPL and needed a further Enter before anything appeared. Smaller stuff: the Utilities bullets give only GitHub repo paths, never the /python_scripts/examples/ path you actually type, and 'set the rail to 5 V yourself' comes with no method - the obvious set_top_rail() does not exist. The direct-path launch route and the adc_basics.py Try-it (four voltages, exactly twice a second, Ctrl+Q stops it) were flawless. No wheel step could be driven on this rig.

needs a hand: The click-wheel launch route (`click` > `Files` > `examples` > pick > `click`) - this rig cannot turn or click the wheel, so whether a wheel click RUNS a script (as opposed to opening the editor, which the terminal file manager does) is untested.; Installing the neopixel module from the ViperIDE package manager - needs a browser at ide.jumperless.org and the USB port this harness holds. Everything test_neopixel.py claims to do is blocked behind it.; test_neopixel.py's six animations, the wheel picking the speed and the probe button picking the mode - needs a real WS2812 strip in rows 14/15/16, a wheel turn and a button press.; The JumperIDE run route ('open the file from the device browser and hit run').

- **[major] HARNESS_ARTIFACT**: page says: Try it step 2: 'Watch the breadboard: four pairs of rows light up one after another as the script connects and disconnects them.'  
  observed: node_connections.py prints 'Node Connections Demo' and 'Cleared all connections' on the terminal and then goes quiet for good. No rows ever light, no nets are created, no further terminal output appears, and the script never reports finishing - it has to be killed with Ctrl+Q. It is not dead at the clear: the OLED reads 'Is connected:' / 'DISCONNECTED', so it reached a connection check afterwards and that check came back negative. Reproduced on three separate runs waiting ~40 s, ~25 s and ~15 s, polling the LED buffer and net list throughout.  
  repro: python3 bench.py run /python_scripts/examples/node_connections.py 20 ; then python3 bench.py leds and python3 bench.py status repeatedly, then python3 bench.py stop  
  suggested: No change is required to fix a bug - the script does what step 2 says. Two small precision edits are worth making, because both feed the misread: the four pairs are not all breadboard rows (they are rows 1-30 and 15-45, then DAC 0 to row 20 and GPIO 1 to row 25), and the whole thing is over in about five seconds, with the script clearing the board on its way out. Suggested: "Watch the breadboard: four connections light up one after another - rows 1 and 30, rows 15 and 45, then DAC 0 to row 20 and GPIO 1 to row 25. It's quick, about half a second each, and the script clears the board when it's done, so watch while it runs." The same excerpt's line 13 ("A few of these call nodes_clear() when they start ... They're marked below") is also worth a glance for this script: node_connections.py clears the board both at the start AND at the end, and the Try-it sends a brand-new reader straight to it three lines later with no warning of its own.

- **[major] CONFIRMED**: page says: Terminal bullet: 'type / to open the file manager and pick one' (implying picking a script runs it, since the sentence continues 'or run one directly with its path').  
  observed: Pressing Enter on a .py in the file manager opens the eKilo text editor, it does not run the script. To actually run it you have to press Ctrl+P afterwards - a step the page only mentions in the separate MicroPython REPL bullet. A new user following the terminal bullet lands in an editor with no idea why. The footer legend lists no 'run' key at all (enter/h/v/nav/./Ctrl+Q/e/n/d/u) - I did not press `h = help`, so a run key may be documented there.  
  repro: python3 bench.py term "/" ; arrow down to python_scripts, Enter; down to examples, Enter; down to async_read.py, Enter -> eKilo editor opens  
  suggested: Replace the Terminal bullet with: "**Terminal:** run one by its path, like `/python_scripts/examples/gpio_basics.py`. Typing `/` on its own opens the file manager, but from the terminal picking a script opens it in the editor - press `Ctrl+P` there to run it."

Same wording elsewhere in the excerpt: no. The click-wheel bullet's "It runs" agrees with the file-manager page's rule that .py files run when you get there from the wheel's Files menu (not testable on this rig - no wheel), and the MicroPython REPL bullet already describes the open-then-Ctrl+P two-step correctly. The Try it steps use the wheel and the direct-path route, not the `/` picker.

- **[major] NOT_REPRODUCED**: page says: Terminal bullet: picking a script in the `/` file manager and using the editor's own 'CTRL-P = Save and load' is the way to run it.  
  observed: Ctrl+P in the editor reached through `/` did not run the script and took the terminal port down with it: the host serial read died with '[Errno 6] Device not configured', the OLED flipped to a splash-style graphic, and when the port re-appeared ~10 s later the board was back at a plain Jumperless prompt with nothing running. The identical keystroke in the identical editor reached through `p` > `files` instead pasted the script into the REPL. Same editor, same key, two different outcomes depending on how you got there. Seen once; not repeated, so a harness artifact cannot be ruled out.  
  repro: python3 bench.py term "/" ; arrow to python_scripts, Enter; to examples, Enter; to async_read.py, Enter; python3 bench.py raw "\x10"  
  suggested: No change to the Terminal bullet on account of this claim — the bullet says "type `/` to open the file manager and pick one, or run one directly with its path" and makes no Ctrl+P promise, so there is nothing here to correct. The Ctrl+P wording that is genuinely in question is the REPL bullet on line 9 ("open a script, and press `Ctrl+P` to run it"), which is ex04's and ex09's own separate minor item: it loads the script into the REPL and waits for an Enter rather than running it, and it sometimes re-enumerates the board's USB instead. The real Terminal-bullet problem — that Enter in the `/` file manager opens the eKilo editor instead of running the script — is ex09's discrepancy #2 and should be ruled on there, not here.

- **[minor] CONFIRMED**: page says: MicroPython REPL bullet: 'open a script, and press Ctrl+P to run it.'  
  observed: After Ctrl+P the terminal sat on '...' continuation prompts and printed nothing for 4 s - in particular the script's own 'Polling stdin, Ctrl+C to exit:' banner never appeared. Echo behaviour only showed up after I pressed Enter. Whether Ctrl+P alone had already started it and it was simply blocked on stdin is ambiguous from what I could see; either way the page's 'press Ctrl+P to run it' does not visibly run anything until you also press Enter.  
  repro: python3 bench.py term "p" ; "files" ; navigate to async_read.py, Enter; python3 bench.py raw "\x10" -> script pasted, not running; python3 bench.py raw "\r" -> now it runs  
  suggested: **MicroPython REPL:** type `p` to get into the REPL, then `files`, open a script, and press `Ctrl+P` - that saves it and loads it into the REPL. Press `Enter` to run it. (Line 9 is the only Ctrl+P in the excerpt, but the same "pick it and it runs" assumption sits on line 8, the terminal `/` bullet - Enter there opens the eKilo editor rather than running anything, already ruled CONFIRMED from the same ledger - and on line 7, the click-wheel bullet, which no rig here can drive and which remains unverified.)

- **[minor] NOT_REPRODUCED**: page says: test_neopixel.py bullet: 'It also shows how to install a module from the ViperIDE package manager.'  
  observed: Running the script shows nothing of the sort - you get a bare 'ImportError: no module named neopixel' traceback with no hint about ViperIDE, package names or steps. The instructions are presumably in the script's source, which the page does not tell you to open. The page itself also gives no ViperIDE install steps.  
  repro: python3 bench.py run /python_scripts/examples/test_neopixel.py 8  
  suggested: No correction is required - the claim is true. If you want to close the discoverability gap the tester actually hit, name where the steps live, since someone who launches the script from the wheel or by path never sees its header: "The comment at the top of the script says how to install `neopixel` from the ViperIDE package manager; do that first or `import neopixel` fails."

- **[minor] CONFIRMED**: page says: test_neopixel.py bullet: 'power from the top rail on row 16 (set the rail to 5 V yourself)'  
  observed: The page never says how to set a rail to 5 V, and there is no link to the page that does. I had to hunt through the MicroPython API to find dac_set(TOP_RAIL, 5.0); the obvious guesses set_top_rail() and set_rail() do not exist.  
  repro: python3 bench.py py "set_top_rail(5.0)" -> NameError; python3 bench.py py "dac_set(TOP_RAIL, 5.0)" -> works  
  suggested: Change "(set the rail to 5 V yourself)" to name a route and point at the page that covers the rest, e.g.: "power from the top rail on `row 16` — set the top rail to 5 V first; from the REPL that's `dac_set(TOP_RAIL, 5.0)`, or use the wheel or terminal (see [Power rails])." Keep it route-open rather than implying Python is the only way, and let the doc author confirm the link target and that `dac_set(TOP_RAIL, 5.0)` is the name to publish — I was scoped to this excerpt and could not check other pages or re-run the board.

- **[minor] NOT_REPRODUCED**: page says: The three Utilities scripts are named only by GitHub links to scripts/ex/<name>.py.  
  observed: On the board they are at /python_scripts/examples/<name>.py. The Utilities section never gives the on-board path, so to run any of these three from the terminal you have to notice the /python_scripts/examples/gpio_basics.py example in the section above and translate the GitHub path yourself.  
  repro: Read the Utilities bullets, then compare with the file listing printed by /python_scripts/examples/viperide_reinit.py  
  suggested: Not needed - the verdict is NOT_REPRODUCED and the page is already correct. Optional non-blocking nit only: the repo folder in the GitHub links (scripts/ex/) does not mirror the on-board folder (/python_scripts/examples/), so a reader who assumes the link mirrors the device filesystem could be briefly misled; one clause at the top of the Utilities list - "on the board these live in /python_scripts/examples/" - would remove that ambiguity. The excerpt's own path usage is consistent and correct everywhere it appears (line 8's /python_scripts/examples/gpio_basics.py and line 18's /python_scripts/examples/adc_basics.py), so the wording is not wrong elsewhere in the excerpt.

- **[minor] NOT_REPRODUCED**: page says: async_read.py bullet: 'Type something and it echoes it. Ctrl+Q to stop.'  
  observed: Both true, but the script's own on-screen banner says 'Polling stdin, Ctrl+C to exit:' and it prints an 'ack' line before each echo. Neither is mentioned, so the terminal does not look like the page describes on first contact.  
  repro: python3 bench.py run /python_scripts/examples/async_read.py 6  
  suggested: No page change. The bullet's two claims are both true on the board, and adding "it prints a banner and an ack line first" would be noise in a one-line summary. (Separately, and not a docs fix: async_read.py line 14 prints "Ctrl+C to exit" where the terminal route needs Ctrl+Q - worth correcting in the script.)

- **[minor] NOT_REPRODUCED**: page says: test_neopixel.py bullet: '*Clears the board first.*'  
  observed: It cannot, for a first-time reader: the import of neopixel fails on line 18 before any clear happens, so a board with connections on it comes back untouched. Connected rows 5-6, ran the script, rows 5-6 were still lit and Net 6 still present.  
  repro: python3 bench.py py "connect(5,6)" ; python3 bench.py run /python_scripts/examples/test_neopixel.py 8 ; python3 bench.py leds  
  suggested: No change needed. The bullet already states the precondition one sentence before the marker ("you have to do this first or `import neopixel` fails"), and "*Clears the board first.*" is accurate to its definition at excerpt line 13 - the script calls nodes_clear() as its first board action when it starts.

screenshots: ex09_async_read_test_neopixel_viperide_reinit_1_board.png, ex09_async_read_test_neopixel_viperide_reinit_1_oled.png, ex09_async_read_test_neopixel_viperide_reinit_3_board.png, ex09_async_read_test_neopixel_viperide_reinit_3_oled.png, ex09_async_read_test_neopixel_viperide_reinit_4_board.png, ex09_async_read_test_neopixel_viperide_reinit_4_oled.png, ex09_async_read_test_neopixel_viperide_reinit_5_board.png, ex09_async_read_test_neopixel_viperide_reinit_5_oled.png, ex09_async_read_test_neopixel_viperide_reinit_6_board.png, ex09_async_read_test_neopixel_viperide_reinit_6_oled.png

## fm_file_manager - File Manager (terminal /) (08-file-manager.md)

steps: {'literal': 30, 'needs_hand': 4, 'could_not_observe': 12}  
summary: The page works. Every File Manager key it documents that this rig could send exists and does what it says: `/` opens the browser, `/adc_basics.py` runs the example straight out of /python_scripts/examples, arrows move the selection, Enter opens directories, Enter on a slot file loads that slot (status went 3 -> 0), Enter on a .py from the terminal `/` opens the editor, and `.`, `/`, Esc, h, v, e, i, r, q and Ctrl+Q all behave as described. The icon table checks out down to the detail that config.txt.hilbak gets the magenta ⚙ because its name starts with `config`, and slot .yaml files get the JSON ⟐. In the editor, Tab, arrows, Ctrl+U and the Esc-doesn't-discard behavior all matched. Seven minor discrepancies: the unsaved-changes Ctrl+Q count is 1 more press, not 3 (so the extra presses the page asks for land in the File Manager, where Ctrl+Q also quits); the root has no `screens/` directory; /slots has only the slot files that have been written (0 and 3), not slot0-2 as drawn; real bridge lines carry no dup:/color:; the Examples section says clicking an example runs it, which contradicts the page's own Navigation row for terminal users; the built-in `h` help disagrees with the page's key table (`..` not `.`, no `q`/Esc/encoder rows); and `e` on some files opens them [READ-ONLY], which the page never mentions. Not testable here: everything behind the rotary encoder (its navigation rows, character selection mode, hold/long-hold), USB Mass Storage (bench.py refuses U/u/Z), the write keys n/d/x/m and Ctrl+S/Ctrl+P (they change the owner's filesystem), `u` memory status (the byte is on the tool's deny list), terminal colors (bench.py strips ANSI), and every OLED claim plus the slot-preview claim - the read-only screenshot/LED/status port goes silent for as long as the File Manager or editor is open.

needs a hand: Rotary encoder as a navigation control in the File Manager (move selection up/down) - bench.py cannot turn the wheel.; Click encoder to open a directory / edit a file.; Hold encoder to go up one directory (and quit at the root); long hold to quit the File Manager.; Editor: rotary encoder horizontal cursor movement, the Save/Cancel menu you get by turning past the end of the file, click-to-enter character selection mode with the OLED letter scroll, and long-hold save-and-quit.; The `.py`-files-run half of the Enter row (entering the File Manager from the clickwheel Files menu rather than terminal `/`).; Clicking a project's wiring.yaml to start that project - its main.py can only be stopped by a wheel hold on this rig.

- **[minor] NEEDS_HAND**: page says: Editor Ctrl+Q: "If you have unsaved changes it asks you to press it 3 more times".  
  observed: The board asks for ONE more press: "WARNING!!! File has unsaved changes. Press Ctrl-Q 1 more times to quit." and the second press quit the editor ("Returning to file manager..."). The Esc hint agrees with the board ("Ctrl-Q x1 quits without saving"). Two presses total, not four - so a user following the page presses Ctrl+Q more times than the editor is waiting for, and the extra presses land in the File Manager, where Ctrl+Q is also quit.  
  repro: Terminal: `/` -> arrow to /python_scripts/examples/adc_basics.py -> Enter -> Tab (header shows "(modified)") -> Ctrl+Q -> read the warning -> Ctrl+Q again.  
  suggested: Conditional on the live-board test above coming back N=1: "**Ctrl+Q**: Quit editor. If you have unsaved changes it warns you; press it once more to quit without saving (Esc doesn't discard your edits, it just tells you how to leave)." No other press count appears in the excerpt, and the Navigation table row "**q** or **CTRL + q** | Quit File Manager (Ctrl+Q also quits the Text Editor)" stays true as written.

- **[minor] CONFIRMED**: page says: The File System Structure tree lists a `screens/` directory (OLED GUI layouts) at the root.  
  observed: There is no `screens/` directory. Root is images/, projects/, python_scripts/, slots/, config.txt, selftest.json, undo_history.txt (+ .hilbak backups).  
  repro: Terminal: `/` and read the root listing ("Files: 10").  
  suggested: Drop the `screens/` line from the File System Structure tree, or annotate it rather than list it as standard: "`screens/` — only after you save an OLED layout (oled_layout_editor.py writes `/screens/layout.json` when you press the probe's REMOVE button)". The annotated form fits the tree's existing style, since it already marks on-demand entries elsewhere. `screens` appears only once in the excerpt (line 30) — the OLED Display Support section and the `python_scripts/lib/` listing never mention it, so there is nothing else to change for this claim. Worth noting separately: the same tree draws `/slots/` as slot0-slot2 "... (up to slot7.yaml)" when only written slots have files, which is the same "shown as always-there when it is created on demand" mistake.

- **[minor] CONFIRMED**: page says: MicroPython Examples section: "The example scripts live in /python_scripts/examples/. Click one in the File Manager to run it."  
  observed: Entering the File Manager the way this page opens it (terminal `/`) and pressing Enter on adc_basics.py opens the eKilo editor; it does not run. The page's own Navigation table says so (".py files run if you got here from the clickwheel Files menu; from the terminal / they open in the editor"), so the two sections contradict each other for a terminal user.  
  repro: Terminal: `/` -> python_scripts -> examples -> Enter on adc_basics.py.  
  suggested: "The example scripts live in `/python_scripts/examples/`. From the terminal, run one by typing `/` and its name, like `/adc_basics.py`. If you opened the File Manager from the clickwheel `Files` menu, clicking a script runs it; from the terminal `/` it opens in the editor instead."

- **[minor] CONFIRMED**: page says: "bridges - Your circuit connections, with `dup:` and `color:` on each line", illustrated by an example where every bridge carries them.  
  observed: The board's own /slots/slot0.yaml writes bare bridges with neither key: "- {n1: 2, n2: ADC0}", "- {n1: 6, n2: RP_GPIO_1}", "- {n1: 18, n2: 23}". dup:/color: are evidently optional, not on each line.  
  repro: Terminal: `/` -> slots -> select slot0.yaml -> `v`.  
  suggested: bridges - your circuit connections, one `{n1: A, n2: B}` pair per line. Net colors and names live under `nets`, which the board writes for you. The example's `dup:` and `color:` keys should be dropped from the bridge lines, or the sentence changed to say they are optional extras the board does not write itself.

- **[minor] CONFIRMED**: page says: The tree draws /slots/ holding slot0.yaml, slot1.yaml, slot2.yaml "... (up to slot7.yaml)".  
  observed: /slots holds only slot0.yaml and slot3.yaml (plus last_active.txt and .hilbak backups) - files exist only for slots that have been written, not one per slot.  
  repro: Terminal: `/` -> slots.  
  suggested: In the tree, draw /slots/ as what a real board holds - the files for the slots that have been used, plus the board's own last_active.txt:

```
├── slots/
│   ├── slot0.yaml          (slot0.yaml to slot7.yaml - one per slot you've used)
│   ├── slot3.yaml
│   └── last_active.txt
```

And give line 62 the same qualifier: "Each slot you've used has a YAML file in `/slots/` (`slot0.yaml` to `slot7.yaml`); a slot you've never put anything in has no file. The global hardware configuration is in `/config.txt`."

Same wording wrong elsewhere in the excerpt: line 62 carries the identical one-file-per-slot implication (fixed above), and line 187's "Opening any `/slots/slotN.yaml` in the onboard editor" reads as though all eight are there - only the files that exist can be opened. Nothing else in the excerpt makes the count claim.

- **[minor] NOT_REPRODUCED**: page says: The Navigation table is the File Manager's key map (`.` up a directory, `q` quits, Esc up/quit, encoder hold/long-hold).  
  observed: All of those keys work, but the File Manager's own built-in help (`h`) disagrees with the page: it writes up-dir as `..` rather than `.`, and lists only CTRL+q to quit - no `q`, no Esc, no encoder hold rows. A new user pressing `h` gets a shorter, differently-worded map than the page's.  
  repro: Terminal: `/` then `h`.  
  suggested: No change. The Navigation table is correct as written - `.`, `/`, Esc, `q` and CTRL+q all work exactly as listed, and the File Manager's on-screen footer also shows the up-dir key as `.`. The mismatch is inside the firmware's own `h` help screen (it says `..` and lists only CTRL+q), which should be fixed there rather than described on the page; adding a note that the built-in help is shorter would only be bloat.

- **[minor] NEEDS_HAND**: page says: `e` edits a file (unconditionally, per the Navigation table).  
  observed: Some files open read-only with no explanation on the page: /python_scripts/history.txt opened as "- 107 lines [READ-ONLY]" while still showing "Ctrl-S = Save" in the footer.  
  repro: Terminal: `/` -> python_scripts -> select history.txt -> `e`.  
  suggested: Conditional on which mechanism the hands-on test finds. If it is per-file (history.txt and other files the board writes for itself): change the `e` row to "Edit file. A few files the board keeps for itself, like `/python_scripts/history.txt`, open read-only - the title bar says [READ-ONLY]." If instead a big file such as `jumperless.pyi` also opens read-only, the clause becomes "Files too large for the editor's memory open read-only - the title bar says [READ-ONLY]." Either way the same sentence belongs on the Enter row, since Enter opens the same editor.

screenshots: fm_file_manager_1_board.png, fm_file_manager_1_oled.png, fm_file_manager_2_board.png, fm_file_manager_2_oled.png

## mp_repl - MicroPython REPL (08-micropython.md)

steps: {'literal': 38, 'could_not_observe': 4, 'substitute': 1, 'needs_hand': 2}  
summary: Firmware 5.7.11.0. Nearly everything the MicroPython REPL section describes works: `p` opens the REPL with the documented banner, the hardware functions are in the global namespace with no import (and so is `time`, which the page's own examples rely on), `import jumperless as j` works, history/save/load/files/edit/context/helpl/help() all exist and behave as written, TAB inserts exactly four spaces, Ctrl+Q quits the REPL and interrupts a running script, `multiline on` + `run` works, the `files` -> Enter -> Ctrl+P flow loads a script into the REPL, exec(open(...).read()) and os see the same filesystem, there are 24 examples in /python_scripts/examples/, and running one by path from the main menu works. The connection contexts behave as documented: in `python` context, is_connected(20, 25) read CONNECTED inside the session and the net was gone after exiting, with the entry state back; in `global` context the same connection survived (rows 20-25 lit blue at the menu). Left/right arrows move the cursor as documented, but pressing Enter after a mid-line edit splits the line into a multiline block instead of running it, and I could only clear that with Ctrl+Q. Two things break for a new user following the page literally. (1) Method 4's `> gpio_set(1, True)` block: the `>` is the actual command character, not a prompt - typed without it the menu eats the first letter as a menu command, so `connect(1, 5)` makes no connection and `adc_get(0)` quietly disconnects the Arduino UART. (2) The multiline auto-indent example: typed with the indentation the page shows, the auto-indent doubles it and the block dies with IndentationError, then NameError on `blink_led()`; typed with no leading spaces it works and looks exactly like the page. Smaller mismatches: the page's REPL command list has `delete` and `multiline` which the board's own help does not list; `save` saved an older command when the command just executed was already in the history; bare expressions (including `> adc_get(0)`) print nothing, so the 'prints nicely' types only show up inside print(); and the REPL banner recommends viper-ide.org while the page recommends ide.jumperless.org. Prompt colours (pink vs blue) could not be checked - the tool strips ANSI. Also worth knowing for future runs: while the REPL is open, or while a script is running, the read-only port stops answering, so `status`, `leds` and `shot` only work from the main menu. Left behind on the board: /python_scripts/bench_repl_test.py, bench_repl_test2.py and _temp_repl_edit.py. Two things to hand back to the owner: my first literal reading of Method 4 left the Arduino UART toggled off ('UART disconnected from Arduino D0 and D1'), and a python-context session leaves the board on slot 99 / slotPython.yaml until a reset.

needs a hand: Clickwheel route to the examples ('From the clickwheel, Files lists the filesystem and clicking a .py file runs it') - the wheel cannot be turned or clicked on this rig; Troubleshooting step 'Unplug / replug your Jumperless'; Method 1, the JumperIDE browser flow at ide.jumperless.org (and the board's own viper-ide.org suggestion) - needs a browser and a person; `delete <name>` was never sent: the harness forbids deleting files on the owner's board. Someone should confirm it exists, since the board's own REPL help does not list it; `new` (create a new script in eKilo) was skipped to avoid creating files on the owner's filesystem; Cleanup: I left three files in /python_scripts on the board - bench_repl_test.py, bench_repl_test2.py and _temp_repl_edit.py (from `edit`). I could not delete them.; Restore the Arduino UART: my first, literal reading of Method 4 (typing `adc_get(0)` at the main menu without the `>`) printed 'UART disconnected from Arduino D0 and D1'. I never toggled it back - undoing it means another undocumented menu letter, so the owner should check it

- **[major] CONFIRMED**: page says: Method 4: 'From the main Jumperless menu, you can execute single commands:' followed by a block whose lines start with `> ` - e.g. `> gpio_set(1, True)`, `> adc_get(0)`, `> connect(1, 5)`  
  observed: The `>` is not a prompt, it is the required command character (the on-board menu lists '> = send Python formatted command'), and the page never says so. Typed as they read - without the `>` - none of the three lines runs: the menu consumes the first letter as a menu command and throws the rest away. `connect(1, 5)` dumped the crossbar map and made no connection (status had no 1-5 net, leds showed no rows lit); `adc_get(0)` toggled the Arduino UART off ('UART disconnected from Arduino D0 and D1'); `gpio_set(1, True)` printed the GPIO status table. So a new user following this section silently changes board state instead of running Python. With the `>` typed literally, all three behave as documented (net 1-5 created, rows 1 and 5 lit pink, gpio_get(1) later reads HIGH)  
  repro: At the main menu type `connect(1, 5)` and press Enter, then check the net list - no net between 1 and 5. Then type `> connect(1, 5)` and check again - Net 6 with nodes 1 and 5.  
  suggested: ### Method 4: Direct Execution

At the main menu, `>` is the command that sends a line to Python. Type it as part of the line - it is not a prompt. Without it the menu reads your first letter as a menu command and discards the rest, so `connect(1, 5)` makes no connection.

```
> gpio_set(1, True)
> print(adc_get(0))
> connect(1, 5)
```

- **[major] CONFIRMED**: page says: Multiline Auto-Indent Mode: the `def blink_led():` block, shown with `...     for i in range(5):` etc.  
  observed: Typed exactly as printed (the text after each `...` prompt, including its leading spaces) the block fails: the REPL supplies the indentation itself, so the typed spaces add to it and each line drifts further right, ending in 'IndentationError: unexpected indent' at line 4 and then "NameError: name 'blink_led' isn't defined" when the page's next line `blink_led()` is run. The same function typed with no leading spaces produces exactly the screen the page shows and works. The page never says the indentation is automatic and must not be typed  
  repro: In the REPL type `def blink_led():`, then `    for i in range(5):` (four typed spaces), then the four body lines with eight typed spaces, then Enter on the blank line.  
  suggested: Add one or two sentences directly under the `def blink_led():` block: "The REPL adds the indentation for you after a line ending in `:`, so type each body line with no leading spaces. The block above is what the screen looks like, not what you type."

- **[minor] NEEDS_HAND**: page says: REPL Commands list on the page includes `exit / quit`, `delete <name>`, and `multiline on|off|auto`  
  observed: The board's own REPL banner and `helpl` list only: quit, Ctrl+Q, helpl, history, save, load, files, new, edit, context, help(). `delete` and `multiline` are absent from the on-board list (multiline does work - `multiline on`/`auto` were confirmed; `delete` was not tested here), and the board says `quit` where the page says `exit / quit` (`exit` does work)  
  repro: Enter the REPL with `p` and compare the printed banner with the page's REPL Commands block.  
  suggested: If `delete zzz_does_not_exist.py` returns a SyntaxError, drop the `delete <name>` line from the REPL Commands block; otherwise leave it alone. Either way `exit / quit` and `multiline on|off|auto` stay as written - both work, the board's banner is just an abbreviated list - and the block should gain a `run - Execute the script accumulated in forced multiline mode` line, which the prose at "type `multiline on` and then `run`" already depends on (that addition is outside the filed discrepancy).

- **[minor] NEEDS_HAND**: page says: save [name] - Save last executed script  
  observed: After executing `connect(20, 25)` and immediately typing `save bench_repl_test.py`, the file written was 26 bytes containing `print('alive', adc_get(0))` - a command from an earlier session that was still at the top of the Recent Commands list. Repeating the test with a brand-new command (`print('SAVE_MARKER')`) saved the right thing, so it looks like re-running a command already in history does not make it the 'last executed script'  
  repro: In the REPL run a command that is already in the history list, then `save name.py`, then `load name.py` and look at what lands on the input line.  
  suggested: Conditional on the hand test - if it shows a re-run command does not become the "last executed" one, change both places to: "save [name] - Save the most recent command in the history list (re-running a command that is already in the list does not move it to the top, so it is not what gets saved). With no name the file is auto-named." If instead the hand test shows the miss only happens across a REPL re-entry, say: "save [name] - Save the last command executed in this REPL session. It does not carry over if you leave the REPL and come back."

- **[minor] NEEDS_HAND**: page says: Method 4 example `> adc_get(0)` implies you see the reading  
  observed: `> adc_get(0)` at the main menu printed nothing at all. Inside the REPL, bare expressions also print nothing - `gpio_get(1)`, `gpio_get_dir(1)`, `gpio_get_pull(1)` and `adc_get(0)` on their own lines produced no output; you have to wrap them in print(). The 'Formatted Output and Custom Types' section reads as if the values show up by themselves  
  repro: At the main menu type `> adc_get(0)`; in the REPL type `adc_get(0)` on its own line. Then compare with `print(adc_get(0))` -> 2.2072658.  
  suggested: Conditional on a hand confirming it, and only for the Method 4 block: change `> adc_get(0)` to `> print(adc_get(0))` and add one line under the block - "Reads return a value but don't print it on their own; wrap them in print() to see it." Leave the 'Formatted Output and Custom Types' block alone - its lines are assignments and its "# Prints" comments describe how the returned type renders, which the tester's own print() results confirm; if it is touched at all, make it "# Prints as" on all three lines rather than rewording the section.

- **[minor] CONFIRMED**: page says: Method 1 (Recommended): JumperIDE at https://ide.jumperless.org/  
  observed: Every time you enter the REPL with `p`, the board prints 'There's now a better way to do this! Go to: https://viper-ide.org/ and connect to the 3rd Jumperless serial port' - a different tool from the one the page recommends, with no mention of JumperIDE  
  repro: Type `p` at the main menu and read the first three lines.  
  suggested: Page fix, under "Starting REPL": "From main menu: Press `p`. The board's first line points you at viper-ide.org - JumperIDE at ide.jumperless.org is the same idea and is what this page recommends." If JumperIDE is in fact the Jumperless-hosted ViperIDE, say that in the clause instead of "the same idea"; if it is not, the better fix is the firmware banner, not the page - one stale string in the REPL greeting is cheaper to change than teaching every reader to ignore it.

Elsewhere in the excerpt: the same wording is not wrong anywhere else. "JumperIDE" appears three more times and each is consistent - line 12 is "JumperIDE for VS Code", a different product that should not be lumped in with the web IDE; lines 58-59 and line 170 ("the 3rd port that JumperIDE uses") both mean the web IDE and agree with each other and with the board on which port to use. The page is internally coherent; the board's banner is the odd one out. Line 59's link anchor (#now-you-can-live-code-with-jumperide) points at a section above the excerpt, so I could not judge it.

- **[minor] NEEDS_HAND**: page says: The `files` / editor flow described in Method 2 (implicitly a clean user-facing flow)  
  observed: Opening a file in eKilo dumps developer debug lines into the user's terminal first: '=== ekilo_run() ENTRY ===', '[ekilo_run] Free heap: 36104 bytes', '[ekilo_open] SharedBuffer unavailable - using per-row heap load', 'FALLBACK: Loaded 1 lines (~21 bytes) onto heap'. Also, leaving eKilo with Ctrl+Q (labelled 'quit') still wrote the file: 'File saved as /python_scripts/_temp_repl_edit.py'  
  repro: In the REPL type `files`, select a .py file with the arrow keys and press Enter; then press Ctrl+Q.  
  suggested: Conditional, since nothing was reproduced. If the debug lines are confirmed on hardware they are a firmware cosmetic issue (developer prints leaking into the user's terminal), not a documentation fix - the page should not be made to document them. If Ctrl+Q-saves-on-quit is confirmed on an unmodified file opened via `files`, add one line to Method 2: "In the editor, Ctrl+P saves the file and loads it into the REPL; Ctrl+Q also saves before closing." Nothing else in the excerpt needs changing - the other Ctrl+Q mentions (the REPL Commands list, the Navigation list, and Troubleshooting) are all about the REPL or a running script, and the tester confirmed each of those behaves as written.

- **[minor] NEEDS_HAND**: page says: Navigation: 'Enter - Execute (empty line in multiline to finish)' alongside 'left/right arrows - Move cursor, edit text'  
  observed: If you use the left arrow to edit in the middle of a line and then press Enter, the REPL does not execute the line - it splits it at the cursor into a multiline block ('>>> print(13' / '... 2)'). From there, Enter kept inserting blank lines in the middle of the block instead of finishing it, and there was no documented way out; I ended the block by force-quitting the REPL with Ctrl+Q. The page gives cursor editing and Enter-to-execute as if they compose  
  repro: In the REPL type `print(12)`, press Left twice, type `3` (line reads print(132)), then press Enter.  
  suggested: Conditional - only if the hand run confirms the split AND both the Down-then-Enter and `run` escapes fail. In that case the Navigation block's Enter line should read: "Enter - Execute when the cursor is at the end of the line; mid-line it starts a new line instead (empty line in multiline to finish, or `run`)." If Down-then-Enter or `run` does finish the block, the fix is smaller: add to line 120 "move to the end of the block before pressing Enter to run it." Elsewhere in the excerpt this is not wrong - line 5 already states the same thing correctly ("any other key will break out of history mode and enter multiline editing"); line 120 is the only place that is incomplete.

- **[minor] NEEDS_HAND**: page says: Connection Context Switching - 'python context: ... the state from when you entered comes back' (implying you end up where you started)  
  observed: The connections do come back, but after a python-context session the board is left on a different slot: status reported slot 99 / /slots/slotPython.yaml instead of the slot 3 it was on before. The page mentions slotPython.yaml nowhere; only the board's `context` message does ('saved to slots/slotPython.yaml')  
  repro: Note the active slot, enter the REPL, `context` to python, make a connection, exit, and read the active slot again.  
  suggested: Conditional, because it cannot be confirmed on this rig. If the wrong slot turns out to be user-visible (OLED or the Slots menu) or if connections made after the session land in slotPython.yaml, add one sentence after line 157: "Leaving a python-context session points the board at a scratch slot file (slots/slotPython.yaml); pick your slot again from Slots before you carry on." If slot 99 only ever appears on the diagnostic status port, leave the page alone - the sentence as written is about connection state and the tester confirmed that part is true.

screenshots: mp_repl_1_board.png, mp_repl_1_oled.png, mp_repl_2_board.png, mp_repl_2_oled.png, mp_repl_3_board.png, mp_repl_3_oled.png, mp_repl_5_board.png, mp_repl_5_oled.png, mp_repl_6_board.png, mp_repl_6_oled.png
