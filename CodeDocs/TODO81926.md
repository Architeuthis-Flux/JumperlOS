add rail voltage setting shortcut, when a rail is highlighted, show an `adjust?` thing on the oled then clicking the clickwheel will change the voltage on that rail. We might want to gate that on having an oled installed

> **implemented 2026-08-20 (in flight, awaiting Kevin's hands)** — rails AND
> DACs; `display.rail_click_adjust` = 0 off / 1 only-with-OLED (default) / 2
> always. Detail in DEV_MERGE_HANDOFF.md "In flight".


make a better guard for the current sense adc cycling (and time domain multiplexing in general), I'm getting reports from users where connections drawing no current are "sparkling", implying the current ants are triggering the treshold on and off. Maybe we add some dead time between rows when scanning, or use multiple adcs (if we have more free ones), and look into how we can generally improve this system. Look at the pic-sdk to see what we have available to us. This works pretty well as-is, I just think we should improve how reliable these measurements are.

> **implemented 2026-08-20 (in flight, same batch)** — stages 0-7: flip-count
> metric, fresh-or-fail ring reads, the TDM settle, post-EMA hysteresis
> deadband, ant vote, longer windows, MEASURED rails/DACs, and pairwise
> differential taps (`debug.net_scan_pair_taps`). Pico-sdk answer: the RP2350B
> has ONE SAR ADC muxed over 8 inputs; round-robin+FIFO+DMA are already fully
> used by the AdcRing — "more ADCs" = using more of the 4 routable channels at
> once, which is exactly the pair-tap stage. On the bench this also fixed the
> task #32 INA disagreement (6.7→4.4 mA vs INA's 4.27). Detail in
> DEV_MERGE_HANDOFF.md "In flight".
>
> **accuracy follow-on 2026-08-20** — MEASURED the real crosspoint
> resistance with INA0 in series (new tool test/hil/measure_crosspoint_r.py):
> mean 41.8 Ω vs the 40 Ω model constant, which is the scan's +3 % INA
> residual almost exactly; ±6 % per-route structure remains. Next: per-board
> calibration phase in SelfTest. Plan 2 in DEV_PLANS_82026.md.
>
> **task #32's role, fixed by wave 2's measurement rework (2026-08-21)** — the
> guided checks now measure through **INA0's shunt register** on a ground-side
> chain, and the **ADC-scan current estimate is a DIAGNOSTIC-ONLY crosscheck**
> there: log-only at I ≥ 2 mA, and a >25 % disagreement prints a note that
> names #32 as open, in the message and in the source comment. **The scan
> estimate does not become a primary current source in guide checks until #32
> closes.** That is the standing rule for anyone tempted to wire it in.


we should also improve our preloaded examples, I think we should have another preloaded folder called projects. Where we have premade projects for different things you'd do on a Jumperless. Like a 555 flashing an LED, and arduino nano in the header driving a display (with a way to choose what type of display, I2C, SPI, Parallel etc. also a way to just have the Jumperless drive these directly, in both cases make it so we can write stuff to the screen over USB) some 7400/4000 series logic circuits, EEPROM dumper, some simple effect pedal audio circuits, transistor tester / curve tracer, etc. 

> **outlined 2026-08-20** — manifest/layout/UI proposals + the five
> decisions Kevin needs to make before a build plan is worth writing
> (project-on-disk format, ship vs unpack, menu entry, the USB-to-screen
> path, v1 scope). Plan 3 in DEV_PLANS_82026.md.
>
> **implemented 2026-08-20/21 on branch `projects-guided-placement`** —
> projects folder + clickwheel launcher + guided placement with electrical
> verify + provisioning + 4 starter projects. Part-ID is designed as a
> follow-up (DESIGN_PART_ID_FOLLOWUP.md); detail in DEV_MERGE_HANDOFF.md.
>
> **wave 2, 2026-08-21/22, same branch — Kevin's bench notes answered.** Nine
> more tasks, driven by the `>>>` notes he wrote through
> `CodeDocs/PROJECTS_BENCH_CHECKLIST.md` (that annotated copy is preserved at
> commit `6dfaf7c`; the file itself is now the wave-2 session script). What
> changed: the **DIP anchor flipped** to the bottom half and 2-leg parts got
> the new `axial2` footprint so they straddle the ravine; **placement became a
> per-part mode** (expanded / compact / custom) with move, snap and a modal
> ADJUST session; the guide became a **browsable ring** with a DONE view, so a
> wheel turn can never end a build; **slots became files** — any YAML can be
> the active context and the board boots the last active one
> (`CodeDocs/DESIGN_SLOT_FILES.md`, new); a project launch now opens a
> **run file**, `/projects/<dir>/<dir>_<N>.yaml`, instead of borrowing a slot
> and asking to keep it; Projects moved to the **top level** of the clickwheel
> menu; the **rails come back where they were** after a guide quits; and the
> **measurement machine was rebuilt** — ground-side shunt, four-wire
> resistance in **ohms**, and the measured value on the OLED as each part goes
> in (item 2's ADC-based sensing feeds it as a crosscheck, see above). Detail
> in DEV_MERGE_HANDOFF.md's wave-2 section, including the downgrade notes and
> what is deferred past the wave.
>
> **wave 3, 2026-08-22, same branch — the SECOND bench pass.** Five more
> tasks, driven by the `>>>` notes Kevin wrote through the wave-2 build
> (requirements record: commit `4545073`). Seven findings; every one is code
> now. What changed: **ADJUST is dead** and the pended confirm with it —
> *"honestly this whole thing is confusing"* — so a wheel click is **instant**
> again; a project keeps **ONE run file**, `<dir>_run.yaml`, silently reused
> instead of piling up numbered ones, with a single resume/start-over prompt
> when you quit a guided build part way; the top-level menu row is **`Guides`**
> (`Projects` was eight characters and split at the S on the LED matrix — the
> directory, and every machine line, deliberately did not move); the **555
> stops pre-wiring two ADC taps** it never needed, its `main.py` making and
> removing them itself; **`i2cscrn` generalized** — tap each signal *or* type
> its row, pick a driver and size, and a beacon that keeps rescanning so the
> panel springs to life as you wire it, then clears the lines it made on exit;
> and **dragging a part no longer smears LEDs** behind it.
>
> Two things the wave FOUND rather than built, both pre-existing and both
> shipped: every load left the state dirty, so the idle auto-save **rewrote the
> file it had just read** (and could persist `color: black` from a cross-core
> race); and a **companion script past about 6 KB never ran at all**, printing
> nothing, because the launcher needed two full-size copies of the source on a
> 19 KB heap — the shipped `eeprom` script is 6441 bytes. Both fixed at the
> root, and the runner now says why when it cannot run something.
>
> The deleted-run-file HardFault could not be reproduced in five faithful
> attempts, so wave 3 shipped the **instrument** instead: an abort latch that
> names the caller next time it happens. Detail, downgrade notes and the
> deferred list in DEV_MERGE_HANDOFF.md's wave-3 section; the bench pass is
> §0.5 of PROJECTS_BENCH_CHECKLIST.md.

